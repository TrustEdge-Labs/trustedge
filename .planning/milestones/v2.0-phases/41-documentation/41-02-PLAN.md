<!--
Copyright (c) 2025 TRUSTEDGE LABS LLC
MPL-2.0: https://mozilla.org/MPL/2.0/
Project: trustedge — Privacy and trust at the edge.
GitHub: https://github.com/TrustEdge-Labs/trustedge
-->


---
phase: 41-documentation
plan: 02
type: execute
wave: 2
depends_on:
  - "41-01"
files_modified:
  - README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-04
  - DOCS-05
must_haves:
  truths:
    - "New user understands what TrustEdge does within 30 seconds of reading the README"
    - "New user can run the demo within 5 minutes using the 3-command quick start"
    - "4 use cases show concrete copy-paste commands for drone, sensor, body cam, and audio"
    - "README is a single self-contained file with architecture linked to docs/"
  artifacts:
    - path: "README.md"
      provides: "Problem statement, quick start, use cases, brief architecture, license"
      contains: "Prove that data from an edge device"
      min_lines: 100
  key_links:
    - from: "README.md"
      to: "docs/architecture.md"
      via: "markdown link"
      pattern: "docs/architecture.md"
    - from: "README.md"
      to: "docs/yubikey-guide.md"
      via: "markdown link"
      pattern: "docs/yubikey-guide.md"
    - from: "README.md"
      to: "scripts/demo.sh"
      via: "quick start command reference"
      pattern: "scripts/demo.sh"
    - from: "README.md"
      to: "deploy/docker-compose.yml"
      via: "quick start command reference"
      pattern: "docker.compose"
---

<objective>
Rewrite the root README.md to lead with TrustEdge's value proposition, provide a 3-command quick start, and show 4 use cases with copy-paste commands.

Purpose: The current README leads with YubiKey demos and architecture details that obscure the core value. The rewrite puts "what does this do and how do I try it" front and center, following the ripgrep/jq README style (problem -> install -> use -> details).

Output: A new README.md that lets a developer evaluator understand TrustEdge and run the demo in under 5 minutes.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/41-documentation/41-CONTEXT.md
@.planning/phases/41-documentation/41-01-SUMMARY.md
@README.md

<interfaces>
<!-- trst CLI wrap subcommand flags for use case examples -->
trst wrap --profile generic --in <PATH> --out <PATH>
  --data-type <DATA_TYPE>    # video, sensor, audio, log, binary
  --source <SOURCE>          # Data source identifier
  --description <DESCRIPTION> # Description text
  --mime-type <MIME_TYPE>     # MIME type
  --device-key <PATH>        # Path to signing key
  --device-pub <PATH>        # Path to public key

trst verify <PATH> --device-pub "ed25519:..."
trst keygen --out-key <PATH> --out-pub <PATH>

<!-- docker-compose command -->
docker compose -f deploy/docker-compose.yml up --build

<!-- demo script -->
./scripts/demo.sh           # Auto-detect mode
./scripts/demo.sh --local   # Local-only
./scripts/demo.sh --docker  # Docker mode
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite README.md with new structure</name>
  <files>README.md</files>
  <read_first>README.md, .planning/phases/41-documentation/41-CONTEXT.md, .planning/phases/41-documentation/41-01-SUMMARY.md, docs/architecture.md, docs/yubikey-guide.md</read_first>
  <action>
Rewrite README.md completely. Preserve the copyright header (lines 1-6) exactly. The new structure follows the decisions from 41-CONTEXT.md:

**Section 1: Badges**
Keep existing badges but update version badge from "1.7" to "2.0". Keep CI, License, Commercial, Rust, Version, YubiKey badges in that order.

**Section 2: Title and one-liner**
Title: "# TrustEdge"
One-liner subtitle (not a heading): "Cryptographic provenance for edge device data."

**Section 3: Problem Statement (DOCS-01)**
2-3 sentences explaining the problem: Edge devices generate data (video, sensor readings, audio, logs) but there is no way to prove the data has not been tampered with between capture and consumption. TrustEdge provides a cryptographic chain of custody: sign at the source, encrypt, wrap into a tamper-evident archive, verify with an independent service, and receive a cryptographic receipt as proof.

**Section 4: Quick Start (DOCS-02)**
Heading: "## Quick Start"
Show exactly 3 commands with brief comments:
```bash
git clone https://github.com/TrustEdge-Labs/trustedge.git && cd trustedge
docker compose -f deploy/docker-compose.yml up -d --build
./scripts/demo.sh
```
Brief explanation after: "This starts the full TrustEdge stack (platform server, PostgreSQL, dashboard) and runs an end-to-end demo: key generation, archive wrapping, server-side verification, and receipt retrieval."

Add a note: "No Docker? Run `./scripts/demo.sh --local` with just Rust installed for local-only verification."

**Section 5: Use Cases (DOCS-05)**
Heading: "## Use Cases"
4 use cases, each with a 2-3 sentence scenario and a copy-paste trst wrap command. Use the generic profile metadata flags (--data-type, --source, --description).

1. **Drone Inspection** -- A drone captures inspection footage of infrastructure. The operator needs to prove the video has not been edited between capture and submission to the client.
```bash
trst keygen --out-key drone.key --out-pub drone.pub
trst wrap --in flight-recording.bin --out inspection.trst \
  --data-type video --source "DJI-Mavic-3E" --description "Bridge inspection flight 2024-03-15" \
  --device-key drone.key --device-pub drone.pub
trst verify inspection.trst --device-pub "$(cat drone.pub)"
```

2. **Sensor Logs** -- Industrial sensors produce continuous readings. Regulators need assurance that the submitted logs match what was actually recorded.
```bash
trst wrap --in sensor-readings.csv --out telemetry.trst \
  --data-type sensor --source "Modbus-RTU-Unit-7" --description "Temperature readings Q1 2024" \
  --device-key sensor.key --device-pub sensor.pub
```

3. **Body Camera** -- Law enforcement body cameras record interactions. The footage must be verifiably unaltered for evidentiary use.
```bash
trst wrap --in bodycam-clip.mp4 --out evidence.trst \
  --data-type video --source "Axon-Body-4" --description "Incident report 2024-0847" \
  --device-key officer.key --device-pub officer.pub
```

4. **Audio Capture** -- A journalist records an interview. The publication needs to prove the audio is the original unedited recording.
```bash
trst wrap --in interview.wav --out recording.trst \
  --data-type audio --source "Zoom-H6" --description "Interview with source, 2024-03-15" \
  --device-key recorder.key --device-pub recorder.pub
```

After all 4, add a brief note: "All examples use the generic profile (default). For cam.video-specific archives with frame rate and segment duration, see [examples/cam.video](examples/cam.video/)."

**Section 6: How It Works**
Heading: "## How It Works"
Brief 5-step description (one line each, no code):
1. **Sign** -- Device generates an Ed25519 keypair and signs data at the point of capture
2. **Encrypt** -- Data is chunked and each chunk is encrypted with AES-256-GCM
3. **Wrap** -- Chunks, manifest, and signature are packaged into a `.trst` archive with a BLAKE3 continuity chain
4. **Verify** -- An independent verification service checks the signature, chain integrity, and manifest
5. **Receipt** -- A cryptographic receipt is issued proving the data was verified at a specific time

**Section 7: Architecture (brief)**
Heading: "## Architecture"
One paragraph: "TrustEdge is a Rust workspace with 9 crates organized as a monolithic core library with thin CLI and WASM shells, plus a platform verification service with PostgreSQL backend and SvelteKit dashboard."

Link: "For crate breakdown, module hierarchy, data flow, and testing details, see [docs/architecture.md](docs/architecture.md)."

Link to YubiKey: "For hardware-backed signing with YubiKey PIV, see [docs/yubikey-guide.md](docs/yubikey-guide.md)."

**Section 8: Commercial Support**
Keep the existing commercial support content but make it brief:
Heading: "## Commercial Support"
"Building edge devices that need cryptographic provenance? We offer commercial SDK, custom hardware integration, fleet management, and compliance consulting."
"Contact: pilot@trustedgelabs.com"

**Section 9: License**
Heading: "## License"
"Mozilla Public License 2.0. See [LICENSE](LICENSE) for details."
"Commercial licenses available for enterprise use. Contact: enterprise@trustedgelabs.com"

Copyright line: "(c) 2025 TrustEdge Labs LLC"

Footer: "*TrustEdge -- Privacy and trust at the edge.*"

**Important rules:**
- Do NOT use emoji anywhere in the README (per CLAUDE.md: "No emoji in code")
- Use plain text markers if needed (dashes, asterisks)
- Keep the total README under 200 lines (target ~150-180)
- Do NOT include release history / changelog (that belongs in CHANGELOG or releases)
- Do NOT include test suite details (moved to docs/architecture.md)
- Do NOT include detailed security properties (link to SECURITY.md instead)
- Do NOT include contributing guide details (link to CONTRIBUTING.md instead)
  </action>
  <verify>
    <automated>grep -c "Prove\|Quick Start\|docker compose\|demo.sh\|Drone\|Sensor\|Body Cam\|Audio Capture\|docs/architecture.md\|docs/yubikey-guide.md" README.md | xargs test 10 -le</automated>
  </verify>
  <acceptance_criteria>
- README.md has copyright header (grep "Copyright (c) 2025 TRUSTEDGE LABS LLC" README.md succeeds)
- Version badge shows 2.0 (grep "version-2.0" README.md succeeds)
- Problem statement present (grep "tamper" README.md succeeds)
- Quick start has 3 commands (grep "git clone" README.md and grep "docker compose" README.md and grep "demo.sh" README.md all succeed)
- 4 use cases present (grep "Drone Inspection" README.md and grep "Sensor Logs" README.md and grep "Body Cam" README.md and grep "Audio Capture" README.md all succeed)
- Use cases have trst wrap commands (grep "trst wrap" README.md succeeds)
- Architecture links to docs (grep "docs/architecture.md" README.md succeeds)
- YubiKey links to docs (grep "docs/yubikey-guide.md" README.md succeeds)
- No emoji in file (grep -P "[\x{1F300}-\x{1F9FF}]" README.md returns no matches)
- File is under 200 lines (wc -l README.md shows less than 200)
- README is self-contained -- no "see X for what TrustEdge does" redirects for core content (DOCS-04)
  </acceptance_criteria>
  <done>README.md is rewritten with problem statement, 3-command quick start, 4 use cases with copy-paste trst commands, brief architecture with links to docs/, and no emoji. Total under 200 lines. A developer evaluator can understand TrustEdge and run the demo in under 5 minutes.</done>
</task>

</tasks>

<verification>
- README.md opens with problem statement explaining cryptographic provenance
- Quick start shows clone, docker-compose, demo.sh as 3 sequential commands
- 4 use cases each have a scenario description and a trst wrap command with --data-type, --source, --description flags
- Architecture section links to docs/architecture.md
- YubiKey section links to docs/yubikey-guide.md
- No emoji used anywhere in the file
- File is concise (under 200 lines)
</verification>

<success_criteria>
- A new user reading README.md can answer "what does TrustEdge do?" from the first section
- A new user can run the demo by copying the 3 quick start commands
- A new user can see how TrustEdge applies to their use case from the 4 examples
- Architecture details are accessible via docs/ links, not cluttering the README
</success_criteria>

<output>
After completion, create `.planning/phases/41-documentation/41-02-SUMMARY.md`
</output>
