<!--
Copyright (c) 2025 TRUSTEDGE LABS LLC
MPL-2.0: https://mozilla.org/MPL/2.0/
Project: trustedge — Privacy and trust at the edge.
GitHub: https://github.com/TrustEdge-Labs/trustedge
-->


---
phase: 40-demo-script
plan: 01
subsystem: cli
tags: [trst, keygen, ed25519, clap, acceptance-tests, tdd]

# Dependency graph
requires: []
provides:
  - trst keygen subcommand generating Ed25519 device key pairs (--out-key / --out-pub)
  - Key files in ed25519:BASE64 format compatible with trst wrap and trst verify
  - 3 acceptance tests covering creates-files, roundtrip, no-overwrite behavior
affects: [40-02-demo-script, any plan that documents or uses trst keygen]

# Tech tracking
tech-stack:
  added: []
  patterns:
    - "handle_keygen follows same sync Result<()> pattern as handle_wrap / handle_verify"
    - "Overwrite guard: check path.exists() before writing; bail! with 'Refusing to overwrite' message"

key-files:
  created: []
  modified:
    - crates/trst-cli/src/main.rs
    - crates/trst-cli/tests/acceptance.rs

key-decisions:
  - "handle_keygen checks both --out-key and --out-pub before generating: fail fast before any crypto work"
  - "Keygen is sync (not async) consistent with handle_wrap and handle_verify; EmitRequest is the only async command"

patterns-established:
  - "TDD RED-GREEN: write failing acceptance tests first, then implement, then cargo fmt"

requirements-completed: [DEMO-04]

# Metrics
duration: 10min
completed: 2026-03-15
---

# Phase 40 Plan 01: trst keygen Subcommand Summary

**`trst keygen --out-key K --out-pub P` added to trst-cli: generates Ed25519 device key pairs in ed25519:BASE64 format, refuses to overwrite existing files, compatible with existing wrap/verify lifecycle**

## Performance

- **Duration:** ~10 min
- **Started:** 2026-03-15T23:38:00Z
- **Completed:** 2026-03-15T23:48:27Z
- **Tasks:** 1 (TDD: RED + GREEN commits)
- **Files modified:** 2

## Accomplishments
- Added `trst keygen --out-key <PATH> --out-pub <PATH>` subcommand to trst-cli
- KeygenCmd struct with two required PathBuf args; handle_keygen with DeviceKeypair::generate() + overwrite guard
- 3 new acceptance tests: creates_files, roundtrip (keygen->wrap->verify), no_overwrite
- All 14 acceptance tests pass (11 existing + 3 new); clippy and cargo fmt clean

## Task Commits

Each task was committed atomically using TDD:

1. **Task 1 (RED): Add failing acceptance tests** - `b64fd76` (test)
2. **Task 1 (GREEN): Implement keygen subcommand** - `ed049d5` (feat)

**Plan metadata:** (docs commit below)

_Note: TDD tasks have two commits (test RED then feat GREEN)_

## Files Created/Modified
- `crates/trst-cli/src/main.rs` - Added KeygenCmd struct, Keygen variant in Commands enum, handle_keygen function
- `crates/trst-cli/tests/acceptance.rs` - Added acceptance_keygen_creates_files, acceptance_keygen_roundtrip, acceptance_keygen_no_overwrite

## Decisions Made
- handle_keygen checks both output paths before generating the keypair (fail fast, no partial writes)
- Keygen is synchronous, consistent with handle_wrap/handle_verify; only EmitRequest uses async
- Overwrite error message contains "overwrite" so tests can assert on stderr

## Deviations from Plan

None - plan executed exactly as written.

## Issues Encountered
- cargo fmt reformatted KeygenCmd arg attributes from single-line to multi-line; pre-commit hook caught it and blocked commit. Fixed by running `cargo fmt -p trustedge-trst-cli` before re-staging.

## User Setup Required

None - no external service configuration required.

## Next Phase Readiness
- `trst keygen` is ready for use in demo script (Plan 02)
- Key files generated by keygen are drop-in compatible with `trst wrap --device-key` and `trst verify --device-pub`

## Self-Check: PASSED

- crates/trst-cli/src/main.rs: FOUND
- crates/trst-cli/tests/acceptance.rs: FOUND
- .planning/phases/40-demo-script/40-01-SUMMARY.md: FOUND
- Commit b64fd76 (test RED): FOUND
- Commit ed049d5 (feat GREEN): FOUND

---
*Phase: 40-demo-script*
*Completed: 2026-03-15*
