---
phase: 74-release-documentation
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - CLAUDE.md
  - docs/README.md
  - docs/architecture.md
  - docs/yubikey-guide.md
  - docs/user/cli.md
  - docs/user/examples.md
  - docs/user/authentication.md
  - docs/developer/development.md
  - docs/developer/testing.md
  - docs/technical/format.md
autonomous: true
requirements: [DOCS-02]

must_haves:
  truths:
    - "CLAUDE.md CLI binary tables match actual binary flags and subcommands in the codebase"
    - "CLAUDE.md feature flag tables match actual Cargo.toml feature definitions"
    - "docs/ user guides reference current CLI syntax including --unencrypted flag"
    - "Architecture docs reflect current crate structure (9 crates + types)"
  artifacts:
    - path: "CLAUDE.md"
      provides: "Comprehensive developer/AI reference"
      contains: "RECEIPT_TTL_SECS"
    - path: "docs/user/cli.md"
      provides: "CLI command reference"
      contains: "--unencrypted"
    - path: "docs/architecture.md"
      provides: "System architecture documentation"
      contains: "trustedge-types"
  key_links:
    - from: "CLAUDE.md"
      to: "crates/*/Cargo.toml"
      via: "feature flag documentation"
      pattern: "Feature.*Purpose"
    - from: "docs/user/cli.md"
      to: "crates/trst-cli/src/main.rs"
      via: "CLI command documentation"
      pattern: "trst"
---

<objective>
Audit and update CLAUDE.md and the docs/ directory to accurately reflect v3.0 codebase state.

Purpose: Developer reference (CLAUDE.md) and user/technical documentation (docs/) must be consistent with actual code so contributors and users have reliable reference material.
Output: Accurate CLAUDE.md and docs/ files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-release-documentation/74-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and update CLAUDE.md for v3.0 accuracy</name>
  <files>CLAUDE.md</files>
  <read_first>
    CLAUDE.md
    Cargo.toml (workspace members, version)
    crates/trustedge-cli/Cargo.toml (features)
    crates/core/Cargo.toml (features)
    crates/platform/Cargo.toml (features)
    crates/trst-cli/Cargo.toml (features, bin name)
    crates/trst-cli/src/main.rs (subcommands)
    crates/trustedge-cli/src/main.rs (subcommands)
    crates/core/src/bin/trustedge-server.rs (server binary)
    crates/core/src/bin/trustedge-client.rs (client binary)
    crates/platform-server/Cargo.toml (bin name)
  </read_first>
  <action>
Read CLAUDE.md and verify every table, command, and description against the current codebase. Per D-02, fix only what is inaccurate.

Sections to verify (per D-01):

1. **Build and Test Commands**: Verify all cargo test commands work. Check test counts are approximately current (was 423+ — verify with `cargo test --workspace 2>&1 | tail -5`). Verify feature flag combinations in test commands.

2. **Architecture Overview**: Verify the 9-crate list matches actual workspace members in root Cargo.toml. Verify trustedge-types description. Check trustedge-platform feature list matches its Cargo.toml.

3. **Key Modules table**: Verify module paths exist under crates/core/src/. Check for any renamed or removed modules.

4. **Feature Flags tables**: Cross-reference trustedge-core features with crates/core/Cargo.toml [features]. Cross-reference trustedge-platform features with crates/platform/Cargo.toml [features]. Add any missing features, remove any that no longer exist.

5. **CLI Binaries table**: Verify each binary name and source path. Check `trustedge-server` binary still exists at listed path. Verify `trst` binary source path.

6. **Working with Archives section**: Verify all `cargo run` commands use current flags. Encrypted key handling (--unencrypted) must be reflected. The emit-request example should match current CLI.

7. **Common Tasks / Network Testing**: Verify server/client commands match current CLI flags.

8. **Platform environment variables**: If CLAUDE.md documents platform config, verify RECEIPT_TTL_SECS and PORT strict validation are mentioned.

Do NOT: Rewrite CLAUDE.md from scratch. Preserve existing structure. Only fix inaccuracies.
  </action>
  <verify>
    <automated>grep -c "RECEIPT_TTL_SECS\|trustedge-types\|unencrypted" CLAUDE.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "trustedge-types" CLAUDE.md returns at least 1 match (types crate documented)
    - grep "--unencrypted" CLAUDE.md returns at least 2 matches (keygen and wrap examples)
    - Feature flag tables in CLAUDE.md match actual Cargo.toml [features] sections (executor cross-references)
    - CLI binary table source paths match actual file locations (executor verifies with ls)
    - All `cargo run` commands in CLAUDE.md use valid flags against current CLI
  </acceptance_criteria>
  <done>CLAUDE.md CLI tables, feature flags, architecture overview, and command examples all match the current v3.0 codebase</done>
</task>

<task type="auto">
  <name>Task 2: Audit and update docs/ directory for v3.0 accuracy</name>
  <files>docs/README.md, docs/architecture.md, docs/yubikey-guide.md, docs/user/cli.md, docs/user/examples.md, docs/user/authentication.md, docs/developer/development.md, docs/developer/testing.md, docs/technical/format.md</files>
  <read_first>
    docs/README.md
    docs/architecture.md
    docs/yubikey-guide.md
    docs/user/cli.md
    docs/user/examples.md
    docs/user/authentication.md
    docs/developer/development.md
    docs/developer/testing.md
    docs/technical/format.md
    docs/technical/protocol.md
    docs/technical/universal-backend.md
    docs/technical/threat-model.md
    docs/user/troubleshooting.md
    docs/developer/coding-standards.md
    crates/trst-cli/src/main.rs (verify CLI docs match)
  </read_first>
  <action>
Read each docs/ file and verify accuracy against the current codebase. Per D-02, only fix inaccurate content. Per D-01, audit ALL docs/ files.

Priority files (most likely to have drift since v2.0):

1. **docs/user/cli.md** (384 lines): Verify all trst and trustedge CLI commands and flags. Must reflect encrypted key handling (--unencrypted), named profiles (sensor/audio/log from v2.1), unwrap command (v2.1). This is the most likely to be outdated.

2. **docs/user/examples.md** (42 lines): Verify examples use current syntax. Short file, quick check.

3. **docs/user/authentication.md** (874 lines): Verify auth flow descriptions match current implementation. Check for references to encrypted key files.

4. **docs/architecture.md** (229 lines): Verify crate descriptions match current workspace. Must mention trustedge-types. Check for removed/renamed crates.

5. **docs/developer/development.md** (627 lines): Verify build commands, test commands, development workflow instructions.

6. **docs/developer/testing.md** (1231 lines): Verify test command examples and test count references.

7. **docs/technical/format.md** (452 lines): Verify .trst archive format description. Check for Envelope::hash() Result return type change (v3.0).

8. **docs/yubikey-guide.md** (113 lines): Quick check — YubiKey workflow unlikely to have changed.

9. **docs/README.md** (240 lines): Verify docs index/navigation is accurate.

Lower priority (read but likely unchanged):
- docs/technical/protocol.md, universal-backend.md, threat-model.md
- docs/developer/coding-standards.md, testing-patterns.md, wasm-testing.md
- docs/user/troubleshooting.md
- docs/hardware/SECURE_NODE_MVP.md
- docs/legal/* (skip unless clearly wrong)

For each file: read it, identify inaccuracies, fix them. If a file is already accurate, leave it unchanged.

Do NOT: Reorganize docs/ structure. Do NOT rewrite files from scratch. Do NOT change legal docs unless factually wrong.
  </action>
  <verify>
    <automated>grep -c "unencrypted\|unwrap" docs/user/cli.md && grep -c "trustedge-types" docs/architecture.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "--unencrypted" docs/user/cli.md returns at least 1 match (encrypted key handling documented)
    - grep "unwrap" docs/user/cli.md returns at least 1 match (v2.1 unwrap command documented)
    - grep "trustedge-types" docs/architecture.md returns at least 1 match (types crate in architecture)
    - docs/user/examples.md commands use current flag syntax
    - No references to removed features or pre-v2.2 key handling patterns in any docs/ file
  </acceptance_criteria>
  <done>All docs/ files accurately reflect v3.0 codebase: CLI references current flags, architecture lists current crates, format docs reflect Envelope::hash() Result change, user guides reference encrypted keys</done>
</task>

</tasks>

<verification>
1. `grep "trustedge-types" CLAUDE.md docs/architecture.md` shows types crate documented in both
2. `grep "\-\-unencrypted" CLAUDE.md docs/user/cli.md` shows encrypted key escape hatch in both
3. `grep "unwrap" docs/user/cli.md` shows v2.1 unwrap command documented
4. Feature flag tables in CLAUDE.md match `grep "\[features\]" -A 20 crates/platform/Cargo.toml`
5. No files reference removed features or outdated flag syntax
</verification>

<success_criteria>
- CLAUDE.md CLI tables match actual binary flags and subcommands
- CLAUDE.md feature flag tables match actual Cargo.toml features
- docs/user/cli.md reflects current CLI with --unencrypted and unwrap
- docs/architecture.md reflects current 9-crate workspace
- All docs/ files checked for accuracy, only inaccurate content updated
</success_criteria>

<output>
After completion, create `.planning/phases/74-release-documentation/74-02-SUMMARY.md`
</output>
