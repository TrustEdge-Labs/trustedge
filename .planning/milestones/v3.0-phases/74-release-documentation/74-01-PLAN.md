<!--
Copyright (c) 2025 TRUSTEDGE LABS LLC
MPL-2.0: https://mozilla.org/MPL/2.0/
Project: trustedge — Privacy and trust at the edge.
GitHub: https://github.com/TrustEdge-Labs/trustedge
-->


---
phase: 74-release-documentation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - scripts/demo.sh
  - deploy/.env.example
autonomous: true
requirements: [DOCS-01]

must_haves:
  truths:
    - "README quick-start commands run against current codebase without modification"
    - "Demo script instructions produce expected output from clean checkout"
    - "deploy/.env.example has accurate usage instructions matching docker-compose.yml"
  artifacts:
    - path: "README.md"
      provides: "User-facing project overview and quick start"
      contains: "TRUSTEDGE-KEY-V1"
    - path: "scripts/demo.sh"
      provides: "End-to-end demo walkthrough"
      contains: "--unencrypted"
    - path: "deploy/.env.example"
      provides: "Environment variable template for Docker deployment"
      contains: "POSTGRES_PASSWORD"
  key_links:
    - from: "README.md"
      to: "scripts/demo.sh"
      via: "demo instructions reference"
      pattern: "demo\\.sh"
    - from: "README.md"
      to: "deploy/.env.example"
      via: "Docker quick-start instructions"
      pattern: "env.example"
---

<objective>
Update README.md and demo-related files to accurately reflect the v3.0 codebase state.

Purpose: Users following README quick-start or demo instructions must succeed without hitting outdated commands or missing steps. This is the first thing new users see.
Output: Accurate README.md, demo script, and env template.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/74-release-documentation/74-CONTEXT.md
@.planning/phases/73-deployment-hardening/73-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Audit and update README.md for v3.0 accuracy</name>
  <files>README.md</files>
  <read_first>
    README.md
    CLAUDE.md (reference for current CLI commands and architecture)
    Cargo.toml (workspace version)
    crates/trst-cli/src/main.rs (current trst subcommands and flags)
    crates/trustedge-cli/src/main.rs (current trustedge CLI flags)
    deploy/docker-compose.yml (current Docker quick-start)
    deploy/.env.example (current env template)
  </read_first>
  <action>
Read README.md and compare every command, flag, and description against the current codebase. Per D-02, focus on accuracy — do not rewrite sections that are already correct.

Key areas to verify and fix if inaccurate (per D-01):
1. **Quick-start commands**: Verify `trst keygen`, `trst wrap`, `trst verify` commands match current CLI. Since v2.2, keygen produces encrypted key files by default (TRUSTEDGE-KEY-V1). Quick-start examples should use `--unencrypted` for simplicity OR note the passphrase prompt.
2. **Docker quick-start**: Must include `cp deploy/.env.example deploy/.env` step before `docker compose up` (v3.0 change from Phase 73). Verify compose file path matches.
3. **Feature list / use cases**: Verify listed features exist. Add any major features missing since v2.0: encrypted keys at rest, named profiles (sensor/audio/log), trst unwrap, configurable receipt TTL.
4. **Architecture summary**: Verify crate names and descriptions match current workspace (9 crates). Check that trustedge-types is mentioned (added v1.5).
5. **Version references**: Update any hardcoded version strings if present.
6. **CLI binary table**: Verify binary names and purposes match CLAUDE.md and actual Cargo.toml bin entries.

Do NOT: Rewrite sections that are already accurate. Do NOT add new sections unless critical information is missing.
  </action>
  <verify>
    <automated>grep -c "unencrypted\|env.example\|TRUSTEDGE-KEY" README.md</automated>
  </verify>
  <acceptance_criteria>
    - grep "\.env\.example" README.md returns at least 1 match (Docker quick-start references env setup)
    - grep "unencrypted\|passphrase" README.md returns at least 1 match (encrypted keys acknowledged)
    - All `cargo run` and `trst` commands in README.md are valid against current CLI (manual spot-check by executor)
    - No references to removed features or pre-v2.2 key handling
  </acceptance_criteria>
  <done>README.md accurately reflects v3.0 codebase: quick-start commands work, Docker instructions include env setup step, encrypted key behavior documented, feature list current</done>
</task>

<task type="auto">
  <name>Task 2: Audit and update demo script and deploy env template</name>
  <files>scripts/demo.sh, deploy/.env.example</files>
  <read_first>
    scripts/demo.sh
    deploy/.env.example
    deploy/docker-compose.yml
    crates/trst-cli/src/main.rs (verify trst subcommands used in demo)
  </read_first>
  <action>
Read scripts/demo.sh and verify all CLI invocations match current trst binary flags. Per D-02, fix only what is inaccurate.

For scripts/demo.sh:
1. **trst keygen**: Must use `--unencrypted` flag since demo runs non-interactively (no passphrase prompt). Verify flags match current CLI.
2. **trst wrap**: Must use `--unencrypted` if key file is unencrypted. Verify profile flags (--profile cam.video, sensor, etc.) match current CLI.
3. **trst verify**: Verify flag syntax matches current CLI.
4. **trst unwrap**: If demo includes unwrap step, verify it uses `--unencrypted`. If demo does NOT include unwrap, consider adding it since unwrap was added in v2.1.
5. **Docker compose commands**: If demo references docker compose, verify it includes the `.env` copy step.
6. **Output messages**: Verify any printed descriptions match current behavior.

For deploy/.env.example:
1. Verify header/usage instructions are accurate (should reference `cp .env.example .env` workflow).
2. Verify all env vars listed match what docker-compose.yml actually reads.
3. Verify RECEIPT_TTL_SECS is documented (v3.0 addition).
4. Verify PORT env var is documented with note about strict validation (v3.0 addition).

Do NOT: Rewrite demo script structure. Do NOT change .env.example defaults that are already correct from Phase 73.
  </action>
  <verify>
    <automated>grep -c "\-\-unencrypted" scripts/demo.sh && grep -c "RECEIPT_TTL_SECS\|PORT" deploy/.env.example</automated>
  </verify>
  <acceptance_criteria>
    - grep "--unencrypted" scripts/demo.sh returns at least 1 match (demo uses non-interactive key handling)
    - All trst commands in demo.sh use flags that exist in current CLI (executor verifies against trst --help)
    - grep "RECEIPT_TTL_SECS" deploy/.env.example returns at least 1 match (v3.0 config documented)
    - deploy/.env.example variables match docker-compose.yml env_file consumption
  </acceptance_criteria>
  <done>Demo script runs successfully with current CLI flags, uses --unencrypted for non-interactive operation, and deploy/.env.example documents all v3.0 env vars including RECEIPT_TTL_SECS and PORT</done>
</task>

</tasks>

<verification>
1. `grep -E "env.example|unencrypted|passphrase" README.md` shows references to v3.0 patterns
2. `bash -n scripts/demo.sh` passes (no syntax errors)
3. `grep "RECEIPT_TTL_SECS" deploy/.env.example` finds the v3.0 config var
4. No references to removed or renamed CLI flags in any modified file
</verification>

<success_criteria>
- README quick-start commands match current CLI behavior
- Docker instructions include env file setup step
- Demo script uses --unencrypted for non-interactive operation
- deploy/.env.example documents all platform env vars
</success_criteria>

<output>
After completion, create `.planning/phases/74-release-documentation/74-01-SUMMARY.md`
</output>
