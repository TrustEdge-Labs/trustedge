---
phase: 88-external-action-product-website
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - actions/attest-sbom-action/action.yml
  - actions/attest-sbom-action/README.md
  - actions/sealedge-attest-sbom-action/action.yml
  - actions/sealedge-attest-sbom-action/README.md
  - actions/sealedge-attest-sbom-action/LICENSE
  - .planning/REQUIREMENTS.md
autonomous: true
requirements:
  - EXT-02
  - EXT-03
tags:
  - rebrand
  - action-source
  - rename-in-place
  - grep-audit
  - phase-88
must_haves:
  truths:
    - "action.yml references `seal` binary (not `trst`), `seal-version` input (not `trst-version`), `Sealedge SBOM Attestation` name, `/TrustEdge-Labs/sealedge/releases` base URL — all substring-swaps per D-01/D-03/D-13 and PATTERNS.md Group A.1"
    - "action.yml's SHA256 verification block (T2 mitigation) still runs `sha256sum` against the downloaded binary, but now fetches `seal.sha256` instead of `trst.sha256`"
    - "action.yml's ephemeral keypair generation step (T4 mitigation) is preserved structurally — only binary name (`trst` → `seal`) and env var names (`TRST_KEY`/`TRST_PUB` → `SEAL_KEY`/`SEAL_PUB`) change"
    - "README.md carries a short `**Renamed from ...**` notice near the top per D-08 and all product-name / URL / example-YAML substrings are swept per D-15 / Phase 85 casing rules"
    - "Monorepo folder is renamed from `actions/attest-sbom-action/` to `actions/sealedge-attest-sbom-action/` via `git mv` per D-06 (git rename tracking preserved)"
    - "`.planning/REQUIREMENTS.md` §Ext EXT-02/EXT-03 wording is amended per D-02 to describe the rename-in-place approach (not the obsolete two-repo migration story)"
    - "Grep audits pass: `grep -c 'trst' actions/sealedge-attest-sbom-action/action.yml` == 0 for binary/input refs; `grep -c 'trustedge' actions/sealedge-attest-sbom-action/action.yml` == 0 (allowlisting header copyright update)"
  artifacts:
    - path: actions/sealedge-attest-sbom-action/action.yml
      provides: "Composite-action YAML with all sealedge/seal substrings substituted; SHA256 verification + ephemeral keygen paths preserved"
      contains: "Sealedge SBOM Attestation"
    - path: actions/sealedge-attest-sbom-action/README.md
      provides: "Public README with top-of-README 'Renamed from attest-sbom-action' notice + full product-name/URL sweep"
      contains: "Renamed from"
    - path: .planning/REQUIREMENTS.md
      provides: "EXT-02/EXT-03 reworded per D-02 suggested language in CONTEXT.md §Specifics"
      contains: "sealedge-attest-sbom-action"
  key_links:
    - from: "actions/sealedge-attest-sbom-action/action.yml (input `seal-version`)"
      to: "https://github.com/TrustEdge-Labs/sealedge/releases (release URL base)"
      via: "composite shell step `curl -fsSL ${BASE_URL}/seal`"
      pattern: "curl.*sealedge/releases.*seal"
    - from: "actions/sealedge-attest-sbom-action/action.yml (SHA256 verification block)"
      to: "seal.sha256 checksum artifact on release"
      via: "curl + sha256sum + expected/actual hash comparison"
      pattern: "sha256sum.*seal"
    - from: "actions/sealedge-attest-sbom-action/README.md (top-of-README notice)"
      to: "old repo URL and @v1 tag explanation (via GitHub 301 redirect once Plan 03 runs)"
      via: "prose notice + unchanged `@v1` frozen tag reference"
      pattern: "Renamed from"
---

<objective>
Update the composite GitHub Action source in-place to reference sealedge/seal (binary name, input name, release URL, marketplace metadata), rename the monorepo folder `actions/attest-sbom-action/` → `actions/sealedge-attest-sbom-action/` via `git mv`, and amend REQUIREMENTS.md §Ext (EXT-02 / EXT-03) to match the rename-in-place approach. This plan lands the in-monorepo content + structural changes that Plan 03 then ships externally via `gh repo rename` + `@v2` tag cut.

Purpose: EXT-02 (new repo-name semantics, preserved SHA256 verification, rebranded binary/input/metadata) + EXT-03 (README pointer/notice for migration) — both delivered in-repo during Phase 88 so Plan 03's external op ships a coherent source tree. The folder rename + content sweep mirrors Phase 87 Plan 01's straggler-file content update pattern (atomic, grep-clean, no YAML reflow).

Output: Renamed folder `actions/sealedge-attest-sbom-action/` with fully swept `action.yml` + `README.md`; `.planning/REQUIREMENTS.md` EXT-02/03 reworded; all committed as 3 atomic commits (content sweep → folder rename → REQUIREMENTS amendment).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/88-external-action-product-website/88-CONTEXT.md
@.planning/phases/88-external-action-product-website/88-PATTERNS.md
@.planning/phases/87-github-repository-rename/87-01-SUMMARY.md
@CLAUDE.md
@actions/attest-sbom-action/action.yml
@actions/attest-sbom-action/README.md

<interfaces>
<!-- Exact substitution tables, verbatim commands, and grep assertions. Executor does NOT need to re-read PATTERNS.md's full excerpt tables; the substitution list below is exhaustive for action.yml. -->

# --- action.yml substitution table (exhaustive, 15-18 line-local edits, NO line insertions/deletions except new blank lines only if already present) ---

Line 3:   `# Project: trustedge — Privacy and trust at the edge.`
    →     `# Project: sealedge — Privacy and trust at the edge.`

Line 5:   `name: 'TrustEdge SBOM Attestation'`
    →     `name: 'Sealedge SBOM Attestation'`

Line 6:   `description: 'Attest a binary artifact with its CycloneDX SBOM using TrustEdge'`
    →     `description: 'Attest a binary artifact with its CycloneDX SBOM using Sealedge'`

Line 23:  `  trst-version:`
    →     `  seal-version:`

Line 24:  `    description: 'TrustEdge release version to download (e.g., v4.0.0)'`
    →     `    description: 'Sealedge release version to download (e.g., v6.0.0)'`

Line 36:  `    - name: Download trst binary`
    →     `    - name: Download seal binary`

Line 39:  `        VERSION="${{ inputs.trst-version }}"`
    →     `        VERSION="${{ inputs.seal-version }}"`

Line 41:  `          BASE_URL="https://github.com/TrustEdge-Labs/trustedge/releases/latest/download"`
    →     `          BASE_URL="https://github.com/TrustEdge-Labs/sealedge/releases/latest/download"`

Line 43:  `          BASE_URL="https://github.com/TrustEdge-Labs/trustedge/releases/download/${VERSION}"`
    →     `          BASE_URL="https://github.com/TrustEdge-Labs/sealedge/releases/download/${VERSION}"`

Line 47:  `        curl -fsSL "${BASE_URL}/trst" -o "${{ runner.temp }}/trst"`
    →     `        curl -fsSL "${BASE_URL}/seal" -o "${{ runner.temp }}/seal"`

Line 48:  `        chmod +x "${{ runner.temp }}/trst"`
    →     `        chmod +x "${{ runner.temp }}/seal"`

Line 51:  `        if curl -fsSL "${BASE_URL}/trst.sha256" -o "${{ runner.temp }}/trst.sha256" 2>/dev/null; then`
    →     `        if curl -fsSL "${BASE_URL}/seal.sha256" -o "${{ runner.temp }}/seal.sha256" 2>/dev/null; then`

Line 53:  `          EXPECTED_HASH=$(awk '{print $1}' "${{ runner.temp }}/trst.sha256")`
    →     `          EXPECTED_HASH=$(awk '{print $1}' "${{ runner.temp }}/seal.sha256")`

Line 54:  `          ACTUAL_HASH=$(sha256sum "${{ runner.temp }}/trst" | awk '{print $1}')`
    →     `          ACTUAL_HASH=$(sha256sum "${{ runner.temp }}/seal" | awk '{print $1}')`

Line 68:  `        "${{ runner.temp }}/trst" keygen \`
    →     `        "${{ runner.temp }}/seal" keygen \`

Line 72:  `        echo "TRST_KEY=${{ runner.temp }}/ephemeral.key" >> "$GITHUB_ENV"`
    →     `        echo "SEAL_KEY=${{ runner.temp }}/ephemeral.key" >> "$GITHUB_ENV"`

Line 73:  `        echo "TRST_PUB=${{ runner.temp }}/ephemeral.pub" >> "$GITHUB_ENV"`
    →     `        echo "SEAL_PUB=${{ runner.temp }}/ephemeral.pub" >> "$GITHUB_ENV"`

Line 79:  `        echo "TRST_KEY=${{ inputs.key }}" >> "$GITHUB_ENV"`
    →     `        echo "SEAL_KEY=${{ inputs.key }}" >> "$GITHUB_ENV"`

Line 82:  `        echo "TRST_PUB=${PUB_PATH}" >> "$GITHUB_ENV"`
    →     `        echo "SEAL_PUB=${PUB_PATH}" >> "$GITHUB_ENV"`

Line 90:  `        "${{ runner.temp }}/trst" attest-sbom \`
    →     `        "${{ runner.temp }}/seal" attest-sbom \`

Line 93:  `          --device-key "$TRST_KEY" \`
    →     `          --device-key "$SEAL_KEY" \`

Line 94:  `          --device-pub "$TRST_PUB" \`
    →     `          --device-pub "$SEAL_PUB" \`

Line 30 (outputs.attestation-path.description) — LEAVE UNCHANGED: already says `.se-attestation.json` (Phase 84 landed this).

# --- README.md top-of-README notice to PREPEND (between current line 1 title and line 3 tagline) ---

Insert after line 1 title rewrite (`# Sealedge SBOM Attestation Action`), BEFORE line 3 (`> Attest ...`), a new blockquote block:

```markdown
> **Renamed from `TrustEdge-Labs/attest-sbom-action`.** This repo was renamed in v6.0 to match the Sealedge product name. `@v1` stays frozen as the pre-rebrand behavior; `@v2+` uses Sealedge binary/URL naming. GitHub's built-in 301 redirect covers existing `uses: TrustEdge-Labs/attest-sbom-action@v1` references.
```

# --- README.md substring sweep (product-name / URL / example-YAML; Phase 85 D-11–D-14 casing rules) ---

  `TrustEdge SBOM Attestation Action`        → `Sealedge SBOM Attestation Action`
  `using TrustEdge` (prose occurrences)       → `using Sealedge`
  `TrustEdge public verifier`                  → `Sealedge public verifier`
  `trst` (binary references, shell examples)   → `seal`
  `trst-version:` (YAML example)               → `seal-version:`
  `TRUSTEDGE_KEY` (GitHub Secret example name) → `SEALEDGE_KEY`
  `TrustEdge-Labs/attest-sbom-action@v1` (YAML examples) → `TrustEdge-Labs/sealedge-attest-sbom-action@v2`
  `https://github.com/TrustEdge-Labs/trustedge` (link URLs) → `https://github.com/TrustEdge-Labs/sealedge`
  `https://verify.trustedge.dev` (if present)  → `https://verify.sealedge.dev`
  `.trst` (archive extension prose, if any)    → `.seal`
  `.te-attestation.json`                        → LEAVE UNCHANGED if already `.se-attestation.json`; swap to `.se-attestation.json` only if a stale `.te-` reference exists

PRESERVE:
  - `TrustEdge Labs` (company brand) — stays
  - `TrustEdge-Labs` in GitHub org URLs — stays
  - `Copyright ... TRUSTEDGE LABS LLC` header if present — stays (Phase 85 D-03 carry-forward)

# --- folder rename command ---

git mv actions/attest-sbom-action actions/sealedge-attest-sbom-action

# --- REQUIREMENTS.md §Ext replacement (CONTEXT.md §Specifics D-02 verbatim) ---

REPLACE these two lines in `.planning/REQUIREMENTS.md` (currently lines 39-40):

  `- [ ] **EXT-02**: New GitHub Action repo created under sealedge naming and published to GitHub Marketplace (equivalent functionality to current \`attest-sbom-action\`); SHA256 checksum verification preserved`
  `- [ ] **EXT-03**: Old \`TrustEdge-Labs/attest-sbom-action\` marketplace listing marked deprecated with README redirect to the new listing`

WITH:

  `- [ ] **EXT-02**: The \`TrustEdge-Labs/attest-sbom-action\` repo is renamed to \`TrustEdge-Labs/sealedge-attest-sbom-action\` via \`gh repo rename\`; action source references sealedge/seal; a new \`@v2\` tag ships the rebranded action; SHA256 checksum verification of the downloaded binary is preserved`
  `- [ ] **EXT-03**: GitHub's built-in 301 redirect covers existing \`uses: TrustEdge-Labs/attest-sbom-action@v1\` references; the pre-rebrand \`@v1\` tag stays frozen; the post-rename README carries a short notice pointing readers to \`@v2\` and the renamed repo`

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sweep action.yml substrings in-place (22 line-local edits, no line insertions/deletions)</name>
  <files>actions/attest-sbom-action/action.yml</files>
  <read_first>
    - actions/attest-sbom-action/action.yml (read full file — 98 lines; confirms current line numbers match the substitution table below before editing)
    - .planning/phases/88-external-action-product-website/88-CONTEXT.md §D-03/D-06/D-13 (renaming substrings rationale), §Specifics (ephemeral-keygen + SHA256-check preservation)
    - .planning/phases/88-external-action-product-website/88-PATTERNS.md §Group A.1 (exhaustive substitution table + Phase 87 Plan 01 discipline quote: "targeted Edit, don't reflow YAML, substring swaps only")
    - .planning/phases/87-github-repository-rename/87-01-PLAN.md Task 1 (the analog — targeted Edit over sed for deterministic YAML changes)
  </read_first>
  <action>
Apply the substitution table from the `<interfaces>` block above — 22 line-local substring swaps across `actions/attest-sbom-action/action.yml`. Use the Edit tool per substitution (preferred over sed for deterministic results on YAML, per Phase 87 Plan 01 Task 1 discipline).

**Discipline rules (Phase 87 carry-forward):**
- Do NOT reflow YAML whitespace, indentation, or line breaks.
- Do NOT reorder steps, inputs, or outputs.
- Do NOT touch line 30 (`description: 'Path to generated .se-attestation.json file'`) — already correct (Phase 84 landed `.se-attestation.json`).
- Do NOT touch lines 2 (`# MPL-2.0: ...`) or 1 (`# Copyright ...`) — legal-entity copyright stays per Phase 85 D-03.
- Every edit is a pure substring swap. Line count before == line count after.

**Pre-edit sanity check** — before touching anything, confirm the current file matches the substitution table's left-hand-side by running the Read tool on the file and verifying line numbers. If line numbers drift by more than ±1 (e.g., someone inserted a blank line), STOP and re-derive the substitution table line numbers from the actual file state, then proceed.

**Commit** (after all edits pass the acceptance grep):

```bash
git add actions/attest-sbom-action/action.yml
git diff --cached --stat  # expect ~22 insertions / ~22 deletions; no line-count delta
git commit -m "$(cat <<'EOF'
fix(88): sweep action.yml substrings — trst→seal, URL→sealedge, metadata→Sealedge

Rename-in-place sweep of the composite action source per 88-CONTEXT.md D-01/D-03/D-13:
- Action name/description metadata → 'Sealedge SBOM Attestation'
- Input rename trst-version → seal-version (clean break, D-13)
- Binary fetch URL base → /TrustEdge-Labs/sealedge/releases/ (relies on Phase 87 rename)
- Binary name trst → seal in download, chmod, keygen, attest-sbom invocations
- Env var rename TRST_KEY/TRST_PUB → SEAL_KEY/SEAL_PUB (Phase 85 D-10 casing rule)
- SHA256 verification block fetches seal.sha256 (T2 mitigation preserved)
- Ephemeral keygen step preserved structurally (T4 mitigation preserved)
- Copyright header Project: trustedge → Project: sealedge (Phase 85 D-04)

Part of Phase 88 EXT-02. Folder rename to actions/sealedge-attest-sbom-action/
lands in the next commit.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <verify>
    <automated>
# Assertion battery (every check must pass):
set -e
F=actions/attest-sbom-action/action.yml

# Old strings are gone (binary/input/URL refs):
test "$(grep -c 'trst-version' "$F")" -eq 0
test "$(grep -c '/TrustEdge-Labs/trustedge/releases' "$F")" -eq 0
test "$(grep -c '/trst"' "$F")" -eq 0
test "$(grep -c 'trst.sha256' "$F")" -eq 0
test "$(grep -c 'TRST_KEY' "$F")" -eq 0
test "$(grep -c 'TRST_PUB' "$F")" -eq 0
test "$(grep -c "name: 'TrustEdge SBOM" "$F")" -eq 0
test "$(grep -c 'using TrustEdge' "$F")" -eq 0
test "$(grep -c '# Project: trustedge' "$F")" -eq 0

# New strings are present (binary/input/URL refs + metadata):
test "$(grep -c 'seal-version' "$F")" -ge 2
test "$(grep -c '/TrustEdge-Labs/sealedge/releases' "$F")" -ge 2
test "$(grep -c '/seal"' "$F")" -ge 2
test "$(grep -c 'seal.sha256' "$F")" -ge 2
test "$(grep -c 'SEAL_KEY' "$F")" -ge 3
test "$(grep -c 'SEAL_PUB' "$F")" -ge 3
test "$(grep -c "name: 'Sealedge SBOM" "$F")" -eq 1
test "$(grep -c 'using Sealedge' "$F")" -eq 1
test "$(grep -c '# Project: sealedge' "$F")" -eq 1

# SHA256 verification block (T2) is intact — sha256sum call still runs against the downloaded binary:
test "$(grep -c 'sha256sum.*runner\.temp.*/seal' "$F")" -ge 1

# Ephemeral keygen step (T4) is structurally preserved — keygen invocation exists:
test "$(grep -c 'seal" keygen' "$F")" -eq 1

# Legal-entity copyright header preserved (Phase 85 D-03):
test "$(grep -c '# Copyright (c) 2025 TRUSTEDGE LABS LLC' "$F")" -eq 1

# Line count unchanged (no reflow):
test "$(wc -l < "$F")" -eq 98

# Commit landed:
git log -1 --pretty=%s | grep -qE '^fix\(88\): sweep action\.yml'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'trst-version' actions/attest-sbom-action/action.yml` returns 0
    - `grep -c '/TrustEdge-Labs/trustedge/releases' actions/attest-sbom-action/action.yml` returns 0
    - `grep -c 'seal-version' actions/attest-sbom-action/action.yml` returns at least 2 (input definition + shell reference)
    - `grep -c '/TrustEdge-Labs/sealedge/releases' actions/attest-sbom-action/action.yml` returns at least 2 (latest URL + versioned URL)
    - `grep -c 'sha256sum.*runner\.temp.*/seal' actions/attest-sbom-action/action.yml` returns at least 1 (T2 mitigation preserved)
    - `grep -c 'seal" keygen' actions/attest-sbom-action/action.yml` returns exactly 1 (T4 ephemeral keygen preserved)
    - `grep -c '# Copyright (c) 2025 TRUSTEDGE LABS LLC' actions/attest-sbom-action/action.yml` returns exactly 1 (legal entity stays)
    - `wc -l < actions/attest-sbom-action/action.yml` returns exactly 98 (no line-count delta — no reflow)
    - Commit subject starts with `fix(88): sweep action.yml`
  </acceptance_criteria>
  <done>action.yml fully swept to sealedge/seal; all assertion greps pass; committed atomically; SHA256 verification block + ephemeral keygen step preserved.</done>
</task>

<task type="auto">
  <name>Task 2: Add top-of-README notice + sweep README.md product-name / URL / example-YAML substrings</name>
  <files>actions/attest-sbom-action/README.md</files>
  <read_first>
    - actions/attest-sbom-action/README.md (read full file — 129 lines; confirms current product-name / URL / example-YAML occurrences match the sweep targets below)
    - .planning/phases/88-external-action-product-website/88-CONTEXT.md §D-08 (top-of-README notice, ~1-3 lines, no full migration section), §D-15 (product vs company-brand distinction)
    - .planning/phases/88-external-action-product-website/88-PATTERNS.md §Group A.2 (MIGRATION.md D-03 top-notice analog + Phase 86 prose sweep D-06)
    - .planning/phases/86-documentation-sweep/86-CONTEXT.md §D-03 (MIGRATION.md hybrid treatment — the direct analog for top-of-README notice pattern)
  </read_first>
  <action>
**Step 2a — Prepend the "Renamed from" notice** between the title line and the tagline blockquote.

Current file state (first 3 lines):
```
# TrustEdge SBOM Attestation Action

> Attest a binary artifact with its CycloneDX SBOM using TrustEdge — one YAML line, cryptographic proof.
```

Transform to (first 6 lines):
```
# Sealedge SBOM Attestation Action

> **Renamed from `TrustEdge-Labs/attest-sbom-action`.** This repo was renamed in v6.0 to match the Sealedge product name. `@v1` stays frozen as the pre-rebrand behavior; `@v2+` uses Sealedge binary/URL naming. GitHub's built-in 301 redirect covers existing `uses: TrustEdge-Labs/attest-sbom-action@v1` references.

> Attest a binary artifact with its CycloneDX SBOM using Sealedge — one YAML line, cryptographic proof.
```

Use the Edit tool with `old_string` = current lines 1-3 and `new_string` = new lines 1-5. This is ONE Edit call covering both the title change and the notice insertion.

**Step 2b — Sweep remaining product-name / URL / example-YAML substrings** per the `<interfaces>` table above. Each is a separate Edit call:

1. `uses: TrustEdge-Labs/attest-sbom-action@v1` → `uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2` (at every occurrence, typically ~3 sites: Example 1, Example 2, How It Works section)
2. `trst-version:` in any YAML example → `seal-version:`
3. `trst keygen` / `trst` binary in shell examples → `seal keygen` / `seal`
4. `TRUSTEDGE_KEY` (GitHub Secret example name) → `SEALEDGE_KEY`
5. Any `TrustEdge-Labs/trustedge` URL → `TrustEdge-Labs/sealedge`
6. Any prose `TrustEdge` PRODUCT reference → `Sealedge` (but PRESERVE `TrustEdge Labs` company brand)
7. Any `verify.trustedge.dev` → `verify.sealedge.dev`
8. Any `.trst` archive extension prose → `.seal` (Phase 83 D-02 carry-forward)
9. Any lingering `.te-attestation.json` → `.se-attestation.json` (if still present; action.yml line 30 already says `.se-attestation.json`, so README should match)

**Preserve:**
- `TrustEdge Labs` company brand (any mention — Phase 85 D-03 legal-entity carry-forward)
- `TrustEdge-Labs/` org prefix in URLs (stays — Phase 85 D-02)
- Copyright notices mentioning `TRUSTEDGE LABS LLC` (stays — Phase 85 D-03)

**Step 2c — Post-sweep grep audit** (Phase 86 D-13 allowlisted pattern):

```bash
# Expected: zero hits (only legal-entity & org mentions should match, allowlisted out):
grep -n "TrustEdge\|trustedge\|TRUSTEDGE" actions/attest-sbom-action/README.md \
  | grep -vE "TrustEdge Labs|TrustEdge-Labs/sealedge|Renamed from \`TrustEdge-Labs/attest-sbom-action\`|uses: TrustEdge-Labs/attest-sbom-action@v1\` references" \
  || echo 'clean'
```

The allowlist intentionally covers (a) the company-brand `TrustEdge Labs` (stays), (b) the `TrustEdge-Labs/sealedge-*` org paths (stays — org brand), and (c) the top-notice's two prose callouts that reference the OLD repo name (which is intentional — that's the whole point of the notice). Anything else that matches `TrustEdge|trustedge|TRUSTEDGE` outside those allowlisted substrings is a miss — fix before committing.

**Step 2d — Commit:**

```bash
git add actions/attest-sbom-action/README.md
git diff --cached --stat
git commit -m "$(cat <<'EOF'
docs(88): sweep action README — top-of-README 'Renamed from' notice + product-name/URL sweep

Per 88-CONTEXT.md D-08 (notice) and D-15 (product vs company-brand sweep):
- Title: 'TrustEdge SBOM Attestation Action' → 'Sealedge SBOM Attestation Action'
- Prepend top-of-README blockquote notice pointing @v1 → @v2 and referencing
  GitHub's built-in 301 redirect
- Sweep example YAML: uses: TrustEdge-Labs/attest-sbom-action@v1 →
  TrustEdge-Labs/sealedge-attest-sbom-action@v2
- Sweep input name trst-version → seal-version in examples
- Sweep binary name trst → seal in shell examples
- Sweep GitHub Secret example name TRUSTEDGE_KEY → SEALEDGE_KEY
- Sweep product-name prose TrustEdge → Sealedge (preserving company brand
  'TrustEdge Labs' and org URLs 'TrustEdge-Labs/')
- Sweep repository links /TrustEdge-Labs/trustedge → /TrustEdge-Labs/sealedge

Part of Phase 88 EXT-02 + EXT-03. Folder rename + REQUIREMENTS.md amendment
follow in subsequent commits.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <verify>
    <automated>
set -e
F=actions/attest-sbom-action/README.md

# Top-of-README notice landed:
test "$(head -3 "$F" | grep -c 'Sealedge SBOM Attestation Action')" -eq 1
test "$(head -6 "$F" | grep -c 'Renamed from')" -eq 1
test "$(head -6 "$F" | grep -c '301 redirect')" -eq 1

# Old substrings gone (allowlisting the intentional notice references):
# Note: the notice contains `TrustEdge-Labs/attest-sbom-action@v1` — that's intentional
test "$(grep -c 'trst-version:' "$F")" -eq 0
test "$(grep -c '/TrustEdge-Labs/trustedge' "$F")" -eq 0
test "$(grep -c 'TRUSTEDGE_KEY' "$F")" -eq 0
test "$(grep -c 'verify\.trustedge\.dev' "$F")" -eq 0
# Example uses: lines should only reference the NEW repo name — count any remaining `uses: TrustEdge-Labs/attest-sbom-action@` outside the single notice line:
test "$(grep -cE '^- uses: TrustEdge-Labs/attest-sbom-action@' "$F")" -eq 0
test "$(grep -cE '^\s*uses: TrustEdge-Labs/attest-sbom-action@' "$F")" -eq 0

# New substrings present:
test "$(grep -c 'seal-version:' "$F")" -ge 1
test "$(grep -c 'TrustEdge-Labs/sealedge-attest-sbom-action@v2' "$F")" -ge 2
test "$(grep -c 'SEALEDGE_KEY' "$F")" -ge 1

# Grep-allowlist audit (Phase 86 D-13 pattern): any unmatched TrustEdge/trustedge/TRUSTEDGE hits beyond the allowlist should be zero.
HITS=$(grep -n "TrustEdge\|trustedge\|TRUSTEDGE" "$F" \
  | grep -vE "TrustEdge Labs|TrustEdge-Labs/sealedge|Renamed from|uses: TrustEdge-Labs/attest-sbom-action@v1" \
  || true)
test -z "$HITS"

# Commit landed:
git log -1 --pretty=%s | grep -qE '^docs\(88\): sweep action README'
    </automated>
  </verify>
  <acceptance_criteria>
    - Line 1 reads exactly `# Sealedge SBOM Attestation Action`
    - A blockquote containing `Renamed from` and `301 redirect` appears within the first 6 lines
    - `grep -c 'trst-version:' actions/attest-sbom-action/README.md` returns 0
    - `grep -c '/TrustEdge-Labs/trustedge' actions/attest-sbom-action/README.md` returns 0
    - `grep -c 'TrustEdge-Labs/sealedge-attest-sbom-action@v2' actions/attest-sbom-action/README.md` returns at least 2 (Example 1 + Example 2)
    - `grep -c 'seal-version:' actions/attest-sbom-action/README.md` returns at least 1
    - `grep -c 'SEALEDGE_KEY' actions/attest-sbom-action/README.md` returns at least 1
    - `grep -c 'TRUSTEDGE_KEY' actions/attest-sbom-action/README.md` returns 0
    - Allowlisted grep audit (see `<verify>` block) returns empty — no stale product refs outside the intentional notice callouts + legal-entity mentions
    - Commit subject starts with `docs(88): sweep action README`
  </acceptance_criteria>
  <done>README.md has top-of-README "Renamed from" notice; all product-name/URL/example-YAML substrings swept; legal-entity & org-brand callouts preserved; allowlisted grep audit is clean; commit landed.</done>
</task>

<task type="auto">
  <name>Task 3: Rename monorepo folder `actions/attest-sbom-action/` → `actions/sealedge-attest-sbom-action/` via `git mv`</name>
  <files>actions/sealedge-attest-sbom-action/action.yml, actions/sealedge-attest-sbom-action/README.md, actions/sealedge-attest-sbom-action/LICENSE</files>
  <read_first>
    - actions/attest-sbom-action/ (confirm directory contents: action.yml, README.md, LICENSE — 3 files)
    - .planning/phases/88-external-action-product-website/88-CONTEXT.md §D-06 (folder rename rationale, canonical source-of-truth), §"Claude's Discretion" → rename commit sequencing (step 2: folder rename as its own commit)
    - .planning/phases/88-external-action-product-website/88-PATTERNS.md §Group B (exact Phase 83 Plan 01 `git mv` analog)
    - .planning/phases/83-crate-and-binary-rename/83-01-PLAN.md (the canonical `git mv` + in-same-commit-update-inbound-refs pattern — adapted here: no workspace/tree refs to update since ci.yml's reference lands in Plan 02)
  </read_first>
  <action>
Run exactly:

```bash
# Confirm no orphan references in the monorepo that need same-commit updating:
grep -rn 'actions/attest-sbom-action' .github/ 2>/dev/null
# Expected: zero results (ci.yml's self-attest job does NOT currently `path:` into the action folder — it uses `./target/release/seal` and inline shell; Plan 02 adds the `uses:` reference in a separate commit). If this grep returns anything non-zero, STOP and triage before renaming — the move must be atomic with any inbound-reference update per Phase 83 Plan 01 discipline.

# Execute the rename (git mv preserves rename tracking, so `git log --follow` works post-rename):
git mv actions/attest-sbom-action actions/sealedge-attest-sbom-action

# Verify the directory state:
test -d actions/sealedge-attest-sbom-action
test ! -d actions/attest-sbom-action
ls actions/sealedge-attest-sbom-action/
# Expected: action.yml, LICENSE, README.md

# Confirm no file contents changed (Phase 83 D-07 discipline: mv-only commit):
git diff --cached --stat
# Expected: all three files show as renamed (RNN in status), no content diff

# Commit:
git commit -m "$(cat <<'EOF'
chore(88): rename actions/attest-sbom-action → actions/sealedge-attest-sbom-action

Per 88-CONTEXT.md D-06: the monorepo folder is canonical source-of-truth for
the GitHub Action. Rename mirrors the external repo rename that Plan 03 will
execute via `gh repo rename`.

Uses `git mv` to preserve rename tracking (git log --follow works post-rename).
No content changes in this commit; content sweep landed in prior commits
(fix(88): sweep action.yml … and docs(88): sweep action README …).

Part of Phase 88 EXT-02.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

**If the pre-commit grep turns up references** to `actions/attest-sbom-action/` in `.github/` or anywhere else in the monorepo that this plan doesn't own: STOP, report the hits, and escalate. Do NOT silently update those references in the rename commit — per Phase 83 Plan 01 discipline, folder-rename commits should only rename, not modify content. Any broader reference sweep belongs to a different plan (Plan 02 for ci.yml `uses:` additions; Plan 03 for external op).
  </action>
  <verify>
    <automated>
set -e

# Directory state:
test -d actions/sealedge-attest-sbom-action
test ! -d actions/attest-sbom-action

# All 3 files made the trip:
test -f actions/sealedge-attest-sbom-action/action.yml
test -f actions/sealedge-attest-sbom-action/README.md
test -f actions/sealedge-attest-sbom-action/LICENSE

# Git rename tracking preserved (git log --follow walks through the rename):
test "$(git log --follow --oneline -- actions/sealedge-attest-sbom-action/action.yml | wc -l)" -ge 2

# No orphan references to the old path anywhere in the monorepo:
test "$(grep -rnE 'actions/attest-sbom-action([/\s"\]]|$)' --include='*.yml' --include='*.md' --include='*.toml' --include='*.sh' . 2>/dev/null | grep -v '^\./\.planning/' | grep -v '^\./\.factory/' | wc -l)" -eq 0

# Commit landed:
git log -1 --pretty=%s | grep -qE '^chore\(88\): rename actions/attest-sbom-action'
    </automated>
  </verify>
  <acceptance_criteria>
    - `test -d actions/sealedge-attest-sbom-action` succeeds (directory exists)
    - `test ! -d actions/attest-sbom-action` succeeds (old directory gone)
    - `ls actions/sealedge-attest-sbom-action/` lists exactly `action.yml`, `LICENSE`, `README.md` (3 files)
    - `git log --follow --oneline -- actions/sealedge-attest-sbom-action/action.yml` returns at least 2 commits (follows pre-rename history through the `git mv`)
    - No tracked file under `.github/`, `scripts/`, `Cargo.toml`, root `*.md`, `docs/`, or any other code/config path contains the old string `actions/attest-sbom-action` (planning artifacts under `.planning/` and `.factory/` are allowed to carry references in historical/context files)
    - Commit subject starts with `chore(88): rename actions/attest-sbom-action`
  </acceptance_criteria>
  <done>Folder renamed via `git mv`; all 3 files relocated; git rename tracking preserved; no orphan references in code/config; commit landed.</done>
</task>

<task type="auto">
  <name>Task 4: Amend `.planning/REQUIREMENTS.md` §Ext EXT-02 / EXT-03 wording per D-02</name>
  <files>.planning/REQUIREMENTS.md</files>
  <read_first>
    - .planning/REQUIREMENTS.md (read lines 36-42; confirms current EXT-02 / EXT-03 wording matches the replacement block below)
    - .planning/phases/88-external-action-product-website/88-CONTEXT.md §D-02 (rationale: original wording assumes two-repo migration, actual plan is rename-in-place), §Specifics "EXT-02/03 reworded language suggestion" (verbatim replacement text)
    - .planning/phases/88-external-action-product-website/88-PATTERNS.md §Group D (commit pattern for atomic REQUIREMENTS amendment)
  </read_first>
  <action>
Using the Edit tool, replace the current EXT-02 and EXT-03 lines in `.planning/REQUIREMENTS.md`.

**Current state (lines 39-40 per Read of `.planning/REQUIREMENTS.md`):**

```markdown
- [ ] **EXT-02**: New GitHub Action repo created under sealedge naming and published to GitHub Marketplace (equivalent functionality to current `attest-sbom-action`); SHA256 checksum verification preserved
- [ ] **EXT-03**: Old `TrustEdge-Labs/attest-sbom-action` marketplace listing marked deprecated with README redirect to the new listing
```

**Replacement (CONTEXT.md §Specifics D-02 verbatim):**

```markdown
- [ ] **EXT-02**: The `TrustEdge-Labs/attest-sbom-action` repo is renamed to `TrustEdge-Labs/sealedge-attest-sbom-action` via `gh repo rename`; action source references sealedge/seal; a new `@v2` tag ships the rebranded action; SHA256 checksum verification of the downloaded binary is preserved
- [ ] **EXT-03**: GitHub's built-in 301 redirect covers existing `uses: TrustEdge-Labs/attest-sbom-action@v1` references; the pre-rebrand `@v1` tag stays frozen; the post-rename README carries a short notice pointing readers to `@v2` and the renamed repo
```

Execute as one Edit call: `old_string` = the two current lines verbatim, `new_string` = the two replacement lines verbatim. Do NOT modify any other line in the file (EXT-01, EXT-04, traceability table, out-of-scope section, footer — all stay as they were).

**Commit:**

```bash
git add .planning/REQUIREMENTS.md
git diff --cached
# Expect 2 lines removed + 2 lines added; no other diff

git commit -m "$(cat <<'EOF'
docs(88): amend EXT-02/EXT-03 to match rename-in-place approach

Per 88-CONTEXT.md D-02: original wording assumed a two-repo migration
(new repo + old listing marked deprecated with README redirect). The actual
execution plan is a rename-in-place backed by GitHub's 301 redirect, matching
Phase 87's pattern. Rewording aligns the requirement with the delivery shape
so auditors don't find a mismatch.

No scope change — EXT-02 still requires rebranded action source + @v2 tag +
SHA256 checksum verification preserved; EXT-03 still requires the redirect
mechanism + frozen @v1 + top-of-README notice.

Part of Phase 88 EXT-02 / EXT-03.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <verify>
    <automated>
set -e
F=.planning/REQUIREMENTS.md

# New wording present:
test "$(grep -c 'sealedge-attest-sbom-action' "$F")" -ge 2
test "$(grep -c 'GitHub.s built-in 301 redirect covers existing' "$F")" -ge 1
test "$(grep -c 'pre-rebrand .@v1. tag stays frozen' "$F")" -ge 1

# Old obsolete phrasing removed:
test "$(grep -c 'marketplace listing marked deprecated' "$F")" -eq 0
test "$(grep -c 'New GitHub Action repo created under sealedge naming' "$F")" -eq 0

# Other requirements untouched (spot-check):
test "$(grep -c '\*\*EXT-01\*\*' "$F")" -eq 1
test "$(grep -c '\*\*EXT-04\*\*' "$F")" -eq 1
test "$(grep -c '\*\*VALID-01\*\*' "$F")" -eq 1

# Commit landed:
git log -1 --pretty=%s | grep -qE '^docs\(88\): amend EXT-02/EXT-03'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'sealedge-attest-sbom-action' .planning/REQUIREMENTS.md` returns at least 2 (EXT-02 explicit name + EXT-03 explicit name)
    - `grep -c 'marketplace listing marked deprecated' .planning/REQUIREMENTS.md` returns 0 (obsolete phrasing removed)
    - `grep -c 'GitHub.s built-in 301 redirect covers existing' .planning/REQUIREMENTS.md` returns 1 (EXT-03 new wording present)
    - `grep -c 'pre-rebrand .@v1. tag stays frozen' .planning/REQUIREMENTS.md` returns 1
    - EXT-01, EXT-04, VALID-01 each appear exactly once (confirmed not accidentally touched)
    - `git diff HEAD~1 -- .planning/REQUIREMENTS.md` shows exactly 2 lines removed + 2 lines added
    - Commit subject starts with `docs(88): amend EXT-02/EXT-03`
  </acceptance_criteria>
  <done>REQUIREMENTS.md §Ext EXT-02/EXT-03 rewording landed in an atomic commit scoped ONLY to those two lines; rest of the requirements document untouched; Phase 88 auditors will find wording that matches the delivery shape.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| action source → future CI consumers | Composite-action YAML executed inside consumer workflows; any regression in SHA256 verification or ephemeral keygen ships silently to consumers on `@v2` |
| monorepo folder rename → downstream `uses:` / `path:` refs | Local folder rename must not leave orphan refs in the monorepo (CI, scripts, docs); refs from external consumers are governed by Plan 03's 301 redirect |
| REQUIREMENTS.md amendment → audit trail | Requirement wording update is the in-repo record that ties delivery to spec; must be unambiguous to future auditors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-01 (T2 carry) | Tampering | SHA256 checksum verification block in `action.yml` (lines 50-62 region) | mitigate | Task 1 acceptance criteria greps specifically for `sha256sum.*runner\.temp.*/seal` to confirm the checksum-verification `sha256sum` invocation survives the sweep unchanged in structure. Only the filename `trst.sha256` → `seal.sha256` and `trst` binary path → `seal` binary path change; verification logic (fetch expected, compute actual, compare, exit 1 on mismatch) is byte-for-byte preserved. |
| T-88-02 (T4 carry) | Tampering | Ephemeral keygen step (lines 64-73 region) in `action.yml` | mitigate | Task 1 acceptance criterion `grep -c 'seal" keygen' == 1` confirms the keygen invocation survives. Step `name: Generate ephemeral keypair (if no key provided)` is untouched structurally (no reordering, no `if:` change). Env var writes `TRST_KEY/TRST_PUB` → `SEAL_KEY/SEAL_PUB` are pure rename — downstream step `- name: Create SBOM attestation` reads the same env vars (Task 1 substitution table lines 93-94). |
| T-88-03 | Tampering | Folder rename may leave orphan references in `.github/workflows/*.yml` or `scripts/*.sh` pointing at old path | mitigate | Task 3 pre-rename grep `grep -rn 'actions/attest-sbom-action' .github/` must return zero before `git mv` proceeds. Post-rename acceptance criterion `grep -rnE 'actions/attest-sbom-action([/\s"\]]|$)' --include='*.yml' --include='*.md' --include='*.toml' --include='*.sh'` (excluding `.planning/` historical context) must also return zero. |
| T-88-04 | Repudiation | REQUIREMENTS.md amendment happens without traceable rationale → future auditors can't tell why EXT-02/03 wording changed | mitigate | Task 4 commit message explicitly cites `88-CONTEXT.md D-02`, explains original-vs-actual approach mismatch, and confirms no scope change (still requires SHA256 verification, @v2 tag, top-of-README notice). `git blame` on the amended lines will surface this rationale directly. |
| T-88-05 | Spoofing | Someone submits a PR renaming the folder back or to a different name between Plan 01 and Plan 03 | accept | Solo dev, no other contributors; rename window is < 1 hour; pre-rename gate in Plan 03 (mirroring Phase 87 D-06 gate) will re-verify folder name before `gh repo rename` fires. Risk = effectively zero. |

**Plan 01 addresses T1 (release-asset integrity preparation — via correct URL flip), T2 (SHA256 verification preservation — via Task 1 assertion battery), T4 (ephemeral keygen preservation — via Task 1 assertion battery) from the Phase 88 threat surface. Overall grade: LOW. All changes are reversible via `git revert` before Plan 03's external operation lands. No runtime behavior changes until Phase 89's first v6.0.0 release run.**
</threat_model>

<verification>
**Plan-level gate (all 4 tasks + 3 atomic commits must pass):**

1. Task 1 assertion battery passes → `actions/attest-sbom-action/action.yml` fully swept, SHA256 verification & ephemeral keygen structurally preserved (T1/T2/T4 mitigation verified)
2. Task 2 allowlisted grep audit is clean → `actions/attest-sbom-action/README.md` has top-of-README notice + all sweeps landed
3. Task 3 folder is at `actions/sealedge-attest-sbom-action/`; old path gone; no orphan refs in `.github/`, `scripts/`, etc.
4. Task 4 `.planning/REQUIREMENTS.md` EXT-02 / EXT-03 reworded; 2-line swap commit landed

**Commit count:** 4 (one per task — content sweep on action.yml, content sweep on README.md, folder rename, REQUIREMENTS amendment). All subjects start with `fix(88):`, `docs(88):`, `chore(88):`, `docs(88):` respectively.

**Rollback** (if any task fails after partial landing):
- Revert the specific bad commit with `git revert <SHA>` — does NOT require rolling back upstream commits since each is scoped
- Plan 02 / Plan 03 do NOT depend on partial state — they either see the fully-landed Plan 01 result or they don't start
- If all 4 must be rolled back: `git reset --hard <pre-plan-01-SHA>` (solo dev, no shared history concern)
</verification>

<success_criteria>
- `actions/sealedge-attest-sbom-action/action.yml` exists with all 22 line-local substrings substituted per the substitution table; SHA256 verification block + ephemeral keygen step structurally unchanged; line count = 98 (no reflow)
- `actions/sealedge-attest-sbom-action/README.md` exists with top-of-README "Renamed from `TrustEdge-Labs/attest-sbom-action`" notice + all product-name / URL / example-YAML substrings swept
- Old `actions/attest-sbom-action/` folder does not exist; `git log --follow` walks through the rename
- No tracked file under `.github/`, `scripts/`, `docs/`, or root contains the string `actions/attest-sbom-action` (`.planning/` historical files are allowlisted)
- `.planning/REQUIREMENTS.md` §Ext EXT-02 and EXT-03 have been rewritten per CONTEXT.md §Specifics D-02 verbatim replacement
- All 4 commits landed on the current branch; all subjects follow `<type>(88): …` convention; atomic per logical step
- Plan 02 (ci.yml changes) and Plan 03 (external op) are unblocked; Plan 04 is independent and can run in parallel with Plan 02
</success_criteria>

<output>
After completion, create `.planning/phases/88-external-action-product-website/88-01-SUMMARY.md` per the gsd summary template. Include:
- File sweep metrics (22 action.yml substitutions, README.md top-notice + sweep, folder rename, REQUIREMENTS amendment)
- Confirmation that SHA256 verification + ephemeral keygen paths are preserved (T2/T4 mitigation evidence)
- Pointer forward to Plan 02 (ci.yml extension + dogfood) and Plan 03 (external `gh repo rename` + `@v2` tag cut)
- Confirmation that Plan 04 (trustedgelabs-website sweep) is independent and can run in parallel with Plan 02
</output>
