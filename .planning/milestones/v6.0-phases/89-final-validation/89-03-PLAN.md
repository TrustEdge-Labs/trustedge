---
phase: 89-final-validation
plan: 03
type: execute
wave: 3
depends_on:
  - 89-01
  - 89-02
files_modified:
  - .planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md
  - .planning/phases/89-final-validation/89-VERIFICATION.md
autonomous: false
requirements:
  - VALID-02
tags:
  - validation
  - release
  - tag-cut
  - milestone-close
  - sealedge
  - rebrand

must_haves:
  truths:
    - "`.planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md` exists, contains the 7 breaking-change bullets (repo rename, crate rename, binary rename, crypto constants, file ext, env vars, GitHub Action) + link to MIGRATION.md, AND is committed to git as its own atomic commit at the end of Task 1 (per B-3: pre-tag Gate 1 requires a clean working tree)"
    - "Developer ran the Phase 87/88 user-driven external-op pattern: (a) confirmed the D-06 6-point pre-tag checklist green (Gate 1 confirms clean working tree — RELEASE-NOTES-v6.0.0.md was committed in Task 1), (b) executed `git tag -a v6.0.0` + `git push origin v6.0.0` from their shell, (c) executed `gh release create v6.0.0 --notes-file ...` to publish the release"
    - "The `v6.0.0` tag push triggered a CI run (release / self-attestation job) that completed with `conclusion: success`"
    - "The tag-push CI run confirmed the dogfood of `sealedge-attest-sbom-action@v2` succeeded (self-attestation job conclusion: success; closing the Phase 88 deferred dogfood loop)"
    - "Release assets uploaded to the v6.0.0 GitHub release include: `seal`, `seal.sha256`, `seal-sbom.cdx.json`, `build.pub`, and a `.se-attestation.json` file"
    - "89-VERIFICATION.md §2.3 populated with tag-push run URL + conclusion + self-attestation job conclusion + release asset list; Status line flipped from 'IN PROGRESS' to 'PASS'; §5 ROADMAP criterion #3 flipped from PARTIAL to PASS — committed as a SEPARATE post-tag-push commit (not grouped with RELEASE-NOTES creation per B-3)"
  artifacts:
    - path: ".planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md"
      provides: "GitHub release body consumable by `gh release create --notes-file`; committed in Task 1 to satisfy pre-tag Gate 1 clean-tree requirement"
      contains: "MIGRATION.md"
      min_lines: 15
    - path: ".planning/phases/89-final-validation/89-VERIFICATION.md"
      provides: "Evidence doc updated with §2.3 tag-push run details + final PASS status (post-tag-push commit, not grouped with Task 1)"
      contains: "Run URL"
  key_links:
    - from: "RELEASE-NOTES-v6.0.0.md"
      to: "MIGRATION.md What renamed table"
      via: "markdown link [MIGRATION.md](MIGRATION.md) referenced for full upgrade guide"
      pattern: "MIGRATION"
    - from: "v6.0.0 tag-push"
      to: "ci.yml self-attestation job"
      via: "tag push triggers `if: startsWith(github.ref, 'refs/tags/v')` branch in ci.yml line 174"
      pattern: "refs/tags/v"
    - from: "ci.yml self-attestation job"
      to: "sealedge-attest-sbom-action@v2"
      via: "`uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2` at ci.yml line 213"
      pattern: "sealedge-attest-sbom-action@v2"
---

<objective>
Cut the `v6.0.0` annotated tag, create the GitHub release with the phase-local release-notes body, watch the tag-push CI run to green completion (including the self-attestation job dogfooding `sealedge-attest-sbom-action@v2`), and populate `89-VERIFICATION.md` §2.3 with the captured evidence. This plan closes VALID-02 §2.3 — the last remaining evidence gap before the milestone can close in Plan 04.

**Commit ordering (per B-3 revision):** Plan 03 produces THREE commits:
1. `docs(89): add RELEASE-NOTES-v6.0.0.md body` (Task 1, before pre-tag gate — required so Gate 1's clean-tree check passes)
2. The user's tag-cut + tag-push + release-create operations (external — no Claude commit; just git tag + git push)
3. `docs(89): close VALID-02 §2.3 — v6.0.0 tag cut + self-attestation dogfood green` (Task 3, post-tag-push, after CI run completes)

Purpose: The tag-push CI run is the single most important integration test in Phase 89. A green run proves, simultaneously, that: (a) the full release workflow builds green under the new names, (b) the `seal` binary uploads cleanly, (c) the Phase 88 `sealedge-attest-sbom-action@v2` dogfood works end-to-end, (d) the attestation uploads with the correct `.se-attestation.json` extension. One run validates VALID-02 and closes the Phase 88 deferred dogfood loop.

Output:
- `RELEASE-NOTES-v6.0.0.md` (new, phase-local, ~15-25 lines, MPL-2.0 HTML-comment header, 7 breaking-change bullets + MIGRATION.md link) — committed at end of Task 1 as its own atomic commit
- v6.0.0 tag created, pushed, and GitHub release published (external to this repo's files — developer-executed)
- Tag-push CI run URL + conclusion + self-attestation job conclusion captured in `89-VERIFICATION.md` §2.3
- `89-VERIFICATION.md` §4 rollback status section updated (typically "not executed")
- `89-VERIFICATION.md` top-line Status flipped from IN PROGRESS to PASS
- `89-VERIFICATION.md` §5 ROADMAP table criterion #3 flipped from PARTIAL to PASS
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-final-validation/89-CONTEXT.md
@.planning/phases/89-final-validation/89-PATTERNS.md
@.planning/phases/89-final-validation/89-01-PLAN.md
@.planning/phases/89-final-validation/89-02-PLAN.md
@.planning/phases/89-final-validation/89-VERIFICATION.md
@.planning/phases/87-github-repository-rename/87-02-PLAN.md
@.planning/phases/88-external-action-product-website/88-03-PLAN.md
@MIGRATION.md
@CHANGELOG.md
@.github/workflows/ci.yml
@CLAUDE.md

<interfaces>
Release-notes body shape (CONTEXT.md §Specifics D-09 sketch verbatim — planner refines; PATTERNS.md File 3 confirms this shape). Note: the four-dash HTML-comment and the body content is what gets written to the file; interfaces just show intent.

Body structure:
- HTML-comment copyright header (one block — MIGRATION.md:1-6 shape)
- H1: `# v6.0.0 — Sealedge Rebrand`
- Two-line opening paragraph: trademark-driven rename + clean-break note
- H2: `## Breaking changes`
- Seven bullets covering: repo rename, crate prefix, binaries (trst→seal etc.), crypto constants, file extensions, env vars, GitHub Action
- Single-line note: `TrustEdge Labs (the company brand) is unchanged. trustedgelabs.com domain unchanged.`
- Link line: `See [MIGRATION.md](MIGRATION.md) for full upgrade guidance.`

D-06 pre-tag gate checklist (Shared.1 in PATTERNS.md — 6 points, each MUST be confirmed green before developer runs `git tag`):

1. `git status` clean on main; `git log origin/main..HEAD` empty (working tree must be clean — RELEASE-NOTES-v6.0.0.md was committed in Task 1 per B-3, so this gate passes without a "staged but uncommitted" stall)
2. `./scripts/validate-v6.sh` exit 0 (satisfied by Plan 02 Task 1)
3. Latest ci.yml on main has conclusion success (via `gh run list --workflow=ci.yml --branch main --limit 1`)
4. wasm-tests.yml + semver.yml workflow_dispatch runs both green (captured by Plan 02 Task 3)
5. Repo-wide D-10 grep audit zero-hit (satisfied by Plan 01 Task 2)
6. `gh pr list --state open` empty

Tag-cut commands (CONTEXT.md §Specifics verbatim — user runs from their shell per D-08):

```
git tag -a v6.0.0 -m "v6.0 Sealedge Rebrand"
git push origin v6.0.0

gh release create v6.0.0 \
  --title "v6.0.0 — Sealedge Rebrand" \
  --notes-file .planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md
```

Tag-push CI watch (CONTEXT.md §Specifics):

```
gh run watch $(gh run list --workflow=ci.yml --event=push --limit=1 --json databaseId -q '.[0].databaseId')
```

(Note: the `event=push` filter is correct because a tag push IS a push event in GitHub Actions parlance; the `ci.yml:174` gate `if: startsWith(github.ref, 'refs/tags/v')` routes it into the self-attestation job.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RELEASE-NOTES-v6.0.0.md, verify MIGRATION.md link target, AND commit (per B-3)</name>
  <files>.planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md</files>
  <read_first>
    - .planning/phases/89-final-validation/89-CONTEXT.md §Specifics "Release notes body sketch" (D-09 verbatim shape)
    - .planning/phases/89-final-validation/89-PATTERNS.md §File 3 (content shape + MIGRATION.md parity check — identifies the action-rename row already added in Plan 01 Task 2)
    - MIGRATION.md (verify the Plan 01 Task 2 action-rename row is present and the rest of the "What renamed" table is intact)
    - CHANGELOG.md (secondary analog — the v5.0/v4.0 entry shape, not copied but informs style)
  </read_first>
  <action>
    Create `.planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md` using the Write tool (not heredoc).

    The body follows the CONTEXT.md §Specifics "Release notes body sketch" verbatim with minor polish. Include (in order):

    1. HTML-comment copyright header (4 lines: Copyright line, MPL-2.0 line, Project: sealedge line — matches MIGRATION.md:1-6 shape)
    2. Blank line
    3. `# v6.0.0 — Sealedge Rebrand`
    4. Blank line
    5. Two-line paragraph: `Trademark-driven rename from "trustedge" to "sealedge" — clean break,` / `no backward-compat decrypt path.`
    6. Blank line
    7. `## Breaking changes`
    8. Blank line
    9. Seven bulleted lines (one per dash, as in the CONTEXT sketch):
       - `- Repo: \`TrustEdge-Labs/trustedge\` → \`TrustEdge-Labs/sealedge\` (GitHub 301 redirect in place)`
       - `- Crates: \`trustedge-*\` → \`sealedge-*\` (workspace + 2 experimental crates)`
       - `- Binaries: \`trst\` → \`seal\`, \`trustedge\` → \`sealedge\`, \`trustedge-server\` → \`sealedge-server\`, etc.`
       - `- Crypto constants: \`TRUSTEDGE-KEY-V1\` → \`SEALEDGE-KEY-V1\`, \`TRUSTEDGE_ENVELOPE_V1\` → \`SEALEDGE_ENVELOPE_V1\``
       - `- File extensions: \`.trst\` → \`.seal\`, \`.te-attestation.json\` → \`.se-attestation.json\``
       - `- Env vars: \`TRUSTEDGE_*\` → \`SEALEDGE_*\``
       - `- GitHub Action: \`TrustEdge-Labs/attest-sbom-action@v1\` → \`TrustEdge-Labs/sealedge-attest-sbom-action@v2\``
    10. Blank line
    11. `TrustEdge Labs (the company brand) is unchanged. \`trustedgelabs.com\` domain unchanged.`
    12. Blank line
    13. `See [MIGRATION.md](MIGRATION.md) for full upgrade guidance.`

    Total target: ~20 lines (including the header block and blanks).

    After writing, verify the link target matches MIGRATION.md's "What renamed" table per PATTERNS.md File 7. Run via the Bash tool:
    ```
    for key in 'TrustEdge-Labs/sealedge' 'sealedge-\\*' 'trst' 'seal' 'SEALEDGE-KEY-V1' 'SEALEDGE_ENVELOPE_V1' '\\.seal\\b' '\\.se-attestation\\.json' 'SEALEDGE_\\*' 'sealedge-attest-sbom-action@v2'; do
        grep -q "$key" MIGRATION.md || echo "MISSING in MIGRATION.md: $key"
    done
    ```

    Expected: zero "MISSING" lines — all keywords should already be in MIGRATION.md (Plan 01 Task 2 added the action-rename row).

    If any keyword is missing, STOP: this is a D-14 hybrid-gate violation that should have been caught in Plan 01. Log in SUMMARY and do not proceed; escalate to developer.

    **Commit the file as `docs(89): add RELEASE-NOTES-v6.0.0.md body` before completing the task** (per B-3 — Task 2's pre-tag Gate 1 requires a clean working tree; if RELEASE-NOTES-v6.0.0.md is staged-but-uncommitted, Gate 1 stalls the user-gated checkpoint).

    Use the Bash tool:
    ```
    git add .planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md
    git commit -m "$(cat <<'EOF'
    docs(89): add RELEASE-NOTES-v6.0.0.md body

    Phase-local release-notes body for `gh release create --notes-file`.
    Covers 7 breaking-change areas + link to MIGRATION.md. Committed
    standalone (not grouped with VERIFICATION.md update) so that pre-tag
    Gate 1 (`git status` clean) passes cleanly in Task 2.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    Verify the commit landed:
    ```
    git log -1 --format=%s | grep -q '^docs(89): add RELEASE-NOTES-v6.0.0.md body'
    git status --short  # Expected: clean (or only unrelated files)
    ```

    The §2.3 VERIFICATION.md update in Task 3 remains its own post-tag-push commit (unchanged from prior plan). Task 1 + Task 3 are NOT grouped — they are two separate atomic commits with the user's `git tag` + `git push` operations sandwiched in between.
  </action>
  <verify>
    <automated>test -f .planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md && wc -l < .planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md | awk '$1 >= 15 && $1 <= 35 {exit 0} {exit 1}' && head -6 .planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md | grep -q 'MPL-2.0' && grep -c '^- ' .planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md | awk '$1 >= 7 {exit 0} {exit 1}' && grep -q 'sealedge-attest-sbom-action' .planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md && grep -q 'MIGRATION.md' .planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md && git log -5 --format=%s | grep -q '^docs(89): add RELEASE-NOTES-v6.0.0.md body' && ! git status --short | grep -q 'RELEASE-NOTES-v6.0.0.md'</automated>
  </verify>
  <acceptance_criteria>
    - File exists at `.planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md`
    - Line count: `wc -l` result between 15 and 35 (short announcement per D-09)
    - Copyright header present: `head -6 | grep -c 'MPL-2.0'` returns ≥ 1
    - HTML-comment form (not `//` or `#`): `head -1 | grep -q '<!--'`
    - H1 title present: `grep -c '^# v6.0.0' RELEASE-NOTES-v6.0.0.md` returns 1
    - "Breaking changes" section: `grep -c '^## Breaking changes' RELEASE-NOTES-v6.0.0.md` returns 1
    - Seven breaking-change bullets: `grep -c '^- ' RELEASE-NOTES-v6.0.0.md` returns ≥ 7
    - Each of the 7 required keywords present in bullet text (run these greps; all must return a match):
      - `grep -q 'TrustEdge-Labs/sealedge' RELEASE-NOTES-v6.0.0.md` (repo)
      - `grep -q 'trustedge-\\*' RELEASE-NOTES-v6.0.0.md` (crates)
      - `grep -q 'trst' RELEASE-NOTES-v6.0.0.md` AND `grep -q 'seal' RELEASE-NOTES-v6.0.0.md` (binaries)
      - `grep -q 'SEALEDGE-KEY-V1' RELEASE-NOTES-v6.0.0.md` (crypto constants)
      - `grep -q '\\.seal' RELEASE-NOTES-v6.0.0.md` AND `grep -q 'se-attestation' RELEASE-NOTES-v6.0.0.md` (file ext)
      - `grep -q 'SEALEDGE_\\*' RELEASE-NOTES-v6.0.0.md` (env vars)
      - `grep -q 'sealedge-attest-sbom-action@v2' RELEASE-NOTES-v6.0.0.md` (GitHub Action)
    - MIGRATION.md link: `grep -c 'MIGRATION.md' RELEASE-NOTES-v6.0.0.md` returns ≥ 1 (the `[MIGRATION.md](MIGRATION.md)` link + any inline mention)
    - MIGRATION.md link-target coverage check: all 10 keyword greps from the Bash loop in <action> return zero "MISSING" lines
    - "TrustEdge Labs (the company brand) is unchanged" clause retained: `grep -c 'TrustEdge Labs.*unchanged' RELEASE-NOTES-v6.0.0.md` returns ≥ 1 (explicitly preserves the brand-not-renamed signal)
    - **Commit landed as its own atomic commit (B-3 hardening): `git log -5 --format=%s | grep -q '^docs(89): add RELEASE-NOTES-v6.0.0.md body'`**
    - **Working tree clean for RELEASE-NOTES file: `git status --short | grep -c 'RELEASE-NOTES-v6.0.0.md'` returns 0** (file was committed; not staged, not modified)
  </acceptance_criteria>
  <done>Release-notes body file written, covers all 7 breaking-change areas, links to MIGRATION.md, MIGRATION.md coverage check passes with zero "MISSING" keywords, AND committed as `docs(89): add RELEASE-NOTES-v6.0.0.md body` (per B-3 — Gate 1 clean-tree requirement).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Pre-tag gate checklist + user-executed git tag + git push + gh release create + tag-push CI watch</name>
  <what-built>
    Plan 01 produced `scripts/validate-v6.sh` + MIGRATION.md action-rename row; Plan 02 ran `validate-v6.sh` green, captured evidence for VALID-01/VALID-03, and confirmed `wasm-tests.yml` + `semver.yml` workflow_dispatch runs are green. Task 1 above created the release-notes body AND committed it (per B-3). All pre-tag prerequisites are in place AND the working tree is clean.

    The final external operation — the `git tag -a v6.0.0 -m "..."` + `git push origin v6.0.0` + `gh release create v6.0.0 --notes-file ...` commands — is the Phase 87/88 user-driven external-op pattern. Claude provides exact commands + the 6-point pre-tag gate checklist; the developer confirms each gate item from the terminal and then runs the tag/push/release commands from their own shell.

    Why human-action and not automated: D-08 explicitly calls for the user to run these from their shell; mirrors Phase 87 D-05 / Phase 88 Plan 03 Task 2 pattern. There is no technical reason Claude's Bash tool cannot run `git tag` and `gh release create`, but the convention is "irreversible external ops are user-executed" so the developer has a deliberate hand on the wheel for the milestone-release moment.
  </what-built>
  <how-to-verify>
    **Phase 1 — Pre-tag 6-point gate confirmation** (all green before running tag commands):

    **Gate 1 — Working tree is clean (RELEASE-NOTES-v6.0.0.md was committed in Task 1) + main pushed:**
    ```
    git status
    git log origin/main..HEAD
    git branch --show-current
    ```
    Expected: "nothing to commit, working tree clean" on `main`; empty log output (or only commits already present locally pre-Plan-03); "main" as branch.

    Per B-3 revision: Task 1 committed `RELEASE-NOTES-v6.0.0.md` as its own `docs(89): add RELEASE-NOTES-v6.0.0.md body` commit, so this gate passes without a staged-but-uncommitted stall. If `git status` shows `RELEASE-NOTES-v6.0.0.md` as modified or staged, STOP — Task 1's commit step did not run. Re-run Task 1's commit, then re-confirm.

    If any of these fail for OTHER reasons: commit + push any pending work, re-run.

    **Gate 2 — validate-v6.sh green (satisfied by Plan 02 Task 1):**
    ```
    cat .planning/phases/89-final-validation/89-02-SUMMARY.md | grep -E 'exit code|Total green tests|Safe to cut'
    ```
    Expected: confirms exit 0, total tests >= 471 (or D-02 justification recorded if --allow-regression was used), "Safe to cut v6.0.0 tag" line.

    If Plan 02 Task 1 was skipped or the state has changed since (new commits landed), re-run `./scripts/validate-v6.sh` fresh and update `89-02-SUMMARY.md` with the new log before proceeding.

    **Gate 3 — Latest main-branch CI run is green:**
    ```
    gh run list --workflow=ci.yml --branch main --limit 5 --json databaseId,status,conclusion,headSha,event,createdAt
    ```
    Expected: most recent `push` / `workflow_dispatch` run on main has `conclusion: success`. The 87-VERIFICATION.md run `24724694867` satisfies this IF no main-branch commits have landed since 2026-04-21; otherwise (e.g., the Task 1 RELEASE-NOTES commit just landed on main), trigger a fresh run via either:
    - `gh workflow run ci.yml --ref main` (workflow_dispatch); OR
    - allow the next push-triggered run from the Task 1 commit to complete naturally
    Then `gh run watch <new-run-id>`.

    Capture the run URL for VERIFICATION.md §2.1 confirmation (may be the same as 87-VERIFICATION.md's, or a newer one).

    **Gate 4 — wasm-tests.yml + semver.yml workflow_dispatch green (satisfied by Plan 02 Task 3):**
    ```
    gh run list --workflow=wasm-tests.yml --event=workflow_dispatch --limit 1 --json conclusion,url
    gh run list --workflow=semver.yml --event=workflow_dispatch --limit 1 --json conclusion,url
    ```
    Expected: both `conclusion: success` (OR `wasm-tests.yml` documented as deferred per Plan 02 Task 3 Step D pre-flight option B). URLs already in `89-VERIFICATION.md` §2.2 per Plan 02 Task 4.

    **Gate 5 — D-10 straggler grep zero-hit (satisfied by Plan 01 Task 2):**
    ```
    git ls-files \
      | xargs grep -nE "trustedge|TRUSTEDGE|\\.trst\\b|\\.te-attestation" 2>/dev/null \
      | grep -vE "TrustEdge-Labs|TrustEdge Labs|trustedgelabs\\.com|MIGRATION\\.md|CHANGELOG\\.md|^\\.planning/(milestones|phases)/|RFC_K256_SUPPORT\\.md|improvement-plan\\.md|security-review-platform\\.md" \
      | wc -l
    ```
    Expected: 0. If non-zero, STOP — a post-Plan-01 regression snuck in. Fix inline as `fix(89):` and re-run.

    **Gate 6 — No open PRs:**
    ```
    gh pr list --state open
    ```
    Expected: empty list (or only PRs unrelated to the rename — but best practice is empty at tag-cut time).

    If ALL 6 gates green, proceed to Phase 2. If ANY gate is red, stop + report back — do not run tag commands.

    ---

    **Phase 2 — Tag cut + release creation** (developer runs from their shell; Claude's Bash tool does NOT invoke these per D-08):

    ```
    git tag -a v6.0.0 -m "v6.0 Sealedge Rebrand"
    git tag -l v6.0.0
    git show v6.0.0 --no-patch

    git push origin v6.0.0
    git ls-remote --tags origin v6.0.0

    gh release create v6.0.0 \
      --title "v6.0.0 — Sealedge Rebrand" \
      --notes-file .planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md

    gh release view v6.0.0 --json name,tagName,url,createdAt
    ```

    Paste the following outputs into the conversation for Claude to capture in VERIFICATION.md:

    1. `git tag -l v6.0.0` — expected: `v6.0.0`
    2. `git ls-remote --tags origin v6.0.0` — expected: `<SHA>  refs/tags/v6.0.0`
    3. `gh release view v6.0.0 --json name,tagName,url,createdAt` — expected: JSON with `tagName: v6.0.0`, `url: https://...`

    ---

    **Phase 3 — Watch the tag-push CI run to completion:**

    A tag push matching `v*` triggers `ci.yml` (per `ci.yml:174` `if: startsWith(github.ref, 'refs/tags/v')`). Watch it:

    ```
    TAG_RUN_ID=$(gh run list --workflow=ci.yml --event=push --limit=1 --json databaseId -q '.[0].databaseId')
    echo "Tag-push run ID: $TAG_RUN_ID"
    gh run watch "$TAG_RUN_ID"
    ```

    Expected: `conclusion: success`. This is VALID-02 §2.3 — the authoritative gate.

    After it completes, capture details:
    ```
    gh run view "$TAG_RUN_ID" --json databaseId,conclusion,url,headSha,jobs
    ```

    Identify the `self-attestation` job in the `jobs` array and capture its conclusion (expected: `success`).

    ```
    gh release view v6.0.0 --json assets -q '.assets[].name'
    ```

    Expected asset names (CONTEXT.md §D-15 + ci.yml self-attestation upload steps):
    - `seal` (the binary)
    - `seal.sha256` (the checksum)
    - `seal-sbom.cdx.json` (the SBOM)
    - `build.pub` (the ephemeral public key)
    - A `.se-attestation.json` file (typically `seal.se-attestation.json` or named per the action's attestation-path output)

    If the tag-push run is red, classify per D-06 / CONTEXT.md §Claude's-Discretion tag-failure recovery:
    - Option A — force-update: fix inline as `fix(89):` commit, run `git tag -f v6.0.0 <new-sha>`, `git push origin v6.0.0 --force`. Document in VERIFICATION.md §4.
    - Option B — cut v6.0.1: leave v6.0.0 in place, apply fix, run `git tag -a v6.0.1 -m "..."`, `git push origin v6.0.1`, also run `gh release create v6.0.1`. Document in VERIFICATION.md §4.
    - Developer chooses; Claude provides command sequence for chosen option.

    Paste all of Phase 3's outputs into the conversation.
  </how-to-verify>
  <resume-signal>
    Reply with one of:
    - `tag-cut` — All 6 pre-tag gates green; `v6.0.0` tag and release successfully created; tag-push CI run completed with `conclusion: success` and self-attestation job green. Paste all the outputs from Phases 2 and 3 into the conversation so Claude can populate VERIFICATION.md §2.3.
    - `tag-failed-force-update` — Tag pushed but CI run failed; force-update path chosen; paste `git tag -f` + `git push --force` outputs + the second (fixed) run URL.
    - `tag-failed-v601` — Tag pushed but CI run failed; v6.0.1 cut instead; paste v6.0.1 tag creation + release + run URLs.
    - `gate-N-red: <description>` — Gate N failed; describe issue; do not proceed.
  </resume-signal>
  <files>(external operation — no files directly written; outputs pasted into conversation for Task 3 consumption)</files>
  <action>Human-executed checkpoint — see &lt;how-to-verify&gt; above for the full three-phase protocol (Phase 1: confirm 6-point pre-tag gate green, including Gate 1's clean-tree check which now passes because Task 1 already committed RELEASE-NOTES-v6.0.0.md per B-3; Phase 2: user runs `git tag -a v6.0.0 -m 'v6.0 Sealedge Rebrand'` + `git push origin v6.0.0` + `gh release create v6.0.0 --notes-file .planning/phases/89-final-validation/RELEASE-NOTES-v6.0.0.md` from their own shell per D-08; Phase 3: user runs `gh run watch` on the tag-push CI run until conclusion: success; user pastes all outputs into the conversation via one of the four resume-signal variants).</action>
  <verify>
    <automated>gh release view v6.0.0 --json tagName -q '.tagName' | grep -q '^v6.0.0$' && gh release view v6.0.0 --json assets -q '.assets[].name' | grep -q '^seal$' && gh release view v6.0.0 --json assets -q '.assets[].name' | grep -q '^seal.sha256$' && gh release view v6.0.0 --json assets -q '.assets[].name' | grep -q 'se-attestation.json$' && gh run list --workflow=ci.yml --event=push --limit=1 --json conclusion -q '.[0].conclusion' | grep -q 'success'</automated>
  </verify>
  <done>v6.0.0 tag created on origin; `gh release view v6.0.0` returns a release with the five expected assets (seal, seal.sha256, seal-sbom.cdx.json, build.pub, seal.se-attestation.json); tag-push CI run conclusion success; self-attestation job conclusion success; developer replied with `tag-cut` (or one of the failure-recovery variants); all outputs pasted into conversation for Task 3 VERIFICATION.md population.</done>
</task>

<task type="auto">
  <name>Task 3: Populate 89-VERIFICATION.md §2.3 tag-push evidence + flip Status + §5 ROADMAP criterion #3 (separate post-tag-push commit per B-3)</name>
  <files>.planning/phases/89-final-validation/89-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/89-final-validation/89-VERIFICATION.md (the draft from Plan 02 Task 4 — identify the §2.3 placeholder block, the Status line at top, and the §5 ROADMAP table)
    - Task 2's resume-signal reply (the pasted outputs from Phase 2 + Phase 3 — source of the run URL, conclusion, self-attestation conclusion, asset list)
    - .planning/phases/89-final-validation/89-PATTERNS.md §File 2 §"Command-and-output evidence-row pattern" (from 88-VERIFICATION.md:54-68 — the exact shape for §2.3)
  </read_first>
  <action>
    Three edits to `89-VERIFICATION.md` via the Edit tool (do NOT use Write — the file already exists with content from Plan 02).

    **Edit 1 — Replace the §2.3 placeholder block.**

    Find the block starting with `### 2.3 Tag-push` and ending before `## 3. VALID-03`. Replace with populated content that includes:

    - The exact `gh run watch` command used
    - Run URL from Task 2 Phase 3 (substitute the actual URL captured)
    - Workflow: CI
    - Conclusion: success
    - headSha captured from `gh run view --json headSha`
    - Self-attestation job section: conclusion success, closing Phase 88 deferred dogfood loop for `sealedge-attest-sbom-action@v2`
    - Release assets uploaded (bulleted list from `gh release view --json assets`):
      - `seal`
      - `seal.sha256`
      - `seal-sbom.cdx.json`
      - `build.pub`
      - The `.se-attestation.json` asset (capture its exact name)
    - Timestamp from tag-creation or CI run createdAt
    - A closing line: "VALID-02 closed. Release cut, self-attestation dogfood verified green, asset list matches D-15 expected set."

    Replace each placeholder with the actual value from Task 2's resume-signal reply.

    **Edit 2 — Flip the §4 rollback status.**

    The Plan 02 draft's §4 reads "Not executed. Plan 03 has not yet cut the v6.0.0 tag..." Replace with one of two outcomes:

    If Task 2 resume-signal was `tag-cut` (happy path): rewrite §4 to state "Not executed. All D-06 gates passed on first run. No force-update, no v6.0.1 needed." Retain the recovery commands block verbatim as documentation.

    If Task 2 resume-signal was `tag-failed-force-update`: rewrite §4 to document the fix commit SHA, the `git tag -f` + `git push --force` outputs, and the second (green) run URL. §2.3 above reflects the second run.

    If Task 2 resume-signal was `tag-failed-v601`: similar shape, documenting v6.0.1 cut instead.

    Use the resume-signal reply to determine which variant applies.

    **Edit 3 — Flip the top-line Status + §5 ROADMAP criterion #3.**

    Status line (line ~13 of VERIFICATION.md) change from the Plan 02 draft's:
    ```
    **Status:** IN PROGRESS — VALID-01 + VALID-03 evidence captured (Plan 02); VALID-02 §2.3 tag-push pending Plan 03
    ```
    To:
    ```
    **Status:** PASS — VALID-01 / VALID-02 / VALID-03 all green; tag cut; self-attestation dogfood verified. Milestone close (ROADMAP/PROJECT/archive) handled by Plan 04.
    ```

    §5 ROADMAP success criteria table criterion #3 change from the Plan 02 draft's PARTIAL marker to:
    ```
    | 3 | All GitHub Actions workflows run green on push to renamed repo | PASS — §2.1 (main CI) + §2.2 (wasm-tests + semver workflow_dispatch) + §2.3 (tag-push v6.0.0 + self-attestation dogfood) |
    ```
    (Keep the UTF-8 tick `✔ PASS` if the other rows use it — match the row-style of criteria 1, 2, 4 in the same table.)

    After the three edits, verify via grep (see acceptance_criteria below).

    **Commit as a SEPARATE post-tag-push commit (per B-3):**

    Per B-3 revision, this VERIFICATION.md update is its OWN commit, distinct from Task 1's RELEASE-NOTES-v6.0.0.md commit (which landed before the user-gated tag cut) and distinct from the user's external `git tag` + `git push` operations (which produced no Claude commit). Commit message:
    ```
    git add .planning/phases/89-final-validation/89-VERIFICATION.md
    git commit -m "$(cat <<'EOF'
    docs(89): close VALID-02 §2.3 — v6.0.0 tag cut + self-attestation dogfood green

    §2.3 populated with tag-push CI run URL, conclusion: success,
    self-attestation job conclusion: success (closing Phase 88 deferred
    dogfood loop for sealedge-attest-sbom-action@v2). Release asset list
    matches D-15 expected set. Status flipped from IN PROGRESS to PASS.
    §5 ROADMAP criterion #3 flipped from PARTIAL to PASS.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    Verify the commit landed:
    ```
    git log -1 --format=%s | grep -q '^docs(89): close VALID-02'
    ```
  </action>
  <verify>
    <automated>test -f .planning/phases/89-final-validation/89-VERIFICATION.md && grep -q 'Status:\*\* PASS' .planning/phases/89-final-validation/89-VERIFICATION.md && ! grep -q 'PENDING — filled in by Plan 03' .planning/phases/89-final-validation/89-VERIFICATION.md && ! grep -q '🟡 PARTIAL' .planning/phases/89-final-validation/89-VERIFICATION.md && grep -q 'sealedge-attest-sbom-action@v2' .planning/phases/89-final-validation/89-VERIFICATION.md && grep -q 'seal.sha256' .planning/phases/89-final-validation/89-VERIFICATION.md && grep -q 'se-attestation.json' .planning/phases/89-final-validation/89-VERIFICATION.md && grep -q 'build.pub' .planning/phases/89-final-validation/89-VERIFICATION.md && git log -3 --format=%s | grep -q '^docs(89): close VALID-02'</automated>
  </verify>
  <acceptance_criteria>
    - Status line at top shows PASS: `grep -c 'Status:\\*\\* PASS' .planning/phases/89-final-validation/89-VERIFICATION.md` returns ≥ 1 (was "IN PROGRESS" in Plan 02 draft)
    - No remaining PENDING placeholder for Plan 03: `! grep -q 'PENDING — filled in by Plan 03'` returns true (tokens removed)
    - §2.3 populated with concrete values: `grep -c '^\\*\\*Run URL:\\*\\*' .planning/phases/89-final-validation/89-VERIFICATION.md` returns ≥ 2 (§2.1 + §2.3 each have a Run URL line; §2.2 may use a different label)
    - Self-attestation dogfood evidence: `grep -c 'sealedge-attest-sbom-action@v2' .planning/phases/89-final-validation/89-VERIFICATION.md` returns ≥ 1
    - Release assets enumerated: `grep -c 'seal.sha256' .planning/phases/89-final-validation/89-VERIFICATION.md` ≥ 1 AND `grep -c 'seal-sbom.cdx.json' .planning/phases/89-final-validation/89-VERIFICATION.md` ≥ 1 AND `grep -c 'build.pub' .planning/phases/89-final-validation/89-VERIFICATION.md` ≥ 1 AND `grep -c 'se-attestation.json' .planning/phases/89-final-validation/89-VERIFICATION.md` ≥ 1
    - §4 rollback status is specific (either "Not executed" with all gates-passed text, or one of the two failure-recovery variants): `grep -cE 'Not executed|Executed — Option [AB]' .planning/phases/89-final-validation/89-VERIFICATION.md` returns ≥ 1
    - §5 ROADMAP table has no PARTIAL markers: `grep -c '🟡 PARTIAL' .planning/phases/89-final-validation/89-VERIFICATION.md` returns 0
    - §5 ROADMAP table has 4 PASS markers: `grep -c '✔ PASS\\|PASS —' .planning/phases/89-final-validation/89-VERIFICATION.md` returns ≥ 4 (one per criterion in the closing table)
    - Conclusion success recorded for tag-push run: `grep -cE 'conclusion: success|Conclusion: success' .planning/phases/89-final-validation/89-VERIFICATION.md` returns ≥ 2 (one for tag-push run, one for self-attestation job; may also have §2.1 and §2.2 conclusions so count may be higher)
    - Tag-push run URL is an actual github.com URL: `grep -cE 'https://github.com/TrustEdge-Labs/sealedge/actions/runs/[0-9]+' .planning/phases/89-final-validation/89-VERIFICATION.md` returns ≥ 1 (could be ≥ 2 if §2.1 reused URL is also expressed as a full URL)
    - **Atomic commit landed (B-3 hardening): `git log -3 --format=%s | grep -q '^docs(89): close VALID-02'`** (separate post-tag-push commit, not grouped with Task 1's RELEASE-NOTES commit)
  </acceptance_criteria>
  <done>89-VERIFICATION.md §2.3 populated with tag-push evidence; §4 rollback status set appropriately; Status flipped to PASS; §5 criterion #3 flipped to PASS. Committed as a separate post-tag-push atomic commit `docs(89): close VALID-02 §2.3 — v6.0.0 tag cut + self-attestation dogfood green` (per B-3 — distinct from Task 1's RELEASE-NOTES commit and from the user's external tag-push operations).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → git tag / git push | Developer runs with their ambient git credentials (SSH/HTTPS token); git push is the authoritative state-change to origin |
| developer shell → gh release create | Uses developer's `gh auth` (repo scope); publishes to GitHub Releases API |
| v6.0.0 tag push → ci.yml self-attestation job → GITHUB_TOKEN | Workflow uses the default GITHUB_TOKEN with `contents: write` permission declared on ci.yml:178 — scoped to this repository only; no cross-repo write |
| ci.yml self-attestation job → sealedge-attest-sbom-action@v2 | External action called by `uses:`; TODO in ci.yml:213 notes plan to pin to commit SHA after first @v2.0.0 tag — currently @v2 floating ref (accepted for first dogfood run) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-03-01 | Tampering | RELEASE-NOTES-v6.0.0.md content mismatches MIGRATION.md truth | mitigate | Task 1 runs the 10-keyword coverage grep against MIGRATION.md post-write; MIGRATION.md "What renamed" table is source of truth; any mismatch blocks the task |
| T-89-03-02 | Repudiation | v6.0.0 tag force-updated without audit | mitigate | D-06 force-update path documented in VERIFICATION.md §4 with fix commit SHA and both (pre-fix + post-fix) run URLs; git reflog retains old tag SHA for 30 days |
| T-89-03-03 | Elevation of privilege | sealedge-attest-sbom-action@v2 is a floating reference, not pinned SHA | accept | ci.yml:213 has `TODO: pin to commit SHA after first @v2.0.0 tag (Plan 03)` — explicit acknowledgement; first dogfood run uses floating @v2; pinning follow-up lands in a future phase (already a known TODO, not a Phase 89 blocker) |
| T-89-03-04 | Tampering | VERIFICATION.md §2.3 data diverges from actual tag-push run outputs | mitigate | Task 3 populates §2.3 using verbatim paste-outputs from Task 2 resume-signal reply; acceptance_criteria require the URL to match `https://github.com/TrustEdge-Labs/sealedge/actions/runs/[0-9]+` pattern |
| T-89-03-05 | Denial of service | Tag-push CI run fails repeatedly, blocking milestone close | accept | D-06 failure recovery documented (force-update OR v6.0.1 cut); user decides at execution time. Scope-expansion bugs defer to hotfix sub-phase per D-14 |
| T-89-03-06 | Information disclosure | Release assets include build.pub — an ephemeral public key | accept | Public key is the INTENDED release asset; it is needed by consumers to verify the `.se-attestation.json` signature. Per v4.0 Phase 79 self-attestation design: private key is ephemeral (destroyed after signing), only the public key is uploaded. No secret exposure |
| T-89-03-07 | Tampering | RELEASE-NOTES-v6.0.0.md staged-but-uncommitted at tag-cut time stalls Gate 1 | mitigate | Per B-3: Task 1 commits the file as its own atomic `docs(89): add RELEASE-NOTES-v6.0.0.md body` commit BEFORE Task 2's pre-tag gate runs; Task 1 acceptance criteria enforce the commit landed and `git status` is clean for the file |

**Severity gate:** All threats are low-to-medium severity. T-89-03-03 (floating action ref) is an ACCEPTED risk with a documented follow-up TODO — no blocking threats for this phase. T-89-03-02 (tag force-update) is a MITIGATED risk via audit-trail requirements in VERIFICATION.md §4. T-89-03-07 (Gate 1 stall) is MITIGATED by the B-3 commit-in-Task-1 pattern.
</threat_model>

<verification>
- `RELEASE-NOTES-v6.0.0.md` exists, is well-formed, covers 7 breaking-change areas, links to MIGRATION.md, AND committed as `docs(89): add RELEASE-NOTES-v6.0.0.md body` (per B-3)
- MIGRATION.md link-target coverage check returns zero "MISSING" keywords
- Pre-tag Gate 1 (`git status` clean) passes — Task 1's RELEASE-NOTES commit landed before the gate runs
- Developer confirms D-06 6-point pre-tag gate checklist all green
- `git tag -a v6.0.0`, `git push origin v6.0.0`, `gh release create v6.0.0` all completed successfully
- Tag-push CI run (identified via `gh run list --workflow=ci.yml --event=push --limit=1`) has `conclusion: success`
- Self-attestation job in that run has `conclusion: success` (dogfood of sealedge-attest-sbom-action@v2 green)
- Release assets include `seal`, `seal.sha256`, `seal-sbom.cdx.json`, `build.pub`, `.se-attestation.json` file
- `89-VERIFICATION.md` Status line flipped from IN PROGRESS to PASS
- `89-VERIFICATION.md` §5 ROADMAP criterion #3 flipped from PARTIAL to PASS
- `89-VERIFICATION.md` §4 rollback status reflects actual outcome (not executed / force-update / v6.0.1)
- Plan 03 produces THREE distinct atomic commits (per B-3): Task 1's RELEASE-NOTES commit, the user's external tag-cut/push (no Claude commit), and Task 3's VERIFICATION.md close commit
</verification>

<success_criteria>
Plan 03 succeeds when:

1. `RELEASE-NOTES-v6.0.0.md` created at phase-local path with all 7 breaking-change bullets and the MIGRATION.md link, AND committed as `docs(89): add RELEASE-NOTES-v6.0.0.md body` at end of Task 1 (per B-3)
2. MIGRATION.md coverage grep returns zero "MISSING" keywords (PATTERNS.md File 7 link-target verification)
3. Developer confirmed all 6 pre-tag gates green (Gate 1 passes because Task 1's commit landed) and executed `git tag -a v6.0.0` + `git push origin v6.0.0` + `gh release create v6.0.0`
4. Tag-push CI run conclusion: success (VALID-02 §2.3 closed)
5. Self-attestation job conclusion: success (Phase 88 dogfood loop closed — `sealedge-attest-sbom-action@v2` verified end-to-end)
6. Release assets include all 5 expected files (`seal`, `seal.sha256`, `seal-sbom.cdx.json`, `build.pub`, `.se-attestation.json`)
7. `89-VERIFICATION.md` §2.3 populated with concrete values (not placeholders); §4 rollback status reflects actual outcome; Status flipped to PASS; §5 ROADMAP criterion #3 flipped to PASS
8. Atomic commit `docs(89): close VALID-02 §2.3 — v6.0.0 tag cut + self-attestation dogfood green` with Co-Authored-By trailer (separate post-tag-push commit per B-3, not grouped with Task 1)
9. If tag-push CI failed: D-06 recovery path executed and documented (force-update OR v6.0.1); §4 reflects actual choice

Plan 04 reads `89-VERIFICATION.md` with Status: PASS to close the milestone.
</success_criteria>

<output>
After completion, create `.planning/phases/89-final-validation/89-03-SUMMARY.md` including:
- One-line outcome
- Release-notes body file path + line count
- Task 1 commit SHA (`docs(89): add RELEASE-NOTES-v6.0.0.md body`)
- MIGRATION.md coverage check result (zero MISSING expected)
- D-06 6-point pre-tag gate checklist outcome (all green / which gates required re-trigger; confirm Gate 1 passed cleanly because Task 1's commit landed)
- `git tag -a v6.0.0` + `git push origin v6.0.0` outputs (from Task 2 resume-signal)
- `gh release create v6.0.0` output (URL + createdAt)
- Tag-push CI run URL + conclusion + self-attestation job conclusion
- Release asset list
- Any D-14 hybrid-gate fixes applied during Task 2 (unlikely in happy path)
- D-06 failure-recovery outcome if triggered (force-update commit SHA, or v6.0.1 details)
- Task 3 commit SHA (`docs(89): close VALID-02 §2.3 — v6.0.0 tag cut + self-attestation dogfood green`)
- Confirmation that the THREE-commit structure landed (per B-3): Task 1 commit + external tag-cut + Task 3 commit
</output>
