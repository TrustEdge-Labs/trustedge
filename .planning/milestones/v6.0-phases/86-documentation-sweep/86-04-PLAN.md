---
phase: 86-documentation-sweep
plan: 04
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli/src/main.rs
  - crates/core/src/lib.rs
  - crates/core/src/vectors.rs
  - crates/core/src/applications/receipts/mod.rs
  - crates/core/src/transport/mod.rs
  - crates/core/src/transport/tcp.rs
  - crates/core/src/transport/quic.rs
  - crates/core/examples/transport_demo.rs
  - crates/seal-protocols/src/lib.rs
  - crates/seal-wasm/src/lib.rs
  - crates/wasm/src/lib.rs
  - crates/platform/tests/platform_integration.rs
  - crates/platform/tests/verify_integration.rs
  - crates/experimental/pubky/src/bin/sealedge-pubky.rs
  - crates/experimental/pubky/examples/your_exact_api.rs
  - crates/experimental/pubky/tests/integration_tests.rs
autonomous: true
requirements:
  - DOCS-03
tags:
  - docs
  - rebrand
  - rustdoc
must_haves:
  truths:
    - "Every `///` and `//!` doc comment in crates/**/*.rs and examples/**/*.rs that referenced trustedge/TrustEdge as the product now says sealedge/Sealedge"
    - "Every `/// Project: trustedge — Privacy and trust at the edge.` rustdoc-form copyright header becomes `/// Project: sealedge — Privacy and trust at the edge.`"
    - "Every rustdoc code block (```rust blocks inside /// or //!) that imports or references a `trustedge-*` crate now references the `sealedge-*` crate"
    - "`cargo doc --workspace --no-deps` exits 0 (rustdoc compiles without warnings introduced by this sweep)"
    - "`cargo test --doc --workspace --locked` exits 0 (doc tests pass — renaming `use trustedge_core::…` to `use sealedge_core::…` in examples does not break the embedded doc tests)"
    - "`grep -rnE '///.*[Tt]rustedge|//!.*[Tt]rustedge' crates/ examples/ --include='*.rs' | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC'` returns no unexpected lines"
  artifacts:
    - path: crates/core/src/lib.rs
      provides: "Top-level sealedge-core rustdoc module documentation"
      contains: "sealedge-core"
    - path: crates/wasm/src/lib.rs
      provides: "WASM crate module doc — previously imported from 'trustedge-wasm'"
      contains: "sealedge-wasm"
    - path: crates/cli/src/main.rs
      provides: "CLI binary rustdoc module/item docs"
      contains: "sealedge"
  key_links:
    - from: crates/core/src/lib.rs
      to: "cargo doc output at target/doc/sealedge_core/index.html"
      via: "module-level //! renders as crate landing page"
      pattern: "//!"
    - from: crates/seal-wasm/src/lib.rs
      to: "cargo test --doc"
      via: "rustdoc code blocks execute as doc tests"
      pattern: "//!.*```"
---

<objective>
Sweep every `///` (item-level) and `//!` (module-level) rustdoc comment in `crates/**/*.rs` and `examples/**/*.rs` that references the product `trustedge`/`TrustEdge` as a brand word, crate identifier, env-var, binary name, or extension. Includes the 6 rustdoc-form MPL-2.0 copyright headers (`/// Project: trustedge`) that Phase 85 missed (Phase 85 handled the `//` line-comment form; these are `///` rustdoc form). Ensure `cargo doc --workspace --no-deps` and `cargo test --doc --workspace --locked` both pass post-sweep.

Purpose: DOCS-03 requires `cargo doc` output to render "sealedge" everywhere. Module-level `//!` on lib.rs files becomes the crate's docs.rs landing page. Item-level `///` becomes the API reference. Stale brand references here are what a developer sees when they run `cargo doc --open` or browse docs.rs.
Output: 16 source files with rustdoc comments updated. All existing doc tests continue to pass. `cargo doc` produces grep-clean target/doc HTML.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/86-documentation-sweep/86-CONTEXT.md
@.planning/phases/85-code-sweep-headers-text-metadata/85-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Full rename tables in 86-01-PLAN.md <interfaces>. -->

Rustdoc-specific scope (from Phase 85 D-18 boundary):
  - `///` on items: functions, structs, enums, fields, const, macros, etc.
  - `//!` on modules: lib.rs / mod.rs / file-level docs
  - Does NOT include `//` line comments (Phase 85 already swept those)

Rename summary:
  trustedge-<X>       → sealedge-<X>           (crate names in prose + code blocks)
  trustedge_<X>       → sealedge_<X>           (Rust identifier path — underscore form)
  trustedge           → sealedge               (binary in prose + code blocks)
  trst                → seal                   (archive binary)
  .trst, .te-*        → .seal, .se-*
  TRUSTEDGE_*         → SEALEDGE_*
  TrustEdge-Labs/trustedge URL → TrustEdge-Labs/sealedge URL

Preserve UNCHANGED: TrustEdge-Labs, TRUSTEDGE LABS LLC, generic "trust"/"edge"

Phase 85 deferred rustdoc surface (CONTEXT.md D-07):
  Scout count: ~188 lines (excludes legal-entity matches)
  Per-file hit counts (executor will discover exact numbers during read):
    crates/cli/src/main.rs             — /// Project: trustedge header + /// Input source for the trustedge application
    crates/core/examples/transport_demo.rs — /// Project: trustedge header
    crates/core/src/transport/{mod,tcp,quic}.rs — /// Project: trustedge headers (3 files)
    crates/core/src/lib.rs             — //! with `trustedge-core = { version = …, features = [...] }` code-block example
    crates/core/src/vectors.rs         — /// comment referencing "Phase 85 Plan 05 test-vector rename: b\"trustedge-test-*\" -> b\"sealedge-test-*\"" (this is a historical-rationale doc comment explaining the rename itself — hybrid judgment: either keep as historical context with Sealedge-perspective framing, or strip the explanation since v6.0 codebase is post-rename)
    crates/core/src/applications/receipts/mod.rs — //! with analogy "(trustedge-core)" + /// "hash of the previous trustedge::Envelope"
    crates/seal-protocols/src/lib.rs   — //! "the browser WASM verifier and the main trustedge-core library"
    crates/seal-wasm/src/lib.rs        — //! "canonical manifest types from `trustedge-trst-protocols`"
    crates/wasm/src/lib.rs             — //! "import init … from 'trustedge-wasm'"
    crates/platform/tests/platform_integration.rs — //! "Migrated from trustedge-platform-api/platform-api/tests/integration_test.rs" + "cargo test -p trustedge-platform --test platform_integration" + env var "TEST_DATABASE_URL (default: postgres://postgres:password@localhost:5432/trustedge_test)"
    crates/platform/tests/verify_integration.rs — //! "Migrated from trustedge-verify-core/tests/integration_tests.rs"
    crates/experimental/pubky/examples/your_exact_api.rs — /// "Inside the new `trustedge-pubky` crate:"
    crates/experimental/pubky/src/bin/sealedge-pubky.rs — /// Project: trustedge header + /// "'trustedge-pubky resolve' to verify it exists" + /// "created with 'trustedge-pubky generate'"
    crates/experimental/pubky/tests/integration_tests.rs — //! "Integration tests for trustedge-pubky CLI"

Doc-test gotcha (from CONTEXT.md "Established Patterns"):
  Rustdoc ```rust blocks compile as doc tests. Renaming `use trustedge_core::…` to `use sealedge_core::…` in a rustdoc example updates both prose AND the compiled doc test. Before running `cargo test --doc`, ensure the renamed imports resolve to crates that actually exist in the workspace at those names — Phase 83 renamed the crates; Phase 85 renamed Cargo.toml metadata; this sweep should be internally consistent.

Historical-rationale comment exception:
  crates/core/src/vectors.rs line 73 is a `///` comment that *explains* the v6.0 rename: "Updated for Phase 85 Plan 05 test-vector rename: b\"trustedge-test-*\" -> b\"sealedge-test-*\"". This is a historical-rationale comment (like D-02/D-03 hybrid for CHANGELOG/MIGRATION). Executor can either: (a) keep the comment verbatim (it documents the rename), (b) rephrase to "Post-v6.0 rebrand: test vectors now use `b\"sealedge-test-*\"`; v5.x-shaped `b\"trustedge-test-*\"` vectors rejected by clean-break checks" — planner's preference is option (b) since it reframes the rationale from the sealedge perspective. Executor decides.
</interfaces>

<doc_test_awareness>
Files likely to have rustdoc code blocks that compile as doc tests:
  - crates/core/src/lib.rs — lib-level //! with usage example
  - crates/seal-protocols/src/lib.rs — //! with example
  - crates/seal-wasm/src/lib.rs — //! with TypeScript import example (NOT compiled as Rust doc test — JS snippets)
  - crates/wasm/src/lib.rs — //! with JS import example (NOT compiled as doc test)
  - crates/core/src/applications/receipts/mod.rs — //! with "Security Guard" analogy (prose only)

Doc-test verification gate:
  `cargo test --doc --workspace --locked`
  Must exit 0 after sweep. If renaming `use trustedge_core::…` to `use sealedge_core::…` in a doc-test block fails because the crate path resolution doesn't exist, the workspace's crate setup is inconsistent (either Cargo.toml or src/ rename was incomplete) and executor must fix that before finishing this task — this plan does NOT include Cargo.toml edits (those were Phase 83/85), so any such failure should be reported back to the planner as a pre-existing gap.
</doc_test_awareness>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer ← cargo doc | Generated rustdoc HTML is the API reference; stale brand in landing page confuses developers |
| CI ← cargo test --doc | Doc tests enforce that example code compiles; rename-mismatch in rustdoc breaks CI |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-30   | Tampering | rustdoc prose | mitigate | Task 1 sweeps all hit sites; Task 2 gates on cargo doc + cargo test --doc green |
| T-86-31   | Denial of service | doc-test compilation | mitigate | Task 2 runs `cargo test --doc --workspace --locked` as acceptance gate |
| T-86-32   | Information disclosure | cargo doc output | accept | Generated HTML is public; rename-only, no secrets exposed |

No source code logic, auth paths, or crypto primitives modified. Attack surface = rustdoc comments only.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Rustdoc sweep across crates/**/*.rs and examples/**/*.rs</name>
  <files>crates/cli/src/main.rs, crates/core/src/lib.rs, crates/core/src/vectors.rs, crates/core/src/applications/receipts/mod.rs, crates/core/src/transport/mod.rs, crates/core/src/transport/tcp.rs, crates/core/src/transport/quic.rs, crates/core/examples/transport_demo.rs, crates/seal-protocols/src/lib.rs, crates/seal-wasm/src/lib.rs, crates/wasm/src/lib.rs, crates/platform/tests/platform_integration.rs, crates/platform/tests/verify_integration.rs, crates/experimental/pubky/src/bin/sealedge-pubky.rs, crates/experimental/pubky/examples/your_exact_api.rs, crates/experimental/pubky/tests/integration_tests.rs</files>
  <read_first>
    - .planning/phases/86-documentation-sweep/86-CONTEXT.md (D-07, D-08)
    - .planning/phases/85-code-sweep-headers-text-metadata/85-CONTEXT.md (D-11..D-14 casing)
    - Each file in `<files>` — focus on `///` and `//!` lines (bulk of .rs content is code bodies, which Phase 85 swept; Phase 86 updates only the rustdoc surface)
  </read_first>
  <behavior>
    - Every `/// Project: trustedge — Privacy and trust at the edge.` header line → `/// Project: sealedge — Privacy and trust at the edge.`
    - Every `/// GitHub: https://github.com/TrustEdge-Labs/trustedge` → `/// GitHub: https://github.com/TrustEdge-Labs/sealedge`
    - Every `//!` module-level docstring mentioning a `trustedge-<X>` crate → `sealedge-<X>` per rename map
    - Every `///` item-level docstring mentioning `trustedge`/`TrustEdge` as product → `sealedge`/`Sealedge` per casing
    - Every rustdoc code block (```rust, ```toml, ```bash, ```javascript, ```typescript) updated to use new crate/binary/extension names
    - Historical-rationale comment in `crates/core/src/vectors.rs:73` is rephrased OR left verbatim (executor's choice per interfaces note)
    - Preserve: `TrustEdge-Labs` (org in URL path), `TRUSTEDGE LABS LLC` (legal entity, if it appears in rustdoc — rare), generic "trust"/"edge" vocabulary
  </behavior>
  <action>
    For each file in `<files>`, walk rustdoc comments (lines starting with `///` or `//!`) and apply the substitution rubric. Body code (non-doc-comment lines) should already be Phase-85 clean — do NOT modify body code in this task.

    **Substitution order (longer/specific first, same as 86-01 Task 1):**

    1. Rustdoc-form MPL-2.0 headers (where `///` is used instead of `//`):
       - `/// Project: trustedge — Privacy and trust at the edge.` → `/// Project: sealedge — Privacy and trust at the edge.`
       - `/// GitHub: https://github.com/TrustEdge-Labs/trustedge` → `/// GitHub: https://github.com/TrustEdge-Labs/sealedge`
       - `/// Copyright (c) 2025 TRUSTEDGE LABS LLC` — LEAVE UNCHANGED (legal entity)
       - `/// MPL-2.0: https://mozilla.org/MPL/2.0/` — LEAVE UNCHANGED (external URL, not brand)

    2. Crate identifiers in prose + code blocks (Cargo.toml snippets, `cargo` command blocks):
       - `trustedge-platform-server` → `sealedge-platform-server`
       - `trustedge-trst-protocols` → `sealedge-seal-protocols`
       - `trustedge-trst-wasm` → `sealedge-seal-wasm`
       - `trustedge-trst-cli` → `sealedge-seal-cli`
       - `trustedge-platform-api` → `sealedge-platform-api` (if this appears as a historical reference in a `//!` "Migrated from …" line, consider whether to hybrid-preserve — see per-file notes below)
       - `trustedge-verify-core` → `sealedge-verify-core` (same historical-note consideration)
       - `trustedge-platform` → `sealedge-platform`
       - `trustedge-server` / `trustedge-client` → `sealedge-server` / `sealedge-client`
       - `trustedge-pubky-advanced` → `sealedge-pubky-advanced`
       - `trustedge-pubky` → `sealedge-pubky`
       - `trustedge-core` → `sealedge-core`
       - `trustedge-types` → `sealedge-types`
       - `trustedge-wasm` → `sealedge-wasm`
       - `trustedge-cli` → `sealedge-cli`

    3. Rust identifier paths (underscore form, in ```rust code blocks or `use` statements inside doc comments):
       - `trustedge_core` → `sealedge_core`
       - `trustedge_types` → `sealedge_types`
       - `trustedge_platform` → `sealedge_platform`
       - `trustedge::Envelope` → `sealedge::Envelope` (prose form treating product name as module path)

    4. Binary names:
       - `inspect-trst` → `inspect-seal`
       - `trst` bare binary → `seal` (in shell code blocks)
       - `trustedge` bare binary → `sealedge` (in shell code blocks; guard against `trustedge-<X>` crate names — those already covered above)

    5. Extensions:
       - `.te-attestation.json` → `.se-attestation.json`
       - `.trst` → `.seal`

    6. Env vars:
       - `TRUSTEDGE_DEVICE_ID` → `SEALEDGE_DEVICE_ID`
       - `TRUSTEDGE_SALT` → `SEALEDGE_SALT`
       - `TEST_DATABASE_URL (default: postgres://postgres:password@localhost:5432/trustedge_test)` → `... /sealedge_test)` (database name reflects product — this is a test-env-only DB name and there's no legacy-data concern)

    7. URLs:
       - `https://github.com/TrustEdge-Labs/trustedge` → `https://github.com/TrustEdge-Labs/sealedge`

    8. Wire constants in prose:
       - `TRUSTEDGE-KEY-V1` → `SEALEDGE-KEY-V1`
       - `TRUSTEDGE_ENVELOPE_V1` → `SEALEDGE_ENVELOPE_V1`

    9. Product-name prose (rustdoc English):
       - `TrustEdge project` → `Sealedge project`
       - `the TrustEdge application` → `the Sealedge application`
       - `trustedge` lowercase in mid-sentence → `sealedge`
       - `TrustEdge` sentence-start or title-cased product → `Sealedge` (guard against `-Labs`, `LABS LLC`)

    **Per-file notes:**

    - `crates/cli/src/main.rs`:
      - Line 8: `/// Project: trustedge — …` → `/// Project: sealedge — …`
      - Line 53: `/// Input source for the trustedge application` → `/// Input source for the sealedge application`

    - `crates/core/src/lib.rs`:
      - Line 86 (approx): `//! trustedge-core = { version = "0.2", features = ["audio"] }` → `//! sealedge-core = { version = "0.6", features = ["audio"] }` (note the version bump — v0.2 referenced pre-v6.0 crate version; v6.0 should use current published version. Check `crates/core/Cargo.toml` `version` field and use that value. If Cargo.toml shows `0.6.0` or similar, use that.)
      - Any surrounding prose in `//!` block — same rubric.

    - `crates/core/src/vectors.rs` line 73: `/// Updated for Phase 85 Plan 05 test-vector rename: b"trustedge-test-*" -> b"sealedge-test-*"`
      - Executor's choice: (a) leave verbatim — documents the rename historically, OR (b) rephrase to `/// Post-v6.0 test vectors: b"sealedge-test-*" (v5.x-shaped b"trustedge-test-*" rejected by clean-break checks).`
      - Preferred: (b) — reframes from sealedge perspective, still documents that the rename happened.
      - If the body code actually still has `b"sealedge-test-*"` literals (Phase 85 should have done this), option (b) aligns doc with body.

    - `crates/core/src/applications/receipts/mod.rs`:
      - Line ~13: `//! … That's the job of the "Security Guard" (trustedge-core).` → `//! … (sealedge-core).`
      - Line ~60: `/// A hash of the previous trustedge::Envelope in the chain.` → `/// A hash of the previous sealedge::Envelope in the chain.`

    - `crates/core/src/transport/{mod,tcp,quic}.rs` + `crates/core/examples/transport_demo.rs`:
      - Each has `/// Project: trustedge — Privacy and trust at the edge.` header. Replace with sealedge. Also update corresponding `/// GitHub: …` line to `/TrustEdge-Labs/sealedge`.

    - `crates/seal-protocols/src/lib.rs`:
      - `//! … the browser WASM verifier and the main trustedge-core library.` → `//! … the main sealedge-core library.`

    - `crates/seal-wasm/src/lib.rs`:
      - `//! … canonical manifest types from \`trustedge-trst-protocols\`.` → `//! … from \`sealedge-seal-protocols\`.`

    - `crates/wasm/src/lib.rs`:
      - `//! import init, { encrypt_data, decrypt_data } from 'trustedge-wasm';` → `//! import init, { encrypt_data, decrypt_data } from 'sealedge-wasm';`
      - Header rustdoc if present.

    - `crates/platform/tests/platform_integration.rs`:
      - `//! Migrated from trustedge-platform-api/platform-api/tests/integration_test.rs.` → historical note. Hybrid choice: rephrase to `//! Migrated from the pre-v6.0 trustedge-platform-api/…` (preserves historical truth since that crate no longer exists under that name) OR update fully to `//! Migrated from sealedge-platform-api/…` if the crate was simply renamed (not removed).
      - Preferred: `//! Migrated from the v5.x trustedge-platform-api crate (renamed to sealedge-platform in v6.0)/…`
      - `//! Run with: \`cargo test -p trustedge-platform --test platform_integration\`` → `//! Run with: \`cargo test -p sealedge-platform --test platform_integration\`` (this is a forward-looking command, not historical — update fully)
      - `//! Environment variable: TEST_DATABASE_URL (default: postgres://…/trustedge_test)` → `//! … /sealedge_test)`

    - `crates/platform/tests/verify_integration.rs`:
      - `//! Migrated from trustedge-verify-core/tests/integration_tests.rs.` → Same hybrid pattern as above: `//! Migrated from the v5.x trustedge-verify-core crate (merged into sealedge-platform in v6.0)/…`

    - `crates/experimental/pubky/examples/your_exact_api.rs`:
      - `/// Inside the new \`trustedge-pubky\` crate:` → `/// Inside the new \`sealedge-pubky\` crate:`
      - (Remove "new" if that framing no longer fits — executor judgment.)

    - `crates/experimental/pubky/src/bin/sealedge-pubky.rs`:
      - Line 8: `/// Project: trustedge` header → `/// Project: sealedge`
      - Line 136: `/// 'trustedge-pubky resolve' to verify it exists.` → `/// 'sealedge-pubky resolve' to verify it exists.`
      - Line 178: `/// The private key file created with 'trustedge-pubky generate'.` → `/// The private key file created with 'sealedge-pubky generate'.`

    - `crates/experimental/pubky/tests/integration_tests.rs`:
      - `//! Integration tests for trustedge-pubky CLI and library functionality` → `//! Integration tests for sealedge-pubky CLI and library functionality`

    **Step: Build + doc + doc-test gate after editing:**
    ```bash
    cargo build --workspace --locked
    cargo doc --workspace --no-deps
    cargo test --doc --workspace --locked
    ```
    All three must exit 0. If doc-tests fail due to rename mismatch (e.g., a `use sealedge_core::…` doesn't resolve because Cargo.toml still says `name = "trustedge-core"`), STOP — do not guess; report the workspace inconsistency back and let planner decide.
  </action>
  <verify>
    <automated>bash -c 'set -e; cargo build --workspace --locked 2>&1 | tail -20; cargo doc --workspace --no-deps 2>&1 | tail -20; cargo test --doc --workspace --locked 2>&1 | tail -20; echo ok-build-doc-doctest'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --workspace --locked` exits 0
    - `cargo doc --workspace --no-deps` exits 0
    - `cargo test --doc --workspace --locked` exits 0
    - `grep -rnE '^///.*Project: trustedge' crates/ examples/ --include='*.rs' | grep -v target/` returns 0 hits (all rustdoc-form copyright headers updated)
    - `grep -rnE '^///.*GitHub: https://github\.com/TrustEdge-Labs/trustedge\b' crates/ examples/ --include='*.rs' | grep -v target/` returns 0 hits
    - `grep -rnE '///.*trustedge-(core|platform|cli|wasm|trst|types|server|client|pubky)|//!.*trustedge-(core|platform|cli|wasm|trst|types|server|client|pubky)' crates/ examples/ --include='*.rs' | grep -v target/ | grep -vE 'TrustEdge-Labs|v5\.x|pre-v6\.0|v5\.x trustedge'` returns 0 hits (i.e., forward-looking rustdoc uses sealedge-<X>; only historical "v5.x trustedge-<X>" or similar hybrid prose is acceptable)
    - `grep -rnE '//!.*\btrustedge_(core|types|platform)\b|///.*\btrustedge_(core|types|platform)\b' crates/ examples/ --include='*.rs' | grep -v target/` returns 0 hits (Rust identifier paths updated)
    - For each file in `<files>` that had a `/// Project: trustedge` header: post-edit `grep -q "/// Project: sealedge" "$f"` succeeds
    - `cargo doc` output: `grep -rni trustedge target/doc/ 2>/dev/null | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|static\.files' | wc -l` returns 0 OR a small number that are all allowlisted historical-note prose (Plan 05 verifies this with exact allowlist)
  </acceptance_criteria>
  <done>
    16 source files have rustdoc updated. `cargo doc --workspace --no-deps` builds clean. `cargo test --doc --workspace --locked` passes. Historical-note `//!` "Migrated from …" comments use hybrid pattern (old name cited as historical, current code references sealedge). `vectors.rs:73` hybrid-rationale comment handled.
  </done>
</task>

</tasks>

<verification>
```bash
# Per-file rustdoc grep check
for f in crates/cli/src/main.rs crates/core/src/lib.rs crates/core/src/vectors.rs crates/core/src/applications/receipts/mod.rs crates/core/src/transport/mod.rs crates/core/src/transport/tcp.rs crates/core/src/transport/quic.rs crates/core/examples/transport_demo.rs crates/seal-protocols/src/lib.rs crates/seal-wasm/src/lib.rs crates/wasm/src/lib.rs crates/platform/tests/platform_integration.rs crates/platform/tests/verify_integration.rs crates/experimental/pubky/src/bin/sealedge-pubky.rs crates/experimental/pubky/examples/your_exact_api.rs crates/experimental/pubky/tests/integration_tests.rs; do
  stale=$(grep -nE '///.*[Tt]rustedge|//!.*[Tt]rustedge' "$f" 2>/dev/null | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|v5\.x trustedge-|pre-v6\.0' | wc -l)
  [ "$stale" -ne 0 ] && echo "STALE rustdoc: $f ($stale hits)"
done
# Expected: no STALE output, or lines whose context makes them hybrid-acceptable

# Gates
cargo build --workspace --locked
cargo doc --workspace --no-deps
cargo test --doc --workspace --locked

# target/doc/ grep (informational for Plan 05)
grep -rni trustedge target/doc/ 2>/dev/null | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|static\.files' | wc -l
```
</verification>

<success_criteria>
- 16 .rs files have rustdoc `///` and `//!` comments updated
- 6 rustdoc-form `/// Project: trustedge` copyright headers converted to `/// Project: sealedge`
- All workspace tests continue to pass (doc tests inclusive)
- `cargo doc --workspace --no-deps` succeeds
- Hybrid-preservation approach applied to historical "Migrated from …" notes and the vectors.rs:73 rename-rationale comment
- DOCS-03 requirement addressed
</success_criteria>

<output>
After completion, create `.planning/phases/86-documentation-sweep/86-04-SUMMARY.md`:
- 16 files updated
- Per-file hit counts (rustdoc-only)
- Hybrid choices made for historical references (v5.x module citations in test //! blocks, vectors.rs:73)
- `cargo doc --workspace --no-deps` exit code + stdout tail
- `cargo test --doc --workspace --locked` exit code + doc-test count
- `grep -rni trustedge target/doc/` hit count (handed off to Plan 05's final audit)
</output>
