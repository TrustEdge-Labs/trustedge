---
phase: 83-crate-and-binary-rename
plan: 02
type: execute
wave: 2
depends_on: ["83-01"]
files_modified:
  - Cargo.toml
  - crates/trst-protocols/Cargo.toml (rename dir to crates/seal-protocols/)
  - crates/trst-cli/Cargo.toml (rename dir to crates/seal-cli/)
  - crates/trst-wasm/Cargo.toml (rename dir to crates/seal-wasm/)
  - crates/core/Cargo.toml (path dep + package dep key updates)
  - crates/core/src/bin/inspect-trst.rs (rename to inspect-seal.rs)
  - examples/cam.video/Cargo.toml (package name rename only; dep key was done in Plan 01)
  - (all .rs files with use trustedge_trst_{protocols,wasm} imports — primary: crates/core/src/**, crates/seal-*/src/**, crates/seal-cli/tests/**)
autonomous: true
requirements: [REBRAND-01, REBRAND-02]

must_haves:
  truths:
    - "cargo metadata shows zero packages whose name starts with trustedge- (including the former trst family)"
    - "The former trst binary is now named seal"
    - "cargo check --workspace --locked compiles clean"
    - "cargo test --workspace passes all existing tests"
    - "No .rs file in the real tree contains use trustedge_trst_protocols or use trustedge_trst_wasm"
  artifacts:
    - path: "crates/seal-cli/Cargo.toml"
      provides: "Renamed trst-cli crate"
      contains: 'name = "sealedge-seal-cli"'
    - path: "crates/seal-protocols/Cargo.toml"
      provides: "Renamed trst-protocols crate"
      contains: 'name = "sealedge-seal-protocols"'
    - path: "crates/seal-wasm/Cargo.toml"
      provides: "Renamed trst-wasm crate"
      contains: 'name = "sealedge-seal-wasm"'
    - path: "crates/core/src/bin/inspect-seal.rs"
      provides: "Renamed inspect-trst binary source"
  key_links:
    - from: "crates/seal-cli/Cargo.toml [[bin]]"
      to: "src/main.rs"
      via: "name = seal"
      pattern: 'name = "seal"'
    - from: "crates/core/Cargo.toml [dependencies]"
      to: "crates/seal-protocols/"
      via: "path = ../seal-protocols"
      pattern: 'path = "\\.\\./seal-protocols"'
    - from: "crates/seal-wasm/Cargo.toml [dependencies]"
      to: "crates/seal-protocols/"
      via: "path = ../seal-protocols"
      pattern: 'path = "\\.\\./seal-protocols"'
---

<objective>
Rename the three trst-family crates (`trustedge-trst-protocols`, `trustedge-trst-cli`, `trustedge-trst-wasm`) to `sealedge-seal-*` equivalents, normalize their directories (`crates/trst-*/` -> `crates/seal-*/`), rename the `trst` binary to `seal`, rename the `inspect-trst` binary source to `inspect-seal`, rename the `trustedge-cam-video-examples` package to `sealedge-cam-video-examples`, and sweep `use trustedge_trst_{protocols,wasm}` identifiers to `use sealedge_seal_{protocols,wasm}` across the tree in one workspace-coherent atomic commit.

Purpose: Completes the package-level rename surface started in Plan 01. After this plan, `cargo metadata` shows zero `trustedge-*` package names across the root workspace.

Output: 3 trst-family crates renamed, 3 crate directories renamed, 1 binary renamed (`trst` -> `seal`), 1 bin source file renamed (`inspect-trst.rs` -> `inspect-seal.rs`), 1 examples package renamed (`trustedge-cam-video-examples` -> `sealedge-cam-video-examples`), and ~20 .rs files updated with `sealedge_seal_*` identifiers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-crate-and-binary-rename/83-CONTEXT.md
@.planning/phases/83-crate-and-binary-rename/83-PATTERNS.md
@.planning/phases/83-crate-and-binary-rename/83-01-SUMMARY.md
@CLAUDE.md
</context>

<interfaces>
<!-- Token transformation table for Plan 02. -->

Package name (Cargo.toml [package] name field):
  trustedge-trst-protocols     -> sealedge-seal-protocols
  trustedge-trst-cli           -> sealedge-seal-cli
  trustedge-trst-wasm          -> sealedge-seal-wasm
  trustedge-cam-video-examples -> sealedge-cam-video-examples

Library name (source code use statements):
  trustedge_trst_protocols -> sealedge_seal_protocols
  trustedge_trst_wasm      -> sealedge_seal_wasm

Binary target:
  trst           -> seal         (crates/seal-cli/Cargo.toml)
  inspect-trst   -> inspect-seal (crates/core/Cargo.toml + src/bin/inspect-seal.rs)

Crate directory rename:
  crates/trst-protocols/ -> crates/seal-protocols/
  crates/trst-cli/       -> crates/seal-cli/
  crates/trst-wasm/      -> crates/seal-wasm/

Binary source file rename:
  crates/core/src/bin/inspect-trst.rs -> crates/core/src/bin/inspect-seal.rs

Path dependency updates (in crates/core/Cargo.toml and crates/seal-wasm/Cargo.toml):
  path = "../trst-protocols" -> path = "../seal-protocols"

**Out of scope for Plan 02:**
- `.trst` extension literals in .rs source code (Plan 03)
- Experimental crates trustedge-pubky / trustedge-pubky-advanced (Plan 06)
- Script/CI binary invocations referencing `-p trustedge-trst-cli` (Plan 05)
- JS binding file renames (Plan 04)
- `anyhow::bail!("Output directory must end with .trst")` runtime strings (Plan 03 — lockstep with extension semantics)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename 3 trst-family crates + cam-video-examples package; rename 3 crate directories; rename trst binary and inspect-trst source</name>
  <read_first>
    - crates/trst-protocols/Cargo.toml
    - crates/trst-cli/Cargo.toml
    - crates/trst-wasm/Cargo.toml
    - crates/core/Cargo.toml (has path dep `../trst-protocols` and `[[bin]] inspect-trst`)
    - examples/cam.video/Cargo.toml
    - Cargo.toml (workspace root members list)
    - .planning/phases/83-crate-and-binary-rename/83-CONTEXT.md (decisions D-01, D-03; Claude's Discretion on dir naming)
    - .planning/phases/83-crate-and-binary-rename/83-PATTERNS.md §"Category 4" (bin block format), §"Category 5" (path deps), §"Category 8" (bin source files), §"Category 16" (examples)
  </read_first>
  <files>
    Cargo.toml,
    crates/trst-protocols/ (rename directory to crates/seal-protocols/),
    crates/trst-cli/ (rename directory to crates/seal-cli/),
    crates/trst-wasm/ (rename directory to crates/seal-wasm/),
    crates/seal-protocols/Cargo.toml (after dir rename),
    crates/seal-cli/Cargo.toml (after dir rename),
    crates/seal-wasm/Cargo.toml (after dir rename),
    crates/core/Cargo.toml,
    crates/core/src/bin/inspect-trst.rs (rename to inspect-seal.rs),
    examples/cam.video/Cargo.toml
  </files>
  <action>
Execute the following substitutions exactly.

**1. Rename 3 crate directories:**
```
git mv crates/trst-protocols crates/seal-protocols
git mv crates/trst-cli crates/seal-cli
git mv crates/trst-wasm crates/seal-wasm
```

**2. Rename bin source file:**
```
git mv crates/core/src/bin/inspect-trst.rs crates/core/src/bin/inspect-seal.rs
```

**3. Update workspace root `Cargo.toml`:**
- In `members` list, replace:
  - `"crates/trst-protocols"` -> `"crates/seal-protocols"`
  - `"crates/trst-cli"` -> `"crates/seal-cli"`
  - `"crates/trst-wasm"` -> `"crates/seal-wasm"`
- (Plan 01 already updated `"crates/trustedge-cli"` -> `"crates/cli"`; do not re-edit.)

**4. `crates/seal-protocols/Cargo.toml` (formerly trst-protocols):**
- `[package] name = "trustedge-trst-protocols"` -> `name = "sealedge-seal-protocols"`
- `[lib] name = "trustedge_trst_protocols"` -> `name = "sealedge_seal_protocols"` (if present).

**5. `crates/seal-cli/Cargo.toml` (formerly trst-cli):**
- `[package] name = "trustedge-trst-cli"` -> `name = "sealedge-seal-cli"`
- `[[bin]] name = "trst"` -> `name = "seal"` (path `src/main.rs` stays).
- `[dependencies]`:
  - `trustedge-core = { path = "../core" }` -> `sealedge-core = { path = "../core" }` (Plan 01 renamed the target; this plan updates the consumer key).
  - `trustedge-types = { workspace = true }` -> `sealedge-types = { workspace = true }`.
  - If present, any `trustedge-trst-protocols` key -> `sealedge-seal-protocols` and path `../trst-protocols` -> `../seal-protocols`.
- `[features]` `yubikey = ["trustedge-core/yubikey"]` -> `yubikey = ["sealedge-core/yubikey"]`.

**6. `crates/seal-wasm/Cargo.toml` (formerly trst-wasm):**
- `[package] name = "trustedge-trst-wasm"` -> `name = "sealedge-seal-wasm"`
- `[lib] name = "trustedge_trst_wasm"` -> `name = "sealedge_seal_wasm"` (if present).
- `[dependencies]` `trustedge-trst-protocols = { path = "../trst-protocols" }` -> `sealedge-seal-protocols = { path = "../seal-protocols" }`. Also any `trustedge-core` dep -> `sealedge-core`.

**7. `crates/core/Cargo.toml`:**
- `[dependencies]` `trustedge-trst-protocols = { path = "../trst-protocols" }` -> `sealedge-seal-protocols = { path = "../seal-protocols" }`.
- `[[bin]]` for inspect-trst: `name = "inspect-trst"` -> `name = "inspect-seal"` and `path = "src/bin/inspect-trst.rs"` -> `path = "src/bin/inspect-seal.rs"`.

**8. `examples/cam.video/Cargo.toml`:**
- `[package] name = "trustedge-cam-video-examples"` -> `name = "sealedge-cam-video-examples"`.
- (Plan 01 already updated its `trustedge-core` dep key to `sealedge-core`; do not re-edit.)

**Do NOT touch in this task:**
- `.trst` extension string literals inside `.rs` source code (Plan 03).
- `anyhow::bail!` messages with `.trst` (Plan 03).
- CLI help text `#[command(about = "TrustEdge .trst archival tool")]` — the brand word "TrustEdge" is Phase 85; the `.trst` substring is Plan 03; leave entirely for now.
- Copyright headers (`Project: trustedge`) — Phase 85.
- Cargo.toml metadata URLs and keywords — Phase 85.
  </action>
  <acceptance_criteria>
    - `rg -n '^name = "trustedge-(trst-protocols|trst-cli|trst-wasm|cam-video-examples)"' --glob 'crates/**/Cargo.toml' --glob 'examples/**/Cargo.toml'` returns 0 matches.
    - `rg -n '^name = "sealedge-(seal-protocols|seal-cli|seal-wasm|cam-video-examples)"' --glob 'crates/**/Cargo.toml' --glob 'examples/**/Cargo.toml'` returns exactly 4 matches.
    - `test ! -e crates/trst-protocols && test ! -e crates/trst-cli && test ! -e crates/trst-wasm` returns true.
    - `test -e crates/seal-protocols/Cargo.toml && test -e crates/seal-cli/Cargo.toml && test -e crates/seal-wasm/Cargo.toml` returns true.
    - `test ! -e crates/core/src/bin/inspect-trst.rs && test -e crates/core/src/bin/inspect-seal.rs` returns true.
    - `rg -n 'name = "trst"' --glob 'crates/**/Cargo.toml'` returns 0 matches.
    - `rg -n 'name = "seal"' crates/seal-cli/Cargo.toml` returns exactly 1 match.
    - `rg -n 'name = "inspect-trst"' --glob 'crates/**/Cargo.toml'` returns 0 matches; `rg -n 'name = "inspect-seal"' crates/core/Cargo.toml` returns 1 match.
    - `rg -n 'path = "\.\./trst-protocols"' --glob 'crates/**/Cargo.toml'` returns 0 matches (and the 2 consumers now use `../seal-protocols`).
  </acceptance_criteria>
  <verify>
    <automated>test ! -d crates/trst-protocols && test ! -d crates/trst-cli && test ! -d crates/trst-wasm && test -d crates/seal-protocols && test -d crates/seal-cli && test -d crates/seal-wasm && ! rg -q '^name = "trustedge-(trst-protocols|trst-cli|trst-wasm|cam-video-examples)"' --glob 'crates/**/Cargo.toml' --glob 'examples/**/Cargo.toml'</automated>
  </verify>
  <done>
    - 3 crate directories renamed, 3 package names renamed in Cargo.toml.
    - 1 example package renamed.
    - `trst` binary target renamed to `seal`; `inspect-trst` binary target renamed to `inspect-seal`.
    - 1 bin source file renamed.
    - Consumer path deps in crates/core/Cargo.toml and crates/seal-wasm/Cargo.toml repointed to `../seal-protocols`.
    - Workspace root `Cargo.toml` members list updated to new dirs.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep `use trustedge_trst_{protocols,wasm}` -> `use sealedge_seal_*` across .rs source; validate workspace compile + test; commit atomically</name>
  <read_first>
    - .planning/phases/83-crate-and-binary-rename/83-PATTERNS.md §"Category 9" (the token-table subset that applies: `trustedge_trst_protocols`, `trustedge_trst_wasm`)
    - .planning/phases/83-crate-and-binary-rename/83-PATTERNS.md §"Verification Greps" #1, #2, #4
    - crates/seal-cli/tests/acceptance.rs, integration_tests.rs, security_*.rs (heavy test file consumers)
    - crates/seal-wasm/src/lib.rs + crates/seal-wasm/tests/*.rs
    - crates/core/src/archive.rs or wherever trstprotocols is imported
    - CLAUDE.md §"Build & Test Commands"
  </read_first>
  <files>
    All .rs files with token `trustedge_trst_protocols` or `trustedge_trst_wasm`. Primary locations:
    - crates/core/src/**/*.rs
    - crates/seal-cli/src/**/*.rs + tests/**/*.rs
    - crates/seal-wasm/src/**/*.rs + tests/**/*.rs
    - crates/seal-protocols/src/**/*.rs (likely self-refs only)
  </files>
  <action>
**Part A: Sweep .rs source identifiers.**

```bash
# Find affected files.
rg -l --type rust 'trustedge_trst_(protocols|wasm)\b' \
  -g '!target/**' -g '!.claude/**' -g '!.planning/**' \
  | tee /tmp/plan02-files.txt

# Apply substitutions.
while read f; do
  sed -i -E \
    -e 's/\btrustedge_trst_protocols\b/sealedge_seal_protocols/g' \
    -e 's/\btrustedge_trst_wasm\b/sealedge_seal_wasm/g' \
    "$f"
done < /tmp/plan02-files.txt
```

The `\b` word boundaries ensure we don't touch `trustedge_trst_protocols_something_else` (shouldn't exist, but defensive).

**Part B: Validate workspace compiles and tests pass.**

```bash
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --locked
```

If `cargo fmt --check` reports diffs (likely from module-path churn), run `cargo fmt` and include the output in this commit.

If clippy warns about crate-name references in doc-comment code blocks that no longer match, those are Phase 86 (prose) concerns — suppress via `#[allow(...)]` only if they're hard-blocking; otherwise record in SUMMARY.md for Phase 86 follow-up.

**Part C: Atomic commit.**

```bash
git add -A
git commit -m "$(cat <<'EOF'
refactor(83-02): rename trst-family crates trustedge-trst-* -> sealedge-seal-*

Renames the three trst-family crates:
  trustedge-trst-protocols -> sealedge-seal-protocols (crates/seal-protocols/)
  trustedge-trst-cli       -> sealedge-seal-cli       (crates/seal-cli/)
  trustedge-trst-wasm      -> sealedge-seal-wasm      (crates/seal-wasm/)

Also renames:
  trst binary         -> seal
  inspect-trst binary -> inspect-seal (src/bin/inspect-seal.rs)
  trustedge-cam-video-examples package -> sealedge-cam-video-examples
  ~20 .rs files updated to use sealedge_seal_* identifiers

With Plan 01, every root-workspace package is now sealedge-*.

.trst extension literals (Plan 03), package.json + JS bindings (Plan 04),
CI + scripts (Plan 05), experimental crates (Plan 06) rename in
subsequent atomic commits.

Workspace coherent: cargo check, cargo test --workspace, clippy, fmt all green.

Requirements: REBRAND-01, REBRAND-02.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <acceptance_criteria>
    - `rg -n 'trustedge_trst_(protocols|wasm)\b' --glob '*.rs' --glob '!target/**' --glob '!.claude/**' --glob '!.planning/**'` returns 0 matches.
    - `rg -n 'sealedge_seal_(protocols|wasm)\b' --glob '*.rs' --glob '!target/**'` returns non-zero matches (sweep confirmed landed).
    - `rg -n 'trustedge-(core|types|platform|platform-server|cli|wasm|trst-protocols|trst-cli|trst-wasm|cam-video-examples)' --glob 'Cargo.toml' --glob 'crates/**/Cargo.toml' --glob 'examples/**/Cargo.toml' --glob '!crates/experimental/**'` returns 0 matches (PATTERNS Verification Grep #1 for root workspace).
    - `cargo fmt --check` exits 0.
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
    - `cargo test --workspace --locked` exits 0.
    - `git log -1 --pretty=%s` returns `refactor(83-02): rename trst-family crates trustedge-trst-* -> sealedge-seal-*`.
  </acceptance_criteria>
  <verify>
    <automated>! rg -q 'trustedge_trst_(protocols|wasm)\b' --glob '*.rs' --glob '!target/**' --glob '!.claude/**' --glob '!.planning/**' && cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace --locked</automated>
  </verify>
  <done>
    - Zero `trustedge_trst_*` identifiers remain in the real tree (outside deferred prose/help-text).
    - Full workspace test suite green.
    - Commit landed atomically with specified message.
    - Ready for Plan 03 (`.trst` -> `.seal` extension literal sweep).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer -> workspace | sed pattern correctness — `trustedge_trst_protocols` token must not accidentally catch or be caught by Plan 01's already-renamed tokens. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-05 | T (Tampering) | sed across .rs files | mitigate | Word-boundary regex; cargo check immediately after sweep. |
| T-83-06 | D (Denial of Service) | Path dep mismatch (Cargo.toml says ../trst-protocols but dir is ../seal-protocols) | mitigate | Task 1 updates all 2 consumers (core, seal-wasm) alongside the dir rename; Task 2's `cargo check` gate catches any miss. |
| T-83-07 | R (Repudiation) | `inspect-trst` binary output path mismatch | mitigate | `[[bin]] path = "src/bin/inspect-seal.rs"` must match the renamed source file; Task 1 acceptance grep verifies. |
</threat_model>

<verification>
- `cargo metadata --format-version 1 | jq '.packages[] | select(.name | startswith("trustedge-")) | .name'` returns empty array (no root-workspace package starts with `trustedge-`). Experimental crates still have trustedge-pubky* names — those belong to Plan 06.
- `cargo test --workspace --locked` green.
- PATTERNS.md Verification Grep #1 (trustedge-* package names in Cargo.toml) returns 0 for root workspace (experimental excluded).
- PATTERNS.md Verification Grep #4 (trst / trustedge-server / trustedge-client / trustedge-platform-server [[bin]] names) returns 0.
</verification>

<success_criteria>
- 3 trst-family crates renamed + directories normalized.
- 1 examples crate package renamed.
- `trst` -> `seal` binary target; `inspect-trst` -> `inspect-seal` binary target + source file.
- ~20 .rs files updated to `sealedge_seal_*`.
- Every root-workspace Cargo.toml package name is now `sealedge-*`.
- Single atomic commit with green cargo test + clippy + fmt.
</success_criteria>

<output>
After completion, create `.planning/phases/83-crate-and-binary-rename/83-02-SUMMARY.md` listing:
- Directories renamed.
- Files renamed.
- Count of .rs files touched by the sealedge_seal_* sweep.
- Final commit SHA.
- Confirmation `cargo metadata` shows zero trustedge-* root packages.
</output>
