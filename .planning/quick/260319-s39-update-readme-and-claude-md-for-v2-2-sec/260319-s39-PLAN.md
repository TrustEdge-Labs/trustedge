---
phase: quick-s39
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
autonomous: true
requirements: [DOC-01]

must_haves:
  truths:
    - "Version badge reads v2.2"
    - "How It Works section reflects v2-only envelope and OAEP-SHA256"
    - "Use-case examples include --unencrypted for CI/automation context"
    - "CLAUDE.md describes encrypted key files and --unencrypted flag"
  artifacts:
    - path: "README.md"
      provides: "User-facing documentation for v2.2 security features"
      contains: "version-2.2"
    - path: "CLAUDE.md"
      provides: "Developer guidance for encrypted keys and --unencrypted flag"
      contains: "--unencrypted"
  key_links:
    - from: "README.md How It Works"
      to: "v2 envelope format"
      via: "step 2 description"
      pattern: "HKDF"
---

<objective>
Update README.md and CLAUDE.md to accurately reflect the v2.2 Security Remediation changes shipped across phases 45-47.

Purpose: Developers and users reading the docs should understand the current security posture — OAEP-SHA256, v2-only envelopes, 300k PBKDF2 minimum, encrypted device keys, and the --unencrypted escape hatch for CI.

Output: README.md with v2.2 badge and updated How It Works; CLAUDE.md with encrypted key file format and --unencrypted flag documented.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@README.md
@CLAUDE.md

Changes shipped in v2.2 that need documentation:
1. RSA OAEP-SHA256 replaces PKCS#1 v1.5 in asymmetric.rs (Phase 45)
2. v1 envelope format removed entirely — v2-only from now on (Phase 46)
3. PBKDF2_MIN_ITERATIONS = 300_000 enforced at builder level (Phase 46)
4. Device keys encrypted at rest using TRUSTEDGE-KEY-V1 format (PBKDF2 + AES-GCM) (Phase 47)
5. --unencrypted flag added to keygen/wrap/unwrap for CI/automation (Phase 47)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md for v2.2</name>
  <files>README.md</files>
  <action>
Make the following targeted edits to README.md:

1. **Version badge** (line 12): Change `version-2.1` to `version-2.2` and the display text from `2.1` to `2.2`. Update the releases tag URL from `v2.1` to `v2.2`.

2. **How It Works section** (lines 117-126): Update steps to reflect v2.2 reality:
   - Step 1 (Sign): keep as-is
   - Step 2 (Encrypt): change from "XChaCha20Poly1305 using an HKDF-derived key" to "Data is chunked and each chunk is AES-256-GCM encrypted using HKDF-derived keys (v2 envelope format)"
   - Add a new step after step 1 or update step 1 to mention: device keys are encrypted at rest with TRUSTEDGE-KEY-V1 format (PBKDF2 + AES-GCM); passphrase is prompted at runtime
   - Keep steps 3-6 as-is (Wrap, Verify, Unwrap, Receipt)

3. **Use-case examples** — in the Drone Inspection example (and/or a CI/automation note), add a brief callout that for CI/testing contexts `--unencrypted` skips passphrase protection. Place it naturally after the keygen example:

   Add a brief note block after the keygen line in the Drone Inspection example:
   ```
   # For CI/automation (no passphrase prompt):
   trst keygen --out-key drone.key --out-pub drone.pub --unencrypted
   ```

   Do NOT change the primary examples to use --unencrypted — keep them showing the secure default.

4. **Security note**: In the How It Works section or just before it, add a short paragraph (2-3 sentences) called "Security Posture" describing the v2.2 improvements: RSA OAEP-SHA256 for asymmetric ops, v2-only envelope format, and encrypted device keys at rest.

Keep all existing content, prose style, and section structure. Only add/modify the specific items above.
  </action>
  <verify>
    <automated>grep -n "version-2.2\|OAEP\|TRUSTEDGE-KEY-V1\|--unencrypted\|AES-256-GCM" /home/john/vault/projects/github.com/trustedge/README.md</automated>
  </verify>
  <done>README.md badge shows v2.2, How It Works mentions v2 envelope and AES-256-GCM, --unencrypted appears in CI callout, security posture note is present.</done>
</task>

<task type="auto">
  <name>Task 2: Update CLAUDE.md for v2.2 developer guidance</name>
  <files>CLAUDE.md</files>
  <action>
Make the following targeted edits to CLAUDE.md:

1. **Working with Archives section** (around the `trst` examples): Update the keygen example to show both the default (encrypted) and --unencrypted variant:

   Replace or augment the existing keygen example to read:
   ```bash
   # Generate device keypair (encrypted at rest — passphrase prompted)
   cargo run -p trustedge-trst-cli -- keygen --out-key device.key --out-pub device.pub

   # For CI/automation (unencrypted key file — no passphrase)
   cargo run -p trustedge-trst-cli -- keygen --out-key device.key --out-pub device.pub --unencrypted
   ```

   Update the wrap example to note that if the key file is encrypted, a passphrase will be prompted. Add `--unencrypted` to the verify example if it uses a key file (verify reads the pub key, not the private key — so no change needed there).

2. **Key Patterns section** — Add a new subsection called "Encrypted Key Files (TRUSTEDGE-KEY-V1)" after the `.trst Archive Structure` subsection:

   ```markdown
   ### Encrypted Key Files (TRUSTEDGE-KEY-V1)

   Device private keys are encrypted at rest using PBKDF2-HMAC-SHA256 (600k iterations) + AES-256-GCM.
   The format header is `TRUSTEDGE-KEY-V1`. A passphrase is prompted at runtime via `rpassword`.

   For CI/automation where interactive prompts are not possible, use `--unencrypted`:
   - `trst keygen --unencrypted` — generates plaintext key file
   - `trst wrap --unencrypted` — reads key without passphrase prompt
   - `trst unwrap --unencrypted` — reads key without passphrase prompt

   Production devices should always use encrypted key files. The `--unencrypted` flag is an
   explicit escape hatch and is never the default.
   ```

3. **Feature Flags table for trustedge-platform** — no changes needed unless there is a v2.2-specific flag (there isn't).

4. Do NOT modify build/test commands, copyright header, code standards, or other sections.
  </action>
  <verify>
    <automated>grep -n "TRUSTEDGE-KEY-V1\|--unencrypted\|Encrypted Key" /home/john/vault/projects/github.com/trustedge/CLAUDE.md</automated>
  </verify>
  <done>CLAUDE.md has "Encrypted Key Files (TRUSTEDGE-KEY-V1)" subsection, both encrypted and --unencrypted keygen examples are present, and the automation escape hatch is clearly documented.</done>
</task>

</tasks>

<verification>
After both tasks complete:
- `grep "version-2.2" README.md` returns the badge line
- `grep "TRUSTEDGE-KEY-V1" README.md CLAUDE.md` returns matches in both files
- `grep "\-\-unencrypted" README.md CLAUDE.md` returns matches in both files
- `grep "OAEP\|OAEP-SHA256" README.md` returns the security posture note
</verification>

<success_criteria>
- README.md version badge is v2.2
- README.md How It Works reflects current v2 envelope format (AES-256-GCM, HKDF)
- README.md has a security posture note mentioning OAEP-SHA256 and encrypted keys at rest
- README.md shows --unencrypted as a CI callout in keygen example
- CLAUDE.md has "Encrypted Key Files (TRUSTEDGE-KEY-V1)" subsection with format, iterations, and --unencrypted guidance
- CLAUDE.md keygen examples show both encrypted (default) and --unencrypted variants
- All existing content and prose style preserved
</success_criteria>

<output>
After completion, create `.planning/quick/260319-s39-update-readme-and-claude-md-for-v2-2-sec/260319-s39-SUMMARY.md`
</output>
