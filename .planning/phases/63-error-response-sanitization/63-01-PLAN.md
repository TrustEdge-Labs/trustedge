---
phase: 63-error-response-sanitization
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/platform/src/http/handlers.rs
  - crates/platform/src/verify/validation.rs
  - crates/platform/tests/verify_integration.rs
autonomous: true
requirements:
  - ERRH-01
must_haves:
  truths:
    - "A request that triggers a verify_to_report error returns a generic message with no library detail"
    - "A request that triggers a receipt signing error returns a generic message with no library detail"
    - "The full error detail appears in server-side warn!() logs for both error paths"
    - "A successful verification response is unaffected by the change"
  artifacts:
    - path: "crates/platform/src/http/handlers.rs"
      provides: "Sanitized error responses in both verify handlers"
      contains: "Cryptographic verification failed"
    - path: "crates/platform/src/verify/validation.rs"
      provides: "Sanitized receipt error in build_receipt_if_requested"
      contains: "Receipt generation failed"
    - path: "crates/platform/tests/verify_integration.rs"
      provides: "Integration tests proving error sanitization"
      contains: "test_verify_error_does_not_leak"
  key_links:
    - from: "crates/platform/src/http/handlers.rs"
      to: "tracing::warn!"
      via: "Full error logged before generic message returned"
      pattern: "warn!.*Verification failed.*\\{.*\\}"
---

<objective>
Sanitize crypto error responses so API clients receive only a category-level message ("Cryptographic verification failed" or "Receipt generation failed") with zero library internals. Full error chains stay in server-side logs via the existing warn!() calls.

Purpose: Close Finding 6 from the security review — raw crypto library error strings were leaking to HTTP clients, potentially revealing algorithm details and internal state.
Output: Patched handlers.rs and validation.rs with generic messages, plus integration tests proving no leakage.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/63-error-response-sanitization/63-CONTEXT.md

@crates/platform/src/http/handlers.rs
@crates/platform/src/verify/validation.rs
@crates/platform/tests/verify_integration.rs
</context>

<interfaces>
<!-- Key types the executor needs -->

From crates/platform/src/verify/validation.rs:
```rust
pub struct ValidationError {
    pub error: String,
    pub detail: String,
}

impl ValidationError {
    pub fn new(error: &str, detail: &str) -> Self { ... }
}
```

From crates/platform/src/http/handlers.rs (error sites to modify):
```rust
// Line 86 (non-postgres verify_handler):
Json(ValidationError::new("verification_failed", &format!("Cryptographic verification failed: {}", e)))

// Line 168 (postgres verify_handler):
Json(ValidationError::new("verification_failed", &format!("Cryptographic verification failed: {}", e)))

// Line 273 (postgres verify_handler, receipt signing):
Json(ValidationError::new("receipt_signing_failed", &format!("Failed to sign receipt: {}", e)))
```

From crates/platform/src/verify/validation.rs (line 155-158, build_receipt_if_requested):
```rust
Err(e) => Err(ValidationError::new(
    "receipt_signing_failed",
    &format!("Failed to sign receipt: {}", e),
)),
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Sanitize error messages in handlers.rs and validation.rs</name>
  <files>crates/platform/src/http/handlers.rs, crates/platform/src/verify/validation.rs</files>
  <read_first>
    crates/platform/src/http/handlers.rs
    crates/platform/src/verify/validation.rs
    .planning/phases/63-error-response-sanitization/63-CONTEXT.md
  </read_first>
  <behavior>
    - After change: format!("Cryptographic verification failed: {}", e) is replaced with static "Cryptographic verification failed" at all three verify error sites (handlers.rs lines ~86, ~168 and validation.rs is handled in Task 1 too)
    - After change: format!("Failed to sign receipt: {}", e) is replaced with static "Receipt generation failed" at both receipt error sites (handlers.rs line ~273 and validation.rs line ~157)
    - The warn!() calls that log the full error detail remain unchanged
    - Existing tests still pass (no behavior change for success paths)
  </behavior>
  <action>
    Per D-01, D-02, D-03: Replace the four format!() calls that leak raw error strings to clients:

    **handlers.rs — non-postgres verify_handler (line ~86):**
    Change `&format!("Cryptographic verification failed: {}", e)` to `"Cryptographic verification failed"`.
    The `warn!("Verification failed: {}", e)` on line 81 already logs the full detail — leave it unchanged per D-04.

    **handlers.rs — postgres verify_handler (line ~168):**
    Change `&format!("Cryptographic verification failed: {}", e)` to `"Cryptographic verification failed"`.
    The `warn!("Verification failed: {}", e)` on line 163 already logs the full detail — leave it unchanged per D-04.

    **handlers.rs — postgres verify_handler receipt signing (line ~273):**
    Change `&format!("Failed to sign receipt: {}", e)` to `"Receipt generation failed"` per D-02.
    The `warn!("Failed to sign receipt: {}", e)` on line 268 already logs the full detail — leave it unchanged.

    **validation.rs — build_receipt_if_requested (line ~157):**
    Change `&format!("Failed to sign receipt: {}", e)` to `"Receipt generation failed"` per D-02.
    Per D-05: Check that a `warn!()` call exists before this error return. If missing, add `warn!("Failed to sign receipt: {}", e);` before the Err return and add `use tracing::warn;` if not already imported in this module.

    Keep the error code strings unchanged ("verification_failed", "receipt_signing_failed") — only the detail message changes.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/trustedge && cargo test -p trustedge-platform --lib && cargo test -p trustedge-platform --test verify_integration</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'format!.*Cryptographic verification failed.*{}' crates/platform/src/http/handlers.rs returns 0
    - grep -c 'format!.*Failed to sign receipt.*{}' crates/platform/src/http/handlers.rs returns 0
    - grep -c 'format!.*Failed to sign receipt.*{}' crates/platform/src/verify/validation.rs returns 0
    - grep -c '"Cryptographic verification failed"' crates/platform/src/http/handlers.rs returns 2 (one per verify_handler)
    - grep -c '"Receipt generation failed"' crates/platform/src/http/handlers.rs returns 1
    - grep -c '"Receipt generation failed"' crates/platform/src/verify/validation.rs returns 1
    - grep -c 'warn!.*Verification failed' crates/platform/src/http/handlers.rs returns 2 (unchanged logging)
    - All existing tests pass
  </acceptance_criteria>
  <done>All four format!() calls replaced with static generic messages. Zero raw library error strings reach API clients. Server-side warn!() logging unchanged.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add integration tests proving error responses contain no library detail</name>
  <files>crates/platform/tests/verify_integration.rs</files>
  <read_first>
    crates/platform/tests/verify_integration.rs
    crates/platform/src/http/handlers.rs
    crates/platform/src/verify/engine.rs
  </read_first>
  <behavior>
    - Test: POST /v1/verify with a structurally invalid manifest (e.g., missing signature field) triggers verify_to_report error path, and the response body contains "Cryptographic verification failed" but does NOT contain any of: "SignatureError", "InvalidSignature", "base64", "decode", "parse"
    - Test: POST /v1/verify with a valid signed manifest and correct key still returns HTTP 200 with a VerifyResponse (success path unaffected)
  </behavior>
  <action>
    Per D-06: Add integration tests to the existing `http_tests` module in verify_integration.rs (inside the `#[cfg(all(feature = "http", not(feature = "postgres")))]` block).

    **Test 1 — `test_verify_error_does_not_leak_library_detail`:**
    1. Build a manifest with a deliberately malformed signature (e.g., set "signature" to "not-valid-base64!!!") and a valid device_pub.
    2. POST to /v1/verify.
    3. Assert response status is 400.
    4. Parse response body as JSON.
    5. Assert body["detail"] == "Cryptographic verification failed" (exact match).
    6. Convert body to string, assert it does NOT contain any of these substrings that would indicate library error leakage: "SignatureError", "InvalidSignature", "base64", "decode error", "verification equation".

    **Test 2 — `test_verify_success_unaffected_by_sanitization`:**
    1. Use the existing `build_signed_manifest` + `build_verify_body` helpers with a valid key.
    2. POST to /v1/verify.
    3. Assert response status is 200.
    4. Parse body as VerifyResponse JSON.
    5. Assert `result.signature_verification.passed == true`.

    Note: This second test may already be covered by `test_verify_round_trip`, but including a focused regression test per D-06 is a safety net. If it would be duplicative, the executor may skip it and note why.

    Run with: `cargo test -p trustedge-platform --test verify_integration --features http`
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/trustedge && cargo test -p trustedge-platform --test verify_integration --features http</automated>
  </verify>
  <acceptance_criteria>
    - grep -c 'test_verify_error_does_not_leak' crates/platform/tests/verify_integration.rs returns at least 1
    - grep -c 'Cryptographic verification failed' crates/platform/tests/verify_integration.rs returns at least 1 (test asserts exact message)
    - grep -c 'SignatureError' crates/platform/tests/verify_integration.rs returns at least 1 (test asserts this substring is absent from response)
    - cargo test -p trustedge-platform --test verify_integration --features http passes
  </acceptance_criteria>
  <done>Integration test proves: (a) error response body contains only the generic message, (b) no library error strings leak, (c) success path is unaffected.</done>
</task>

</tasks>

<verification>
1. `cargo test -p trustedge-platform --lib` — all unit tests pass
2. `cargo test -p trustedge-platform --test verify_integration --features http` — all integration tests pass including new sanitization tests
3. `cargo clippy -p trustedge-platform --features "http" -- -D warnings` — no warnings
4. `grep -r 'format!.*Cryptographic verification failed' crates/platform/` returns zero matches
5. `grep -r 'format!.*Failed to sign receipt' crates/platform/` returns zero matches
</verification>

<success_criteria>
- Zero format!() calls that append raw error strings to client-facing messages in handlers.rs or validation.rs
- New integration test proves error responses contain only generic category messages
- All 22+ existing integration tests continue to pass
- Server-side warn!() logging of full error details is preserved unchanged
</success_criteria>

<output>
After completion, create `.planning/phases/63-error-response-sanitization/63-01-SUMMARY.md`
</output>
