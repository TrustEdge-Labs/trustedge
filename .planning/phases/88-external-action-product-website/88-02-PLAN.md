---
phase: 88-external-action-product-website
plan: 02
type: execute
wave: 2
depends_on:
  - 88-01
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - EXT-02
tags:
  - rebrand
  - ci-workflow
  - release-artifact
  - dogfood
  - phase-88
must_haves:
  truths:
    - "`.github/workflows/ci.yml` self-attest release job uploads `seal` binary + `seal.sha256` checksum file to the GitHub release (D-12) — without these, the new action cannot download anything (T3 mitigation)"
    - "The `seal.sha256` file is generated with the same `sha256sum | awk '{print $1 \"  seal\"}'` format the action expects to parse (CONTEXT.md §Specifics)"
    - "The inline attest step chain (`Build seal binary` → `Generate CycloneDX SBOM` → `Generate ephemeral Ed25519 keypair` → `Create SBOM attestation`) is converted to use `TrustEdge-Labs/sealedge-attest-sbom-action@v2` (D-14 dogfood conversion)"
    - "The existing `continue-on-error: true` guard (line 176) stays — it covers the Phase 88 → Phase 89 bootstrap window when `@v2` exists but the first release hasn't fired yet (D-11)"
    - "The stale `.te-attestation.json` extension (lines 212, 220 — pre-Phase-84 gap) disappears via the dogfood conversion because the `Create SBOM attestation` inline step is deleted entirely (PATTERNS.md Option A)"
    - "The workflow YAML still parses cleanly after the edit (yq check)"
  artifacts:
    - path: .github/workflows/ci.yml
      provides: "self-attest release job with (a) seal + seal.sha256 upload step, (b) dogfood `uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2` step"
      contains: "sealedge-attest-sbom-action@v2"
  key_links:
    - from: ".github/workflows/ci.yml (release trigger: push tag v*)"
      to: "GitHub Release assets (seal, seal.sha256, attestation, build.pub)"
      via: "`gh release upload ${{ github.ref_name }} --clobber` (two steps: attestation+pubkey existing, binary+checksum new)"
      pattern: "gh release upload.*seal\\.sha256|gh release upload.*seal "
    - from: ".github/workflows/ci.yml (Attest SBOM step)"
      to: "TrustEdge-Labs/sealedge-attest-sbom-action@v2 (external action, ships in Plan 03)"
      via: "composite action invocation with `binary:` + `sbom:` + `seal-version:` inputs"
      pattern: "uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2"
    - from: "Action's SHA256 verification block (downloads seal.sha256 from release)"
      to: "ci.yml's Compute + Upload seal.sha256 step (produces the file at release time)"
      via: "shared release-artifact convention — filename `seal.sha256`, format `<hash>  seal`"
      pattern: "sha256sum.*seal.*awk.*seal"
---

<objective>
Extend `.github/workflows/ci.yml`'s self-attest release job to (1) upload `seal` binary + `seal.sha256` checksum to the release (D-12 — without this, the new action cannot download anything — T3 mitigation), and (2) convert the inline 4-step attest chain (Build seal → Generate CycloneDX SBOM → Generate ephemeral keypair → Create SBOM attestation) to a single `uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2` step (D-14 dogfood). The existing `continue-on-error: true` guard (line 176) stays — it covers the chicken-and-egg bootstrap window per D-11.

Purpose: EXT-02 — the SHA256 checksum verification preserved by Plan 01 in `action.yml` is functional only if the checksum file exists in the release. D-12 closes that gap. D-14 dogfoods the action so Phase 89's first v6.0.0 release run proves `@v2` end-to-end.

Output: `.github/workflows/ci.yml` edited in-place with two logical commits: (a) add seal + seal.sha256 upload steps, (b) convert attest step chain to `uses: @v2` dogfood step. Both commits land in the monorepo on main; the dogfood step does not fire end-to-end until Phase 89 pushes the first v6.0.0 tag (per D-11, guarded by `continue-on-error: true`).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/88-external-action-product-website/88-CONTEXT.md
@.planning/phases/88-external-action-product-website/88-PATTERNS.md
@.planning/phases/88-external-action-product-website/88-01-SUMMARY.md
@CLAUDE.md
@.github/workflows/ci.yml

<interfaces>
<!-- Current state of the self-attest job (ci.yml:171-222 as of Phase 88 start) -->

```yaml
  # ── Self-attestation (release tags only) ─────────────────────
  self-attestation:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build-and-test
    continue-on-error: true
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libpcsclite-dev pkg-config pkgconf

      - name: Build seal binary
        run: cargo build -p sealedge-seal-cli --release --locked

      - name: Generate CycloneDX SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          path: ./target/release/seal
          format: cyclonedx-json
          output-file: seal-sbom.cdx.json
          upload-artifact: false

      - name: Generate ephemeral Ed25519 keypair
        run: |
          ./target/release/seal keygen \
            --out-key ephemeral.key \
            --out-pub build.pub \
            --unencrypted

      - name: Create SBOM attestation
        run: |
          ./target/release/seal attest-sbom \
            --binary ./target/release/seal \
            --sbom seal-sbom.cdx.json \
            --device-key ephemeral.key \
            --device-pub build.pub \
            --out seal.te-attestation.json \
            --unencrypted

      - name: Upload attestation assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.ref_name }}" \
            seal.te-attestation.json \
            build.pub \
            --clobber
```

<!-- Target state after Commit 1 (seal + seal.sha256 upload added, Build-seal step retained, SHA256 step inserted after Build but before upload): -->

```yaml
  # ── Self-attestation (release tags only) ─────────────────────
  self-attestation:
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    needs: build-and-test
    continue-on-error: true
    permissions:
      contents: write
    steps:
      - uses: actions/checkout@34e114876b0b11c390a56381ad16ebd13914f8d5 # v4
      - uses: dtolnay/rust-toolchain@631a55b12751854ce901bb631d5902ceb48146f7 # stable
      - uses: Swatinem/rust-cache@e18b497796c12c097a38f9edb9d0641fb99eee32 # v2

      - name: Install system dependencies
        run: sudo apt-get update && sudo apt-get install -y libpcsclite-dev pkg-config pkgconf

      - name: Build seal binary
        run: cargo build -p sealedge-seal-cli --release --locked

      - name: Compute seal binary SHA256 checksum
        run: |
          sha256sum ./target/release/seal | awk '{print $1 "  seal"}' > seal.sha256

      - name: Upload seal binary + checksum to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.ref_name }}" \
            ./target/release/seal \
            seal.sha256 \
            --clobber

      - name: Generate CycloneDX SBOM
        uses: anchore/sbom-action@e22c389904149dbc22b58101806040fa8d37a610 # v0.24.0
        with:
          path: ./target/release/seal
          format: cyclonedx-json
          output-file: seal-sbom.cdx.json
          upload-artifact: false

      - name: Generate ephemeral Ed25519 keypair
        run: |
          ./target/release/seal keygen \
            --out-key ephemeral.key \
            --out-pub build.pub \
            --unencrypted

      - name: Create SBOM attestation
        run: |
          ./target/release/seal attest-sbom \
            --binary ./target/release/seal \
            --sbom seal-sbom.cdx.json \
            --device-key ephemeral.key \
            --device-pub build.pub \
            --out seal.te-attestation.json \
            --unencrypted

      - name: Upload attestation assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.ref_name }}" \
            seal.te-attestation.json \
            build.pub \
            --clobber
```

<!-- Target state after Commit 2 (dogfood conversion — replaces 3 inline steps Generate ephemeral keypair + Create SBOM attestation [and implicitly deletes the stale seal.te-attestation.json upload from the Upload attestation assets step, which then becomes redundant]): -->

<!-- Per PATTERNS.md Option A: the `Create SBOM attestation` inline step is deleted entirely; the dogfood action writes its attestation to runner.temp/<binary>.se-attestation.json (action.yml line 30). We need to capture that output and upload IT to the release, replacing the stale `seal.te-attestation.json` path. -->

```yaml
      # ... (everything above the Generate ephemeral Ed25519 keypair step stays as in Commit 1 above) ...

      - name: Attest SBOM (dogfood sealedge-attest-sbom-action)
        id: attest
        uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2  # TODO: pin to commit SHA after first @v2.0.0 tag (Plan 03) + stable Phase 89 release run
        with:
          binary: ./target/release/seal
          sbom: seal-sbom.cdx.json

      - name: Upload attestation + ephemeral pubkey to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.ref_name }}" \
            "${{ steps.attest.outputs.attestation-path }}" \
            --clobber
```

<!-- NOTE on the ephemeral pubkey: with the dogfood conversion, the action generates its own ephemeral keypair inside ${{ runner.temp }}/ephemeral.{key,pub}; the public key is embedded inside the signed .se-attestation.json itself (attestations are self-verifying via embedded pubkey). Therefore `build.pub` is no longer needed as a separate release asset. The upload step simplifies to just the attestation file. -->

<!-- Line count delta expectations for the full Commit 2 conversion: -->
<!--   - DELETE: `Generate ephemeral Ed25519 keypair` step (5 lines) -->
<!--   - DELETE: `Create SBOM attestation` step (10 lines) -->
<!--   - DELETE: `Upload attestation assets` step uploading `seal.te-attestation.json` + `build.pub` (7 lines) -->
<!--   - ADD: `Attest SBOM (dogfood …)` step with `uses: @v2` (6 lines) -->
<!--   - ADD: `Upload attestation to release` step uploading `${{ steps.attest.outputs.attestation-path }}` (7 lines) -->
<!--   Net: ~22 lines removed, ~13 lines added, ~9 line reduction. `seal.te-attestation.json` string count drops to 0. -->

</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `Compute seal.sha256` + `Upload seal binary + checksum` steps to the self-attest job (D-12 / T3 mitigation)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (read lines 171-222, the full self-attest job, to confirm current state before inserting the two new steps)
    - .planning/phases/88-external-action-product-website/88-CONTEXT.md §D-12 (rationale: action needs seal + seal.sha256 in release), §Specifics "Seal binary upload step" (exact command shape)
    - .planning/phases/88-external-action-product-website/88-PATTERNS.md §Group C (self-analog extension: new steps mirror existing `Upload attestation assets` shape + `--clobber` flag)
  </read_first>
  <action>
Insert two new steps into `.github/workflows/ci.yml`'s `self-attestation` job, between the existing `- name: Build seal binary` step (line 188) and the existing `- name: Generate CycloneDX SBOM` step (line 190).

The two steps are verbatim (copy-paste exactly from the `<interfaces>` block's target state above — see the Commit 1 target excerpt):

```yaml
      - name: Compute seal binary SHA256 checksum
        run: |
          sha256sum ./target/release/seal | awk '{print $1 "  seal"}' > seal.sha256

      - name: Upload seal binary + checksum to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.ref_name }}" \
            ./target/release/seal \
            seal.sha256 \
            --clobber
```

**How to insert:** Use the Edit tool. Find the exact existing block:

```yaml
      - name: Build seal binary
        run: cargo build -p sealedge-seal-cli --release --locked

      - name: Generate CycloneDX SBOM
```

Replace with:

```yaml
      - name: Build seal binary
        run: cargo build -p sealedge-seal-cli --release --locked

      - name: Compute seal binary SHA256 checksum
        run: |
          sha256sum ./target/release/seal | awk '{print $1 "  seal"}' > seal.sha256

      - name: Upload seal binary + checksum to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.ref_name }}" \
            ./target/release/seal \
            seal.sha256 \
            --clobber

      - name: Generate CycloneDX SBOM
```

**Discipline rules:**
- YAML indentation: 6 spaces for `- name:`, 8 spaces for step body keys (`run:`, `env:`, `with:`). Match the file's existing pattern exactly. Do NOT introduce tabs.
- The two new steps sit AFTER `Build seal binary` (the binary must exist on disk before `sha256sum` runs, and the release must be created by the earlier release-creation job — which is assumed already present in this workflow; the `Upload attestation assets` step already runs `gh release upload` against `${{ github.ref_name }}`, proving the release exists at this point).
- Do NOT reorder any other steps.

**Verify the YAML still parses cleanly** before committing:

```bash
yq '.jobs.self-attestation.steps | length' .github/workflows/ci.yml
# Expected: 11 (was 9 — added 2 steps)

yq '.jobs.self-attestation.steps[] | .name' .github/workflows/ci.yml
# Expected output includes (in order):
#   Install system dependencies
#   Build seal binary
#   Compute seal binary SHA256 checksum
#   Upload seal binary + checksum to release
#   Generate CycloneDX SBOM
#   Generate ephemeral Ed25519 keypair
#   Create SBOM attestation
#   Upload attestation assets
# (plus the 3 unnamed `- uses: …` steps at the top: checkout, rust-toolchain, rust-cache)
```

**Commit:**

```bash
git add .github/workflows/ci.yml
git diff --cached
# Expected: ~11 insertions, 0 deletions; only new lines between Build seal and Generate CycloneDX SBOM

git commit -m "$(cat <<'EOF'
feat(88): upload seal binary + seal.sha256 checksum on release (D-12)

Per 88-CONTEXT.md D-12: the renamed sealedge-attest-sbom-action@v2 downloads
`seal` + `seal.sha256` from /TrustEdge-Labs/sealedge/releases/. Without these
artifacts in the release, the action has nothing to download (T3). This
commit adds two steps to the existing self-attest job:

  - Compute seal binary SHA256 checksum
    (sha256sum ./target/release/seal | awk '{print $1 "  seal"}' > seal.sha256)

  - Upload seal binary + checksum to release
    (gh release upload ${{ github.ref_name }} ./target/release/seal seal.sha256 --clobber)

Step ordering: inserted AFTER `Build seal binary` (binary must exist before
sha256sum runs) and BEFORE `Generate CycloneDX SBOM` (keeps release-asset
upload steps grouped near the top of the job body).

The SHA256 checksum format — `<hash>  seal` with 2 spaces — matches what
action.yml's verification block parses via `awk '{print $1}'`
(see actions/sealedge-attest-sbom-action/action.yml lines 51-58).

Part of Phase 88 EXT-02 / T3 mitigation.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <verify>
    <automated>
set -e
F=.github/workflows/ci.yml

# Both new steps present:
test "$(grep -c 'name: Compute seal binary SHA256 checksum' "$F")" -eq 1
test "$(grep -c 'name: Upload seal binary + checksum to release' "$F")" -eq 1

# Exact sha256sum command present (format must match what action.yml's awk parser expects):
test "$(grep -c "sha256sum \./target/release/seal | awk" "$F")" -eq 1

# seal + seal.sha256 appear in gh release upload:
test "$(grep -cE "gh release upload .*seal\.sha256" "$F")" -ge 1
test "$(grep -cE "\./target/release/seal\b" "$F")" -ge 2

# YAML parses cleanly (executor needs `yq` installed — if not, fall back to python -c 'import yaml; yaml.safe_load(open("...")'):
if command -v yq >/dev/null 2>&1; then
  yq '.jobs.self-attestation.steps | length' "$F" | grep -qE '^11$'
elif command -v python3 >/dev/null 2>&1; then
  python3 -c "import yaml; y = yaml.safe_load(open('$F')); assert len(y['jobs']['self-attestation']['steps']) == 11, 'expected 11 steps'"
fi

# Commit landed:
git log -1 --pretty=%s | grep -qE '^feat\(88\): upload seal binary'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'name: Compute seal binary SHA256 checksum' .github/workflows/ci.yml` returns exactly 1
    - `grep -c 'name: Upload seal binary + checksum to release' .github/workflows/ci.yml` returns exactly 1
    - `grep -c "sha256sum \./target/release/seal | awk" .github/workflows/ci.yml` returns exactly 1
    - `.github/workflows/ci.yml` contains `seal.sha256` in a `gh release upload` line (at least 1 such line)
    - YAML parses cleanly — `yq` (or `python3 -c "yaml.safe_load"`) confirms `jobs.self-attestation.steps` length is exactly 11 (was 9 before)
    - The new steps appear AFTER `Build seal binary` and BEFORE `Generate CycloneDX SBOM` in the steps array (order verified by reading the file)
    - Indentation of new steps matches the existing 6-space / 8-space convention (no tabs)
    - Commit subject starts with `feat(88): upload seal binary`
  </acceptance_criteria>
  <done>Two new steps inserted into the self-attest job; SHA256 checksum generated with the exact format action.yml parses; seal + seal.sha256 uploaded to release with `--clobber`; YAML parses cleanly; commit landed. T3 mitigation in place.</done>
</task>

<task type="auto">
  <name>Task 2: Convert inline attest chain to `uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2` dogfood step (D-14)</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (re-read lines 171-240 AFTER Task 1's commit — line numbers have shifted; use Read first to get the current state)
    - actions/sealedge-attest-sbom-action/action.yml (confirm action interface: inputs `binary`, `sbom`, `key`, `seal-version`; output `attestation-path`)
    - .planning/phases/88-external-action-product-website/88-CONTEXT.md §D-14 (dogfood rationale), §D-11 (chicken-and-egg timing — `continue-on-error: true` stays), §"Out of scope" bullet 7 (stale `.te-attestation.json` cleanup is folded into this refactor — PATTERNS.md Option A)
    - .planning/phases/88-external-action-product-website/88-PATTERNS.md §Group C (dogfood conversion diff shape: 4 inline steps → 1 `uses:` step)
  </read_first>
  <action>
Convert the inline attest step chain to use the dogfooded action.

**Step 2a — Delete the 3 inline attest steps.** Using the Edit tool, find this exact block (lines will have shifted after Task 1's commit — read the file first to confirm current line numbers):

```yaml
      - name: Generate ephemeral Ed25519 keypair
        run: |
          ./target/release/seal keygen \
            --out-key ephemeral.key \
            --out-pub build.pub \
            --unencrypted

      - name: Create SBOM attestation
        run: |
          ./target/release/seal attest-sbom \
            --binary ./target/release/seal \
            --sbom seal-sbom.cdx.json \
            --device-key ephemeral.key \
            --device-pub build.pub \
            --out seal.te-attestation.json \
            --unencrypted

      - name: Upload attestation assets
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.ref_name }}" \
            seal.te-attestation.json \
            build.pub \
            --clobber
```

Replace with:

```yaml
      - name: Attest SBOM (dogfood sealedge-attest-sbom-action)
        id: attest
        uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2  # TODO: pin to commit SHA after first @v2.0.0 tag (Plan 03)
        with:
          binary: ./target/release/seal
          sbom: seal-sbom.cdx.json

      - name: Upload attestation to release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          gh release upload "${{ github.ref_name }}" \
            "${{ steps.attest.outputs.attestation-path }}" \
            --clobber
```

**What this conversion does:**
- DELETES the ephemeral-keygen step (action.yml handles keygen when `key:` input is empty — our invocation omits `key:`, so the action generates its own ephemeral keypair at `${{ runner.temp }}/ephemeral.{key,pub}`)
- DELETES the `Create SBOM attestation` inline step (action.yml does the attest-sbom call internally; output path is exposed via `steps.attest.outputs.attestation-path`)
- DELETES the original `Upload attestation assets` step (it uploaded `seal.te-attestation.json` + `build.pub`; neither is needed — attestations are self-verifying via embedded pubkey, and the filename is `.se-attestation.json` per action.yml line 30)
- ADDS the `uses:` step with `id: attest` so the upload step can reference its output
- ADDS a new upload step using `${{ steps.attest.outputs.attestation-path }}` (the action writes to a `.se-attestation.json` path under `runner.temp`)

**D-11 bootstrap window:** `continue-on-error: true` at line 176 already exists — leave it in place. It covers:
- Between Plan 02 landing and Plan 03 cutting `@v2`: the `uses: @v2` reference resolves to nothing (the action repo hasn't been renamed + tagged yet), so the step will fail. `continue-on-error` prevents this from failing the whole CI run.
- Between Plan 03 `@v2` tag cut and Phase 89's first v6.0.0 release: on a release-tag push, `seal` + `seal.sha256` become available in the release per Task 1's upload steps — the action can then download and verify.

**Step 2b — YAML parse check:**

```bash
if command -v yq >/dev/null 2>&1; then
  yq '.jobs.self-attestation.steps | length' .github/workflows/ci.yml
  # Expected: 8 (Task 1 left it at 11; deleted 3 steps + added 2 = net -1 → 10 . Wait, recount:
  # Task 1 result: 11 steps. Task 2 deletes 3 steps (Generate ephemeral keypair, Create SBOM attestation, Upload attestation assets) and adds 2 (Attest SBOM dogfood, Upload attestation to release). Net: 11 - 3 + 2 = 10.
  # Expected: 10.
fi
```

(Correct the assertion to `length == 10`, not 8.)

Verify no stale references remain:

```bash
grep -c 'seal.te-attestation.json' .github/workflows/ci.yml  # expected: 0 (PATTERNS.md Option A)
grep -c 'seal attest-sbom'          .github/workflows/ci.yml  # expected: 0 (inline call deleted)
grep -c 'seal keygen'               .github/workflows/ci.yml  # expected: 0 (inline keygen deleted)
grep -c 'TrustEdge-Labs/sealedge-attest-sbom-action@v2' .github/workflows/ci.yml  # expected: >= 1
```

**Step 2c — Commit:**

```bash
git add .github/workflows/ci.yml
git diff --cached
# Expected: ~22 deletions + ~13 insertions (net ~9-line reduction)

git commit -m "$(cat <<'EOF'
refactor(88): dogfood sealedge-attest-sbom-action@v2 in CI self-attest job (D-14)

Per 88-CONTEXT.md D-14: the CI self-attest release job now dogfoods
TrustEdge-Labs/sealedge-attest-sbom-action@v2 instead of running the attest
steps inline. This serves two goals:

  1. First green release run on v6.0.0 (Phase 89) proves the action works
     end-to-end (downloads seal + seal.sha256 from release, verifies SHA256,
     generates ephemeral keypair, runs attest-sbom, produces .se-attestation.json)
  2. Re-establishes the Phase 79/80 self-attestation pattern against the
     rebranded action

DELETED inline steps:
  - Generate ephemeral Ed25519 keypair (action handles this when `key:` is empty)
  - Create SBOM attestation (action performs attest-sbom internally)
  - Upload attestation assets (uploaded stale .te-attestation.json + redundant build.pub)

ADDED:
  - Attest SBOM (dogfood sealedge-attest-sbom-action) — uses: @v2 with
    binary + sbom inputs; outputs attestation-path
  - Upload attestation to release — uploads ${{ steps.attest.outputs.attestation-path }}
    (which is .se-attestation.json per action.yml line 30 — fixes the pre-existing
    stale `.te-attestation.json` extension gap from Phase 84 via refactor)

Bootstrap window: `continue-on-error: true` (line 176, unchanged) covers the
period between Plan 02 landing and Plan 03 cutting @v2 on the renamed repo.
The first end-to-end green run happens on Phase 89's v6.0.0 release-tag push.

TODO (Plan 03 / Phase 89): pin the action to a commit SHA once @v2.0.0 is
cut and a stable release run confirms the chain.

Part of Phase 88 EXT-02 dogfood conversion.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```
  </action>
  <verify>
    <automated>
set -e
F=.github/workflows/ci.yml

# Dogfood step present:
test "$(grep -c 'uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2' "$F")" -eq 1
test "$(grep -c 'Attest SBOM (dogfood' "$F")" -eq 1
test "$(grep -c 'id: attest' "$F")" -ge 1

# Attestation-path output wired:
test "$(grep -cE "\\\$\\{\\{ steps\\.attest\\.outputs\\.attestation-path \\}\\}" "$F")" -eq 1

# Inline attest steps removed:
test "$(grep -c 'name: Generate ephemeral Ed25519 keypair' "$F")" -eq 0
test "$(grep -c 'name: Create SBOM attestation' "$F")" -eq 0
test "$(grep -c 'name: Upload attestation assets' "$F")" -eq 0

# Stale .te-attestation.json reference eliminated (PATTERNS.md Option A):
test "$(grep -c 'seal.te-attestation.json' "$F")" -eq 0
test "$(grep -c '\.te-attestation\.json' "$F")" -eq 0

# Inline `seal attest-sbom` / `seal keygen` calls removed:
test "$(grep -c 'seal attest-sbom' "$F")" -eq 0
test "$(grep -cE '/seal keygen' "$F")" -eq 0

# continue-on-error: true still present (D-11 bootstrap window guard):
test "$(grep -c 'continue-on-error: true' "$F")" -ge 1

# Task 1's seal + seal.sha256 upload step still intact:
test "$(grep -c 'Upload seal binary + checksum to release' "$F")" -eq 1

# YAML parses cleanly:
if command -v yq >/dev/null 2>&1; then
  yq '.jobs.self-attestation.steps | length' "$F" | grep -qE '^10$'
elif command -v python3 >/dev/null 2>&1; then
  python3 -c "import yaml; y = yaml.safe_load(open('$F')); assert len(y['jobs']['self-attestation']['steps']) == 10, 'expected 10 steps, got ' + str(len(y['jobs']['self-attestation']['steps']))"
fi

# Commit landed:
git log -1 --pretty=%s | grep -qE '^refactor\(88\): dogfood sealedge-attest-sbom-action'
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2' .github/workflows/ci.yml` returns exactly 1
    - `grep -c 'id: attest' .github/workflows/ci.yml` returns at least 1
    - `${{ steps.attest.outputs.attestation-path }}` appears exactly once in a `gh release upload` line
    - `grep -c 'name: Generate ephemeral Ed25519 keypair' .github/workflows/ci.yml` returns 0 (inline keygen deleted)
    - `grep -c 'name: Create SBOM attestation' .github/workflows/ci.yml` returns 0 (inline attest-sbom deleted)
    - `grep -c 'name: Upload attestation assets' .github/workflows/ci.yml` returns 0 (old upload step deleted)
    - `grep -c '\.te-attestation\.json' .github/workflows/ci.yml` returns 0 (PATTERNS.md Option A — stale extension gone)
    - `grep -c 'continue-on-error: true' .github/workflows/ci.yml` returns at least 1 (D-11 bootstrap guard preserved)
    - `grep -c 'Upload seal binary + checksum to release' .github/workflows/ci.yml` returns 1 (Task 1's step still in place)
    - YAML parses cleanly — `yq` (or `python3 -c "yaml.safe_load"`) confirms `jobs.self-attestation.steps` length is exactly 10 (was 11 after Task 1, now 10 after refactor)
    - Commit subject starts with `refactor(88): dogfood sealedge-attest-sbom-action`
  </acceptance_criteria>
  <done>Self-attest job uses `TrustEdge-Labs/sealedge-attest-sbom-action@v2`; inline attest chain deleted; stale `.te-attestation.json` extension eliminated via refactor; `continue-on-error: true` preserved for bootstrap window; Task 1's seal + seal.sha256 upload step intact; YAML parses cleanly; commit landed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → GitHub Release | `gh release upload` pushes binary + checksum assets to the public release; any regression = action cannot verify what it downloads |
| CI runner → renamed external action repo | `uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2` pulls action source from GitHub; relies on Plan 03 having cut the @v2 tag |
| action's SHA256 verification block → ci.yml's sha256 generator | Shared format convention — `<hash>  seal` (2 spaces); any mismatch = verification fails with false positive |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-06 (T3 carry) | Tampering | `seal.sha256` missing from release → action downloads binary but cannot verify it → falls through to "No SHA256 checksum file found for this release. Skipping verification." warning path in action.yml line 61 | mitigate | Task 1 adds the `Upload seal binary + checksum to release` step explicitly. Acceptance criterion `grep -cE "gh release upload .*seal\.sha256" ci.yml >= 1` confirms the upload is present. `--clobber` flag ensures idempotency if the release already exists (re-run safety). |
| T-88-07 | Tampering | SHA256 generator produces a format the action cannot parse → verification silently fails open | mitigate | Task 1 acceptance criterion `grep -c "sha256sum \./target/release/seal | awk" ci.yml == 1` asserts the exact generator command. action.yml line 53 parses via `awk '{print $1}'` (first whitespace-separated field), which matches our `awk '{print $1 "  seal"}'` output (hash is first field, "seal" is second). Format contract is explicitly documented in the Task 1 commit message. |
| T-88-08 | Denial of Service | Dogfood step fails on every release tag for the duration of the bootstrap window (Plan 02 merged but Plan 03 not yet executed → @v2 doesn't exist → action resolution errors) | mitigate | `continue-on-error: true` at line 176 (existing, preserved in Task 2 acceptance criteria) converts the failure to a soft-fail. Job still runs; release still ships; other CI jobs still gate merge. Phase 89's first v6.0.0 release run post-Plan-03 confirms the chain. |
| T-88-09 | Tampering | Task 2's Edit of a large YAML block introduces accidental indentation drift → YAML parses but step structure is wrong | mitigate | Task 2 acceptance criteria runs `yq` (or fallback `python3 yaml.safe_load`) to parse the file and assert `steps` length == 10 exactly. Mismatch = YAML is wrong; executor fixes before committing. |
| T-88-10 | Information Disclosure | Ephemeral keypair written to `${{ runner.temp }}` (action-managed) is accessible to any later step in the same job run | accept | Private key never persisted beyond the runner's ephemeral filesystem; runner is single-tenant per job per commit; no secret state remains after the job completes. ASVS L1 doesn't apply (no PII). Same risk profile as the inline ephemeral-keygen step being replaced. |
| T-88-11 | Repudiation | No artifact signed by `build.pub` survives in the release (Task 2 deletes the `build.pub` upload) → cannot verify which key signed the attestation | accept | Ephemeral key's public component is embedded INSIDE the signed `.se-attestation.json` (attestations are self-verifying). Uploading `build.pub` separately was redundant — the JSON is canonical. Downstream verifiers (`seal verify-attestation`, `sealedge-platform-server /v1/verify-attestation`) extract the pubkey from the JSON itself. |

**Plan 02 addresses T3 (release-asset integrity — `seal.sha256` now exists in release) directly. T1 (release-asset integrity post-rename) is Plan 03's responsibility but relies on Plan 02 having shipped the seal binary upload into existing v1-v5 release history BEFORE the repo rename. Overall grade: LOW. Bootstrap window risk is explicitly accepted via `continue-on-error: true`; end-to-end functional proof lands in Phase 89.**
</threat_model>

<verification>
**Plan-level gate (both tasks + 2 atomic commits must pass):**

1. Task 1: YAML parses with 11 steps in `self-attestation` job; `sha256sum` command matches action.yml's parser expectation; seal + seal.sha256 appear in `gh release upload` command; `feat(88):` commit landed
2. Task 2: YAML parses with 10 steps; `uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2` present; stale `.te-attestation.json` gone; `continue-on-error: true` preserved; Task 1's seal upload still in place; `refactor(88):` commit landed

**Commit count:** 2 (feat(88) + refactor(88)).

**End-to-end functional proof:** does NOT happen in Phase 88. The dogfood step won't resolve cleanly until Plan 03 cuts `@v2`, and won't run green end-to-end until Phase 89's v6.0.0 release tag triggers the self-attest job. `continue-on-error: true` is the bootstrap-window guard per D-11. This is expected and correct.

**Rollback:** `git revert` either or both commits. Plan 03 is independent (operates on external repo) — Plan 02 changes don't block Plan 03.
</verification>

<success_criteria>
- `.github/workflows/ci.yml` self-attest release job contains:
  - `Compute seal binary SHA256 checksum` step running `sha256sum ./target/release/seal | awk '{print $1 "  seal"}' > seal.sha256`
  - `Upload seal binary + checksum to release` step running `gh release upload ${{ github.ref_name }} ./target/release/seal seal.sha256 --clobber`
  - `Attest SBOM (dogfood sealedge-attest-sbom-action)` step with `uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2` and `id: attest`
  - `Upload attestation to release` step using `${{ steps.attest.outputs.attestation-path }}`
- Inline `Generate ephemeral Ed25519 keypair`, `Create SBOM attestation`, `Upload attestation assets` steps are deleted
- Stale `seal.te-attestation.json` string is gone (PATTERNS.md Option A achieved)
- `continue-on-error: true` preserved (D-11 bootstrap window guard)
- `jobs.self-attestation.steps` length is exactly 10 (verified via yq or yaml.safe_load)
- Both commits landed: `feat(88): upload seal binary ...` then `refactor(88): dogfood sealedge-attest-sbom-action ...`
- T3 mitigation verified (release will contain `seal` + `seal.sha256` on next release-tag push)
- Plan 03 (external `gh repo rename` + `@v2` cut) is unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/88-external-action-product-website/88-02-SUMMARY.md` per the gsd summary template. Include:
- The two ci.yml edits (add seal+sha256 upload, dogfood conversion)
- Confirmation that `continue-on-error: true` stays intact for the bootstrap window
- Confirmation that stale `.te-attestation.json` reference is eliminated via the refactor (PATTERNS.md Option A)
- Pointer forward to Plan 03 (which cuts `@v2` and runs the external `gh repo rename`)
- Note that end-to-end functional proof lands in Phase 89's first v6.0.0 release run
</output>
