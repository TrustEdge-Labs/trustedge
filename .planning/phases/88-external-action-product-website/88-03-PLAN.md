---
phase: 88-external-action-product-website
plan: 03
type: execute
wave: 3
depends_on:
  - 88-01
  - 88-02
files_modified:
  - .planning/phases/88-external-action-product-website/88-VERIFICATION.md
autonomous: false
requirements:
  - EXT-02
  - EXT-03
tags:
  - rebrand
  - external-operation
  - gh-repo-rename
  - release-tag
  - verification
  - phase-88
must_haves:
  truths:
    - "Pre-rename gate is green (CONTEXT.md §Specifics adapted from Phase 87 D-06): working tree clean on main, main pushed to origin, no open PRs on the old `attest-sbom-action` repo, `gh auth status` shows `repo` scope active"
    - "Monorepo-side folder `actions/sealedge-attest-sbom-action/` (from Plan 01) has been pushed to `origin/main` so the subtree-push in Step 4 has a clean source"
    - "External repo is renamed: `gh repo rename sealedge-attest-sbom-action -R TrustEdge-Labs/attest-sbom-action` succeeds (D-01)"
    - "`https://github.com/TrustEdge-Labs/attest-sbom-action` returns HTTP 301 redirecting to `/sealedge-attest-sbom-action` (D-09 curl check, verbatim from CONTEXT.md §Specifics)"
    - "Monorepo folder `actions/sealedge-attest-sbom-action/` content is pushed to the renamed external repo's `main` branch (using isolated subtree-push mechanism that does not leak other monorepo files — T5 mitigation)"
    - "A new `v2.0.0` tag + floating `v2` tag are cut on the renamed action repo, both pointing at the post-rename commit on the external repo's `main`"
    - "Existing `@v1` tag is left frozen — NO force-push, NO deletion, NO re-tag (D-07 clean-break discipline)"
    - "Marketplace listing title is verified to show `Sealedge SBOM Attestation` post-@v2-publish (D-10 user check; manual re-publish fallback documented)"
    - "Existing `@v1` release assets on the renamed repo remain accessible: `gh release view v1.0 -R TrustEdge-Labs/sealedge-attest-sbom-action` confirms asset URLs still resolve (T1 mitigation)"
    - "`88-VERIFICATION.md` captures all 5 evidence artifacts: pre-rename gate output, rename operation timestamp + output, curl 301 redirect output, @v2 tag push output, Marketplace listing screenshot/text + @v1 asset accessibility check"
    - "Rollback command is documented verbatim in `88-VERIFICATION.md` for future reference: `gh repo rename attest-sbom-action -R TrustEdge-Labs/sealedge-attest-sbom-action`"
  artifacts:
    - path: .planning/phases/88-external-action-product-website/88-VERIFICATION.md
      provides: "Phase-close evidence capture — all 5 D-01/D-09/D-10/D-11 evidence artifacts plus rollback command"
      contains: "301"
    - path: .planning/phases/88-external-action-product-website/88-03-SUMMARY.md
      provides: "Plan 03 summary with rename timeline + @v2 tag cut + marketplace check + T1/T5/T6 threat-mitigation evidence"
      contains: "gh repo rename"
  key_links:
    - from: "User shell (`gh repo rename sealedge-attest-sbom-action -R TrustEdge-Labs/attest-sbom-action`)"
      to: "GitHub repo rename API (action repo)"
      via: "`gh` CLI with `repo` scope already granted per `gh auth status`"
      pattern: "gh repo rename sealedge-attest-sbom-action"
    - from: "Monorepo `actions/sealedge-attest-sbom-action/` working tree"
      to: "External repo `TrustEdge-Labs/sealedge-attest-sbom-action` main branch"
      via: "Isolated subtree-push / clean-clone-copy pattern (not force-push of monorepo; T5 mitigation)"
      pattern: "push.*sealedge-attest-sbom-action.*main"
    - from: "Renamed external repo + post-rename commit on main"
      to: "`@v2.0.0` + floating `@v2` tags"
      via: "`gh release create v2.0.0` (or `git tag` + push) after the content push"
      pattern: "v2\\.0\\.0"
    - from: "Marketplace listing"
      to: "latest `@v2` tag's `action.yml` name + description fields"
      via: "GitHub Actions Marketplace auto-refresh on tag publish"
      pattern: "Sealedge SBOM Attestation"
---

<objective>
Execute the external GitHub operation: rename `TrustEdge-Labs/attest-sbom-action` → `TrustEdge-Labs/sealedge-attest-sbom-action` via `gh repo rename` (D-01), push the monorepo-side `actions/sealedge-attest-sbom-action/` contents to the renamed external repo's main branch using an isolated mechanism that does not leak unrelated monorepo files (T5 mitigation), cut `v2.0.0` + floating `v2` tags on the renamed external repo (D-04, D-11), verify the 301 redirect (D-09), check the Marketplace listing reflects the rebranded metadata (D-10), and capture all evidence in `88-VERIFICATION.md` (CONTEXT.md §"In scope" last bullet).

Purpose: EXT-02 (rebranded action repo + @v2 ships) + EXT-03 (GitHub's built-in 301 redirect is the only compatibility mechanism; the old `uses: TrustEdge-Labs/attest-sbom-action@v1` references continue to resolve). This plan is the Phase 87 D-05 analog — a user-gated external one-shot operation. Claude provides verbatim commands + verification harnesses; the user runs the rename and the tag-cut from their own shell per "careful actions" policy.

Output: Renamed external repo; `@v2.0.0` + `@v2` tags pushed; `88-VERIFICATION.md` with 5 evidence artifacts; `88-03-SUMMARY.md` closing the plan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/88-external-action-product-website/88-CONTEXT.md
@.planning/phases/88-external-action-product-website/88-PATTERNS.md
@.planning/phases/88-external-action-product-website/88-01-SUMMARY.md
@.planning/phases/88-external-action-product-website/88-02-SUMMARY.md
@.planning/phases/87-github-repository-rename/87-02-PLAN.md
@.planning/phases/87-github-repository-rename/87-VERIFICATION.md
@CLAUDE.md

<interfaces>
<!-- Verbatim commands for copy-paste. Same shape as Phase 87 Plan 02 with action-repo-specific substitutions. -->

# --- Pre-rename gate (adapted from Phase 87 D-06) ---
git status                                                                      # clean working tree on main
git log origin/main..HEAD                                                        # empty (no unpushed commits)
git log --oneline -5                                                             # confirms Plan 01 + Plan 02 commits landed
gh pr list -R TrustEdge-Labs/attest-sbom-action --state open                     # empty (no open PRs on the action repo)
gh auth status                                                                   # confirms `repo` scope + HTTPS git protocol

# --- The rename (USER RUNS — Claude provides string + resume-signal) ---
gh repo rename sealedge-attest-sbom-action -R TrustEdge-Labs/attest-sbom-action
# Expected: ✓ Renamed repository TrustEdge-Labs/sealedge-attest-sbom-action

# --- D-09 curl redirect check (verbatim from CONTEXT.md §Specifics) ---
curl -I -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' \
  https://github.com/TrustEdge-Labs/attest-sbom-action
# Expected: 301 -> https://github.com/TrustEdge-Labs/sealedge-attest-sbom-action

# --- Follow-redirect cross-check ---
curl -s -o /dev/null -w '%{http_code} (final URL: %{url_effective})\n' -L \
  https://github.com/TrustEdge-Labs/attest-sbom-action
# Expected: 200 (final URL: https://github.com/TrustEdge-Labs/sealedge-attest-sbom-action/...)

# --- Subtree-push content push (isolated mechanism; T5 mitigation) ---
# Strategy: fresh clone of the renamed external repo, copy monorepo folder contents,
# commit in the clone, push. This ensures ONLY the action folder's files land in
# the external repo — no risk of leaking monorepo secrets, other crates, etc.
# The fresh-clone approach is simpler and more auditable than `git subtree push`.

EXT_CLONE_DIR=$(mktemp -d /tmp/sealedge-action-XXXXXX)
cd "$EXT_CLONE_DIR"
gh repo clone TrustEdge-Labs/sealedge-attest-sbom-action
cd sealedge-attest-sbom-action

# Remove all pre-existing tracked files EXCEPT .git (so we start from a clean slate at the tip of main):
git rm -r --ignore-unmatch '*' 2>/dev/null || true
git clean -fxd

# Copy monorepo folder contents into the clone:
cp -a /home/john/vault/projects/github.com/trustedge/actions/sealedge-attest-sbom-action/. .

# Stage, verify, commit:
git status
git add .
git diff --cached --stat
# Expect: action.yml, LICENSE, README.md — exactly 3 tracked files

git commit -m "$(cat <<'EOF'
feat(v6.0): rebrand to sealedge — sealedge/seal naming, @v2 baseline

This is the first commit of the rebranded action. Matches the monorepo
source-of-truth at actions/sealedge-attest-sbom-action/ (sealedge rev <MONOREPO_REV>).

Key changes from @v1:
- Binary fetch: trst → seal
- Input rename: trst-version → seal-version (clean break)
- Release URL base: /TrustEdge-Labs/trustedge/releases → /TrustEdge-Labs/sealedge/releases
- SHA256 verification block: trst.sha256 → seal.sha256 (verification logic unchanged)
- Marketplace metadata: 'Sealedge SBOM Attestation' / 'Attest ... using Sealedge'

@v1 stays frozen as the pre-rebrand behavior. GitHub's built-in 301 redirect
covers existing `uses: TrustEdge-Labs/attest-sbom-action@v1` consumer refs.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

git push origin main

# --- Cut @v2.0.0 + floating @v2 tags (D-04, D-11) ---
git tag -a v2.0.0 -m "v2.0.0: sealedge rebrand baseline"
git tag -f v2 v2.0.0
git push origin v2.0.0
git push origin v2 --force  # floating @v2 rewrites to latest — this is the expected semantics per D-04

# --- Cleanup temp clone ---
cd /home/john/vault/projects/github.com/trustedge
rm -rf "$EXT_CLONE_DIR"

# --- T1 mitigation check: existing @v1 release assets still accessible on the renamed repo ---
gh release view v1.0 -R TrustEdge-Labs/sealedge-attest-sbom-action \
  --json tagName,assets \
  --jq '{tag: .tagName, asset_count: (.assets | length), asset_urls: [.assets[].url]}'
# Expected: tagName == v1.0, asset_count > 0, asset_urls resolve under the renamed repo path
# (If v1.0 tag doesn't exist on the repo — possible if the repo's first versioned tag was different —
# substitute the actual pre-rename tag name; grep `gh release list -R TrustEdge-Labs/sealedge-attest-sbom-action`
# for the tag that existed before this plan.)

# --- D-10 Marketplace listing check ---
# User-only step (Marketplace UI is browser-only). After tag push, visit:
#   https://github.com/marketplace/actions/sealedge-sbom-attestation
# (URL slug is derived from the action.yml `name:` field — `Sealedge SBOM Attestation` → likely slug `sealedge-sbom-attestation`;
# GitHub may keep the old slug `trustedge-sbom-attestation` or `attest-sbom-action` until a manual re-publish.)
# Fallback URL (old slug): https://github.com/marketplace/actions/trustedge-sbom-attestation
# If the listing still shows the old title/description after a few minutes, the user must manually re-publish via GitHub UI:
#   Open the repo → Releases → Edit the v2.0.0 release → check "Publish this Action to the GitHub Marketplace" → Save

# --- Rollback command (VERBATIM, kept in PLAN.md + VERIFICATION.md for fast copy-paste) ---
gh repo rename attest-sbom-action -R TrustEdge-Labs/sealedge-attest-sbom-action
# Note: rollback does NOT undo the @v2 tag or the content push. If the rollback is needed AFTER the content push + tag,
# additional cleanup: `gh release delete v2.0.0 -R TrustEdge-Labs/attest-sbom-action --yes && git tag -d v2.0.0 && git tag -d v2`
# on the restored-name repo. Solo dev, no downstream consumers in the bootstrap window — safe.

</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-rename gate — confirm Plan 01 + Plan 02 landed, main pushed, no open PRs on the action repo</name>
  <files>(checkpoint task — Claude runs gate checks and reports results; no file edits)</files>
  <read_first>
    - .planning/phases/88-external-action-product-website/88-01-SUMMARY.md (confirm 4 commits landed: action.yml sweep, README.md sweep, folder rename, REQUIREMENTS amendment)
    - .planning/phases/88-external-action-product-website/88-02-SUMMARY.md (confirm 2 commits landed: seal+sha256 upload, dogfood conversion)
    - .planning/phases/87-github-repository-rename/87-02-PLAN.md Task 1 (the Phase 87 pre-rename gate pattern; Phase 88 reuses the shape adapted for the action repo)
    - .planning/phases/88-external-action-product-website/88-CONTEXT.md §Specifics "Pre-rename gate checks" (3 concrete checks)
  </read_first>
  <action>
    <what-built>
      Plan 01 committed the action-source sweep + folder rename + REQUIREMENTS amendment (4 commits). Plan 02 committed the ci.yml extension + dogfood conversion (2 commits). Before the user runs `gh repo rename`, all pre-rename gate checks must be green. Claude runs the checks and presents results for user confirmation.
    </what-built>
    <how-to-verify>
      Claude runs the checks below in order and reports each with a ✔ or ✖ marker.

      **Check 1 — Working tree clean + main pushed:**
      ```
      git status                     # expected: `working tree clean` on `main`
      git log origin/main..HEAD       # expected: empty (nothing unpushed)
      ```

      **Check 2 — Plan 01 + Plan 02 commits landed on origin:**
      ```
      git log --oneline origin/main -10 | grep -E '(fix|docs|chore|feat|refactor)\(88\):'
      # Expected: at least 6 commits from Plans 01 and 02 visible
      ```
      Specifically, look for (order may vary):
      - `fix(88): sweep action.yml`
      - `docs(88): sweep action README`
      - `chore(88): rename actions/attest-sbom-action`
      - `docs(88): amend EXT-02/EXT-03`
      - `feat(88): upload seal binary`
      - `refactor(88): dogfood sealedge-attest-sbom-action`

      **Check 3 — Monorepo folder is at the new name:**
      ```
      test -d actions/sealedge-attest-sbom-action && echo "folder at new name" || echo "MISSING"
      test ! -d actions/attest-sbom-action && echo "old folder gone" || echo "OLD STILL PRESENT"
      ```
      Both should echo the success line.

      **Check 4 — No open PRs on the action repo:**
      ```
      gh pr list -R TrustEdge-Labs/attest-sbom-action --state open
      ```
      Expected: empty list (the `attest-sbom-action` repo has no active contributors other than the solo dev; PRs are unlikely).

      **Check 5 — `gh auth status` shows `repo` scope + HTTPS:**
      ```
      gh auth status
      ```
      Expected: `Logged in to github.com` with `repo` in the scope list (for creating tags / uploading releases on the action repo).

      **Check 6 — Content integrity on the monorepo side** (guard against Plan 01 / 02 accidentally having diverged):
      ```
      grep -c 'sealedge-attest-sbom-action@v2' .github/workflows/ci.yml     # expected: >= 1
      grep -c 'seal.sha256' actions/sealedge-attest-sbom-action/action.yml  # expected: >= 2 (fetch + parse)
      grep -c 'Renamed from' actions/sealedge-attest-sbom-action/README.md  # expected: >= 1
      ```
      If any of these return 0, Plan 01 or Plan 02 didn't fully land — STOP and re-run the relevant plan before approving this task.

      **Decision:** After running all 6 checks, report results and wait for user approval. If all 6 are ✔, user types `approved` to proceed to Task 2. If any are ✖, user types `defer` + describes the issue.
    </how-to-verify>
  </action>
  <verify>Gate check — user must type `approved` as the resume signal after confirming all 6 pre-rename checks are ✔. This is a human-verified checkpoint; no single automated command captures the "commits landed as expected" semantic fully, so user judgment is the final gate.</verify>
  <resume-signal>Type `approved` if all 6 checks are ✔. Type `defer` + reason if any check fails.</resume-signal>
  <acceptance_criteria>
    - Claude has run Checks 1-6 and reported each result with explicit output
    - Check 1: `git status` shows `working tree clean`; `git log origin/main..HEAD` is empty
    - Check 2: At least 6 Phase 88 commits visible in `git log origin/main -10` matching `(fix|docs|chore|feat|refactor)\(88\):`
    - Check 3: `actions/sealedge-attest-sbom-action/` exists; `actions/attest-sbom-action/` does not
    - Check 4: `gh pr list -R TrustEdge-Labs/attest-sbom-action --state open` returns an empty list (or user has explicitly decided how to handle any open PR)
    - Check 5: `gh auth status` shows `repo` scope and HTTPS as active git protocol
    - Check 6: action.yml references `seal.sha256`; README.md contains `Renamed from`; ci.yml references `sealedge-attest-sbom-action@v2`
    - User has typed `approved` as the resume signal
  </acceptance_criteria>
  <done>All 6 pre-rename gate checks pass; user has approved proceeding to the external rename operation.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User executes `gh repo rename sealedge-attest-sbom-action -R TrustEdge-Labs/attest-sbom-action`</name>
  <files>(checkpoint task — user runs the rename from their shell; no local files modified)</files>
  <read_first>
    - .planning/phases/88-external-action-product-website/88-CONTEXT.md §D-01 (rename-in-place rationale), §D-15 (rollback command), §"Claude's Discretion" → step 4 (user-driven external op, matches Phase 87 D-05 pattern)
    - .planning/phases/87-github-repository-rename/87-02-PLAN.md Task 2 (the Phase 87 analog — user-runs-rename shape)
  </read_first>
  <action>
    <what-built>
      This is the one-shot external operation for Phase 88. The user runs `gh repo rename` from their shell — Claude cannot drive this per "careful actions" policy / Phase 87 D-05 pattern. Claude provides the verbatim command + the rollback command + the resume protocol.
    </what-built>
    <how-to-verify>
      **Run this command from your shell** (not Claude's Bash tool; `gh auth status` per Task 1 Check 5 confirms you have `repo` scope + HTTPS):

      ```
      gh repo rename sealedge-attest-sbom-action -R TrustEdge-Labs/attest-sbom-action
      ```

      `gh` will prompt for confirmation, then perform the rename. Expected output (similar to):

      ```
      ✓ Renamed repository TrustEdge-Labs/sealedge-attest-sbom-action
      ```

      Note the timestamp when the rename completes — this goes into `88-VERIFICATION.md` (Task 5).

      **Post-rename spot-check** (before typing resume signal):

      Open these URLs in a browser (or `curl -I`):
      - `https://github.com/TrustEdge-Labs/sealedge-attest-sbom-action` — should load (HTTP 200)
      - `https://github.com/TrustEdge-Labs/attest-sbom-action` — should 301-redirect to the new URL (the browser may silently follow the 301 and show the new URL; that's expected)

      **If anything looks wrong** (`gh` errored, new URL doesn't load, old URL 404s instead of redirecting), execute the rollback from your shell:

      ```
      gh repo rename attest-sbom-action -R TrustEdge-Labs/sealedge-attest-sbom-action
      ```

      Then type `rollback` + describe what went wrong.

      **If everything looks clean**, type `renamed` + paste the exact `gh` success output into the conversation for `88-VERIFICATION.md` capture.

      **Timing note** (Phase 87 D-13 §1 / 30-second retry carry-forward): GitHub's rename redirect is typically immediate. If Task 3's `curl -I` check returns something other than 301, wait 30 seconds and retry once before declaring failure.

      **Rollback command kept verbatim for fast copy-paste:**
      ```
      gh repo rename attest-sbom-action -R TrustEdge-Labs/sealedge-attest-sbom-action
      ```

      NOTE: Rollback at this stage (pre-content-push, pre-tag) is fully safe — only the repo name has changed, and rollback restores it. Once Task 4 pushes content + cuts `@v2`, rollback becomes more expensive (tag deletion + content revert on the restored-name repo); see the `<interfaces>` block's extended rollback notes.
    </how-to-verify>
  </action>
  <verify>Gate check — user must run `gh repo rename` from their own shell and paste the success output (`renamed` resume signal), OR describe the rollback path taken (`rollback` resume signal). Task 3's automated `curl -I` 301 check provides downstream verification.</verify>
  <resume-signal>Type `renamed` + paste the `gh` success output if rename succeeded. Type `rollback` + describe the failure if you executed the rollback.</resume-signal>
  <acceptance_criteria>
    - User ran `gh repo rename sealedge-attest-sbom-action -R TrustEdge-Labs/attest-sbom-action` from their shell (not Claude's Bash tool)
    - `gh` reported success (no error); output contains `Renamed repository TrustEdge-Labs/sealedge-attest-sbom-action`
    - User pasted the success output into the conversation for `88-VERIFICATION.md` capture
    - Rollback command documented verbatim in this plan and ready if downstream verification fails
  </acceptance_criteria>
  <done>Action repo is renamed to `TrustEdge-Labs/sealedge-attest-sbom-action`; user confirmed via `renamed` resume signal and pasted the `gh` output.</done>
</task>

<task type="auto">
  <name>Task 3: Verify GitHub 301 redirect (D-09) + `@v1` release-asset integrity (T1 mitigation)</name>
  <files>(no file edits — evidence capture only, for Task 5)</files>
  <read_first>
    - .planning/phases/88-external-action-product-website/88-CONTEXT.md §D-09 (verbatim curl command), §"Claude's Discretion" (30-second retry buffer)
    - .planning/phases/88-external-action-product-website/88-PATTERNS.md §Shared.1 (copy-paste curl commands)
    - Task 2 resume-signal output (the `gh` success timestamp)
  </read_first>
  <action>
**Step 3a — D-09 curl redirect check** (verbatim from CONTEXT.md §Specifics):

```
curl -I -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' \
  https://github.com/TrustEdge-Labs/attest-sbom-action
```

Expected: `301 -> https://github.com/TrustEdge-Labs/sealedge-attest-sbom-action` (possibly with trailing slash).

**If the first call returns something else** (e.g., 200 with no redirect_url, 404, or a different redirect target), apply the 30-second retry buffer:

```
sleep 30 && curl -I -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' \
  https://github.com/TrustEdge-Labs/attest-sbom-action
```

Expected after retry: same 301 line. If still wrong after the retry, declare failure and execute the D-15 rollback from Task 2.

**Step 3b — Follow-redirect cross-check:**

```
curl -s -o /dev/null -w '%{http_code} (final URL: %{url_effective})\n' -L \
  https://github.com/TrustEdge-Labs/attest-sbom-action
```

Expected: `200 (final URL: https://github.com/TrustEdge-Labs/sealedge-attest-sbom-action/…)` — the final URL's path ends in `/sealedge-attest-sbom-action` (possibly with a trailing slash or sub-path).

**Step 3c — T1 mitigation: verify existing `@v1` release assets survive the rename.**

First, discover the exact pre-rename tag name (the repo may have used `v1`, `v1.0`, `v1.0.0`, or a custom scheme):

```
gh release list -R TrustEdge-Labs/sealedge-attest-sbom-action --limit 10 --json tagName,publishedAt,url
```

Then for the latest pre-rename tag (most likely `v1`), verify assets are still accessible:

```
PRE_RENAME_TAG=$(gh release list -R TrustEdge-Labs/sealedge-attest-sbom-action --limit 10 --json tagName --jq '.[0].tagName')
echo "Pre-rename tag: $PRE_RENAME_TAG"
gh release view "$PRE_RENAME_TAG" -R TrustEdge-Labs/sealedge-attest-sbom-action --json tagName,assets --jq '{tag: .tagName, asset_count: (.assets | length), asset_urls: [.assets[].url]}'
```

Expected:
- `asset_count` > 0 for any pre-existing release (action repos usually have release notes/assets even if minimal)
- Each URL in `asset_urls` resolves under `/TrustEdge-Labs/sealedge-attest-sbom-action/releases/…` (the renamed path)

Spot-check one asset URL:

```
ONE_URL=$(gh release view "$PRE_RENAME_TAG" -R TrustEdge-Labs/sealedge-attest-sbom-action --json assets --jq '.assets[0].url')
if [ -n "$ONE_URL" ]; then
  curl -I -s -o /dev/null -w '%{http_code}\n' "$ONE_URL"
  # Expected: 200 (or 302 → 200 following CDN redirect, whichever Github uses)
fi
```

If the repo has NO pre-existing releases (possible for a new action repo), document this as `asset_count: 0` with a note that the T1 threat doesn't apply — nothing to preserve.

**Step 3d — Capture all 3 outputs verbatim** for Task 5's verification file. Specifically:
- D-09 curl 301 output
- Follow-redirect 200 output
- T1 asset-integrity check output

Write these to a scratch file `/tmp/88-03-evidence.txt` for Task 5 to pull from, OR keep them in the conversation buffer for Task 5 to reference.
  </action>
  <verify>
    <automated>
set -e

# D-09 curl 301 check with 30-second retry buffer:
check_redirect() {
  curl -I -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' \
    https://github.com/TrustEdge-Labs/attest-sbom-action
}
OUT=$(check_redirect)
if ! echo "$OUT" | grep -qE '^301 -> https://github\.com/TrustEdge-Labs/sealedge-attest-sbom-action/?$'; then
  sleep 30
  OUT=$(check_redirect)
fi
echo "D-09 curl output: $OUT"
echo "$OUT" | grep -qE '^301 -> https://github\.com/TrustEdge-Labs/sealedge-attest-sbom-action/?$'

# Follow-redirect cross-check:
FOLLOW=$(curl -s -o /dev/null -w '%{http_code} (final URL: %{url_effective})\n' -L \
  https://github.com/TrustEdge-Labs/attest-sbom-action)
echo "Follow-redirect output: $FOLLOW"
echo "$FOLLOW" | grep -qE 'sealedge-attest-sbom-action'
echo "$FOLLOW" | grep -qE '^200 '

# T1 check: pre-rename release tag is accessible post-rename:
PRE_TAG=$(gh release list -R TrustEdge-Labs/sealedge-attest-sbom-action --limit 10 --json tagName --jq '.[0].tagName // empty')
if [ -n "$PRE_TAG" ]; then
  gh release view "$PRE_TAG" -R TrustEdge-Labs/sealedge-attest-sbom-action --json tagName --jq '.tagName' | grep -q "$PRE_TAG"
  echo "T1 mitigation: tag '$PRE_TAG' accessible post-rename"
else
  echo "T1 mitigation: no pre-existing releases on the repo; T1 threat does not apply"
fi
    </automated>
  </verify>
  <acceptance_criteria>
    - `curl -I -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' https://github.com/TrustEdge-Labs/attest-sbom-action` returns `301 -> https://github.com/TrustEdge-Labs/sealedge-attest-sbom-action` (possibly with trailing slash), either on first call or after one 30-second retry
    - Follow-redirect `curl -L` returns `200` with `url_effective` path ending in `/sealedge-attest-sbom-action` (or sub-path thereof)
    - `gh release list -R TrustEdge-Labs/sealedge-attest-sbom-action` returns accessible releases (if any existed pre-rename) OR returns empty, in which case T1 documented as N/A
    - If any pre-existing tag exists, `gh release view <tag> -R TrustEdge-Labs/sealedge-attest-sbom-action` returns its metadata with assets still resolvable
    - Outputs captured for Task 5's `88-VERIFICATION.md` ingestion
  </acceptance_criteria>
  <done>GitHub 301 redirect is confirmed; follow-redirect ends at renamed URL with HTTP 200; pre-existing `@v1` (or equivalent) release assets either remain accessible OR the repo has no pre-existing releases and T1 is documented N/A.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 4: Push monorepo action folder contents to renamed external repo + cut `@v2.0.0` + floating `@v2` tags (D-04, D-11, T5 mitigation)</name>
  <files>(checkpoint task — user runs the push + tag commands from their shell; no local monorepo files modified; external repo gains a new commit + 2 tags)</files>
  <read_first>
    - .planning/phases/88-external-action-product-website/88-CONTEXT.md §D-04 (v2.0.0 + floating v2 semantics), §D-11 (tag-timing: cut @v2 during Phase 88, validated by Phase 89 release run), §"Claude's Discretion" → step 5 "Cut @v2.0.0 tag on the renamed action repo"
    - .planning/phases/88-external-action-product-website/88-PATTERNS.md §Shared.5 ("External GitHub operation" vs in-repo commit separation — this is a user-driven step)
    - actions/sealedge-attest-sbom-action/action.yml (source-of-truth content to push)
    - actions/sealedge-attest-sbom-action/README.md (source-of-truth content to push)
    - actions/sealedge-attest-sbom-action/LICENSE (source-of-truth content to push)
  </read_first>
  <action>
    <what-built>
      The monorepo-side `actions/sealedge-attest-sbom-action/` is the canonical source-of-truth (D-06). Task 4 pushes exactly these 3 files (action.yml, README.md, LICENSE) to the renamed external repo's `main` branch using an ISOLATED mechanism — a fresh clone of the external repo, wiped to empty, then populated with the monorepo folder contents and committed. This is the T5 mitigation: it is mechanically impossible for unrelated monorepo files (other crates, secrets, etc.) to leak into the external repo, because we never run `git push` from the monorepo into the external remote.
    </what-built>
    <how-to-verify>
      Run this block from your shell. **Read the commands carefully before executing** — this touches two repos.

      ```bash
      # Define paths explicitly (avoid pushd/popd confusion):
      MONOREPO=/home/john/vault/projects/github.com/trustedge
      EXT_CLONE_DIR=$(mktemp -d /tmp/sealedge-action-XXXXXX)
      MONOREPO_REV=$(cd "$MONOREPO" && git rev-parse --short HEAD)

      # 1. Fresh clone of the renamed external repo:
      cd "$EXT_CLONE_DIR"
      gh repo clone TrustEdge-Labs/sealedge-attest-sbom-action
      cd sealedge-attest-sbom-action

      # Confirm the clone is on `main`:
      git branch --show-current
      # Expected: main

      # 2. Snapshot current state for evidence (pre-push):
      echo "Pre-push state of external repo:"
      ls -la
      git log --oneline -5

      # 3. Wipe the working tree (remove tracked files; preserve .git):
      git rm -r --ignore-unmatch '*' 2>/dev/null || true
      # Any untracked files too (.gitignore'd or otherwise):
      git clean -fxd

      # 4. Copy monorepo's canonical source into the clone (dot-suffix copies directory CONTENTS, not the directory itself):
      cp -a "$MONOREPO/actions/sealedge-attest-sbom-action/." .

      # 5. Verify exactly 3 files + the .git directory remain:
      ls -la
      # Expected visible: ., .., .git, action.yml, LICENSE, README.md
      test -f action.yml
      test -f LICENSE
      test -f README.md
      test ! -e Cargo.toml   # sanity: no monorepo crate files should appear
      test ! -e src          # sanity: no monorepo source directories

      # 6. Stage + review + commit:
      git add .
      git status
      git diff --cached --stat
      # Expected: action.yml, LICENSE, README.md shown (3 files); for a first rebrand push, diff vs @v1 will show many lines changed

      git commit -m "$(cat <<EOF
feat(v6.0): rebrand to sealedge — sealedge/seal naming, @v2 baseline

This is the first commit of the rebranded action. Matches the monorepo
source-of-truth at actions/sealedge-attest-sbom-action/ (sealedge rev ${MONOREPO_REV}).

Key changes from @v1:
- Binary fetch: trst → seal
- Input rename: trst-version → seal-version (clean break)
- Release URL base: /TrustEdge-Labs/trustedge/releases → /TrustEdge-Labs/sealedge/releases
- SHA256 verification block: trst.sha256 → seal.sha256 (verification logic unchanged)
- Marketplace metadata: 'Sealedge SBOM Attestation' / 'Attest ... using Sealedge'

@v1 stays frozen as the pre-rebrand behavior. GitHub's built-in 301 redirect
covers existing \`uses: TrustEdge-Labs/attest-sbom-action@v1\` consumer refs.

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"

      # 7. Push to the renamed external repo's main branch:
      git push origin main

      # 8. Cut v2.0.0 tag + floating v2 tag:
      git tag -a v2.0.0 -m "v2.0.0: sealedge rebrand baseline"
      git tag -f v2 v2.0.0

      # 9. Push tags:
      git push origin v2.0.0
      git push origin v2 --force   # floating @v2 rewrites to latest — this is expected semantics per D-04

      # 10. Verify the tags are visible on the renamed repo:
      gh release list -R TrustEdge-Labs/sealedge-attest-sbom-action --limit 10 --json tagName,publishedAt,isLatest
      # Expected: v2.0.0 in the list (if you optionally create a release for the tag), AND
      # `git ls-remote --tags origin` includes v2.0.0 + v2 refs

      git ls-remote --tags origin | grep -E 'refs/tags/v2(\.0\.0)?$'

      # 11. Cleanup:
      cd "$MONOREPO"
      rm -rf "$EXT_CLONE_DIR"
      ```

      **Optional: create a GitHub Release for `v2.0.0`** (not strictly required — the tag alone is sufficient for `uses: @v2` resolution — but creating a release surfaces the rebrand in the repo's Releases tab and can help with Marketplace auto-refresh):

      ```bash
      gh release create v2.0.0 \
        -R TrustEdge-Labs/sealedge-attest-sbom-action \
        --title "v2.0.0: sealedge rebrand baseline" \
        --notes "$(cat <<'EOF'
First release of the rebranded action under the sealedge product name.

### Changes from @v1

- Binary fetch: `trst` → `seal`
- Input rename: `trst-version` → `seal-version` (clean break, no alias)
- Release URL base: `/TrustEdge-Labs/trustedge/releases` → `/TrustEdge-Labs/sealedge/releases`
- SHA256 verification block updated for `seal.sha256` filename (logic unchanged)
- Marketplace metadata: "Sealedge SBOM Attestation"

### Migration

Existing workflows using `uses: TrustEdge-Labs/attest-sbom-action@v1` continue
to work via GitHub's built-in 301 redirect. To migrate to `@v2`:

- Update `uses: TrustEdge-Labs/sealedge-attest-sbom-action@v2`
- Rename input `trst-version:` → `seal-version:` (if used)
- Ensure consuming workflow uploads `seal` + `seal.sha256` to releases (pre-v6.0 `trst` releases stay frozen on the old repo)

### Compatibility

`@v1` stays frozen as pre-rebrand behavior. Do not `--force` delete or re-tag.
EOF
)"
      ```

      **After all steps complete**, type `pushed-and-tagged` + paste the output of:
      - `git push origin main` (the commit SHA)
      - `git push origin v2.0.0` (the tag push result)
      - `git push origin v2 --force` (the floating-tag push result)
      - `git ls-remote --tags origin | grep v2` (confirms both tags exist on the remote)

      Paste these into the conversation for `88-VERIFICATION.md` capture.

      **If anything fails** (clone fails, push is rejected, tag push errors): type `push-failed` + describe. Partial state on the external repo can be cleaned up by:
      - `git push origin :main` (delete main) — DO NOT DO THIS without explicit backup; prefer reset to the pre-push commit via `gh api` if recovery is needed
      - Better path: run the rollback from Task 2 (`gh repo rename attest-sbom-action -R TrustEdge-Labs/sealedge-attest-sbom-action`) to restore the old name, then diagnose without time pressure
    </how-to-verify>
  </action>
  <verify>Gate check — user must run the full push + tag block from their own shell and paste the push/tag outputs into the conversation (`pushed-and-tagged` resume signal). The commands touch an external repo that Claude's Bash tool can manipulate but the discipline of having the user drive the external op (Phase 87 D-05 / CONTEXT.md "Claude's Discretion" step 4-5) preserves review + audit trail symmetry.</verify>
  <resume-signal>Type `pushed-and-tagged` + paste the push + tag outputs if everything succeeded. Type `push-failed` + describe if any step errored.</resume-signal>
  <acceptance_criteria>
    - External repo `TrustEdge-Labs/sealedge-attest-sbom-action` has a new commit on `main` whose tree contains exactly `action.yml`, `LICENSE`, `README.md` (no monorepo leakage — T5 mitigation verified)
    - `action.yml` on the external repo's `main` branch matches the monorepo source byte-for-byte (`diff <(curl -sL https://raw.githubusercontent.com/TrustEdge-Labs/sealedge-attest-sbom-action/main/action.yml) actions/sealedge-attest-sbom-action/action.yml` returns empty)
    - `git ls-remote --tags https://github.com/TrustEdge-Labs/sealedge-attest-sbom-action.git` includes both `refs/tags/v2.0.0` and `refs/tags/v2`
    - Both tags resolve to the same commit SHA (the rebrand-baseline commit)
    - Existing `@v1` tag is untouched (`git ls-remote --tags …` still shows it at its original commit)
    - User has pasted the push + tag outputs into the conversation
    - Temp clone directory has been cleaned up
  </acceptance_criteria>
  <done>External repo has the rebrand-baseline commit on main + `@v2.0.0` + floating `@v2` tags; `@v1` is untouched; no monorepo files leaked (T5 mitigation verified); user pasted evidence for Task 5.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Marketplace listing check (D-10) + write `88-VERIFICATION.md` evidence file</name>
  <files>.planning/phases/88-external-action-product-website/88-VERIFICATION.md</files>
  <read_first>
    - .planning/phases/87-github-repository-rename/87-VERIFICATION.md (the Phase 87 template Phase 88 copies — structure: pre-rename gate → rename op → redirect check → external integrations → rollback status)
    - .planning/phases/88-external-action-product-website/88-PATTERNS.md §Shared.4 (the 8-section VERIFICATION.md shape adapted for Phase 88)
    - .planning/phases/88-external-action-product-website/88-CONTEXT.md §D-10 (Marketplace check + manual re-publish fallback)
    - Tasks 1-4 output (pre-rename gate result, rename timestamp, curl 301 output, push + tag output)
    - @$HOME/.claude/get-shit-done/templates/summary.md (for the plan summary in Task 6 / phase close)
  </read_first>
  <action>
    <what-built>
      Tasks 1-4 produced all the operational evidence. This task:
      (a) has the user check the GitHub Actions Marketplace listing (D-10 — Marketplace UI is browser-only),
      (b) writes `88-VERIFICATION.md` aggregating all Phase 88 evidence,
      (c) commits the file in the monorepo.
    </what-built>
    <how-to-verify>
      **Step 5a — Marketplace listing check (D-10 — user-driven, browser-only).**

      The GitHub Actions Marketplace listing is derived from the `action.yml` `name:` + `description:` fields on the repo's latest release. After Task 4's `v2.0.0` tag + (optional) release publish, the Marketplace listing should auto-refresh to:
      - Title: **Sealedge SBOM Attestation**
      - Description: "Attest a binary artifact with its CycloneDX SBOM using Sealedge"

      Open the Marketplace listing in a browser. The slug may be any of:
      - `https://github.com/marketplace/actions/sealedge-sbom-attestation`
      - `https://github.com/marketplace/actions/trustedge-sbom-attestation` (old slug, may still redirect or display old title)
      - `https://github.com/marketplace/actions/attest-sbom-action` (pre-publish slug if Marketplace was never manually published)

      **Cross-check** via `gh api`:

      ```
      gh api repos/TrustEdge-Labs/sealedge-attest-sbom-action --jq '.name, .full_name, .description, .html_url'
      ```

      Expected:
      - `.name`: `sealedge-attest-sbom-action`
      - `.full_name`: `TrustEdge-Labs/sealedge-attest-sbom-action`
      - `.html_url`: `https://github.com/TrustEdge-Labs/sealedge-attest-sbom-action`

      **Happy path:** Marketplace listing shows the new title + description. Take a screenshot (or record the title/description text) for the verification file. Type `marketplace-ok` + describe what you saw.

      **Re-publish fallback path** (per D-10 — if the listing still shows old title/description after a few minutes): on the Marketplace listing page, click the **Draft a release** or **Edit release** button. In the release form, check the **Publish this Action to the GitHub Marketplace** checkbox, then Save. This forces a fresh Marketplace publish against the latest tag's `action.yml`. Document the re-publish in `88-VERIFICATION.md`.

      **Step 5b — Write `88-VERIFICATION.md`** using the Write tool:

      ```markdown
      <!--
      Copyright (c) 2025 TRUSTEDGE LABS LLC
      MPL-2.0: https://mozilla.org/MPL/2.0/
      Project: sealedge — Privacy and trust at the edge.
      -->

      # Phase 88 Verification — External Action & Product Website (Action Repo)

      **Phase:** 88-external-action-product-website
      **Plan scope:** 88-03 (action repo rename + @v2 tag cut + Marketplace check)
      **Requirements:** EXT-02, EXT-03
      **Date:** <YYYY-MM-DD from Task 2 resume signal>
      **Status:** PASS (or FAIL + rollback notes if rollback was executed)

      ## 1. Pre-Rename Gate

      (Task 1 outputs — working tree clean, 6 Phase 88 commits visible on origin/main, `actions/sealedge-attest-sbom-action/` exists, no open PRs on action repo, `gh auth status` shows `repo` scope, content integrity greps pass)

      ```
      <paste Task 1 Check 1-6 output>
      ```

      ## 2. Rename Operation (D-01)

      Command (user-executed from shell — not Claude's Bash tool, per CONTEXT.md "Claude's Discretion"):

          gh repo rename sealedge-attest-sbom-action -R TrustEdge-Labs/attest-sbom-action

      Timestamp: <from Task 2 resume — the moment the rename completed>
      Output:

          ✓ Renamed repository TrustEdge-Labs/sealedge-attest-sbom-action

      ## 3. D-09 Redirect Verification

      Command:

          curl -I -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' https://github.com/TrustEdge-Labs/attest-sbom-action

      Output:

          301 -> https://github.com/TrustEdge-Labs/sealedge-attest-sbom-action

      Follow-redirect cross-check:

          curl -s -o /dev/null -w '%{http_code} (final URL: %{url_effective})\n' -L https://github.com/TrustEdge-Labs/attest-sbom-action

      Output:

          200 (final URL: https://github.com/TrustEdge-Labs/sealedge-attest-sbom-action)

      ## 4. T1 Mitigation — @v1 Release Assets Accessible Post-Rename

      Pre-rename tag discovered: <PRE_RENAME_TAG from Task 3 / or "no pre-existing releases">
      Asset count: <N>
      Sample asset URL + HTTP status: <one URL + 200>

      Result: **T1 mitigated** — existing @v1 assets remain accessible under the renamed path (or N/A if no pre-existing releases).

      ## 5. Content Push + Tag Cut (D-04, D-11, T5 Mitigation)

      Mechanism: fresh clone of renamed external repo → wipe → copy monorepo-side `actions/sealedge-attest-sbom-action/` contents → commit → push → tag.

      Monorepo source revision: <MONOREPO_REV from Task 4>
      External-repo `main` commit SHA after push: <from `git push origin main` output in Task 4>
      External-repo `v2.0.0` tag SHA: <from `git push origin v2.0.0` output>
      External-repo floating `v2` tag SHA: same as v2.0.0 (per `git tag -f v2 v2.0.0`)

      T5 mitigation verified: only `action.yml`, `LICENSE`, `README.md` committed to external repo (3 files, no monorepo leakage).

      `git ls-remote --tags` output:

          <paste Task 4 tag-push evidence>

      @v1 tag untouched (not force-pushed, not deleted — per D-07 frozen-@v1 discipline).

      ## 6. D-10 Marketplace Listing Check

      Listing URL (resolved): <actual URL user visited>
      Listing title after @v2 publish: **Sealedge SBOM Attestation** (or observed title)
      Listing description: "Attest a binary artifact with its CycloneDX SBOM using Sealedge" (or observed description)

      Auto-refresh path: <immediate | required manual re-publish via Edit release + check "Publish to Marketplace">

      gh api spot-check:

          gh api repos/TrustEdge-Labs/sealedge-attest-sbom-action --jq '.name, .full_name, .description'

      Output: <paste>

      ## 7. Rollback Status

      Command (documented verbatim; NOT executed):

          gh repo rename attest-sbom-action -R TrustEdge-Labs/sealedge-attest-sbom-action

      (Solo-dev safety: rollback at Task 2 stage was trivial. Post-Task-4 rollback would additionally require `gh release delete v2.0.0` + `git tag -d v2.0.0 v2` on the restored-name repo, then re-push. Path documented for future reference.)

      ## 8. Success Criteria Status — EXT-02, EXT-03

      | Req | Criterion | Status |
      |-----|-----------|--------|
      | EXT-02 | Repo renamed to sealedge-attest-sbom-action | ✔ |
      | EXT-02 | action source references sealedge/seal (ci.yml + action.yml) | ✔ Plan 01+02 |
      | EXT-02 | `@v2` tag ships rebranded action | ✔ Task 4 |
      | EXT-02 | SHA256 checksum verification preserved (T2) | ✔ Plan 01 Task 1 assertion + T1 mitigation here |
      | EXT-03 | GitHub 301 redirect covers `@v1` references | ✔ Task 3 (D-09) |
      | EXT-03 | `@v1` tag frozen | ✔ (not touched in Task 4) |
      | EXT-03 | README has top-of-README migration notice | ✔ Plan 01 Task 2 |
      ```

      Fill in the `<placeholder>` fields with the actual data collected across Tasks 1-4 + Step 5a.

      **Step 5c — Commit the verification file.** Stage specifically (no `git add -A`):

      ```bash
      git add .planning/phases/88-external-action-product-website/88-VERIFICATION.md
      git diff --cached --stat
      git commit -m "$(cat <<'EOF'
      docs(88): verification evidence for action repo rename + @v2 tag cut

      Captures Plan 03 evidence per 88-PATTERNS.md Shared.4:
      - Pre-rename gate output (6 checks)
      - Rename operation command + timestamp + output
      - D-09 curl 301 redirect verified
      - T1 @v1 asset-integrity check
      - Content push + @v2 / v2.0.0 tag cut via isolated clone mechanism (T5 mitigated)
      - D-10 Marketplace listing check
      - Rollback command documented verbatim (not executed)
      - EXT-02 / EXT-03 success-criteria status table

      Part of Phase 88 close.

      Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
      EOF
      )"
      git push origin main
      ```
    </how-to-verify>
  </action>
  <verify>Gate check — user confirms Marketplace listing title/description match the rebrand (`marketplace-ok` resume signal) OR describes that a manual re-publish was required. Claude's Bash tool can run the `gh api` cross-check automatically; the visual Marketplace check is user-only per D-10.</verify>
  <resume-signal>Type `marketplace-ok` + listing URL + observed title if auto-refresh worked. Type `marketplace-republished` + describe if you had to manually re-publish. Type `marketplace-broken` if listing still shows old metadata after manual re-publish (escalate).</resume-signal>
  <acceptance_criteria>
    - `gh api repos/TrustEdge-Labs/sealedge-attest-sbom-action --jq '.full_name'` returns `TrustEdge-Labs/sealedge-attest-sbom-action`
    - User has confirmed Marketplace listing reflects `Sealedge SBOM Attestation` title (either auto-refresh or manual re-publish path)
    - `.planning/phases/88-external-action-product-website/88-VERIFICATION.md` exists and contains:
      - The curl 301 output line `301 -> https://github.com/TrustEdge-Labs/sealedge-attest-sbom-action`
      - The rename-command timestamp + `✓ Renamed repository` success line
      - The push + tag SHAs from Task 4
      - Marketplace listing URL + observed title
      - The rollback command documented verbatim
      - An EXT-02 / EXT-03 success-criteria status table
    - File committed with subject starting `docs(88): verification evidence`
    - `git rev-parse HEAD == git rev-parse origin/main` (commit pushed to renamed monorepo)
  </acceptance_criteria>
  <done>Marketplace listing confirmed to reflect rebrand; `88-VERIFICATION.md` written and committed; Plan 03 ready for 88-03-SUMMARY.md (phase close).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user shell → GitHub (`gh repo rename`) | Live operation on public repo name; audience = all consumers of the old `uses: TrustEdge-Labs/attest-sbom-action@v1` ref |
| monorepo working tree → external renamed repo (via fresh-clone-copy-push) | Isolated mechanism prevents monorepo leakage; push scope = 3 files only |
| external repo latest tag → GitHub Actions Marketplace | Listing metadata sourced from `action.yml` `name:` + `description:` on latest tag; auto-refresh is documented behavior |
| `@v1` tag → post-rename frozen reference | GitHub preserves tag history across rename (documented); `@v1` remains resolvable |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-88-12 (T1 carry) | Tampering / DoS | `@v1` release assets could become inaccessible post-rename | mitigate | Task 3 Step 3c explicitly calls `gh release list` + `gh release view <PRE_RENAME_TAG>` on the renamed repo AND spot-checks one asset URL via `curl -I` for HTTP 200. Outputs captured in `88-VERIFICATION.md` §4. GitHub's documented behavior preserves release assets on rename; this check verifies empirically. |
| T-88-13 (T5) | Information Disclosure | Pushing monorepo content to external repo leaks secrets/unrelated files | mitigate | Task 4 uses an ISOLATED fresh-clone mechanism: `mktemp -d` → `gh repo clone external` → wipe tracked + untracked files → `cp -a monorepo/actions/sealedge-attest-sbom-action/.` → commit → push. The fresh clone has NO knowledge of the monorepo's `.git` directory. Task 4 acceptance criteria explicitly verifies the external repo's tree contains exactly 3 files (`action.yml`, `LICENSE`, `README.md`) and asserts absence of monorepo-specific files (`Cargo.toml`, `src/`, etc.). |
| T-88-14 (T6) | Tampering / DoS | GitHub rename redirect for `uses:` action references fails for some consumers | mitigate | Task 3 Step 3a runs the D-09 curl check with a 30-second retry buffer. Task 3 Step 3b follows the redirect with `curl -L` to verify end-to-end resolution. Both outputs captured in `88-VERIFICATION.md` §3. GitHub's `uses:` redirect is documented behavior (confirmed in CONTEXT.md §"External service docs"); Task 3 verifies empirically. |
| T-88-15 | Tampering | `@v2` tag accidentally points at a commit missing the rebrand changes (e.g., if push sequence is wrong) | mitigate | Task 4 step ordering: (a) wipe clone, (b) copy monorepo contents, (c) `git commit`, (d) `git push origin main`, (e) `git tag -a v2.0.0` (tagged from latest main HEAD, which IS the rebrand commit), (f) `git push origin v2.0.0`. Acceptance criterion `git ls-remote --tags … | grep v2` confirms tag reachable; `diff <(curl -sL .../main/action.yml) actions/sealedge-attest-sbom-action/action.yml` confirms content byte-for-byte match. |
| T-88-16 | Elevation | `git push origin v2 --force` requires force-push permission — are there branch protection rules blocking this? | accept | Force-pushing a tag (not a branch) does not cross branch-protection rules on the action repo (the action repo has no active protection policy per solo-dev history; if protection ever gets added in Phase 89+, the `--force` on the `v2` floating tag will error clearly and the user can either relax the rule or use a different floating-tag update strategy). |
| T-88-17 | Repudiation | No record of WHEN the rename happened or who ran it (Phase 87 analog) | mitigate | Task 2 resume signal requires user to paste the `gh` success output INCLUDING timestamp. Task 4 resume signal requires pasting push + tag output (includes SHAs). Task 5 writes `88-VERIFICATION.md` with all these artifacts + the rollback command. `git blame` on `88-VERIFICATION.md` provides audit trail. |
| T-88-18 | DoS (bootstrap window) | Between Plan 02 merging and Plan 03 cutting @v2, the dogfood step in ci.yml fails every release tag push | accept | Plan 02 preserved `continue-on-error: true` (line 176 of ci.yml) which explicitly covers this window. Phase 88's plan ordering (Plan 02 wave 2, Plan 03 wave 3) means Plan 03 runs BEFORE any Phase 89 v6.0.0 release tag, closing the window. Risk = zero during the planned execution window. |
| T-88-19 | Spoofing | Someone else with `repo` scope on TrustEdge-Labs could run `gh repo rename` to a different name between Task 1 and Task 2 | accept | Solo-dev context, no other collaborators on the action repo; time window is minutes; Task 1 gate confirms `gh auth status` shows only the expected user. Risk = effectively zero. |

**Plan 03 addresses T1 (release-asset integrity post-rename), T5 (cross-repo push doesn't leak monorepo secrets), and T6 (rename redirect survives `uses:` resolution) from the Phase 88 threat surface. Overall grade: MEDIUM — external op with reversibility window, but mitigations are concrete and evidence is captured. Rollback path is documented verbatim in VERIFICATION.md §7.**
</threat_model>

<verification>
**Plan-level gate (all 5 tasks + VERIFICATION.md commit must pass):**

1. Task 1 Checks 1-6 all ✔ (Plan 01 + Plan 02 landed; monorepo folder at new name; no open PRs on action repo; `gh auth status` green)
2. Task 2: `gh repo rename sealedge-attest-sbom-action -R TrustEdge-Labs/attest-sbom-action` executed by user (`renamed` resume signal)
3. Task 3: D-09 curl returns `301 -> /sealedge-attest-sbom-action` (with retry tolerance); follow-redirect returns 200; T1 asset-integrity check passes or documented N/A
4. Task 4: External repo has new commit on main with exactly 3 files (T5 mitigation); `@v2.0.0` + floating `@v2` tags pushed; `@v1` untouched
5. Task 5: Marketplace listing confirms `Sealedge SBOM Attestation` (auto-refresh or manual re-publish); `88-VERIFICATION.md` written and committed with all 8 sections populated

**If any task fails:**
- Execute rollback per Task 2's documented command: `gh repo rename attest-sbom-action -R TrustEdge-Labs/sealedge-attest-sbom-action`
- If rollback is post-Task-4, additionally: `gh release delete v2.0.0 -R TrustEdge-Labs/attest-sbom-action --yes; git push origin :refs/tags/v2.0.0; git push origin :refs/tags/v2` (from restored-name repo clone)
- Diagnose root cause; re-run from the appropriate task

**The rollback window is bounded** — within GitHub's rename-history preservation (effectively indefinite for a solo-dev repo with no active consumers). Solo-dev safety posture: all rollback paths are low-cost.
</verification>

<success_criteria>
- External repo accessible at `https://github.com/TrustEdge-Labs/sealedge-attest-sbom-action` (200 OK)
- `https://github.com/TrustEdge-Labs/attest-sbom-action` returns HTTP 301 redirecting to the renamed path
- External repo has exactly 3 tracked files on `main`: `action.yml`, `LICENSE`, `README.md` (T5 mitigation verified)
- External repo's `main` branch has one new commit matching the monorepo `actions/sealedge-attest-sbom-action/` source byte-for-byte
- `@v2.0.0` tag + floating `@v2` tag exist on the external repo and point at the rebrand-baseline commit
- `@v1` tag is untouched (still points at its pre-rebrand commit)
- GitHub Actions Marketplace listing reflects `Sealedge SBOM Attestation` title + rebranded description (auto-refresh OR manual re-publish documented)
- `88-VERIFICATION.md` exists with all 8 required sections + rollback command verbatim + EXT-02/03 status table
- Commit `docs(88): verification evidence` pushed to monorepo origin/main
- EXT-02 is satisfied (new repo name, rebranded source, `@v2` shipped, SHA256 verification preserved via Plan 01)
- EXT-03 is satisfied (301 redirect covers `@v1` refs, `@v1` frozen, README has top-of-README notice via Plan 01)
- Phase 88 is positioned for close once Plan 04 (website sweep) completes
</success_criteria>

<output>
After completion, create `.planning/phases/88-external-action-product-website/88-03-SUMMARY.md` per the gsd summary template. Include:
- External rename + @v2 tag cut timeline
- Reference to `88-VERIFICATION.md` for the full evidence record
- Confirmation that rollback was NOT needed (or, if it was, describe the path taken)
- Pointer forward to Plan 04 (trustedgelabs-website sweep) for EXT-04 closure
- Pointer forward to Phase 89's first v6.0.0 release run as the end-to-end functional proof of the dogfooded action
</output>
