---
phase: 45-rsa-oaep-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/src/asymmetric.rs
  - .cargo/audit.toml
autonomous: true
requirements:
  - RSA-01
  - RSA-02

must_haves:
  truths:
    - "RSA encrypt produces OAEP-SHA256 ciphertext, not PKCS#1 v1.5"
    - "RSA decrypt uses OAEP-SHA256 and rejects PKCS#1 v1.5 ciphertext"
    - "All existing RSA tests pass with OAEP padding"
    - "cargo-audit no longer flags RUSTSEC-2023-0071 as risk-accepted for encrypt/decrypt"
  artifacts:
    - path: "crates/core/src/asymmetric.rs"
      provides: "RSA OAEP-SHA256 encrypt and decrypt"
      contains: "Oaep::new::<Sha256>()"
    - path: ".cargo/audit.toml"
      provides: "Clean audit config without RSA Marvin Attack exception"
  key_links:
    - from: "crates/core/src/asymmetric.rs"
      to: "rsa::Oaep"
      via: "use rsa::Oaep"
      pattern: "Oaep::new::<Sha256>"
---

<objective>
Replace RSA PKCS#1 v1.5 padding with OAEP-SHA256 in asymmetric.rs encrypt and decrypt functions, and remove the RUSTSEC-2023-0071 risk acceptance from audit.toml.

Purpose: Eliminate padding oracle attack vulnerability (Marvin Attack) in RSA key exchange path.
Output: Hardened RSA operations using OAEP-SHA256, clean cargo-audit.
</objective>

<execution_context>
@/home/john/.claude/get-shit-done/workflows/execute-plan.md
@/home/john/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/45-rsa-oaep-migration/45-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace PKCS#1 v1.5 with OAEP-SHA256 in RSA encrypt and decrypt</name>
  <files>crates/core/src/asymmetric.rs</files>
  <read_first>crates/core/src/asymmetric.rs</read_first>
  <action>
In `crates/core/src/asymmetric.rs`, make two changes:

1. In `rsa_encrypt_key` (line ~300):
   - Remove: `use rsa::{Pkcs1v15Encrypt, RsaPublicKey};`
   - Add: `use rsa::{Oaep, RsaPublicKey};`
   - Change the encrypt call from:
     `rsa_public.encrypt(&mut OsRng, Pkcs1v15Encrypt, session_key)`
     to:
     `rsa_public.encrypt(&mut OsRng, Oaep::new::<sha2::Sha256>(), session_key)`

2. In `rsa_decrypt_key` (line ~318):
   - Remove: `use rsa::{Pkcs1v15Encrypt, RsaPrivateKey};`
   - Add: `use rsa::{Oaep, RsaPrivateKey};`
   - Change the decrypt call from:
     `rsa_private.decrypt(Pkcs1v15Encrypt, encrypted_key)`
     to:
     `rsa_private.decrypt(Oaep::new::<sha2::Sha256>(), encrypted_key)`

No other changes needed. The `sha2` crate is already a dependency of trustedge-core. Existing tests use `encrypt_key_asymmetric` and `decrypt_key_asymmetric` which delegate to these functions, so they will automatically exercise OAEP.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/trustedge && cargo test -p trustedge-core test_rsa_key_encryption -- --nocapture</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "Oaep::new::<sha2::Sha256>()" crates/core/src/asymmetric.rs (OAEP used in encrypt)
    - grep -c "Pkcs1v15Encrypt" crates/core/src/asymmetric.rs returns 0 (no PKCS#1 v1.5 references remain)
    - cargo test -p trustedge-core test_rsa_key_encryption passes (round-trip works with OAEP)
  </acceptance_criteria>
  <done>Both rsa_encrypt_key and rsa_decrypt_key use Oaep::new::<Sha256>() instead of Pkcs1v15Encrypt. The test_rsa_key_encryption test passes with OAEP padding.</done>
</task>

<task type="auto">
  <name>Task 2: Remove RUSTSEC-2023-0071 from cargo-audit risk-accepted list</name>
  <files>.cargo/audit.toml</files>
  <read_first>.cargo/audit.toml</read_first>
  <action>
In `.cargo/audit.toml`, remove the entire RUSTSEC-2023-0071 entry from the `ignore` list. The advisory is for RSA Marvin Attack timing sidechannel on PKCS#1 v1.5 padding. Since we now use OAEP, the advisory no longer applies to our encrypt/decrypt path.

After removing, if the `ignore` list is empty, keep the structure as:
```toml
[advisories]
ignore = []
```

Keep the file header comment and the general documentation comment about the file's purpose.

Then run `cargo audit` to confirm no new advisories are flagged (RUSTSEC-2023-0071 may still appear for the yubikey transitive dependency on rsa 0.7.2, but we only need it removed from our risk-accepted list since we no longer use PKCS#1 v1.5 in our own code). If cargo-audit flags RUSTSEC-2023-0071 for the yubikey transitive dep, re-add it with an updated comment noting it only applies to the yubikey transitive dependency (rsa 0.7.2), not our direct usage.

Finally, run the full test suite: `cargo test -p trustedge-core --lib` to confirm nothing is broken.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/trustedge && cargo test -p trustedge-core --lib</automated>
  </verify>
  <acceptance_criteria>
    - The RUSTSEC-2023-0071 comment in .cargo/audit.toml no longer mentions "RSA encryption" or "production code paths" as a rationale (either removed entirely or scoped to yubikey transitive dep only)
    - cargo test -p trustedge-core --lib passes (full unit test suite green)
  </acceptance_criteria>
  <done>RUSTSEC-2023-0071 is either removed from audit.toml (if no transitive dep needs it) or scoped to yubikey-only rationale. Full trustedge-core unit tests pass.</done>
</task>

</tasks>

<verification>
1. `cargo test -p trustedge-core --lib` — all unit tests pass
2. `grep -c "Pkcs1v15Encrypt" crates/core/src/asymmetric.rs` — returns 0
3. `grep -c "Oaep" crates/core/src/asymmetric.rs` — returns at least 2 (one per function)
4. `cargo clippy -p trustedge-core -- -D warnings` — no warnings
</verification>

<success_criteria>
- RSA encrypt and decrypt use OAEP-SHA256 (no PKCS#1 v1.5 references in asymmetric.rs)
- test_rsa_key_encryption passes with round-trip OAEP encrypt/decrypt
- Full trustedge-core unit test suite passes
- cargo-audit config updated to reflect OAEP migration
</success_criteria>

<output>
After completion, create `.planning/phases/45-rsa-oaep-migration/45-01-SUMMARY.md`
</output>
