---
phase: 87-github-repository-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/ISSUE_TEMPLATE/config.yml
  - .github/workflows/cla.yml
  - deploy/digitalocean/app.yaml
autonomous: true
requirements:
  - EXT-01
tags:
  - rebrand
  - urls
  - straggler-cleanup
  - phase-87
must_haves:
  truths:
    - "All 3 tracked files Phase 85/86 didn't scope (`.github/ISSUE_TEMPLATE/config.yml`, `.github/workflows/cla.yml`, `deploy/digitalocean/app.yaml`) point at `/TrustEdge-Labs/sealedge` (per D-01)"
    - "Allowlisted repo-wide grep (`git ls-files | xargs grep -n 'TrustEdge-Labs/trustedge' | grep -vE '^\\.planning/|^\\.factory/|actions/attest-sbom-action/|improvement-plan\\.md|RFC_K256_SUPPORT\\.md|security-review-platform\\.md|CHANGELOG\\.md|MIGRATION\\.md'`) returns zero results (per D-13 §3 / §Specifics verbatim audit)"
    - "The straggler cleanup lands as a single atomic commit with the D-03-prescribed message `fix(87): point last tracked URL refs at /TrustEdge-Labs/sealedge` (per D-03)"
    - "The straggler commit lands and is pushed to `origin/main` **before** any rename-operation work begins (per D-04 / D-07 step 1-2)"
    - "CHANGELOG.md and MIGRATION.md are NOT modified by this plan (per D-02, hybrid historical treatment)"
  artifacts:
    - path: .github/ISSUE_TEMPLATE/config.yml
      provides: "GitHub issue router pointing at renamed repo (3 URL refs replaced)"
      contains: "TrustEdge-Labs/sealedge"
    - path: .github/workflows/cla.yml
      provides: "CLA Assistant config + prose pointing at renamed repo (2 URL refs replaced)"
      contains: "TrustEdge-Labs/sealedge/blob/main/CLA.md"
    - path: deploy/digitalocean/app.yaml
      provides: "DO App Platform source spec pointing at renamed repo (1 `repo:` field replaced)"
      contains: "repo: TrustEdge-Labs/sealedge"
    - path: .planning/phases/87-github-repository-rename/87-01-SUMMARY.md
      provides: "Plan summary with exact grep audit output"
      contains: "zero results"
  key_links:
    - from: .github/ISSUE_TEMPLATE/config.yml
      to: "GitHub issue-creation UI (security advisories, discussions, README link)"
      via: "New-issue landing page consumes `url:` fields"
      pattern: "TrustEdge-Labs/sealedge"
    - from: .github/workflows/cla.yml
      to: "CLA Assistant GitHub App (comments on PRs with the CLA link)"
      via: "`path-to-document:` + prose message template"
      pattern: "TrustEdge-Labs/sealedge/blob/main/CLA.md"
    - from: deploy/digitalocean/app.yaml
      to: "DigitalOcean App Platform build/deploy pipeline"
      via: "`github.repo:` field resolves source repo"
      pattern: "repo: TrustEdge-Labs/sealedge"
    - from: "Phase 87 Plan 02 (rename + verify)"
      to: "This plan's commit"
      via: "Ordering dependency — stragglers committed + pushed BEFORE rename, per D-04/D-07"
      pattern: "wave: 2 depends_on 87-01"
---

<objective>
Sweep the last 3 tracked files containing `/TrustEdge-Labs/trustedge` URL references that Phase 85/86 did not scope, land them as a single atomic commit per D-03, push to `origin/main` (against the still-old remote name), and verify the allowlisted repo-wide grep audit (D-13 §3 verbatim) returns zero results. This must complete and be pushed before Plan 02 can trigger the GitHub rename operation (D-04 / D-07 step 1-2).

Purpose: EXT-01 requires the renamed monorepo to have in-repo references resolving correctly. Phase 85/86 covered `.rs`, markdown, rustdoc, and scripts; these 3 `.yml`/`.yaml` config files fell outside those boundaries. Fixing them pre-rename means new clones of the renamed repo have clean URL strings from commit 1, and the GitHub 301 redirect only has to cover the tiny window between push and rename.
Output: 3 tracked files updated (6 total URL substitutions), 1 atomic commit per D-03, grep audit proof.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-github-repository-rename/87-CONTEXT.md
@CLAUDE.md
@.github/ISSUE_TEMPLATE/config.yml
@.github/workflows/cla.yml
@deploy/digitalocean/app.yaml

<interfaces>
<!-- Exact strings that must change, per grep of the repo head as of phase start: -->

.github/ISSUE_TEMPLATE/config.yml (3 URLs):
  line 8:  https://github.com/TrustEdge-Labs/trustedge/security/advisories/new
           -> https://github.com/TrustEdge-Labs/sealedge/security/advisories/new
  line 11: https://github.com/TrustEdge-Labs/trustedge/discussions
           -> https://github.com/TrustEdge-Labs/sealedge/discussions
  line 14: https://github.com/TrustEdge-Labs/trustedge/blob/main/README.md
           -> https://github.com/TrustEdge-Labs/sealedge/blob/main/README.md

.github/workflows/cla.yml (2 URLs):
  line 30: 'https://github.com/TrustEdge-Labs/trustedge/blob/main/CLA.md'
           -> 'https://github.com/TrustEdge-Labs/sealedge/blob/main/CLA.md'
  line 49: https://github.com/TrustEdge-Labs/trustedge/blob/main/CLA.md
           -> https://github.com/TrustEdge-Labs/sealedge/blob/main/CLA.md

deploy/digitalocean/app.yaml (1 ref):
  line 18: repo: TrustEdge-Labs/trustedge
           -> repo: TrustEdge-Labs/sealedge

<!-- D-13 §3 verbatim allowlisted grep audit (must return 0 hits post-edit): -->

git ls-files | xargs grep -n "TrustEdge-Labs/trustedge" 2>/dev/null \
  | grep -vE '^\.planning/|^\.factory/|actions/attest-sbom-action/|improvement-plan\.md|RFC_K256_SUPPORT\.md|security-review-platform\.md|CHANGELOG\.md|MIGRATION\.md'

<!-- D-03 verbatim commit message: -->

fix(87): point last tracked URL refs at /TrustEdge-Labs/sealedge
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace URLs in the 3 straggler files</name>
  <files>.github/ISSUE_TEMPLATE/config.yml, .github/workflows/cla.yml, deploy/digitalocean/app.yaml</files>
  <read_first>
    - .github/ISSUE_TEMPLATE/config.yml (see current state; exactly 3 matches for `TrustEdge-Labs/trustedge`)
    - .github/workflows/cla.yml (see current state; exactly 2 matches)
    - deploy/digitalocean/app.yaml (see current state; exactly 1 match on line 18 `repo:` field)
    - .planning/phases/87-github-repository-rename/87-CONTEXT.md §D-01 (scope), §D-02 (CHANGELOG/MIGRATION explicitly out of scope), §D-03 (atomic commit), §D-04 (ordering rationale)
  </read_first>
  <action>
    Perform exactly 6 URL substitutions across the 3 files. In every case, replace the literal string `TrustEdge-Labs/trustedge` with `TrustEdge-Labs/sealedge`. Do NOT touch any other strings; do NOT rewrite the `name: trustedge-verifier` field on line 12 of `deploy/digitalocean/app.yaml` (DO App Platform's internal app name — not a repo URL, and renaming the DO app is out of scope per D-11; only the `repo:` field on line 18 is in scope). Do NOT touch the MPL-2.0 copyright header `Project: trustedge — Privacy and trust at the edge.` in `deploy/digitalocean/app.yaml` line 3 or in the other two files — Phase 85 owned copyright-header sweeps and those headers were deliberately left in place here since the 3 files were not in Phase 85's scope; rewriting them now would expand this plan beyond D-01 scope. (If the planner needs the header fixed, that is a Phase 85 follow-up, not Phase 87 scope — per D-01.)

    Use the Edit tool for each file (preferred over `sed` for deterministic outcomes and because these are 3 tracked config files). Concretely:

    File 1 — `.github/ISSUE_TEMPLATE/config.yml`, 3 edits:
      - Line 8  : change `https://github.com/TrustEdge-Labs/trustedge/security/advisories/new` → `https://github.com/TrustEdge-Labs/sealedge/security/advisories/new`
      - Line 11 : change `https://github.com/TrustEdge-Labs/trustedge/discussions` → `https://github.com/TrustEdge-Labs/sealedge/discussions`
      - Line 14 : change `https://github.com/TrustEdge-Labs/trustedge/blob/main/README.md` → `https://github.com/TrustEdge-Labs/sealedge/blob/main/README.md`

    File 2 — `.github/workflows/cla.yml`, 2 edits:
      - Line 30 : change `'https://github.com/TrustEdge-Labs/trustedge/blob/main/CLA.md'` → `'https://github.com/TrustEdge-Labs/sealedge/blob/main/CLA.md'` (single-quoted string in YAML — preserve the quotes)
      - Line 49 : change `https://github.com/TrustEdge-Labs/trustedge/blob/main/CLA.md` → `https://github.com/TrustEdge-Labs/sealedge/blob/main/CLA.md` (inside the `comment-to-post:` block scalar)

    File 3 — `deploy/digitalocean/app.yaml`, 1 edit:
      - Line 18 : change `      repo: TrustEdge-Labs/trustedge` → `      repo: TrustEdge-Labs/sealedge` (preserve the 6-space indentation under `github:`)

    Keep line numbering intact — the edits are pure substring swaps, no line insertions or deletions. Do NOT reflow YAML whitespace.
  </action>
  <verify>
    <automated>
# Per-file counts — exact expectations per D-01 scope:
test "$(grep -c 'TrustEdge-Labs/trustedge' .github/ISSUE_TEMPLATE/config.yml)" = "0" \
  && test "$(grep -c 'TrustEdge-Labs/sealedge' .github/ISSUE_TEMPLATE/config.yml)" = "3" \
  && test "$(grep -c 'TrustEdge-Labs/trustedge' .github/workflows/cla.yml)" = "0" \
  && test "$(grep -c 'TrustEdge-Labs/sealedge' .github/workflows/cla.yml)" = "2" \
  && test "$(grep -c 'TrustEdge-Labs/trustedge' deploy/digitalocean/app.yaml)" = "0" \
  && test "$(grep -c 'TrustEdge-Labs/sealedge' deploy/digitalocean/app.yaml)" = "1"
    </automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "TrustEdge-Labs/trustedge" .github/ISSUE_TEMPLATE/config.yml` returns `0`
    - `grep -c "TrustEdge-Labs/sealedge" .github/ISSUE_TEMPLATE/config.yml` returns `3`
    - `grep -c "TrustEdge-Labs/trustedge" .github/workflows/cla.yml` returns `0`
    - `grep -c "TrustEdge-Labs/sealedge" .github/workflows/cla.yml` returns `2`
    - `grep -c "TrustEdge-Labs/trustedge" deploy/digitalocean/app.yaml` returns `0`
    - `grep -c "TrustEdge-Labs/sealedge" deploy/digitalocean/app.yaml` returns `1`
    - `grep -n "repo:" deploy/digitalocean/app.yaml` shows `18:      repo: TrustEdge-Labs/sealedge` (indentation preserved)
    - `git diff --stat .github/ISSUE_TEMPLATE/config.yml .github/workflows/cla.yml deploy/digitalocean/app.yaml` shows 3 files changed, 6 insertions(+), 6 deletions(-)
  </acceptance_criteria>
  <done>3 files show exactly the per-file substitution counts above; `git diff` is clean (no unrelated lines changed); no files outside the 3-file scope are modified.</done>
</task>

<task type="auto">
  <name>Task 2: Run allowlisted repo-wide grep audit (D-13 §3 verbatim)</name>
  <files>(no file edits — audit-only; output captured into 87-01-SUMMARY.md in Task 3)</files>
  <read_first>
    - .planning/phases/87-github-repository-rename/87-CONTEXT.md §D-13 (verification gate) and §Specifics (exact audit command)
    - Output of Task 1 (the 3 files now point at sealedge)
  </read_first>
  <action>
    Run the D-13 §3 / §Specifics verbatim audit command, no modifications to the regex or allowlist:

    ```
    git ls-files | xargs grep -n "TrustEdge-Labs/trustedge" 2>/dev/null \
      | grep -vE '^\.planning/|^\.factory/|actions/attest-sbom-action/|improvement-plan\.md|RFC_K256_SUPPORT\.md|security-review-platform\.md|CHANGELOG\.md|MIGRATION\.md'
    ```

    Expected output: empty (zero lines). Capture the exact command + its output (stdout + exit status) for the summary. If the command returns ANY hit outside the allowlist, STOP — the Task 1 sweep missed a file or a new `TrustEdge-Labs/trustedge` reference was introduced after Phase 86. Surface the output and do NOT proceed to commit (Task 3). Resolve by extending Task 1's scope to cover the hit, re-running the audit, then continuing.

    Run the same command without the allowlist filter to confirm the only remaining `TrustEdge-Labs/trustedge` references are in allowlisted locations (sanity check):

    ```
    git ls-files | xargs grep -l "TrustEdge-Labs/trustedge" 2>/dev/null | sort
    ```

    Expected: only files under `.planning/`, `.factory/`, `actions/attest-sbom-action/`, or explicitly allowlisted historical files (`improvement-plan.md`, `RFC_K256_SUPPORT.md`, `security-review-platform.md`, `CHANGELOG.md`, `MIGRATION.md`).
  </action>
  <verify>
    <automated>
# D-13 §3 verbatim audit — must return zero lines + exit status 1 from the final grep -vE
# (grep exits 1 when no lines match, which is what we want here)
OUT=$(git ls-files | xargs grep -n "TrustEdge-Labs/trustedge" 2>/dev/null \
  | grep -vE '^\.planning/|^\.factory/|actions/attest-sbom-action/|improvement-plan\.md|RFC_K256_SUPPORT\.md|security-review-platform\.md|CHANGELOG\.md|MIGRATION\.md')
test -z "$OUT"
    </automated>
  </verify>
  <acceptance_criteria>
    - D-13 §3 verbatim audit command (above) emits zero stdout lines
    - Sanity-check `git ls-files | xargs grep -l 'TrustEdge-Labs/trustedge' 2>/dev/null` returns ONLY paths matching the allowlist regex (paths under `.planning/`, `.factory/`, `actions/attest-sbom-action/`, or the 5 allowlisted historical `.md` files)
    - If either check fails, Task 3 (commit) does NOT run until Task 1 is re-scoped to cover the hits
  </acceptance_criteria>
  <done>D-13 §3 audit is clean (zero hits outside allowlist); ready for atomic commit in Task 3.</done>
</task>

<task type="auto">
  <name>Task 3: Commit straggler fixes atomically and push to origin/main</name>
  <files>(commit + push only; no new file contents)</files>
  <read_first>
    - .planning/phases/87-github-repository-rename/87-CONTEXT.md §D-03 (exact commit message), §D-04 (push-before-rename rationale), §D-07 step 1-2 (commit → push sequence)
    - Output of Tasks 1-2 (3 files modified, audit clean)
  </read_first>
  <action>
    Stage only the 3 in-scope files (do NOT use `git add -A` or `git add .` — scope discipline per D-03 "single atomic commit" and to avoid capturing any unrelated working-tree dirt):

    ```
    git add .github/ISSUE_TEMPLATE/config.yml .github/workflows/cla.yml deploy/digitalocean/app.yaml
    ```

    Confirm the staged diff is exactly the 6 substitutions from Task 1 and nothing else:

    ```
    git diff --cached --stat
    # expected: 3 files changed, 6 insertions(+), 6 deletions(-)
    git diff --cached | grep -c '^+'  # expected: 9  (6 '+' lines + 3 file markers)
    git diff --cached | grep -c '^-'  # expected: 9  (6 '-' lines + 3 file markers)
    ```

    Create the commit with the D-03 verbatim message (use a HEREDOC to ensure consistent formatting, including the Co-Authored-By trailer per gsd repo commit conventions):

    ```
    git commit -m "$(cat <<'EOF'
    fix(87): point last tracked URL refs at /TrustEdge-Labs/sealedge

    Sweeps the 3 tracked files Phase 85/86 did not scope:
    - .github/ISSUE_TEMPLATE/config.yml (3 URLs: security advisories, discussions, README blob)
    - .github/workflows/cla.yml (2 URLs: CLA Assistant path-to-document + prose)
    - deploy/digitalocean/app.yaml (repo: source field)

    Lands before the GitHub rename (Phase 87 Plan 02) so new clones of the
    renamed repo have clean URL strings from commit 1. GitHub's 301 redirect
    covers the tiny window between push and rename.

    Per Phase 87 CONTEXT.md D-01/D-03/D-04. Related: EXT-01.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    ```

    Push to `origin/main` (the remote still points at `TrustEdge-Labs/trustedge` at this stage — the rename happens in Plan 02):

    ```
    git push origin main
    ```

    Verify the push succeeded (no rejection, remote ref advanced):

    ```
    git rev-parse HEAD
    git rev-parse origin/main
    # the two SHAs must match post-push
    ```

    Write `87-01-SUMMARY.md` per the gsd summary template (summary.md already referenced in execution_context). Include:
    - The exact `git diff --cached --stat` output (3 files, 6+/6−)
    - The exact D-13 §3 audit command + its empty output (proof of zero hits)
    - The commit SHA (from `git rev-parse HEAD`)
    - The `origin/main` SHA match proof (same rev as HEAD after push)
    - Pointer forward to Plan 02 for the rename operation
  </action>
  <verify>
    <automated>
# Post-commit + push verification:
test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" \
  && git log -1 --pretty=%s | grep -qE '^fix\(87\): point last tracked URL refs at /TrustEdge-Labs/sealedge$' \
  && git log -1 --stat | grep -q '3 files changed, 6 insertions(+), 6 deletions(-)' \
  && test -f .planning/phases/87-github-repository-rename/87-01-SUMMARY.md
    </automated>
  </verify>
  <acceptance_criteria>
    - `git log -1 --pretty=%s` returns exactly `fix(87): point last tracked URL refs at /TrustEdge-Labs/sealedge`
    - `git log -1 --stat` shows `3 files changed, 6 insertions(+), 6 deletions(-)`
    - `git rev-parse HEAD` == `git rev-parse origin/main` (commit is pushed)
    - `.planning/phases/87-github-repository-rename/87-01-SUMMARY.md` exists and contains the D-13 §3 audit output
    - No other files (outside the 3) are staged or modified in the working tree (`git status --porcelain` against tracked files is clean)
  </acceptance_criteria>
  <done>Atomic straggler commit landed and pushed to `origin/main` (still `TrustEdge-Labs/trustedge` at this moment); SUMMARY.md committed; Plan 02 can now proceed to execute the rename.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| working tree → `origin/main` (GitHub) | Push advances public `main` on still-old repo name; audience = any reader of `TrustEdge-Labs/trustedge` (soon `/sealedge`) |
| `deploy/digitalocean/app.yaml` → DigitalOcean App Platform | DO reads the file out-of-band to resolve source; mis-edit could knock verify-page offline |
| `.github/workflows/cla.yml` → CLA Assistant GitHub App | GitHub App reads `path-to-document:` to resolve the CLA URL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-01   | Tampering | `deploy/digitalocean/app.yaml` (edit breaks YAML syntax) | mitigate | Task 1 uses targeted Edit (not sed/awk); Task 1 acceptance criteria check `grep -n "repo:"` line number + indentation preserved; Plan 02 Task 5 verifies DO auto-deploy fires end-to-end |
| T-87-02   | Denial of Service | `deploy/digitalocean/app.yaml` `repo:` misspelled / pointing at nonexistent repo | mitigate | Task 1 acceptance criteria verifies exactly one `TrustEdge-Labs/sealedge` literal in the file; Plan 02 Task 5 confirms DO auto-deploy completes successfully on post-rename push |
| T-87-03   | Information Disclosure | `.github/workflows/cla.yml` URL points at wrong repo → CLA signing flow routes to broken link | mitigate | Task 1 acceptance criteria confirms both lines reference `TrustEdge-Labs/sealedge/blob/main/CLA.md`; CLA Assistant signatures are repo-ID-linked (survives rename per D-12) |
| T-87-04   | Elevation of Privilege | Scope creep — this plan modifies files beyond the 3-file scope | mitigate | Task 3 stages only the 3 named files (not `git add -A`); Task 3 acceptance criteria verifies `3 files changed, 6 insertions(+), 6 deletions(-)` exactly |
| T-87-05   | Repudiation | Commit message doesn't match D-03 verbatim → reviewers can't trace back to the decision | mitigate | Task 3 uses D-03 verbatim commit message in HEREDOC; acceptance criteria regex-matches the subject line exactly |
| T-87-06   | Spoofing (supply-chain) | Someone adds new `/TrustEdge-Labs/trustedge` reference between Phase 86 close and Phase 87 start | accept | Task 2 D-13 §3 audit catches any hit in any tracked file; if hit found outside allowlist, Task 3 (commit) is blocked until re-scoped. Risk = low (solo dev, linear phase execution, no concurrent contributors on main) |

**Overall grade: LOW.** This plan edits 3 tracked config files with 6 literal substitutions, commits atomically, and pushes. No new dependencies, no new attack surface, no crypto changes. The operational risk (DO/CLA Assistant integrations) is mitigated by in-plan acceptance criteria plus Plan 02's post-rename verification gate.
</threat_model>

<verification>
**Plan-level gate (must all be true before marking plan complete):**
1. Task 1 acceptance criteria pass (6 substitutions, per-file grep counts match)
2. Task 2 D-13 §3 verbatim audit returns zero lines
3. Task 3 commit subject matches `fix(87): point last tracked URL refs at /TrustEdge-Labs/sealedge` exactly
4. Task 3 `git log -1 --stat` shows `3 files changed, 6 insertions(+), 6 deletions(-)`
5. Commit is pushed — `git rev-parse HEAD == git rev-parse origin/main`
6. `87-01-SUMMARY.md` exists with audit output captured

If any of the above fails, DO NOT mark the plan complete. Plan 02 depends on the push landing before the rename fires.
</verification>

<success_criteria>
- 3 tracked files (`.github/ISSUE_TEMPLATE/config.yml`, `.github/workflows/cla.yml`, `deploy/digitalocean/app.yaml`) no longer contain the literal `TrustEdge-Labs/trustedge`
- Allowlisted repo-wide grep (D-13 §3 verbatim) returns zero hits
- Atomic commit `fix(87): point last tracked URL refs at /TrustEdge-Labs/sealedge` lands on `main` and is pushed to `origin/main` (still `TrustEdge-Labs/trustedge` upstream — rename is Plan 02)
- CHANGELOG.md and MIGRATION.md are NOT modified (per D-02)
- No files outside the 3-file scope are modified (scope discipline, clean rollback path)
</success_criteria>

<output>
After completion, create `.planning/phases/87-github-repository-rename/87-01-SUMMARY.md` using `@$HOME/.claude/get-shit-done/templates/summary.md`. Include:
- The exact `git diff --cached --stat` output (3 files, 6+/6−)
- The exact D-13 §3 audit command + proof it returned empty
- The commit SHA (from `git rev-parse HEAD`)
- Pointer to Plan 02 for the rename operation itself
</output>
