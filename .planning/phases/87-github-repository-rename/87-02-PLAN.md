---
phase: 87-github-repository-rename
plan: 02
type: execute
wave: 2
depends_on:
  - 87-01
files_modified:
  - .planning/phases/87-github-repository-rename/87-VERIFICATION.md
autonomous: false
requirements:
  - EXT-01
tags:
  - rebrand
  - github-rename
  - external-operation
  - verification
  - phase-87
must_haves:
  truths:
    - "Pre-rename gate is green (D-06): working tree clean on `main`, `main` pushed to `origin`, latest CI on `main` is green, no open PRs requiring rebase, no scheduled workflows currently in-progress"
    - "GitHub monorepo is accessible at `https://github.com/TrustEdge-Labs/sealedge` (per ROADMAP §Phase 87 success criterion #1 / D-13 §1)"
    - "`https://github.com/TrustEdge-Labs/trustedge` returns HTTP 301 redirect to `https://github.com/TrustEdge-Labs/sealedge` (GitHub's built-in redirect, per D-13 §1)"
    - "Local `origin` remote URL is `https://github.com/TrustEdge-Labs/sealedge.git` (HTTPS retained per D-08 / D-13 §2 / ROADMAP success criterion #2)"
    - "`git fetch origin` and `git ls-remote origin` against the renamed remote succeed without manual URL fixes (per D-13 §2)"
    - "One CI workflow run on the renamed repo completes green post-rename (per D-13 §3)"
    - "DigitalOcean App Platform auto-deploy fires on the post-rename push; verify page stays live (per D-13 §4 / D-11)"
    - "Phase 87 is reversible: `gh repo rename trustedge -R TrustEdge-Labs/sealedge` is documented verbatim in this plan as the rollback path (per D-15)"
    - "`87-VERIFICATION.md` captures the 4 D-13 evidence artifacts: curl output with 301 line, `git fetch` output, CI run URL, DO auto-deploy timestamp (per D-16)"
  artifacts:
    - path: .planning/phases/87-github-repository-rename/87-VERIFICATION.md
      provides: "Phase-close evidence capture — all 4 D-13 gate outputs with URLs/SHAs/timestamps"
      contains: "301"
    - path: .planning/phases/87-github-repository-rename/87-02-SUMMARY.md
      provides: "Plan summary with rename operation timeline + rollback-command-not-needed confirmation"
      contains: "gh repo rename"
  key_links:
    - from: "User shell (`gh repo rename sealedge -R TrustEdge-Labs/trustedge`)"
      to: "GitHub repo rename API"
      via: "`gh` CLI with `repo` scope already granted per `gh auth status`"
      pattern: "gh repo rename sealedge -R TrustEdge-Labs/trustedge"
    - from: "Local working clone"
      to: "Renamed remote `TrustEdge-Labs/sealedge`"
      via: "`git remote set-url origin https://github.com/TrustEdge-Labs/sealedge.git` (HTTPS retained per D-08)"
      pattern: "origin.*sealedge\\.git"
    - from: "`TrustEdge-Labs/trustedge` URL (old name)"
      to: "`TrustEdge-Labs/sealedge` URL (new name)"
      via: "GitHub built-in 301 redirect"
      pattern: "curl -I.*301.*redirect_url.*sealedge"
    - from: "Renamed repo push/CI"
      to: "DigitalOcean App Platform (`deploy/digitalocean/app.yaml` + DO webhook)"
      via: "DO watches `main` branch on the renamed repo; GitHub forwards webhook on rename"
      pattern: "auto-deploy fires post-rename"
---

<objective>
Execute the GitHub repo rename from `TrustEdge-Labs/trustedge` to `TrustEdge-Labs/sealedge`, update the local `origin` remote URL, and run the D-13 4-check verification gate to confirm the rename operation succeeded end-to-end. If any check fails, execute the D-15 rollback (`gh repo rename trustedge -R TrustEdge-Labs/sealedge`) and diagnose before retry. Capture evidence in `87-VERIFICATION.md` per D-16.

Purpose: EXT-01 — the one external one-shot operation of v6.0. The monorepo must live at `/TrustEdge-Labs/sealedge` with working redirects, `origin` must point at the new URL, and downstream integrations (CI, DigitalOcean App Platform, CLA Assistant) must still function. Phase 89 will run the broader VALID-01/02/03 test matrix; this plan proves the rename operation itself.

Output: Renamed GitHub repo, updated local remote, `87-VERIFICATION.md` with 4 evidence artifacts, Phase 87 closed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/87-github-repository-rename/87-CONTEXT.md
@.planning/phases/87-github-repository-rename/87-01-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Exact commands, verbatim, for copy-paste reliability: -->

# D-06 Pre-rename gate (4 checks, all must pass):
git status                                                      # expected: clean working tree on branch main
git log origin/main..HEAD                                        # expected: empty (no unpushed commits)
gh run list --branch main --limit 1                              # expected: status=completed, conclusion=success
gh pr list --state open                                          # expected: empty OR each PR is deferrable
gh run list --workflow=* --status in_progress                    # expected: empty

# D-05 Rename command (USER RUNS — Claude provides string + resume-signal):
gh repo rename sealedge -R TrustEdge-Labs/trustedge

# D-07 step 5 — Update local origin remote (HTTPS retained per D-08):
git remote set-url origin https://github.com/TrustEdge-Labs/sealedge.git

# D-13 §1 — Redirect verification:
curl -I -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' https://github.com/TrustEdge-Labs/trustedge
# expected: 301 -> https://github.com/TrustEdge-Labs/sealedge
# fallback per §Specifics "30-second retry": if first call returns something other than 301, sleep 30 && retry once

# D-13 §2 — Fetch + push verification:
git fetch origin                                                 # expected: no errors, refs updated
git ls-remote origin                                             # expected: lists refs from /TrustEdge-Labs/sealedge
git remote get-url origin                                        # expected: https://github.com/TrustEdge-Labs/sealedge.git

# D-13 §3 — CI green post-rename:
gh run list --branch main --limit 1 --json status,conclusion,url  # expected: status=completed, conclusion=success
# Note: CI must fire post-rename. If no workflow has run since the rename, either (a) wait for the next scheduled run,
# (b) push an empty commit to trigger workflows, or (c) manually dispatch via `gh workflow run ci.yml`.

# D-13 §4 — DigitalOcean auto-deploy verification (user-only — Claude can't drive the DO UI):
# Push a trivial commit post-rename (README whitespace nudge or a verification commit), then watch DO App Platform:
#   https://cloud.digitalocean.com/apps → trustedge-verifier → Activity
# Expected: a new deploy fires, completes successfully; verify page (verify.sealedge.dev or current deploy URL) returns 200.

# D-15 Rollback command (VERBATIM — copy-paste ready if needed):
gh repo rename trustedge -R TrustEdge-Labs/sealedge
# After rollback, also run: git remote set-url origin https://github.com/TrustEdge-Labs/trustedge.git
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 1: Pre-rename gate (D-06) — confirm green before user runs rename</name>
  <files>(checkpoint task — no direct file edits; Claude runs 4 gate checks and reports results to user)</files>
  <read_first>
    - .planning/phases/87-github-repository-rename/87-CONTEXT.md §D-06 (4 pre-rename checks), §"Claude's Discretion" → PR rebasing (default: rebase-and-land an open PR before rename, or defer rename if it can't land quickly)
    - .planning/phases/87-github-repository-rename/87-01-SUMMARY.md (confirms Plan 01 pushed)
  </read_first>
  <action>
    <what-built>
      Plan 01 committed the 3 straggler files and pushed to `origin/main`. Before the user runs `gh repo rename`, all 4 D-06 pre-rename checks must be green. Claude runs the checks automatically and presents the results for user verification.
    </what-built>
    <how-to-verify>
      Claude will run the 4 D-06 checks and report results. Confirm all 4 are green before typing `approved` to continue.

      Claude runs (in order):

      1. **Working tree clean + `main` pushed** — `git status` should show `working tree clean` on branch `main`; `git log origin/main..HEAD` should be empty (no unpushed commits beyond Plan 01's commit, which is already pushed per Plan 01 Task 3).

      2. **Latest CI on `main` is green** — `gh run list --branch main --limit 1 --json status,conclusion,headSha,url`. Expected: `status: "completed"`, `conclusion: "success"`. If the latest run is still in-flight, wait for completion; if it failed, do NOT rename — investigate first.

      3. **No open PRs requiring rebase** — `gh pr list --state open --json number,title,author,mergeable`. Default per §"Claude's Discretion": rebase-and-land the PR before rename; if it can't land quickly, defer the rename until it does or until the author agrees to rebase post-rename. (The 30-day GitHub rename-redirect window for PRs is relevant, but the cleaner path is "land or explicitly defer, don't leave in-flight work stranded.")

      4. **No scheduled workflows currently in-progress** — `gh run list --status in_progress --limit 10 --json workflowName,status,url`. Expected: empty. If a workflow is mid-run, wait for it to complete (renaming mid-run could orphan the log location or cause artifact upload to fail).

      After running the 4 checks, Claude prints a single-line summary per check (✔ or ✖ + evidence). If all 4 are ✔, type `approved` to proceed to Task 2. If any are ✖, type `defer` + describe how you want to handle the blocker (land PR, wait for CI, etc.).

      **Do NOT skip any check.** The rename is reversible per D-15 but rollback costs time and creates noise in the git timeline. Gate checks are cheap; run them all.
    </how-to-verify>
  </action>
  <verify>Gate check — user must type `approved` as the resume signal after confirming all 4 D-06 checks are ✔. This is a human-verified checkpoint; no automated command replaces user judgment on the "open PRs" item, which requires context about whether deferral is acceptable.</verify>
  <resume-signal>Type `approved` if all 4 D-06 checks are ✔ and you're ready to run `gh repo rename`. Type `defer` + reason if any check fails or you want to wait.</resume-signal>
  <acceptance_criteria>
    - `git status` shows `nothing to commit, working tree clean` on branch `main`
    - `git log origin/main..HEAD` is empty (zero output lines)
    - `gh run list --branch main --limit 1 --json status,conclusion` returns `{"status":"completed","conclusion":"success"}` for the most recent run
    - `gh pr list --state open` returns an empty array OR the user has confirmed how open PRs are being handled
    - `gh run list --status in_progress --limit 10` returns empty
    - User has typed `approved` as the resume signal
  </acceptance_criteria>
  <done>All 4 D-06 gate checks are green; user has approved proceeding to the rename command.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: User executes `gh repo rename` (D-05) + rollback command on standby</name>
  <files>(checkpoint task — user runs `gh repo rename` from their shell; no files modified locally until Task 3's remote URL update)</files>
  <read_first>
    - .planning/phases/87-github-repository-rename/87-CONTEXT.md §D-05 (user-runs-rename rationale), §D-07 step 4 (rename command in sequence), §D-15 (rollback path), §"Claude's Discretion" → "timing window for redirect stabilization"
  </read_first>
  <action>
    <what-built>
      **This is the one-shot external operation.** The user runs `gh repo rename sealedge -R TrustEdge-Labs/trustedge` from their shell. Claude cannot drive this — it's a live operation on a shared surface (the GitHub repo), and per D-05 / "careful actions" default, user runs it. Claude's role is to provide the exact command string and stand by with the rollback command (D-15) if needed.
    </what-built>
    <how-to-verify>
      **Run this command from your shell** (not Claude's Bash tool; `gh auth status` confirms you have HTTPS + `repo` scope):

      ```
      gh repo rename sealedge -R TrustEdge-Labs/trustedge
      ```

      `gh` will print a confirmation and perform the rename. Expected output (or close to it):

      ```
      ✓ Renamed repository TrustEdge-Labs/sealedge
      ```

      Note the timestamp (local time) when the rename completes — this timestamp goes into `87-VERIFICATION.md` (D-16 evidence).

      **After the rename completes**, open the new URL in a browser (or `curl -I`) to confirm the repo is reachable:

      - https://github.com/TrustEdge-Labs/sealedge should load (200)
      - https://github.com/TrustEdge-Labs/trustedge should 301-redirect to the new URL (typed into the browser bar — some browsers follow the 301 silently and show the new URL; that's fine, that's the expected behavior)

      **If anything looks wrong** (rename errored, new URL doesn't load, old URL 404s instead of redirecting), execute the rollback verbatim from your shell:

      ```
      gh repo rename trustedge -R TrustEdge-Labs/sealedge
      ```

      Then type `rollback` as the resume signal and describe what went wrong. The rollback path is safe because (a) the repo name and (b) the local remote URL are the only state — both cheaply reversible per D-15.

      If everything looks clean, type `renamed` as the resume signal + paste the exact `gh` output (success line) into the conversation so it can be captured in `87-VERIFICATION.md`.

      **Timing note (per §"Claude's Discretion"):** GitHub's rename redirect is typically immediate. If the follow-up curl check in Task 4 returns something other than 301, wait 30 seconds and retry once before declaring failure — GitHub's DNS / edge caches can occasionally lag.

      **Rollback command kept verbatim below for fast copy-paste if ANY downstream check (Tasks 4-7) fails:**

      ```
      gh repo rename trustedge -R TrustEdge-Labs/sealedge
      git remote set-url origin https://github.com/TrustEdge-Labs/trustedge.git
      ```
    </how-to-verify>
  </action>
  <verify>Gate check — user must run `gh repo rename sealedge -R TrustEdge-Labs/trustedge` from their own shell and paste the success line into the conversation (`renamed` resume signal), OR describe the rollback path taken (`rollback` resume signal). Task 4's automated `curl -I` 301 check provides downstream verification of the rename itself.</verify>
  <resume-signal>Type `renamed` + paste the `gh` success output if the rename succeeded. Type `rollback` + describe the failure if you executed the rollback command.</resume-signal>
  <acceptance_criteria>
    - User ran `gh repo rename sealedge -R TrustEdge-Labs/trustedge` from their shell (not Claude's Bash tool — this is D-05 by design)
    - `gh` reported success (no error)
    - User has pasted the success output into the conversation for `87-VERIFICATION.md` capture
    - Rollback command is documented verbatim and ready if downstream verification fails
  </acceptance_criteria>
  <done>GitHub repo is renamed to `TrustEdge-Labs/sealedge`; user has confirmed via the `renamed` resume signal and pasted the `gh` output; or, on failure, user has executed rollback and described the issue.</done>
</task>

<task type="auto">
  <name>Task 3: Update local `origin` remote URL to the renamed repo (D-07 step 5 / D-08)</name>
  <files>(local `.git/config` — not a tracked file, but a persistent local-clone change)</files>
  <read_first>
    - .planning/phases/87-github-repository-rename/87-CONTEXT.md §D-07 step 5 (remote URL update ordering), §D-08 (HTTPS retained, don't switch to SSH)
    - Current remote state: `git remote get-url origin` (should still show `.../trustedge.git` at task start — GitHub's redirect will work against the old URL but we're being explicit and matching the new name)
  </read_first>
  <action>
    Run exactly these two commands:

    ```
    git remote set-url origin https://github.com/TrustEdge-Labs/sealedge.git
    git remote get-url origin
    ```

    The second command confirms the update landed. Expected output from `git remote get-url origin`:

    ```
    https://github.com/TrustEdge-Labs/sealedge.git
    ```

    Then validate the remote is reachable under the new URL:

    ```
    git fetch origin
    git ls-remote origin | head -5
    ```

    `git fetch origin` should succeed with no errors. `git ls-remote origin` should list refs (HEAD + branches + tags) from `/TrustEdge-Labs/sealedge`. If either errors, STOP and either (a) verify the rename actually happened (visit the new URL in a browser), or (b) execute the D-15 rollback per Task 2's rollback instructions.

    **Do NOT switch to SSH** (`git@github.com:...`). Per D-08, the existing remote is HTTPS (per `gh auth status` which shows HTTPS as the active git protocol), and switching protocols is unrelated to the rename and adds risk (new auth path to validate). Keep HTTPS.

    Capture the before/after output of `git remote get-url origin` for `87-VERIFICATION.md` (Task 7).
  </action>
  <verify>
    <automated>
# Post-update verification (D-13 §2 partial):
test "$(git remote get-url origin)" = "https://github.com/TrustEdge-Labs/sealedge.git" \
  && git fetch origin \
  && git ls-remote origin HEAD >/dev/null
    </automated>
  </verify>
  <acceptance_criteria>
    - `git remote get-url origin` returns exactly `https://github.com/TrustEdge-Labs/sealedge.git`
    - `git fetch origin` completes without errors
    - `git ls-remote origin` returns a list of refs (non-empty stdout)
    - Remote URL uses HTTPS (not SSH) per D-08
  </acceptance_criteria>
  <done>Local `origin` points at the renamed repo via HTTPS; fetch and ls-remote both succeed; ready for D-13 §1 curl check.</done>
</task>

<task type="auto">
  <name>Task 4: Verify GitHub 301 redirect (D-13 §1)</name>
  <files>(no file edits — evidence capture only)</files>
  <read_first>
    - .planning/phases/87-github-repository-rename/87-CONTEXT.md §D-13 §1 (curl redirect check), §"Claude's Discretion" → "30-second retry" buffer
  </read_first>
  <action>
    Run the D-13 §1 verbatim command:

    ```
    curl -I -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' https://github.com/TrustEdge-Labs/trustedge
    ```

    Expected output:

    ```
    301 -> https://github.com/TrustEdge-Labs/sealedge
    ```

    **If the first call returns something other than `301 -> https://github.com/TrustEdge-Labs/sealedge`** (e.g., `200`, `404`, `301 -> <wrong URL>`), apply the §"Claude's Discretion" 30-second retry buffer — GitHub's edge cache can occasionally lag:

    ```
    sleep 30 && curl -I -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' https://github.com/TrustEdge-Labs/trustedge
    ```

    Expected after retry: `301 -> https://github.com/TrustEdge-Labs/sealedge`. If still wrong after the retry, declare failure — execute the D-15 rollback from Task 2's rollback block and raise as a blocker for diagnostics.

    Also run an explicit follow-redirect check as a cross-check (curl follows the 301 and fetches the new URL):

    ```
    curl -s -o /dev/null -w '%{http_code} (final URL: %{url_effective})\n' -L https://github.com/TrustEdge-Labs/trustedge
    ```

    Expected: `200 (final URL: https://github.com/TrustEdge-Labs/sealedge)` or similar (the exact final URL may have trailing-slash variations — the key checks are (a) status 200, (b) final URL path ends in `/sealedge`).

    Capture both outputs verbatim for `87-VERIFICATION.md` (Task 7).
  </action>
  <verify>
    <automated>
# D-13 §1 verbatim curl check — accept 30-second retry on first miss:
check_redirect() {
  curl -I -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' https://github.com/TrustEdge-Labs/trustedge
}
OUT=$(check_redirect)
if ! echo "$OUT" | grep -qE '^301 -> https://github.com/TrustEdge-Labs/sealedge/?$'; then
  sleep 30
  OUT=$(check_redirect)
fi
echo "$OUT" | grep -qE '^301 -> https://github.com/TrustEdge-Labs/sealedge/?$'
    </automated>
  </verify>
  <acceptance_criteria>
    - `curl -I -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' https://github.com/TrustEdge-Labs/trustedge` returns `301 -> https://github.com/TrustEdge-Labs/sealedge` (possibly with trailing slash), either on first call or after one 30-second retry
    - Follow-redirect check (`curl -L`) ends at a `200` response with a `url_effective` path ending in `/sealedge`
    - Both outputs captured verbatim for `87-VERIFICATION.md`
    - If both checks fail after the 30-second retry, D-15 rollback is triggered and this task does NOT pass
  </acceptance_criteria>
  <done>GitHub 301 redirect is confirmed working; ROADMAP §Phase 87 success criterion #1 / D-13 §1 is satisfied.</done>
</task>

<task type="auto">
  <name>Task 5: Verify CI + DigitalOcean auto-deploy post-rename (D-13 §3 + §4)</name>
  <files>(no new files — may push one trivial commit to trigger CI + DO auto-deploy if neither has fired post-rename)</files>
  <read_first>
    - .planning/phases/87-github-repository-rename/87-CONTEXT.md §D-13 §3 (CI green), §D-13 §4 (DO auto-deploy), §D-11 (DO App Platform touch level — user may need to reconfigure source in DO if auto-deploy doesn't fire), §Specifics ("DO App auto-deploy verification — push a trivial touch-up commit...")
  </read_first>
  <action>
    **Step 5a — Trigger CI + DO by pushing a trivial commit.** After the rename + remote update, the most recent CI run is pre-rename. To verify post-rename CI + DO auto-deploy in one operation, push a minimal touch-up commit. Per §Specifics, a README whitespace nudge is recommended — but we'll keep it even more explicit: write a one-line phase-87-verification marker into the commit message, with NO code changes (use `--allow-empty`):

    ```
    git commit --allow-empty -m "$(cat <<'EOF'
    chore(87): post-rename CI + DO verification trigger

    Empty commit to fire CI workflows and DigitalOcean App Platform auto-deploy
    against the renamed repo. No file changes. Verifies D-13 §3 (CI green) and
    D-13 §4 (DO auto-deploy) on the post-rename remote.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    git push origin main
    ```

    Capture the commit SHA from `git rev-parse HEAD` — needed for CI run correlation.

    **Step 5b — Verify CI (D-13 §3).** Wait for the CI workflow to complete, then check status:

    ```
    # Poll every 30s up to 10 minutes for the run corresponding to the verification commit
    CI_SHA=$(git rev-parse HEAD)
    for i in $(seq 1 20); do
      STATUS=$(gh run list --branch main --limit 5 --json status,conclusion,headSha,url,workflowName \
        --jq ".[] | select(.headSha == \"$CI_SHA\") | {status, conclusion, url, workflowName}")
      if [ -n "$STATUS" ]; then
        echo "$STATUS"
        # If any run for this SHA is still in_progress, keep polling
        echo "$STATUS" | grep -q '"status": *"in_progress"' || break
      fi
      sleep 30
    done
    # Final check — any run for the verification SHA with conclusion != "success" is a failure
    gh run list --branch main --limit 5 --json status,conclusion,headSha,workflowName \
      --jq ".[] | select(.headSha == \"$CI_SHA\")"
    ```

    Expected: at least one workflow run for the verification commit SHA with `status: "completed"` and `conclusion: "success"`. Capture the run URL from `gh run list ... --json url` for `87-VERIFICATION.md`.

    If CI fails, do NOT rollback immediately — inspect `gh run view <run-id> --log-failed` first. A CI failure post-rename likely points to a Phase 85/86 leftover or a workflow that hardcodes the old repo name. If the failure is rename-related (not a flake or unrelated test regression), execute the D-15 rollback per Task 2's rollback block.

    **Step 5c — Verify DigitalOcean App Platform auto-deploy (D-13 §4).** This step has a user-only component (Claude cannot drive the DO UI per D-11).

    Claude: After the push in Step 5a, report to the user:
    - The verification commit SHA (`git rev-parse HEAD`)
    - The expected DO deploy trigger URL: `https://cloud.digitalocean.com/apps` → `trustedge-verifier` app → Activity tab
    - The verify page URL to spot-check post-deploy (the current DO app deploys to — look up the app's domain in the DO control panel; past v5.0 deploys used `verify.sealedge.dev` per Phase 86 §D-09 context, but Plan 02 should not assume the exact domain — use whatever the DO app resolves to)

    Then **wait for the user to report DO auto-deploy status** (pass as a second resume signal in Task 6's checkpoint). If DO did NOT fire auto-deploy (webhook lost on rename), the user must reconfigure the DO App source via the DO control panel per D-11 — this is user-only work, Claude can't drive the DO UI. Capture the DO deploy timestamp (or "DO webhook reconfigured at <time>" if that path was taken) for `87-VERIFICATION.md`.

    Note: per D-11, DO may need a control-panel re-pointing if the webhook didn't auto-follow the GitHub rename. This is a known-possible branch; handle it as part of this task, not as a blocker.
  </action>
  <verify>
    <automated>
# D-13 §3 — Verify the verification commit triggered a green CI run on the renamed repo.
# Note: this runs AFTER Step 5a has pushed the empty commit and the human has confirmed
# DO deploy status in Task 6 (the DO piece is human-verified). This automated check ONLY
# covers the CI side of §3; DO §4 is covered in the human-verify checkpoint.
CI_SHA=$(git rev-parse HEAD)
RUN_JSON=$(gh run list --branch main --limit 10 --json status,conclusion,headSha \
  --jq "[.[] | select(.headSha == \"$CI_SHA\")]")
# At least one run must exist for this SHA
test "$(echo "$RUN_JSON" | jq 'length')" -gt 0 \
  && echo "$RUN_JSON" | jq -e 'all(.status == "completed")' >/dev/null \
  && echo "$RUN_JSON" | jq -e 'all(.conclusion == "success")' >/dev/null
    </automated>
  </verify>
  <acceptance_criteria>
    - `git rev-parse HEAD` returns a new SHA (the verification-trigger empty commit)
    - `git push origin main` succeeds against the renamed remote
    - `gh run list --branch main --limit 10 --json status,conclusion,headSha` shows at least one workflow run matching the verification SHA with `status: "completed"` and `conclusion: "success"`
    - Run URL captured for evidence
    - (Human-verified in Task 6) DigitalOcean App Platform reports a successful auto-deploy for the verification commit, OR user reports they reconfigured the DO source per D-11 and the subsequent deploy succeeded
    - Verify page (DO app's deployed URL) returns 200 on a follow-up `curl -I -s -w '%{http_code}\n'` check
  </acceptance_criteria>
  <done>CI is green on the renamed repo for the verification commit; DO auto-deploy fired (or was reconfigured + fired); verify page is live. D-13 §3 and §4 satisfied.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 6: User confirms DigitalOcean auto-deploy + verify-page status</name>
  <files>(checkpoint task — user inspects DigitalOcean App Platform control panel; no local file edits)</files>
  <read_first>
    - .planning/phases/87-github-repository-rename/87-CONTEXT.md §D-11 (DO touch level), §D-13 §4 (auto-deploy gate), §Specifics ("DO App auto-deploy verification" paragraph)
  </read_first>
  <action>
    <what-built>
      Task 5 pushed an empty verification commit. CI is automated and verified in Task 5's automated check. DigitalOcean App Platform auto-deploy is user-only (per D-11, Claude can't drive the DO UI). This checkpoint captures user confirmation of the DO deploy timeline for `87-VERIFICATION.md`.
    </what-built>
    <how-to-verify>
      Open the DigitalOcean App Platform control panel:

      1. Visit https://cloud.digitalocean.com/apps
      2. Click on the `trustedge-verifier` app (current DO-side name — NOTE: the DO-side app name is `trustedge-verifier` per `deploy/digitalocean/app.yaml` line 12; renaming the DO app is out-of-scope per D-11, only the `repo:` field was updated in Plan 01)
      3. Go to the Activity tab
      4. Look for a deploy event corresponding to the verification commit SHA (from `git rev-parse HEAD` at the time of Task 5's push)

      **Happy path:** A new deploy fired automatically when the verification commit landed on `main`. It completes successfully. The verify page URL (visible in the DO App → Overview) returns 200 and serves the page content.

      **Webhook-lost path:** No deploy fires within ~2 minutes of the push. Per D-11, reconfigure the source in DO App settings:

      1. App → Settings → Components → platform (the service definition)
      2. Under "Source", click Edit
      3. Confirm the Repo field shows `TrustEdge-Labs/sealedge` (it may still show `TrustEdge-Labs/trustedge` if the webhook didn't auto-follow the rename)
      4. If it still shows the old name, click "Re-link" or re-select `TrustEdge-Labs/sealedge` from the dropdown
      5. Save, then push a new empty commit (`git commit --allow-empty -m "chore(87): DO webhook re-verification" && git push origin main`) to verify auto-deploy now fires

      **After confirming:** Type `do-deployed` + paste the DO deploy timestamp (e.g., `Deploy #N, started 2026-04-20 22:47 UTC, completed 22:49 UTC`) and the verify-page HTTP status check (`curl -I -s -o /dev/null -w '%{http_code}\n' <verify-url>` should return `200`). These pieces go into `87-VERIFICATION.md`.

      If DO deploy fails (not a webhook issue — an actual build/deploy failure), type `do-failed` + describe. DO-side build failures are unlikely rename-caused (the source is the same code; only the GitHub URL changed), but if they are, execute the D-15 rollback from Task 2.
    </how-to-verify>
  </action>
  <verify>Gate check — user must inspect DO App Platform control panel and paste the deploy timestamp + verify-page HTTP status into the conversation (`do-deployed` resume signal). Claude cannot drive the DO control panel UI per D-11; this is the only step in Plan 02 that is inherently user-only.</verify>
  <resume-signal>Type `do-deployed` + deploy timestamp + verify-page HTTP status if DO auto-deploy (or re-linked deploy) succeeded. Type `do-failed` + diagnostic notes if it did not.</resume-signal>
  <acceptance_criteria>
    - DigitalOcean App Platform shows a successful deploy for the verification commit SHA (either auto-fired, or fired after user re-linked the source per D-11)
    - Verify-page URL (DO app's current deployed domain) returns HTTP 200 on `curl -I`
    - User has pasted the DO deploy timestamp + verify-page status into the conversation for `87-VERIFICATION.md` capture
    - User has typed `do-deployed` as the resume signal
  </acceptance_criteria>
  <done>DO auto-deploy confirmed working (either natively or after D-11 re-link); verify page is live; Task 7 can capture all 4 D-13 checks into `87-VERIFICATION.md`.</done>
</task>

<task type="auto">
  <name>Task 7: Write `87-VERIFICATION.md` evidence capture + Plan 02 SUMMARY (D-16)</name>
  <files>.planning/phases/87-github-repository-rename/87-VERIFICATION.md, .planning/phases/87-github-repository-rename/87-02-SUMMARY.md</files>
  <read_first>
    - .planning/phases/87-github-repository-rename/87-CONTEXT.md §D-13 (4 verification checks), §D-16 (evidence capture format), §D-15 (rollback command for the "did-not-need-to-rollback" confirmation)
    - Tasks 1-6 outputs (pre-rename gate results, rename timestamp, remote URL update, curl redirect output, CI run URL, DO deploy timestamp, verify page HTTP status)
    - @$HOME/.claude/get-shit-done/templates/summary.md (Plan 02 summary format)
  </read_first>
  <action>
    **Step 7a — Write `87-VERIFICATION.md`** with the D-16 4 evidence artifacts.

    Structure (follow the Phase 83-86 verification pattern — command output + URLs + timestamps, no screenshots required per D-16):

    ```markdown
    <!--
    Copyright (c) 2025 TRUSTEDGE LABS LLC
    MPL-2.0: https://mozilla.org/MPL/2.0/
    -->

    # Phase 87 Verification — GitHub Repository Rename

    **Phase:** 87-github-repository-rename
    **Requirement:** EXT-01
    **Date:** <YYYY-MM-DD from rename timestamp>
    **Status:** PASS (or FAIL + rollback notes if D-15 was executed)

    ## D-06 Pre-Rename Gate

    (Task 1 outputs — git status, CI run status for last pre-rename commit, open PRs, in-progress workflows)

    ## D-05 Rename Operation

    Command executed by user:
        gh repo rename sealedge -R TrustEdge-Labs/trustedge

    Timestamp: <local time from Task 2 resume>
    Output: ✓ Renamed repository TrustEdge-Labs/sealedge
    Rollback command (NOT executed; documented for future reference):
        gh repo rename trustedge -R TrustEdge-Labs/sealedge
        git remote set-url origin https://github.com/TrustEdge-Labs/trustedge.git

    ## D-13 §1 — GitHub 301 Redirect

    Command:
        curl -I -s -o /dev/null -w '%{http_code} -> %{redirect_url}\n' https://github.com/TrustEdge-Labs/trustedge

    Output:
        301 -> https://github.com/TrustEdge-Labs/sealedge

    Follow-redirect cross-check:
        curl -s -o /dev/null -w '%{http_code} (final: %{url_effective})\n' -L https://github.com/TrustEdge-Labs/trustedge
    Output:
        200 (final: https://github.com/TrustEdge-Labs/sealedge)

    ## D-13 §2 — Local Remote + Fetch/Push

    Before (pre-Task 3): https://github.com/TrustEdge-Labs/trustedge.git
    After  (post-Task 3): https://github.com/TrustEdge-Labs/sealedge.git

    `git fetch origin`: success (no errors)
    `git ls-remote origin`: <paste first 3 refs>
    Verification-trigger push (Task 5a): commit <SHA> pushed to origin/main

    ## D-13 §3 — CI Green on Renamed Repo

    Verification commit SHA: <Task 5a SHA>
    CI run URL: <gh run list --json url output>
    Workflow: <ci.yml or whichever ran>
    Status: completed
    Conclusion: success
    Completion time: <from gh run list>

    ## D-13 §4 — DigitalOcean Auto-Deploy

    Deploy trigger: post-rename verification commit <SHA>
    DO Activity timestamp: <from Task 6 resume>
    DO deploy status: successful (or "successful after D-11 re-link" if that path taken)
    Verify page URL: <DO app domain>
    Verify page HTTP status: 200

    ## External Integrations Post-Rename

    - GitHub built-in redirect: ✔ 301 -> /sealedge
    - CLA Assistant GitHub App: <pass-through status — check on next PR per D-12>
    - Dependabot: auto-followed per D-09 — no config changes needed
    - DigitalOcean App Platform: ✔ post-rename deploy succeeded

    ## Rollback Status

    Not needed — all 4 D-13 checks passed. Rollback command retained verbatim above
    for future reference (D-15 pattern).
    ```

    Fill in the `<placeholder>` fields with the actual data collected across Tasks 1-6. Prefer verbatim command output (with the actual timestamps / SHAs / URLs) over paraphrased summaries.

    **Step 7b — Write `87-02-SUMMARY.md`** per `@$HOME/.claude/get-shit-done/templates/summary.md`. Include:
    - Rename operation timeline (pre-gate pass → `gh repo rename` → remote set-url → curl 301 confirmed → verification commit → CI green → DO auto-deploy)
    - References to `87-VERIFICATION.md` for the full evidence record
    - Confirmation that rollback was NOT needed
    - Pointer forward to Phase 88 (EXT-02/03/04 — action repo, marketplace deprecation, product-page content) and Phase 89 (VALID-01/02/03 full test matrix)

    **Step 7c — Commit both files.** Stage specifically (no `git add -A`):

    ```
    git add .planning/phases/87-github-repository-rename/87-VERIFICATION.md \
            .planning/phases/87-github-repository-rename/87-02-SUMMARY.md
    git commit -m "$(cat <<'EOF'
    docs(87): verification evidence + plan 02 summary — Phase 87 close

    Captures the 4 D-13 evidence artifacts (curl 301 redirect, git fetch/remote URL,
    CI run URL, DO auto-deploy timestamp) per D-16. Rollback was not needed — all
    gate checks passed. Phase 87 closes EXT-01.

    Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
    EOF
    )"
    git push origin main
    ```
  </action>
  <verify>
    <automated>
# Both files exist and are committed + pushed
test -f .planning/phases/87-github-repository-rename/87-VERIFICATION.md \
  && test -f .planning/phases/87-github-repository-rename/87-02-SUMMARY.md \
  && grep -qE '^301 -> https://github.com/TrustEdge-Labs/sealedge/?$' .planning/phases/87-github-repository-rename/87-VERIFICATION.md \
  && grep -q 'sealedge\.git' .planning/phases/87-github-repository-rename/87-VERIFICATION.md \
  && git log -1 --pretty=%s | grep -qE '^docs\(87\):' \
  && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)"
    </automated>
  </verify>
  <acceptance_criteria>
    - `.planning/phases/87-github-repository-rename/87-VERIFICATION.md` exists and contains:
      - The curl 301 redirect output line `301 -> https://github.com/TrustEdge-Labs/sealedge`
      - The `git remote set-url` before/after strings (including `sealedge.git` with HTTPS)
      - The CI run URL for the verification commit with `conclusion: success`
      - The DO deploy timestamp + verify-page HTTP 200 confirmation
      - The D-15 rollback command documented verbatim (even though not executed)
    - `.planning/phases/87-github-repository-rename/87-02-SUMMARY.md` exists and follows the gsd summary template
    - Both files are committed under a single commit with subject starting `docs(87):`
    - `git rev-parse HEAD == git rev-parse origin/main` (commit pushed to renamed repo)
  </acceptance_criteria>
  <done>Phase 87 evidence captured and committed to the renamed repo; Plan 02 summary written; EXT-01 is satisfied; phase ready to close.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| user shell → GitHub (`gh repo rename`) | Live operation on public repo name; audience = all consumers of the old URL |
| working clone → renamed remote (HTTPS, `repo`-scoped PAT/gh-auth) | Local push/fetch authenticates against GitHub with user's `gh auth` credentials |
| GitHub webhook → DigitalOcean App Platform | Auto-deploy webhook; DO re-resolves source on rename (or fails silently, handled per D-11) |
| GitHub webhook → CLA Assistant GitHub App | Signatures stored by repo ID, not URL; survives rename per D-12 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-87-07 | Spoofing (squatting) | `TrustEdge-Labs/trustedge` could be claimed by a new repo after rename | accept | GitHub reserves the old name for redirect purposes per GitHub's "About repository renaming" docs. Risk = low; org-scoped namespace, not a public-namespace collision surface. No new mitigation needed. |
| T-87-08 | Tampering | User accidentally runs `gh repo rename` on wrong org/wrong name | mitigate | D-05 requires user to run the exact verbatim command (`gh repo rename sealedge -R TrustEdge-Labs/trustedge`). Task 2 presents the command explicitly; `gh` itself will error if the `-R` ref doesn't exist. Rollback (D-15) is trivial if a wrong-name rename happens. |
| T-87-09 | Repudiation | Rename operation leaves no in-repo audit trail | mitigate | `87-VERIFICATION.md` (Task 7) captures: the exact `gh` command + output (Task 2 resume signal paste), the timestamp, the pre-rename gate evidence, all 4 post-rename gate outputs. Commit message references D-13/D-16. Provenance is fully traceable via `git log`. |
| T-87-10 | Information Disclosure | Low — rename doesn't expose private data; public repo stays public, private repo stays private | accept | `gh repo rename` preserves visibility; no scope change. ASVS L1 not applicable (no new data exposure surface). |
| T-87-11 | Denial of Service | DO App Platform webhook fails to follow the GitHub rename → verify page goes offline | mitigate | D-11 handles this explicitly: if auto-deploy doesn't fire, user re-links the source in DO App settings. Task 6 captures the "auto-fired vs re-linked" path in `87-VERIFICATION.md`. MTTR = minutes (DO control-panel re-link). |
| T-87-12 | Denial of Service | CI workflows on the renamed repo fail because a workflow hardcodes `/TrustEdge-Labs/trustedge` | mitigate | Plan 01 Task 2 (D-13 §3 pre-audit) confirms no tracked `.github/workflows/*.yml` files hardcode the old URL outside the allowlist. CI runs green in Plan 02 Task 5 against the renamed repo. If a workflow does fail post-rename, inspect logs first; rollback per D-15 only if the failure is rename-caused and non-trivial to fix. |
| T-87-13 | Elevation of Privilege | `gh repo rename` requires `repo` scope — does the user's gh auth have it? | accept | Per CONTEXT.md §"Code Context" → "gh CLI already authenticated", `gh auth status` confirms HTTPS + `repo` + `workflow` + `read:org` scopes. No new scope escalation. If somehow the scope is revoked between now and Task 2, `gh` will error clearly and user can re-auth. |
| T-87-14 | Spoofing (supply-chain) | Someone force-pushes to `main` on the old repo name during the rename window | accept | Rename window is measured in seconds; user is the only authorized pusher; no active contributors on `main`. Risk = effectively zero. |
| T-87-15 | Tampering | Local clone's `origin` URL not updated post-rename; future `git push`/`git pull` operates on stale URL (technically works via GitHub's redirect but creates drift) | mitigate | Task 3 explicitly updates `git remote set-url origin https://github.com/TrustEdge-Labs/sealedge.git`, verified via `git remote get-url origin` in acceptance criteria. HTTPS retained per D-08. |

**Overall grade: LOW.** This plan executes one external rename operation (reversible per D-15) and runs 4 verification checks. No new secrets, no new dependencies, no new attack surface. Operational risks (DO webhook, CI, CLA Assistant) are covered by in-plan verification + documented remediation paths (re-link, rollback, diagnose).
</threat_model>

<verification>
**Plan-level gate (ALL 4 D-13 checks + 87-VERIFICATION.md must pass):**
1. Task 1: D-06 pre-rename gate ✔ (user approved via `approved` resume signal)
2. Task 2: `gh repo rename sealedge -R TrustEdge-Labs/trustedge` executed successfully by user (`renamed` resume signal)
3. Task 3: `git remote get-url origin` == `https://github.com/TrustEdge-Labs/sealedge.git` (HTTPS per D-08); `git fetch origin` + `git ls-remote origin` both succeed (D-13 §2)
4. Task 4: `curl -I` returns `301 -> https://github.com/TrustEdge-Labs/sealedge` (D-13 §1), with 30-second retry tolerance
5. Task 5: verification commit pushed → CI run for the verification SHA returns `status: completed, conclusion: success` (D-13 §3)
6. Task 6: DigitalOcean App Platform fires auto-deploy (or user reconfigures per D-11) → verify page returns HTTP 200 (D-13 §4)
7. Task 7: `87-VERIFICATION.md` captures all 4 D-13 artifacts per D-16; `87-02-SUMMARY.md` written; both committed to the renamed repo and pushed

**If any D-13 check fails:**
- Execute D-15 rollback verbatim: `gh repo rename trustedge -R TrustEdge-Labs/sealedge && git remote set-url origin https://github.com/TrustEdge-Labs/trustedge.git`
- Diagnose root cause (check GitHub status, DO webhook config, CI logs)
- Retry from Task 2 after root cause is resolved

Rollback is explicitly safe per D-15 because (a) the repo name and (b) the local remote URL are the only state — both cheaply reversible within GitHub's rename-history window.
</verification>

<success_criteria>
- GitHub repo is renamed to `TrustEdge-Labs/sealedge` and accessible at `https://github.com/TrustEdge-Labs/sealedge`
- `https://github.com/TrustEdge-Labs/trustedge` returns HTTP 301 redirecting to `/sealedge` (ROADMAP §Phase 87 criterion #1)
- Local working clone's `origin` remote points at `https://github.com/TrustEdge-Labs/sealedge.git` via HTTPS (ROADMAP criterion #2, D-08)
- `git push`/`git pull`/`git fetch` operate against the renamed remote without manual URL fixes (ROADMAP criterion #2)
- One CI workflow run on the renamed repo completes green post-rename (D-13 §3)
- DigitalOcean App Platform auto-deploy fires (or is re-linked per D-11); verify page stays live (D-13 §4)
- `87-VERIFICATION.md` captures all 4 D-13 evidence artifacts per D-16; rollback command documented verbatim
- `87-02-SUMMARY.md` written and committed
- EXT-01 is satisfied; Phase 87 closes; Phase 88 is unblocked
</success_criteria>

<output>
After completion, create `.planning/phases/87-github-repository-rename/87-VERIFICATION.md` (evidence capture per D-16) AND `.planning/phases/87-github-repository-rename/87-02-SUMMARY.md` (plan summary per `@$HOME/.claude/get-shit-done/templates/summary.md`). Both files are committed to the renamed repo and pushed in Task 7.
</output>
