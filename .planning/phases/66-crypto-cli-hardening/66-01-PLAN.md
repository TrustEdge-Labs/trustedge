---
phase: 66-crypto-cli-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/core/src/lib.rs
  - crates/core/src/transport/tcp.rs
  - crates/core/src/transport/quic.rs
autonomous: true
requirements:
  - CRYPT-01
must_haves:
  truths:
    - "NetworkChunk::new() requires a nonce argument — code calling the old 3-argument signature fails to compile"
    - "No zero-nonce default exists anywhere in the codebase"
    - "All existing callers compile and pass tests with explicit nonces"
  artifacts:
    - path: "crates/core/src/lib.rs"
      provides: "NetworkChunk with mandatory nonce constructor"
      contains: "pub fn new(seq: u64, encrypted_data: Vec<u8>, manifest_bytes: Vec<u8>, nonce: [u8; NONCE_LEN])"
  key_links:
    - from: "crates/core/src/transport/tcp.rs"
      to: "NetworkChunk::new"
      via: "test code calling constructor with nonce"
      pattern: "NetworkChunk::new\\(.*nonce"
    - from: "crates/core/src/transport/quic.rs"
      to: "NetworkChunk::new"
      via: "test code calling constructor with nonce"
      pattern: "NetworkChunk::new\\(.*nonce"
---

<objective>
Make NetworkChunk::new() require an explicit nonce parameter, eliminating the zero-nonce default constructor.

Purpose: The current `new()` silently uses a zero nonce, which is a cryptographic hazard. Merging `new_with_nonce()` into `new()` forces all callers to provide a real nonce.
Output: Updated NetworkChunk constructor, all callers updated, all tests pass.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/66-crypto-cli-hardening/66-CONTEXT.md
@crates/core/src/lib.rs
@crates/core/src/transport/tcp.rs
@crates/core/src/transport/quic.rs
@crates/core/src/envelope.rs
@crates/core/src/bin/trustedge-client.rs

<interfaces>
<!-- Current NetworkChunk API in crates/core/src/lib.rs -->
From crates/core/src/lib.rs (lines 208-240):
```rust
pub const NONCE_LEN: usize = 12;

impl NetworkChunk {
    // OLD — zero-nonce default, to be REMOVED:
    pub fn new(seq: u64, encrypted_data: Vec<u8>, manifest_bytes: Vec<u8>) -> Self

    // OLD name — correct signature, to be RENAMED to new():
    pub fn new_with_nonce(seq: u64, encrypted_data: Vec<u8>, manifest_bytes: Vec<u8>, nonce: [u8; NONCE_LEN]) -> Self
}
```

Callers of old `new()` (zero-nonce — MUST be updated):
- crates/core/src/transport/tcp.rs:305 — `NetworkChunk::new(2, test_data.to_vec(), manifest)`
- crates/core/src/transport/tcp.rs:365 — `NetworkChunk::new(3, large_data.clone(), manifest)`
- crates/core/src/transport/quic.rs:811 — `NetworkChunk::new(1, test_data.to_vec(), manifest)`

Callers of `new_with_nonce()` (correct — just rename call to `new()`):
- crates/core/src/envelope.rs:372
- crates/core/src/bin/trustedge-client.rs:449, 560, 759, 874
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Merge new_with_nonce into new and update all callers</name>
  <files>crates/core/src/lib.rs, crates/core/src/transport/tcp.rs, crates/core/src/transport/quic.rs, crates/core/src/envelope.rs, crates/core/src/bin/trustedge-client.rs</files>
  <read_first>
    - crates/core/src/lib.rs (lines 195-245 — NetworkChunk struct and impl)
    - crates/core/src/transport/tcp.rs (lines 300-370 — test usages)
    - crates/core/src/transport/quic.rs (lines 805-820 — test usage)
    - crates/core/src/envelope.rs (lines 365-380 — new_with_nonce call)
    - crates/core/src/bin/trustedge-client.rs (lines 445-460, 555-565, 755-765, 870-880 — new_with_nonce calls)
  </read_first>
  <behavior>
    - Test: Code calling NetworkChunk::new(seq, data, manifest) without nonce fails to compile
    - Test: NetworkChunk::new(seq, data, manifest, nonce) constructs correctly with the provided nonce
    - Test: All existing workspace tests pass (cargo test --workspace)
  </behavior>
  <action>
    Per D-01, D-02, D-03:

    1. In crates/core/src/lib.rs:
       - DELETE the old `pub fn new(seq, encrypted_data, manifest_bytes)` method (lines ~211-223) that sets `nonce: [0; NONCE_LEN]`
       - RENAME `pub fn new_with_nonce(...)` to `pub fn new(...)` keeping its 4-parameter signature: `pub fn new(seq: u64, encrypted_data: Vec<u8>, manifest_bytes: Vec<u8>, nonce: [u8; NONCE_LEN]) -> Self`
       - The body stays identical (timestamp from SystemTime, struct fields including the passed nonce)

    2. Update callers of old `new()` (3 sites in test code — use a dummy nonce [1u8; NONCE_LEN] since these are tests):
       - crates/core/src/transport/tcp.rs line 305: `NetworkChunk::new(2, test_data.to_vec(), manifest, [1u8; NONCE_LEN])`
       - crates/core/src/transport/tcp.rs line 365: `NetworkChunk::new(3, large_data.clone(), manifest, [1u8; NONCE_LEN])`
       - crates/core/src/transport/quic.rs line 811: `NetworkChunk::new(1, test_data.to_vec(), manifest, [1u8; NONCE_LEN])`
       - Add `use crate::NONCE_LEN;` if not already imported in these test modules.

    3. Update callers of old `new_with_nonce()` (5 sites — just rename the call):
       - crates/core/src/envelope.rs line 372: change `NetworkChunk::new_with_nonce(` to `NetworkChunk::new(`
       - crates/core/src/bin/trustedge-client.rs lines 449, 560, 759, 874: change `NetworkChunk::new_with_nonce(` to `NetworkChunk::new(`

    4. Search the entire workspace for any other references: `grep -rn "NetworkChunk::new\|new_with_nonce" crates/` and fix any missed callers.

    5. Run `cargo build --workspace` to confirm compilation. Then `cargo test --workspace` to confirm all tests pass.
  </action>
  <verify>
    <automated>cd /home/john/vault/projects/github.com/trustedge && cargo test --workspace 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "new_with_nonce" crates/ returns zero matches
    - grep -n "nonce: \[0; NONCE_LEN\]" crates/core/src/lib.rs returns zero matches
    - grep -n "pub fn new(seq: u64, encrypted_data: Vec<u8>, manifest_bytes: Vec<u8>, nonce:" crates/core/src/lib.rs returns exactly 1 match
    - cargo test --workspace exits 0
  </acceptance_criteria>
  <done>NetworkChunk::new() requires 4 arguments including nonce. Zero-nonce constructor is gone. All callers updated. All tests pass.</done>
</task>

</tasks>

<verification>
- `cargo build --workspace` succeeds with no warnings about NetworkChunk
- `cargo test --workspace` — all tests pass
- `grep -rn "new_with_nonce" crates/` returns nothing
- `grep -rn "nonce: \[0;" crates/core/src/lib.rs` returns nothing
</verification>

<success_criteria>
NetworkChunk::new() signature requires nonce as 4th parameter. No zero-nonce default exists. All workspace tests pass.
</success_criteria>

<output>
After completion, create `.planning/phases/66-crypto-cli-hardening/66-01-SUMMARY.md`
</output>
