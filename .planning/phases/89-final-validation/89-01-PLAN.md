---
phase: 89-final-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/validate-v6.sh
  - MIGRATION.md
autonomous: true
requirements:
  - VALID-01
  - VALID-03
tags:
  - validation
  - milestone-close
  - sealedge
  - rebrand

must_haves:
  truths:
    - "A reusable shell script `scripts/validate-v6.sh` exists, is executable, has the MPL-2.0 header, and mirrors the D-01 feature-matrix plus WASM + Docker + demo gates"
    - "`bash -n scripts/validate-v6.sh` parses clean; `chmod +x` set; `head -5` shows the MPL-2.0 copyright header and `Project: sealedge` line"
    - "Repo-wide grep `git ls-files | xargs grep -nE 'trustedge|TRUSTEDGE|\\.trst\\b|\\.te-attestation'` with the Phase 85/86 allowlist returns zero actionable hits outside historical artifacts"
    - "Post-task grep of .github/workflows/ci.yml for 'te-attestation|\\.te-' returns zero hits — either because Phase 88 Plan 02 already cleaned the strings (record as 'verified clean, not modified' in commit body) OR because Plan 01 applied a Phase 87-pattern line-local substring swap in this commit."
    - "`MIGRATION.md` §'What renamed' table includes a row mapping `TrustEdge-Labs/attest-sbom-action@v1` → `TrustEdge-Labs/sealedge-attest-sbom-action@v2` (D-14 hybrid-gate addition per PATTERNS.md File 3 known gap)"
  artifacts:
    - path: "scripts/validate-v6.sh"
      provides: "Reusable CI-parity matrix runner + WASM + dashboard + docker gate"
      contains: "cargo test --workspace --no-default-features --locked"
      min_lines: 120
    - path: "MIGRATION.md"
      provides: "Existing migration guide with new action-rename row inserted"
      contains: "sealedge-attest-sbom-action@v2"
  key_links:
    - from: "scripts/validate-v6.sh"
      to: "scripts/ci-check.sh"
      via: "shell-out delegation for Steps 1-12 (hybrid pattern per PATTERNS.md File 1 recommendation)"
      pattern: "./scripts/ci-check.sh"
    - from: "MIGRATION.md"
      to: "future release notes / downstream consumers"
      via: "'What renamed' table covers the GitHub Action rename so release notes can link back"
      pattern: "sealedge-attest-sbom-action"
---

<objective>
Build the reusable `scripts/validate-v6.sh` v6.0 validation-gate script and close out the D-10 pre-tag straggler sweep (including the MIGRATION.md D-14 hybrid-gate addition for the GitHub Action rename) in a single atomic-commit set. This plan lands BEFORE Plan 02 (which runs the script) and BEFORE the v6.0.0 tag cut.

Purpose: The v6.0 milestone close requires a single command that reproduces the full CI matrix locally plus the WASM / dashboard / Docker gates from D-11/D-12/D-13. Without this script, Plan 02's evidence capture would have to improvise six separate cargo invocations plus four post-gates. With it, the gate is pinned, reusable for v7+ milestones (YAGNI-guarded — start v6-specific per CONTEXT deferred), and produces a single `validate-v6.log` for VERIFICATION.md excerpts.

Output:
- `scripts/validate-v6.sh` (new, ~130-180 lines, MPL-2.0 header, mirrors `scripts/ci-check.sh` shape)
- `MIGRATION.md` "What renamed" table amended with the action-rename row (single-row addition, D-14 hybrid gate)
- Zero-result repo-wide straggler grep audit recorded in the SUMMARY (ci.yml branch-on-grep: either confirmed clean OR fixed inline with this plan's commit)
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/89-final-validation/89-CONTEXT.md
@.planning/phases/89-final-validation/89-PATTERNS.md
@scripts/ci-check.sh
@scripts/demo.sh
@MIGRATION.md
@CLAUDE.md

<interfaces>
Key patterns the executor must copy rather than reinvent:

From `scripts/ci-check.sh:1-16` (copy the boilerplate verbatim, only subject line changes):
```bash
#!/bin/bash
# Copyright (c) 2025 TRUSTEDGE LABS LLC
# MPL-2.0: https://mozilla.org/MPL/2.0/
# Project: sealedge — Privacy and trust at the edge.
#
# Full v6.0 validation gate — mirrors ci.yml build-and-test feature matrix
# plus WASM / dashboard build / docker compose e2e / demo roundtrip.
# Run before cutting the v6.0.0 tag to prove every gate is green.
#
# Usage:
#   ./scripts/validate-v6.sh                     # Full run
#   ./scripts/validate-v6.sh --skip-docker       # Skip docker gate (e.g. no docker daemon)

set -e
cd "$(dirname "$0")/.."
```

From `scripts/ci-check.sh:22-50` (pass/fail/skip/warn counters + step() helpers — copy verbatim):
```bash
PASS=0; FAIL=0; SKIP=0; WARN=0
step() { echo; echo "■ $1"; }
pass() { echo "  ✔ $1"; PASS=$((PASS + 1)); }
fail() { echo "  ✖ $1"; FAIL=$((FAIL + 1)); }
skip() { echo "  ⚠ $1 (skipped)"; SKIP=$((SKIP + 1)); }
warn() { echo "  ⚠ $1 (warning)"; WARN=$((WARN + 1)); }
```

From `scripts/ci-check.sh:317-328` (summary block — copy verbatim, adjust final line):
```bash
echo
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
echo "  Results: $PASS passed, $FAIL failed, $WARN warnings, $SKIP skipped"
echo "━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━"
if [ $FAIL -gt 0 ]; then
    echo "  ✖ Fix failures before tagging v6.0.0."
    exit 1
else
    echo "  ✔ All v6.0 validation gates passed. Safe to cut v6.0.0 tag."
fi
```

D-01 matrix commands (CONTEXT.md §Specifics verbatim) — embed inside validate-v6.sh Step 2:
```bash
cargo test --workspace --no-default-features --locked
cargo test -p sealedge-core --features "audio,git-attestation,keyring,insecure-tls" --locked
cargo test -p sealedge-core --features yubikey --lib --locked
cargo test -p sealedge-platform --lib --locked
cargo test -p sealedge-platform --test verify_integration --locked
cargo test -p sealedge-platform --test verify_integration --features http --locked
```

D-11 WASM gate commands (CONTEXT.md §Specifics + PATTERNS.md File 1 WASM excerpt):
```bash
cargo check -p sealedge-wasm --target wasm32-unknown-unknown
cargo check -p sealedge-seal-wasm --target wasm32-unknown-unknown
(cd crates/wasm && wasm-pack build --target web --release)
(cd crates/seal-wasm && wasm-pack build --target web --release)
# Size floor: sealedge-wasm < 2097152 bytes (2 MB) parity with wasm-tests.yml
```

D-13 Docker gate commands (CONTEXT.md §Specifics verbatim):
```bash
docker compose -f deploy/docker-compose.yml up --build -d
until curl -sf http://localhost:3001/healthz > /dev/null; do sleep 2; done  # with 30-retry guard
./scripts/demo.sh
docker compose -f deploy/docker-compose.yml down
```

MIGRATION.md "What renamed" table — the existing final row before the Preserved section (MIGRATION.md:40):
```markdown
| GitHub repo URL | `TrustEdge-Labs/trustedge` | `TrustEdge-Labs/sealedge` |
```

Insert the new action-rename row AFTER this line, BEFORE the blank line that precedes the "Preserved unchanged:" header (MIGRATION.md:41-42).

**Confirmed table column count: 3** (Surface | Before (v5.x) | After (v6.0)). The new row MUST also be 3 columns.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create scripts/validate-v6.sh with full v6.0 validation matrix</name>
  <files>scripts/validate-v6.sh</files>
  <read_first>
    - scripts/ci-check.sh (primary analog — 328 lines; MUST read the full file to copy the header, step/pass/fail helpers, Step 6 build-and-test block, and Step 12 summary block)
    - scripts/demo.sh (lines 1-60 for the auto-detect docker-mode pattern and the `set -uo pipefail` convention — the recommendation in PATTERNS.md is `set -e` for parity with ci-check.sh, but demo.sh is worth reading so the script matches project conventions)
    - .planning/phases/89-final-validation/89-CONTEXT.md §Specifics (D-01 matrix, D-11 WASM, D-13 Docker — copy command strings verbatim)
    - .planning/phases/89-final-validation/89-PATTERNS.md §File 1 (recommended hybrid approach: shell out to ci-check.sh for Steps 1-12, append four new gate-blocks — WASM cargo check, WASM wasm-pack + size, dashboard build+typecheck, docker compose + demo)
    - CLAUDE.md §"No emoji in code" rule — uses UTF-8 symbols ✔ ✖ ⚠ ● ♪ ■ — copy verbatim, do NOT use emojis
    - .github/workflows/wasm-tests.yml (size-check job — for parity with the `< 2MB` floor per D-11)
  </read_first>
  <action>
    Create `scripts/validate-v6.sh` at repo root under `scripts/`. Make it executable via `chmod +x`. Use the Write tool (not heredoc).

    **Exact structure** (per PATTERNS.md File 1 hybrid recommendation — D-03):

    1. **Header block (lines 1-16)** — copy `scripts/ci-check.sh:1-15` verbatim, only change the subject-line comment:
       ```
       # Full v6.0 validation gate — mirrors ci.yml build-and-test feature matrix
       # plus WASM / dashboard build / docker compose e2e / demo roundtrip.
       # Run before cutting the v6.0.0 tag to prove every gate is green.
       #
       # Usage:
       #   ./scripts/validate-v6.sh                     # Full run
       #   ./scripts/validate-v6.sh --skip-docker       # Skip docker gate
       #   ./scripts/validate-v6.sh --allow-regression "<justification>"   # D-02 escape hatch
       ```
       Keep `set -e` (matches ci-check.sh Step 1). Keep `cd "$(dirname "$0")/.."`.

    2. **Flag parsing (after boilerplate)** — two optional flags:
       - `--skip-docker` sets `SKIP_DOCKER=true`. Default `SKIP_DOCKER=false`.
       - `--allow-regression "<justification>"` sets `ALLOW_REGRESSION=true` AND `REGRESSION_JUSTIFICATION="<justification>"`. Default `ALLOW_REGRESSION=false`. The justification string MUST be non-empty when the flag is present; if empty, exit 1 with an error message.

    3. **Log capture** — immediately after flag parsing: `exec > >(tee validate-v6.log) 2>&1` so every subsequent line is captured. Document in a comment that `validate-v6.log` is the evidence file excerpted into `89-VERIFICATION.md` per D-03.

    4. **Counter block (PASS/FAIL/SKIP/WARN + step/pass/fail/skip/warn helpers)** — copy `scripts/ci-check.sh:22-50` verbatim. Zero changes.

    5. **Banner (after counters)** — mirror `ci-check.sh:52-58`:
       ```
       echo "● Sealedge v6.0 validation gate"
       echo "  Mode: full matrix + WASM + dashboard + docker e2e"
       if $SKIP_DOCKER; then echo "  Docker gate: SKIPPED (--skip-docker)"; fi
       if $ALLOW_REGRESSION; then echo "  D-02 floor: REGRESSION ALLOWED — $REGRESSION_JUSTIFICATION"; fi
       echo
       ```

    6. **Step 1: Delegate to ci-check.sh** — per PATTERNS.md File 1 hybrid recommendation (single source of truth for the CI-parity feature matrix):
       ```
       step "Step 1: Local CI parity matrix (via ci-check.sh)"
       if ./scripts/ci-check.sh --clean; then
           pass "ci-check.sh --clean (full feature matrix)"
       else
           fail "ci-check.sh --clean"
       fi
       ```

    7. **Step 2: WASM cargo check** — copy the pattern from PATTERNS.md File 1 WASM excerpt verbatim. Use `rustup target list --installed | grep -q wasm32-unknown-unknown` guard; skip with warn if target missing. On guard-pass: run both `cargo check -p sealedge-wasm --target wasm32-unknown-unknown` and `cargo check -p sealedge-seal-wasm --target wasm32-unknown-unknown`; pass or fail accordingly.

    8. **Step 3: WASM build + size check** — guarded by `command -v wasm-pack`. On guard-pass:
       - `(cd crates/wasm && wasm-pack build --target web --release)`
       - `(cd crates/seal-wasm && wasm-pack build --target web --release)`
       - Measure both with `wc -c`: `SEALEDGE_WASM_SIZE=$(wc -c < crates/wasm/pkg/sealedge_wasm_bg.wasm 2>/dev/null || echo 0)` and same for `crates/seal-wasm/pkg/sealedge_seal_wasm_bg.wasm`
       - Echo both sizes as informational lines
       - Fail if `SEALEDGE_WASM_SIZE > 2097152` (2 MB, wasm-tests.yml parity per D-11); pass otherwise. `sealedge-seal-wasm` size is informational (record only, no floor per D-11).
       Skip with warn if `wasm-pack` not installed.

    9. **Step 4: Dashboard build + typecheck** — `cd web/dashboard` guard: verify the directory exists first. On directory-present:
       - `npm ci` (fail/pass row)
       - `npm run build` (fail/pass row)
       - `npm run check` (fail/pass row)
       - `cd ../..` to return to repo root
       Do NOT start `npm run dev` — that is a manual browser-smoke step done in Plan 02 per D-12, not automated here.

    10. **Step 5: Docker compose + demo roundtrip** — guarded by `$SKIP_DOCKER` AND `command -v docker && docker compose version`:
        - `docker compose -f deploy/docker-compose.yml up --build -d`
        - Wait-for-health loop copied VERBATIM from PATTERNS.md File 1 Docker excerpt — 30 retries × 2s = 60s max; fail + `docker compose down` + exit 1 if health fails; pass "docker stack healthy" otherwise
        - `./scripts/demo.sh` (auto-detects docker mode — scripts/demo.sh:49 `SERVER_AVAILABLE` gate); pass or fail "demo roundtrip"
        - ALWAYS run `docker compose -f deploy/docker-compose.yml down` in the post-step, even on failure (use a trap or an explicit guard)

    11. **Summary block (line ~170-185)** — copy `ci-check.sh:317-328` verbatim, change the final success message to `"All v6.0 validation gates passed. Safe to cut v6.0.0 tag."`.

    12. **D-02 test-count floor enforcement (after summary block)** — append the block from PATTERNS.md File 1 D-02 excerpt with the `--allow-regression` escape hatch:
        ```
        TOTAL_TESTS=$(grep -oE 'test result: ok\. [0-9]+ passed' validate-v6.log 2>/dev/null | awk '{s+=$4} END {print s+0}')
        echo "  Total green tests: $TOTAL_TESTS (floor: 471)"
        if [ "$TOTAL_TESTS" -lt 471 ]; then
            if $ALLOW_REGRESSION; then
                echo "  ⚠ Test count below v6.0 floor (471), but --allow-regression set."
                echo "  D-02 JUSTIFICATION: $REGRESSION_JUSTIFICATION"
                # Log the justification so 89-VERIFICATION.md §1 can cite it.
                # Per D-02 escape hatch — caller MUST also record this justification
                # text in 89-VERIFICATION.md §1 ("D-02 justification: <text>").
            else
                echo "  ✖ Test count below v6.0 floor — record justification in 89-VERIFICATION.md before proceeding."
                echo "  Use --allow-regression \"<justification>\" to acknowledge and continue (D-02 escape hatch)."
                exit 1
            fi
        fi
        ```
        Keep the floor hardcoded at 471 (D-02). Comment "# D-02 floor — see .planning/phases/89-final-validation/89-CONTEXT.md §D-02" next to the `< 471` check. The default behavior (no flag) MUST hard-exit on regression; the `--allow-regression` flag is the documented escape hatch and writes `D-02 JUSTIFICATION: <text>` to `validate-v6.log`.

    After writing: run `chmod +x scripts/validate-v6.sh` via Bash tool.

    **Do NOT run the script** — the actual run is Plan 02's job. This plan only produces the script artifact and verifies syntax.

    **Style discipline:** No emojis (CLAUDE.md forbids). UTF-8 symbols ✔ ✖ ⚠ ● ♪ ■ only. No `set -x` debug output. No trailing whitespace. Keep the file under ~220 lines total (slightly more headroom for the new flag-parsing + escape-hatch branching).
  </action>
  <verify>
    <automated>test -x scripts/validate-v6.sh && bash -n scripts/validate-v6.sh && head -5 scripts/validate-v6.sh | grep -q 'MPL-2.0' && head -5 scripts/validate-v6.sh | grep -q 'Project: sealedge' && grep -q 'cargo test --workspace --no-default-features --locked\|./scripts/ci-check.sh' scripts/validate-v6.sh && grep -q 'wasm-pack build --target web --release' scripts/validate-v6.sh && grep -q 'docker compose -f deploy/docker-compose.yml up' scripts/validate-v6.sh && grep -q '2097152' scripts/validate-v6.sh && grep -q '\\-lt 471' scripts/validate-v6.sh && grep -q 'allow-regression' scripts/validate-v6.sh && grep -q 'D-02 JUSTIFICATION:' scripts/validate-v6.sh</automated>
  </verify>
  <acceptance_criteria>
    - `test -x scripts/validate-v6.sh` → exit 0 (executable bit set)
    - `bash -n scripts/validate-v6.sh` → exit 0 (syntax-valid shell)
    - `head -5 scripts/validate-v6.sh | grep -c 'MPL-2.0'` → ≥ 1 (copyright header present)
    - `head -5 scripts/validate-v6.sh | grep -c 'Project: sealedge'` → ≥ 1 (project line per Phase 85 header discipline)
    - `grep -c 'cargo test --workspace --no-default-features --locked' scripts/validate-v6.sh` → ≥ 1 IF not delegated to ci-check.sh; OR `grep -c './scripts/ci-check.sh' scripts/validate-v6.sh` → ≥ 1 IF delegated (either is valid per PATTERNS.md hybrid recommendation)
    - `grep -c 'wasm-pack build --target web --release' scripts/validate-v6.sh` → ≥ 2 (one per wasm crate)
    - `grep -c 'docker compose -f deploy/docker-compose.yml' scripts/validate-v6.sh` → ≥ 2 (up + down)
    - `grep -c '2097152' scripts/validate-v6.sh` → ≥ 1 (2 MB size floor, wasm-tests.yml parity)
    - `grep -cE '\\b471\\b' scripts/validate-v6.sh` → ≥ 1 (D-02 test-count floor)
    - `grep -c 'npm run build' scripts/validate-v6.sh` → ≥ 1 (dashboard gate per D-12, build-and-typecheck subset)
    - `grep -c 'npm run check' scripts/validate-v6.sh` → ≥ 1 (typecheck per D-12)
    - `grep -c 'allow-regression' scripts/validate-v6.sh` → ≥ 1 (D-02 escape-hatch flag wired)
    - `grep -c 'D-02 JUSTIFICATION:' scripts/validate-v6.sh` → ≥ 1 (escape-hatch writes justification line to validate-v6.log)
    - Line count: `wc -l < scripts/validate-v6.sh` → between 120 and 240 (copy-from-ci-check.sh + 4 new gates + escape-hatch flag fits this range)
    - `./scripts/ci-check.sh --clean` Step 1 (copyright-headers check) MUST include the new file — run it at end of task to confirm; exit 0 required
    - No emoji characters in file: `grep -cP '[\\x{1F300}-\\x{1F9FF}]' scripts/validate-v6.sh` → 0
  </acceptance_criteria>
  <done>scripts/validate-v6.sh committed; executable; syntax-clean; contains full D-01 matrix (directly or via ci-check.sh delegation) + D-11 WASM + D-12 dashboard + D-13 docker + D-02 floor enforcement WITH `--allow-regression "<justification>"` escape hatch (per CONTEXT D-02); ci-check.sh copyright-header check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Repo-wide D-10 straggler grep audit + MIGRATION.md hybrid-gate action-rename row</name>
  <files>MIGRATION.md</files>
  <read_first>
    - .planning/phases/89-final-validation/89-CONTEXT.md §D-10 (grep command + allowlist — copy verbatim)
    - .planning/phases/89-final-validation/89-PATTERNS.md §Shared.3 (allowlist rationale — which strings stay, which get swept)
    - .planning/phases/89-final-validation/89-PATTERNS.md §File 4 (ci.yml re-grep recommendation — CONTEXT says lines 212/220 but Phase 88 Plan 02 refactor may have already cleaned them; re-grep first, branch on result)
    - .planning/phases/89-final-validation/89-PATTERNS.md §File 7 (MIGRATION.md link-target verification — D-09 / D-14 hybrid gate — identifies `sealedge-attest-sbom-action` as missing from the "What renamed" table)
    - **MIGRATION.md lines 16-41 in full** — read the full "What renamed" table. **Confirm the exact column count.** Per planning-phase grep this table has 3 columns (`| Surface | Before (v5.x) | After (v6.0) |`). If the table has changed shape since planning (e.g., a 4th column was added: `| Area | Old | New | Notes |`), extend the inserted row to match the actual column count.
    - .github/workflows/ci.yml lines 170-225 (self-attestation job — D-10 candidate; re-grep before editing)
  </read_first>
  <action>
    This task has three sequential steps. Each runs as part of the same atomic commit.

    **Step A: Repo-wide D-10 straggler grep audit (read-only discovery).**

    Run CONTEXT.md §Specifics D-10 grep command verbatim via the Bash tool:
    ```
    git ls-files \
      | xargs grep -nE "trustedge|TRUSTEDGE|\\.trst\\b|\\.te-attestation" 2>/dev/null \
      | grep -vE "TrustEdge-Labs|TrustEdge Labs|trustedgelabs\\.com|MIGRATION\\.md|CHANGELOG\\.md|^\\.planning/(milestones|phases)/|RFC_K256_SUPPORT\\.md|improvement-plan\\.md|security-review-platform\\.md"
    ```

    Expected outcome: zero hits (PATTERNS.md File 4 notes the `ci.yml` straggler was likely already cleaned in Phase 88 Plan 02's refactor).

    If hits appear, classify each:
    - Phase 83-86 allowlisted (TrustEdge-Labs org/brand, company legal entity, .planning/ archives) → should have been caught by the `grep -vE` allowlist; if not, extend the allowlist pattern in the SUMMARY's audit-output section (do NOT modify the file in that case; it's an allowlist gap, not a straggler).
    - True straggler (missed by prior phases) → D-14 hybrid gate: fix inline in this same commit via Edit tool; log every fix in the SUMMARY's straggler-fixes table.

    Capture the raw grep output (even if empty) for the SUMMARY under an "Audit output (literal)" fenced block.

    **Step B: ci.yml targeted re-grep + branch-on-result (CONTEXT D-10 known-straggler check).**

    Run:
    ```
    grep -nE 'te-attestation|\\.te-' .github/workflows/ci.yml
    grep -nE 'te-attestation|\\.te-' .github/workflows/*.yml
    ```

    **Branch on the result of the grep — do NOT pre-declare the outcome:**

    - **IF the grep returns ZERO hits** (expected per PATTERNS.md File 4 analysis — Phase 88 Plan 02's refactor to `${{ steps.attest.outputs.attestation-path }}` eliminated the hardcoded `.te-attestation.json` filename): record in SUMMARY as "ci.yml verified clean of `te-attestation` strings, not modified — Phase 88 Plan 02 refactor superseded the CONTEXT D-10 reference." Add the note `verified clean, not modified` to the commit body for this plan's atomic commit. No file edit performed.

    - **IF the grep returns NON-ZERO hits**: apply Phase 87 Plan 01 Task 1 verbatim line-local substring swap via Edit tool: `seal.te-attestation.json` → `seal.se-attestation.json`. Keep line numbering intact. Do NOT reflow YAML whitespace. Re-grep to confirm zero hits post-fix. Append `+ ci.yml straggler fix` to the commit message subject (so the atomic commit becomes `feat(89): add scripts/validate-v6.sh validation gate + MIGRATION.md action-rename row + ci.yml straggler fix (D-14 hybrid fix)`). Record both pre-fix grep output and post-fix grep output in the SUMMARY.

    The post-task acceptance criterion is the same in both branches: post-task grep returns zero hits. The PATH to that outcome (no-op vs. inline fix) branches on what the pre-grep finds.

    **Step C: MIGRATION.md hybrid-gate addition (D-14 per PATTERNS.md File 7 known gap).**

    First, verify the gap still exists:
    ```
    grep -c 'attest-sbom-action' MIGRATION.md
    ```
    Expected: 0 (confirmed via planning-phase grep).

    If count > 0, log in SUMMARY as "already-present — no edit needed" and stop. Otherwise:

    Edit `MIGRATION.md` to insert a new row in the "What renamed" table. Use the Edit tool (not sed).

    **Confirm the column count first via a read of MIGRATION.md:16-41.** Per planning-phase verification, the table is 3 columns: `| Surface | Before (v5.x) | After (v6.0) |`. If the executor finds the table has been extended to 4+ columns (e.g., `| Area | Old | New | Notes |`), match the actual column count when inserting the new row.

    Locate the existing row:
    ```
    | GitHub repo URL | `TrustEdge-Labs/trustedge` | `TrustEdge-Labs/sealedge` |
    ```
    Insert the following row IMMEDIATELY AFTER it (same table, same column alignment — match the pipe/space formatting of the surrounding rows exactly). For the confirmed 3-column shape:
    ```
    | GitHub Action | `TrustEdge-Labs/attest-sbom-action@v1` | `TrustEdge-Labs/sealedge-attest-sbom-action@v2` |
    ```

    After the edit, re-verify with:
    ```
    grep -c 'sealedge-attest-sbom-action@v2' MIGRATION.md
    ```
    Expected: ≥ 1.

    Also re-verify the table structure didn't break:
    ```
    grep -c '^| GitHub Action ' MIGRATION.md
    ```
    Expected: 1 (exactly one new row inserted).

    **Column-consistency check (W-5 hardening) — confirm every `|`-prefixed line in the "What renamed" table has the same pipe count:**
    ```
    awk '/^### What renamed/{flag=1; next} /^### /{flag=0} flag && /^\\|/ {n=gsub(/\\|/,"|"); print n}' MIGRATION.md | sort -u | wc -l
    ```
    Expected: 1 (all rows in the table have the same pipe count). If output is > 1, the new row's column count is wrong — fix and re-check.

    **Commit discipline (Phase 88 Shared.2 carry-forward):** This task's file changes are committed together with Task 1's `scripts/validate-v6.sh` as the plan's atomic commit. Commit message template (the plan's per-commit post-task): `feat(89): add scripts/validate-v6.sh validation gate + MIGRATION.md action-rename row (D-14 hybrid fix)`. If Step B branched into the inline-fix path, append `+ ci.yml straggler fix` to the subject. Executor does NOT commit inside the task; the plan's close-commit sweep handles it.
  </action>
  <verify>
    <automated>grep -c 'sealedge-attest-sbom-action@v2' MIGRATION.md | grep -qE '^[1-9]' && grep -cE 'te-attestation|\.te-' .github/workflows/ci.yml | grep -q '^0$' && grep -c '| GitHub Action ' MIGRATION.md | grep -q '^1$' && git ls-files | xargs grep -nE 'trustedge|TRUSTEDGE|\.trst\b|\.te-attestation' 2>/dev/null | grep -vE "TrustEdge-Labs|TrustEdge Labs|trustedgelabs\.com|MIGRATION\.md|CHANGELOG\.md|^\.planning/(milestones|phases)/|RFC_K256_SUPPORT\.md|improvement-plan\.md|security-review-platform\.md" | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - Audit grep (D-10 command verbatim with allowlist): zero hits outside allowlisted historical artifacts (raw output captured in SUMMARY as evidence)
    - `grep -cE 'te-attestation|\\.te-' .github/workflows/ci.yml` → 0 (post-task; either confirmed clean and no edit, or inline-fix applied — see Step B branch)
    - `grep -c '| GitHub Action ' MIGRATION.md` → 1 (exactly one new row inserted with the GitHub Action label)
    - `grep -c 'attest-sbom-action' MIGRATION.md` → ≥ 1 (previously 0 per planning-phase grep; after edit: row added)
    - `grep -c 'sealedge-attest-sbom-action@v2' MIGRATION.md` → ≥ 1 (new row present)
    - `grep -c 'TrustEdge-Labs/attest-sbom-action@v1' MIGRATION.md` → ≥ 1 (old name in the "Before" column of the new row)
    - MIGRATION.md table column-consistency (W-5): `awk '/^### What renamed/{flag=1; next} /^### /{flag=0} flag && /^\\|/ {n=gsub(/\\|/,"|"); print n}' MIGRATION.md | sort -u | wc -l` → 1 (all rows have the same pipe count)
    - No other MIGRATION.md content modified: `git diff MIGRATION.md | grep -c '^+'` → ≤ 3 (one new row + maybe one blank-line normalization max; `git diff | grep '^-' | grep -v '^---'` → 0 lines removed)
    - Copyright header on MIGRATION.md preserved: `head -6 MIGRATION.md | grep -c 'Project: sealedge'` → ≥ 1 (unchanged)
    - SUMMARY.md (written at plan close) includes an "Audit output (literal)" fenced block with the raw grep output (zero hits expected)
    - SUMMARY.md includes a "ci.yml D-10 disposition" note that BRANCHES on the actual grep result: either "verified clean, not modified" (zero-hit path) OR "inline straggler fix applied — see commit body" (non-zero-hit path)
  </acceptance_criteria>
  <done>MIGRATION.md "What renamed" table includes one new row for the GitHub Action rename (column count matches existing rows); `.github/workflows/ci.yml` has zero `te-attestation` hits post-task (path to that outcome — verified clean OR inline fix — recorded in SUMMARY); repo-wide D-10 grep returns zero allowlist-filtered hits; all evidence captured in the SUMMARY.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer shell → scripts/ | `validate-v6.sh` is invoked interactively; all commands run with the developer's ambient authority (no elevated privileges added) |
| scripts/validate-v6.sh → docker daemon | Docker commands (`docker compose up/down`) run under the developer's docker group membership; no new docker trust delegation |
| scripts/validate-v6.sh → local filesystem | Writes `validate-v6.log` to `$PWD`; reads/writes under `crates/wasm/pkg/`, `crates/seal-wasm/pkg/`, `web/dashboard/build/`, `target/` (all under the repo workspace) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-89-01-01 | Tampering | `scripts/validate-v6.sh` committed to git | mitigate | File committed under MPL-2.0 header + atomic commit discipline; `./scripts/ci-check.sh --clean` Step 1 (copyright-headers) includes the new file, catching tamper/forget at pre-commit time |
| T-89-01-02 | Information disclosure | `validate-v6.log` output written to $PWD | accept | Log contains only cargo/npm/docker stdout — no secrets, no tokens, no private keys (all commands read-only or build-artifact-producing). Solo-dev context; log is excerpted into `89-VERIFICATION.md` manually. If log path ever needs persisting, add to `.gitignore` (not required v1; script writes to cwd which is repo root — future work if the pattern spreads) |
| T-89-01-03 | Denial of service | Docker stack startup blocks v6.0.0 tag cut | accept | Gate is the correct blocking behavior — if docker stack can't come up healthy, release is not safe to ship. `--skip-docker` flag is the deliberate escape hatch for no-docker dev environments; recorded in SUMMARY if used |
| T-89-01-04 | Tampering | MIGRATION.md table single-row addition | mitigate | Edit tool used (deterministic) not sed; re-grep verifies exactly one row added; column-consistency `awk` check confirms pipe-count parity; git diff bounded to ≤ 3 added lines; copyright header preserved |
| T-89-01-05 | Spoofing | action-rename row maps `@v1` → `@v2` in migration table | mitigate | The row values match: (a) Phase 88 Plan 03 external rename output, (b) ci.yml:213 `uses:` line, (c) Phase 88 CONTEXT.md D-11 new-tag target. Three-way consistency check via re-grep in acceptance_criteria |
| T-89-01-06 | Repudiation | D-02 `--allow-regression` flag could be used to silently bypass the test floor | mitigate | Flag REQUIRES a non-empty justification string; script writes `D-02 JUSTIFICATION: <text>` to `validate-v6.log`; Plan 02 Task 1 `<verify>` cross-checks the justification text appears in 89-VERIFICATION.md §1 ("D-02 justification: <text>") — both must match for the gate to advance |

**Severity gate:** All threats are low severity (developer-tool surface, not production runtime). No blocking threats.
</threat_model>

<verification>
- `./scripts/validate-v6.sh` syntax-valid via `bash -n`
- `./scripts/validate-v6.sh` executable via `test -x`
- `./scripts/validate-v6.sh` accepts `--allow-regression "<justification>"` flag (D-02 escape hatch)
- `./scripts/ci-check.sh --clean` Step 1 (copyright-headers) passes with the new file included
- D-10 repo-wide grep: zero hits outside allowlisted artifacts
- ci.yml `te-attestation` grep: zero hits post-task (regardless of whether Step B branched into the inline-fix path or recorded "verified clean, not modified")
- MIGRATION.md action-rename row present and correctly formatted (pipe-delimited, 3 columns matching existing table shape)
- MIGRATION.md table column-consistency: all rows in the "What renamed" table have the same pipe count (W-5 check)
</verification>

<success_criteria>
Plan 01 succeeds when:

1. `scripts/validate-v6.sh` exists, is executable, has MPL-2.0 header with `Project: sealedge`, covers the full D-01 matrix (directly or via ci-check.sh), runs WASM cargo-check + wasm-pack + size-floor, dashboard npm ci/build/check, docker compose up/down + demo, enforces the D-02 test-count floor ≥ 471, AND supports the `--allow-regression "<justification>"` escape hatch per CONTEXT D-02
2. `MIGRATION.md` "What renamed" table has a new row mapping `TrustEdge-Labs/attest-sbom-action@v1` → `TrustEdge-Labs/sealedge-attest-sbom-action@v2`; column count matches the existing table shape (W-5 check passes)
3. `.github/workflows/ci.yml` has zero `te-attestation` hits post-task (Step B branched on actual grep result: either "verified clean, not modified" OR inline straggler fix applied; path recorded in SUMMARY and commit body)
4. Repo-wide D-10 grep (CONTEXT.md §Specifics verbatim + allowlist) returns zero actionable hits
5. Atomic commit `feat(89): add scripts/validate-v6.sh validation gate + MIGRATION.md action-rename row (D-14 hybrid fix)` lands with Co-Authored-By trailer (commit subject extended with `+ ci.yml straggler fix` if Step B applied an inline fix)

Plan 02 depends on artifact (1); Plan 03 depends on implicit state (3 + 4) being recorded so the pre-tag gate can cite this plan's SUMMARY.
</success_criteria>

<output>
After completion, create `.planning/phases/89-final-validation/89-01-SUMMARY.md` including:
- One-line outcome
- D-10 audit output (literal grep command + output, zero-hit expected)
- ci.yml disposition (branched on actual grep result: "verified clean, not modified" OR "inline fix applied — see commit body")
- MIGRATION.md diff excerpt (just the new row + column-consistency check result)
- `validate-v6.sh` structural outline (step list with pass/fail row count per step) including the `--allow-regression` flag
- Commit SHA of the atomic commit
- Any hybrid-gate straggler fixes discovered during the audit (expected: none beyond the MIGRATION.md row, possibly one ci.yml line-local swap if Step B branched into inline-fix path)
</output>
