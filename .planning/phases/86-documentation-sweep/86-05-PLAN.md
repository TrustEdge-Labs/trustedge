---
phase: 86-documentation-sweep
plan: 05
type: execute
wave: 2
depends_on:
  - 86-01
  - 86-02
  - 86-03
  - 86-04
files_modified:
  - scripts/demo-attestation.sh
  - scripts/fast-bench.sh
  - scripts/consolidate-docs.sh
  - scripts/project/add-copyright.sh
autonomous: true
requirements:
  - DOCS-04
tags:
  - docs
  - rebrand
  - scripts
  - final-audit
must_haves:
  truths:
    - "scripts/*.sh prose and string templates that Phase 85 left behind (comment headers, printf template strings, verify-endpoint URL) reference sealedge"
    - "scripts/demo-attestation.sh:30 ENDPOINT uses verify.sealedge.dev"
    - "scripts/consolidate-docs.sh:44 copyright-header template string reads `Project: sealedge — Privacy and trust at the edge.`"
    - "Phase-wide repo-wide grep `grep -rin \"trustedge\" --include=… | grep -vE \"<allowlist>\"` returns empty (per D-13)"
    - "`cargo doc --workspace --no-deps` output at target/doc/ is grep-clean under the same allowlist (per D-14)"
    - "`cargo test --doc --workspace --locked` still passes (regression check)"
  artifacts:
    - path: scripts/demo-attestation.sh
      provides: "Demo shell script with updated verify endpoint"
      contains: "verify.sealedge.dev"
    - path: scripts/consolidate-docs.sh
      provides: "Doc consolidation script with updated copyright template"
      contains: "Project: sealedge"
    - path: .planning/phases/86-documentation-sweep/86-05-SUMMARY.md
      provides: "Phase-close audit report with exact grep output"
      contains: "allowlist"
  key_links:
    - from: scripts/consolidate-docs.sh
      to: "any future `.rs`/`.md` files generated by the script"
      via: "printf/heredoc template for MPL-2.0 copyright header"
      pattern: "Project: sealedge"
    - from: Final grep audit
      to: "D-13 (CONTEXT.md) allowlist regex"
      via: "exact command enforcement"
      pattern: "TrustEdge-Labs\\|TRUSTEDGE LABS LLC\\|…"
---

<objective>
Close Phase 86 with (1) the scripts prose sweep that Phase 85 deferred (4 known sites per D-09), (2) the repo-wide D-13 grep audit with the exact allowlist, (3) the D-14 `cargo doc` target/doc grep-clean verification, and (4) a final doc-test regression check. This plan runs after Plans 01–04 so the audit reflects the complete Phase 86 state.

Purpose: DOCS-04 requires scripts and examples under `scripts/` + `examples/cam.video/` to invoke new binary names and reference the new attestation extension. The `.rs` side of examples/ is covered by Plan 04 (rustdoc). The `.md` side of examples/ is covered by Plan 03 (README). This plan handles the remaining `scripts/*.sh` prose carve-outs from Phase 85's boundary + the final phase-close audit gate.
Output: 4 updated scripts. Phase-close audit report with exact grep output + target/doc scan.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/86-documentation-sweep/86-CONTEXT.md
@.planning/phases/85-code-sweep-headers-text-metadata/85-CONTEXT.md
@CLAUDE.md
@.planning/phases/86-documentation-sweep/86-01-SUMMARY.md
@.planning/phases/86-documentation-sweep/86-02-SUMMARY.md
@.planning/phases/86-documentation-sweep/86-03-SUMMARY.md
@.planning/phases/86-documentation-sweep/86-04-SUMMARY.md

<interfaces>
<!-- Final phase allowlist regex (from D-13 verbatim): -->

ALLOWLIST="TrustEdge-Labs|TRUSTEDGE LABS LLC|\\.planning/|\\.git/|target/|node_modules/|\\.claude/worktrees/|improvement-plan\\.md|RFC_K256_SUPPORT\\.md|security-review-platform\\.md|actions/attest-sbom-action/"

Full repo-wide grep (D-13):
  grep -rin "trustedge" \
    --include='*.md' --include='*.rs' --include='*.sh' --include='*.toml' \
    --include='*.svelte' --include='*.ts' --include='*.json' --include='*.html' \
    . \
    | grep -vE "$ALLOWLIST"

Expected post-Phase-86 result: empty (or explicitly-accepted exceptions documented in this plan's SUMMARY).

Known-acceptable exceptions (pass through but must be enumerated in SUMMARY):
  - Phase 84 D-02 shadow consts (`OLD_ENVELOPE_DOMAIN = b"TRUSTEDGE_ENVELOPE_V1"`, `OLD_KEY_HEADER = b"TRUSTEDGE-KEY-V1"`) in core/src/{envelope,crypto}.rs — clean-break test fixtures
  - Phase 85 D-02 shadow consts (`OLD_CHUNK_KEY_DOMAIN`, `OLD_SESSION_KEY_DOMAIN`, `OLD_GENESIS_SEED`, `OLD_MANIFEST_DOMAIN_SEP`, `OLD_X25519_DERIVATION`, `OLD_V2_SESSION_KEY`)
  - `TrustEdgeError` public type + lib.rs re-export (Plan 05 semver-breaking deferral)
  - `HYBRID_MAGIC = b"TRHY"` comment `// TRustEdge HYbrid` (Plan 05 documented deferral)
  - `[package.metadata.trustedge]` Cargo.toml sections (Plan 03/05 dead-metadata deferral)
  - `crates/wasm/examples/basic-usage.html` (Plan 05 Phase 86 ambiguous-scope deferral — check during this audit whether Plan 03 swept it)
  - `"trustedge"` bench fixture label in `benches/crypto_benchmarks.rs:353` (Plan 05 legacy-detection fixture)
  - Historical-note prose in rustdoc (from Plan 04): `//! Migrated from the v5.x trustedge-platform-api crate…` and similar
  - `vectors.rs` hybrid-rationale comment if executor chose option (a)
  - MIGRATION.md v6.0 rename-map table `Before (v5.x)` column — all the `trustedge-*` entries intentional
  - CHANGELOG.md past-version entries verbatim (D-02 hybrid)

cargo doc grep (D-14):
  cargo doc --workspace --no-deps
  grep -rni "trustedge" target/doc/ 2>/dev/null \
    | grep -vE "$ALLOWLIST" \
    | grep -vE "static\\.files|search-index|trait\\.impl|type\\.impl|implementors"
  # Expected: empty (rendered docs clean)
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| script output ← template string | scripts/consolidate-docs.sh produces files using an in-script copyright-header template. If the template still says "trustedge" the script would silently regenerate stale headers. |
| CI ← verify endpoint | demo-attestation.sh calls ENDPOINT="…verify.<domain>.dev"; wrong domain = broken demo |
| operator ← final audit | Phase-close audit is the contractual gate; false pass = regressions ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-40   | Tampering | script template strings | mitigate | Task 1 edits the 4 known carve-outs and runs a scripts/*.sh grep gate |
| T-86-41   | Repudiation | phase-close audit | mitigate | Task 2 writes the full grep output + cargo doc scan into 86-05-SUMMARY.md with exact commands |
| T-86-42   | Denial of service | cargo test --doc regression | mitigate | Task 3 runs `cargo test --workspace --locked` including doc tests as the final gate |

No source code or crypto modifications. Attack surface = script prose and audit gating.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Scripts prose carve-outs (D-09)</name>
  <files>scripts/demo-attestation.sh, scripts/fast-bench.sh, scripts/consolidate-docs.sh, scripts/project/add-copyright.sh</files>
  <read_first>
    - .planning/phases/86-documentation-sweep/86-CONTEXT.md (D-09, D-11)
    - Each file in `<files>` end-to-end
  </read_first>
  <behavior>
    - `scripts/demo-attestation.sh:30` ENDPOINT URL updates from `https://verify.trustedge.dev` to `https://verify.sealedge.dev` (aspirational/placeholder domain — no live DNS change, per D-09)
    - `scripts/fast-bench.sh:13` prose tidy for sealedge-final consistency
    - `scripts/consolidate-docs.sh:44` in-script copyright-template string updates `Project: trustedge — Privacy and trust at the edge.` → `Project: sealedge — Privacy and trust at the edge.`
    - `scripts/project/add-copyright.sh:33` verified — the grep check `copyright.*trustedge labs llc` is legitimate (it checks for the legal-entity string, which is correct); the script's output template (if any) is swept for any `trustedge` output strings
    - Shell-script function output (echo/printf) is sealedge-branded where it emits user-visible text
  </behavior>
  <action>
    **scripts/demo-attestation.sh** (line 30 target):
    - Replace `ENDPOINT="https://verify.trustedge.dev"` with `ENDPOINT="https://verify.sealedge.dev"`.
    - Also walk the entire file for any `echo`/`printf` prose or comments referencing `trustedge`/`.trst`/`.te-attestation.json`/`TRUSTEDGE_*` and apply the substitution rubric (same as 86-01 Task 1 Step C). Preserve `TrustEdge-Labs` org in any GitHub URL.

    **scripts/fast-bench.sh** (line 13 target):
    - Current comment per CONTEXT.md: "Change to the sealedge core crate directory (Phase 83 renamed crates/trustedge-core → crates/core)". The rename narrative is now fully landed; simplify to: "Change to the sealedge-core crate directory". Remove the parenthetical rename history now that sealedge is the stable reality — OR retain as a historical breadcrumb, executor's choice. Preferred: drop the parenthetical since the post-Phase-86 state is the new baseline.
    - Walk the rest of the file for any remaining prose/comment hits.

    **scripts/consolidate-docs.sh** (line 44 target):
    - The in-script template string at line 44 reads:
      ```
      Project: trustedge — Privacy and trust at the edge.
      ```
      This is a heredoc/printf template used to generate MPL-2.0 headers in other files. Change to:
      ```
      Project: sealedge — Privacy and trust at the edge.
      ```
    - Also scan for any other `trustedge` prose in comments or help text — the script's purpose may name the product in a `--help` output. Update to sealedge.
    - Preserve `TRUSTEDGE LABS LLC` in the copyright-header template if it appears (legal entity, per D-05).

    **scripts/project/add-copyright.sh** (line 33 target):
    - The grep check at line 33 (per CONTEXT.md: `copyright.*trustedge labs llc`) is a **legal-entity check** — it verifies that files have the `TRUSTEDGE LABS LLC` copyright line (case-insensitive match). This is intentional and correct; LEAVE the grep line unchanged.
    - But verify the script's output template (the MPL-2.0 header it writes when adding a copyright to a new file) says `Project: sealedge`, not `Project: trustedge`. If the template is stored in this script, update it. If the template is sourced from elsewhere (e.g., reads `scripts/fix-copyright.sh` Phase 85 template), verify that source is correct.
    - Walk the rest of the file for prose hits.

    **Per-file grep after editing (including all scripts/*.sh, not just the 4 named):**
    ```bash
    grep -rnE "[Tt]rustedge|\\.trst\\b|\\.te-attestation|TRUSTEDGE_|verify\\.trustedge\\.dev" scripts/ \
      | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|trustedge labs llc'
    ```
    Expected: empty.
  </action>
  <verify>
    <automated>bash -c 'set -e; grep -q "verify\\.sealedge\\.dev" scripts/demo-attestation.sh; grep -q "Project: sealedge" scripts/consolidate-docs.sh; hits=$(grep -rnE "[Tt]rustedge|\\.trst\\b|\\.te-attestation|TRUSTEDGE_|verify\\.trustedge\\.dev" scripts/ | grep -vE "TrustEdge-Labs|TRUSTEDGE LABS LLC|trustedge labs llc" | wc -l); [ "$hits" -eq 0 ] || { echo "STALE scripts hits: $hits"; exit 1; }; echo ok'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -q "https://verify\\.sealedge\\.dev" scripts/demo-attestation.sh` succeeds
    - `grep -c "https://verify\\.trustedge\\.dev" scripts/demo-attestation.sh` returns 0
    - `grep -q "Project: sealedge" scripts/consolidate-docs.sh` succeeds
    - `grep -c "Project: trustedge" scripts/consolidate-docs.sh` returns 0
    - `scripts/project/add-copyright.sh` still has the `trustedge labs llc` case-insensitive legal-entity check at line ~33 (verify with `grep -i "trustedge labs llc" scripts/project/add-copyright.sh`)
    - Repo-scoped grep: `grep -rnE "[Tt]rustedge|\\.trst\\b|\\.te-attestation|TRUSTEDGE_|verify\\.trustedge\\.dev" scripts/ | grep -vE "TrustEdge-Labs|TRUSTEDGE LABS LLC|trustedge labs llc" | wc -l` returns 0
    - `./scripts/ci-check.sh` still runs (if it depends on any of the modified scripts — regression check)
  </acceptance_criteria>
  <done>
    4 scripts updated. Endpoint URL points to aspirational verify.sealedge.dev. Copyright template generates sealedge headers. Legal-entity grep check preserved. No stale brand references in scripts/.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Phase-close repo-wide grep audit (D-13)</name>
  <files>.planning/phases/86-documentation-sweep/86-05-SUMMARY.md</files>
  <read_first>
    - .planning/phases/86-documentation-sweep/86-CONTEXT.md (D-13 allowlist regex — use verbatim)
    - 86-01-SUMMARY.md, 86-02-SUMMARY.md, 86-03-SUMMARY.md, 86-04-SUMMARY.md (inputs from Wave 1 plans)
  </read_first>
  <behavior>
    - The exact D-13 grep runs and returns empty (or documented exceptions only)
    - Exceptions are enumerated in the SUMMARY with rationale matching the Known-Acceptable-Exceptions list in <interfaces>
    - No new source code or docs are modified in this task — it's an audit gate that WRITES a report but only makes changes if a gap is found that the Wave-1 plans missed
  </behavior>
  <action>
    1. Run the exact D-13 grep:
       ```bash
       grep -rin "trustedge" \
         --include='*.md' --include='*.rs' --include='*.sh' --include='*.toml' \
         --include='*.svelte' --include='*.ts' --include='*.json' --include='*.html' \
         . \
         | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|\.planning/|\.git/|target/|node_modules/|\.claude/worktrees/|improvement-plan\.md|RFC_K256_SUPPORT\.md|security-review-platform\.md|actions/attest-sbom-action/' \
         | tee /tmp/phase86-grep.txt
       ```

    2. Classify each remaining hit into one of three categories:
       - **KEEP (known-acceptable exception):** Matches one of the Known-Acceptable-Exceptions in <interfaces> (shadow consts, TrustEdgeError, HYBRID_MAGIC, package.metadata.trustedge, basic-usage.html if Plan 03 didn't sweep it, bench fixture label, rustdoc historical notes, MIGRATION.md v6.0 rename-map "Before" column, CHANGELOG.md past version entries).
       - **HISTORICAL-PRESERVATION (acceptable):** Inside a hybrid-treatment section like MIGRATION.md's v6.0 rename-map "Before" column or CHANGELOG.md's pre-v6.0 entries. These are intentional.
       - **GAP (must fix):** Not covered above. This is a miss from Wave 1 plans.

    3. If any hit classifies as GAP:
       - Edit the file to fix the gap (apply the 86-01-Task-1 substitution rubric).
       - Re-run the grep to confirm empty.
       - Document the gap + fix in the SUMMARY.

    4. Write `.planning/phases/86-documentation-sweep/86-05-SUMMARY.md` with:

       ```
       # Phase 86 Plan 05 — Scripts + Final Audit SUMMARY

       ## Task 1: Scripts sweep
       - scripts/demo-attestation.sh: endpoint URL updated
       - scripts/fast-bench.sh: line 13 prose tidy
       - scripts/consolidate-docs.sh: line 44 copyright template updated
       - scripts/project/add-copyright.sh: legal-entity grep preserved, template verified
       Grep check: empty ✓

       ## Task 2: Repo-wide D-13 grep audit

       **Command (exact):**
       ```
       grep -rin "trustedge" --include='*.md' --include='*.rs' --include='*.sh' \
         --include='*.toml' --include='*.svelte' --include='*.ts' --include='*.json' \
         --include='*.html' . \
         | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|\.planning/|\.git/|target/|node_modules/|\.claude/worktrees/|improvement-plan\.md|RFC_K256_SUPPORT\.md|security-review-platform\.md|actions/attest-sbom-action/'
       ```

       **Raw result count:** <N>

       **Classified:**
       | Category | Count | Examples |
       |---|---|---|
       | KEEP — shadow consts | … | `crates/core/src/envelope.rs:858 OLD_ENVELOPE_DOMAIN …` |
       | KEEP — TrustEdgeError type (documented Plan 05 deferral) | … | … |
       | KEEP — HYBRID_MAGIC comment | … | … |
       | KEEP — package.metadata.trustedge Cargo.toml sections | … | … |
       | KEEP — basic-usage.html (still in scope check) | … | … |
       | KEEP — bench fixture label | … | … |
       | HISTORICAL — MIGRATION.md v6.0 rename-map before-column | … | `MIGRATION.md: Workspace crate prefix | trustedge-* | sealedge-*` |
       | HISTORICAL — CHANGELOG.md pre-v6.0 entries | … | … |
       | HISTORICAL — rustdoc "Migrated from v5.x trustedge-…" notes | … | … |
       | GAP — fixed in this plan | 0 (or the gap found + fix) | … |

       **Final (post-fix) grep result:** empty under allowlist ✓

       ## Task 3: cargo doc grep audit (D-14)

       **Commands:**
       ```
       cargo doc --workspace --no-deps
       grep -rni "trustedge" target/doc/ | grep -vE '<allowlist>' | wc -l
       ```
       **Result:** <N>
       **Classification:** static.files (rustdoc internals), TrustEdge-Labs org in doc page footer, TRUSTEDGE LABS LLC entity in doc page footer. No stale product references.

       ## Task 4: Regression check
       ```
       cargo test --workspace --locked
       ```
       **Result:** <N passed / 0 failed>

       ## Phase 86 Complete
       - 4 plans executed (86-01, 86-02, 86-03, 86-04 in Wave 1; 86-05 in Wave 2)
       - 81+ documentation files updated
       - All 4 DOCS requirement IDs addressed
       - D-13 grep-clean (with documented exceptions)
       - D-14 cargo doc grep-clean
       - All workspace tests green
       ```

    Do NOT edit any source/doc files unless Step 3 identified a GAP and the fix is trivial (same substitution rubric). For non-trivial gaps, stop and flag back to planner.
  </action>
  <verify>
    <automated>bash -c 'set -e; ALLOWLIST="TrustEdge-Labs|TRUSTEDGE LABS LLC|\\.planning/|\\.git/|target/|node_modules/|\\.claude/worktrees/|improvement-plan\\.md|RFC_K256_SUPPORT\\.md|security-review-platform\\.md|actions/attest-sbom-action/"; hits=$(grep -rin "trustedge" --include="*.md" --include="*.rs" --include="*.sh" --include="*.toml" --include="*.svelte" --include="*.ts" --include="*.json" --include="*.html" . 2>/dev/null | grep -vE "$ALLOWLIST" | wc -l); echo "raw-allowlisted-hits: $hits"; test -f .planning/phases/86-documentation-sweep/86-05-SUMMARY.md'</automated>
  </verify>
  <acceptance_criteria>
    - 86-05-SUMMARY.md exists at `.planning/phases/86-documentation-sweep/86-05-SUMMARY.md`
    - 86-05-SUMMARY.md documents exact D-13 grep command (verbatim from CONTEXT.md)
    - 86-05-SUMMARY.md enumerates every allowlisted hit with its category (KEEP / HISTORICAL / GAP-fixed)
    - Post-fix (if any GAP was fixed), the D-13 grep `grep -rin "trustedge" … | grep -vE "$ALLOWLIST"` produces only hits classifiable as KEEP or HISTORICAL (documented)
    - 86-05-SUMMARY.md documents the cargo doc grep result with classification
    - 86-05-SUMMARY.md documents `cargo test --workspace --locked` exit code and test count
  </acceptance_criteria>
  <done>
    D-13 grep audit complete. Any gap found during audit fixed in place. SUMMARY enumerates every remaining grep hit with justification. Phase 86 scope-fidelity confirmed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: cargo doc + doc-test regression gate (D-14)</name>
  <files>.planning/phases/86-documentation-sweep/86-05-SUMMARY.md (appended to)</files>
  <read_first>
    - .planning/phases/86-documentation-sweep/86-CONTEXT.md (D-14)
    - 86-04-SUMMARY.md (which already ran cargo doc + cargo test --doc — this task re-runs at phase close)
  </read_first>
  <behavior>
    - `cargo doc --workspace --no-deps` succeeds
    - `grep -rni "trustedge" target/doc/` under the same D-13 allowlist + static-file exclusions returns only explicitly-acceptable hits (org in page footer, entity in attribution)
    - `cargo test --workspace --locked` still passes (post-documentation-sweep regression check)
  </behavior>
  <action>
    1. Clean any stale docs:
       ```bash
       cargo clean --doc 2>/dev/null || true
       ```

    2. Build docs:
       ```bash
       cargo doc --workspace --no-deps 2>&1 | tee /tmp/phase86-cargo-doc.log
       ```
       Verify exit code 0. Capture the log for SUMMARY.

    3. Run the target/doc grep (D-14):
       ```bash
       grep -rni "trustedge" target/doc/ 2>/dev/null \
         | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|static\\.files|search-index|trait\\.impl|type\\.impl|implementors|source-files' \
         | tee /tmp/phase86-cargo-doc-grep.txt
       ```

    4. Classify remaining hits:
       - rustdoc-internal tokens (URL encoding, type IDs)
       - legitimate TrustEdge-Labs org or TRUSTEDGE LABS LLC entity references in the generated page footer (preserved)
       - GAPs (rustdoc prose that should have been swept in Plan 04 but wasn't rendered clean)

    5. If any GAP: locate the originating `.rs` file, apply the Plan 04 substitution, re-run `cargo doc --workspace --no-deps`, re-run the grep. If non-trivial, flag back.

    6. Run full test regression:
       ```bash
       cargo test --workspace --locked 2>&1 | tail -30 | tee /tmp/phase86-test.log
       ```
       Expected: all tests pass. Phase 85 left exactly one test failing (`wiring.rs:56`) which was fixed in a post-phase gap closure (per 85-VERIFICATION.md); this should continue to pass.

    7. Append to `.planning/phases/86-documentation-sweep/86-05-SUMMARY.md`:
       ```
       ## Task 3: cargo doc + test regression gate

       ### cargo doc
       - Exit: 0
       - Warnings: <N>
       - target/doc grep: <N> hits, all classified as <category>

       ### cargo test --workspace --locked
       - Exit: 0
       - Tests: <N passed>, <N failed>
       - Doc tests: <N passed>

       Phase 86 GATES GREEN ✓
       ```

    Do NOT modify source code unless a cargo-doc-specific GAP is found.
  </action>
  <verify>
    <automated>bash -c 'set -e; cargo doc --workspace --no-deps 2>&1 | tail -3; cargo test --workspace --locked 2>&1 | tail -5; echo phase-86-gates-green'</automated>
  </verify>
  <acceptance_criteria>
    - `cargo doc --workspace --no-deps` exits 0
    - `grep -rni "trustedge" target/doc/ 2>/dev/null | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|static\\.files|search-index|trait\\.impl|type\\.impl|implementors|source-files'` returns only lines classifiable as rustdoc-internal or legal-attribution (no stale product references)
    - `cargo test --workspace --locked` exits 0
    - Full test count matches Phase 85's ending baseline (300+) — no tests vanished
    - 86-05-SUMMARY.md has appended Task 3 section with exit codes and hit classification
  </acceptance_criteria>
  <done>
    `cargo doc` green. `cargo test --workspace --locked` green. target/doc grep-clean under allowlist. Phase 86 ready for verifier handoff.
  </done>
</task>

</tasks>

<verification>
Final phase-close gate:
```bash
# 1. Scripts clean
grep -rnE "[Tt]rustedge|\\.trst\\b|\\.te-attestation|TRUSTEDGE_|verify\\.trustedge\\.dev" scripts/ | grep -vE "TrustEdge-Labs|TRUSTEDGE LABS LLC|trustedge labs llc"
# Expected: empty

# 2. D-13 repo-wide grep (full phase gate)
ALLOWLIST="TrustEdge-Labs|TRUSTEDGE LABS LLC|\\.planning/|\\.git/|target/|node_modules/|\\.claude/worktrees/|improvement-plan\\.md|RFC_K256_SUPPORT\\.md|security-review-platform\\.md|actions/attest-sbom-action/"
grep -rin "trustedge" --include='*.md' --include='*.rs' --include='*.sh' --include='*.toml' --include='*.svelte' --include='*.ts' --include='*.json' --include='*.html' . | grep -vE "$ALLOWLIST"
# Expected: empty OR only Known-Acceptable-Exceptions from <interfaces>

# 3. cargo doc gate (D-14)
cargo doc --workspace --no-deps && \
  grep -rni "trustedge" target/doc/ 2>/dev/null | grep -vE "$ALLOWLIST|static\\.files|search-index|trait\\.impl|type\\.impl|implementors|source-files"
# Expected: empty OR only rustdoc-internal + legal-entity

# 4. Full test regression
cargo test --workspace --locked
# Expected: exit 0, all tests pass

# 5. Summary exists
test -f .planning/phases/86-documentation-sweep/86-05-SUMMARY.md && cat .planning/phases/86-documentation-sweep/86-05-SUMMARY.md | head -80
```
</verification>

<success_criteria>
- 4 scripts updated (demo-attestation, fast-bench, consolidate-docs, add-copyright)
- D-13 grep audit run with exact allowlist; SUMMARY enumerates every remaining hit with classification
- D-14 `cargo doc` output grep-clean under the allowlist
- `cargo test --workspace --locked` green (no regression introduced by doc sweep)
- 86-05-SUMMARY.md captures audit evidence for Phase 86 verifier handoff
- DOCS-04 requirement fully addressed
</success_criteria>

<output>
After completion, `.planning/phases/86-documentation-sweep/86-05-SUMMARY.md` is the Phase 86 closing report. It contains:
- Scripts sweep outcome (4 files, grep clean)
- D-13 audit output with classification table
- D-14 cargo doc audit output
- cargo test regression results
- Final list of known-acceptable exceptions (shadow consts, TrustEdgeError, etc.) with justification
</output>
