---
phase: 86-documentation-sweep
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/cli/README.md
  - crates/core/README.md
  - crates/core/AUTHENTICATION.md
  - crates/core/BENCHMARKS.md
  - crates/core/PERFORMANCE.md
  - crates/core/SOFTWARE_HSM_TEST_REPORT.md
  - crates/seal-cli/README.md
  - crates/seal-protocols/README.md
  - crates/seal-wasm/README.md
  - crates/seal-wasm/pkg-bundler/README.md
  - crates/wasm/README.md
  - crates/wasm/pkg-bundler/README.md
  - crates/experimental/pubky/README.md
  - crates/experimental/pubky-advanced/README.md
  - examples/cam.video/README.md
  - deploy/digitalocean/README-deploy.md
  - web/demo/README.md
  - .github/README.md
  - .github/pull_request_template.md
  - scripts/README.md
  - scripts/project/README.md
autonomous: true
requirements:
  - DOCS-01
  - DOCS-02
  - DOCS-04
tags:
  - docs
  - rebrand
  - crate-readmes
  - github-templates
must_haves:
  truths:
    - "Every crate-level README and crate-internal .md file under crates/** reflects sealedge product naming, Phase 83 crate renames, and post-rename shell snippets"
    - "examples/cam.video/README.md invokes `seal` binary, references `.seal` archives, and points to the new repo URL"
    - "deploy/digitalocean/README-deploy.md references sealedge-platform-server and updated env var names"
    - "web/demo/README.md and .github/*.md describe the product as sealedge"
    - "scripts/README.md and scripts/project/README.md describe the scripts under the sealedge brand"
  artifacts:
    - path: crates/core/README.md
      provides: "Core crate README — densest source of crate-name references"
      contains: "sealedge-core"
    - path: examples/cam.video/README.md
      provides: "cam.video example walkthrough"
      contains: "seal "
    - path: .github/pull_request_template.md
      provides: "PR template — visible on every new PR"
      contains: "sealedge"
    - path: deploy/digitalocean/README-deploy.md
      provides: "Operator deploy guide"
      contains: "sealedge-platform-server"
  key_links:
    - from: crates/core/README.md
      to: "Cargo.toml [package] name = \"sealedge-core\""
      via: "cargo add snippets in README must match crate's own Cargo.toml name"
      pattern: "sealedge-core"
    - from: examples/cam.video/README.md
      to: "examples/cam.video/Cargo.toml + record_and_wrap.rs invocations"
      via: "shell examples must match the crate's actual bin names"
      pattern: "cargo run.*--example\\|./target/release/seal"
---

<objective>
Sweep all non-root, non-docs/ markdown surfaces: crate-level READMEs and technical docs under `crates/**/`, the `.github/` templates, `deploy/digitalocean/`, `web/demo/`, `examples/cam.video/README.md`, and `scripts/README.md` / `scripts/project/README.md` (the two script-directory READMEs that also need the sweep). 21 files total.

Purpose: D-06 inventories the "other .md surfaces" that neither Plan 01 (root) nor Plan 02 (docs/) covers. These include crate-level READMEs (developers discover per-crate), GitHub templates (pasted into every PR/issue), deploy guides (operator surface), and example README (first hands-on surface). Every one must match the v6.0 brand.
Output: 21 updated markdown files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/86-documentation-sweep/86-CONTEXT.md
@.planning/phases/85-code-sweep-headers-text-metadata/85-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Same rename map as 86-01 / 86-02. Full tables in 86-01-PLAN.md <interfaces>. -->

Summary:
  Crates:     trustedge-*       → sealedge-*
  Binaries:   trustedge, trst, inspect-trst, trustedge-{server,client,platform-server}
              → sealedge, seal, inspect-seal, sealedge-{server,client,platform-server}
  Exts:       .trst             → .seal
              .te-attestation.json → .se-attestation.json
  Envvars:    TRUSTEDGE_*       → SEALEDGE_*
  URL:        TrustEdge-Labs/trustedge → TrustEdge-Labs/sealedge
  Wire:       TRUSTEDGE-KEY-V1  → SEALEDGE-KEY-V1
              TRUSTEDGE_ENVELOPE_V1 → SEALEDGE_ENVELOPE_V1

Preserve UNCHANGED: TrustEdge-Labs, TRUSTEDGE LABS LLC, trustedgelabs.com, "trust"/"edge" generic

Crate-directory inventory (from `ls crates/`):
  crates/cli/          (sealedge-cli crate)
  crates/core/         (sealedge-core crate)
  crates/seal-cli/     (sealedge-seal-cli crate — renamed from trustedge-trst-cli)
  crates/seal-protocols/ (sealedge-seal-protocols crate)
  crates/seal-wasm/    (sealedge-seal-wasm crate)
  crates/wasm/         (sealedge-wasm crate — THIS IS A POTENTIAL PHASE 83 LEFTOVER; see CONTEXT.md Claude's Discretion)
  crates/experimental/pubky/          (sealedge-pubky)
  crates/experimental/pubky-advanced/ (sealedge-pubky-advanced)
</interfaces>

<crates_wasm_investigation>
Per CONTEXT.md Claude's Discretion: `crates/wasm/` AND `crates/seal-wasm/` both exist. Investigation is required to confirm whether `crates/wasm/` is a rename leftover or a distinct crate. Task 1 sub-step runs `ls crates/wasm/ crates/seal-wasm/` and reads their Cargo.toml files to confirm.

If `crates/wasm/` is a leftover (Cargo.toml shows `name = "sealedge-wasm"` same as seal-wasm, or the directory contains only stale content), flag it in the plan's SUMMARY as needing a cleanup follow-up — do NOT delete the directory in Phase 86 (per CONTEXT.md Claude's Discretion: "Do not rename or delete directories in Phase 86").

If they are distinct crates (different Cargo.toml `name` values, different source content), sweep BOTH `crates/wasm/README.md` AND `crates/seal-wasm/README.md` as separate in-scope files.
</crates_wasm_investigation>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer ← crate README | cargo add snippets must match actual published/workspace crate names |
| new contributor ← .github/ templates | PR template text appears in every PR contributor creates |
| operator ← deploy/digitalocean README | Deploy commands must match the actual binary name shipped in Docker images |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-20   | Tampering | crate READMEs | mitigate | Acceptance criterion asserts crate-name in README matches `[package] name` line in same crate's Cargo.toml |
| T-86-21   | Information disclosure | .github/pull_request_template.md | accept | Template text is public; rename-only, no secrets |
| T-86-22   | Denial of service | deploy/digitalocean README | mitigate | Deploy snippets updated to sealedge-platform-server so operators don't invoke a non-existent binary |

No source code or crypto modifications. Attack surface = markdown prose only.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Crate READMEs + crate-internal tech docs + crates/wasm investigation</name>
  <files>crates/cli/README.md, crates/core/README.md, crates/core/AUTHENTICATION.md, crates/core/BENCHMARKS.md, crates/core/PERFORMANCE.md, crates/core/SOFTWARE_HSM_TEST_REPORT.md, crates/seal-cli/README.md, crates/seal-protocols/README.md, crates/seal-wasm/README.md, crates/seal-wasm/pkg-bundler/README.md, crates/wasm/README.md, crates/wasm/pkg-bundler/README.md, crates/experimental/pubky/README.md, crates/experimental/pubky-advanced/README.md</files>
  <read_first>
    - .planning/phases/86-documentation-sweep/86-CONTEXT.md (D-06, D-07, D-12, D-14)
    - Each file in `<files>` end-to-end
    - crates/cli/Cargo.toml, crates/core/Cargo.toml, crates/seal-cli/Cargo.toml, crates/seal-protocols/Cargo.toml, crates/seal-wasm/Cargo.toml, crates/wasm/Cargo.toml (to confirm actual crate names — the README must match what `[package] name =` declares)
  </read_first>
  <behavior>
    - Every crate README's title, description, cargo add snippet, and usage examples use the crate's actual v6.0 name (from Cargo.toml)
    - crates/core/README.md (66 hits — densest) sweeps fully
    - crates/core/AUTHENTICATION.md + BENCHMARKS.md + PERFORMANCE.md + SOFTWARE_HSM_TEST_REPORT.md update product references; benchmarks/perf numbers stay verbatim (they're historical measurements)
    - crates/wasm/ vs crates/seal-wasm/ investigation flagged in SUMMARY
    - experimental/pubky/README.md + pubky-advanced/README.md: swept; crate names `sealedge-pubky` / `sealedge-pubky-advanced`
  </behavior>
  <action>
    **Step 0 (investigation — do this FIRST):**
    Compare `crates/wasm/Cargo.toml` and `crates/seal-wasm/Cargo.toml`:
    ```bash
    grep '^name' crates/wasm/Cargo.toml crates/seal-wasm/Cargo.toml
    ls crates/wasm/src/ crates/seal-wasm/src/
    ```
    Record the findings (same crate name? different source files? duplicate content?) in a comment at the top of the SUMMARY file. Regardless of the answer, sweep both `crates/wasm/README.md` and `crates/seal-wasm/README.md` in this task — do not delete directories (per CONTEXT.md Claude's Discretion).

    **Step 1 (apply substitution rubric from 86-01 Task 1 Step C to every file in `<files>`):**

    Subs in order (longer/specific first):
    1. `trustedge-platform-server` → `sealedge-platform-server`
    2. `trustedge-trst-protocols` → `sealedge-seal-protocols`
    3. `trustedge-trst-wasm` → `sealedge-seal-wasm`
    4. `trustedge-trst-cli` → `sealedge-seal-cli`
    5. `trustedge-platform` → `sealedge-platform`
    6. `trustedge-server` → `sealedge-server`
    7. `trustedge-client` → `sealedge-client`
    8. `trustedge-pubky-advanced` → `sealedge-pubky-advanced`
    9. `trustedge-pubky` → `sealedge-pubky`
    10. `trustedge-core` → `sealedge-core`
    11. `trustedge-types` → `sealedge-types`
    12. `trustedge-wasm` → `sealedge-wasm`
    13. `trustedge-cli` → `sealedge-cli`
    14. `inspect-trst` → `inspect-seal`
    15. `TrustEdge-Labs/trustedge` (URL) → `TrustEdge-Labs/sealedge`
    16. `TRUSTEDGE_DEVICE_ID` → `SEALEDGE_DEVICE_ID`
    17. `TRUSTEDGE_SALT` → `SEALEDGE_SALT`
    18. `TRUSTEDGE-KEY-V1` → `SEALEDGE-KEY-V1`
    19. `TRUSTEDGE_ENVELOPE_V1` → `SEALEDGE_ENVELOPE_V1`
    20. `.te-attestation.json` → `.se-attestation.json`
    21. `.trst` → `.seal`
    22. `` `trst` `` and bare `trst` binary in shell blocks → `` `seal` `` / `seal`
    23. Bare `trustedge` binary in shell blocks → `sealedge` (guard against `trustedge-` prefixes and `TrustEdge-Labs`)
    24. Lowercase prose `trustedge` product name → `sealedge`
    25. Sentence-start `TrustEdge` product name → `Sealedge` (NOT `TrustEdge-Labs`, NOT `TRUSTEDGE LABS LLC`)
    26. Rust identifier `trustedge_core` (underscore form in `use` statements / type paths) → `sealedge_core`
    27. Rust identifier `trustedge_types` → `sealedge_types`
    28. Rust identifier `trustedge_platform` → `sealedge_platform`

    **Step 2 (per-file highlights):**

    - `crates/core/README.md` (66 hits): Densest. Every cargo command, every crate-name reference, every `use trustedge_core::` import, every feature-flag example. The README opens with a title `# TrustEdge Core` or similar — becomes `# Sealedge Core`. Includes a large feature-flags section; feature names (`audio`, `yubikey`, `keyring`, `git-attestation`, `insecure-tls`) are NOT brand — keep. Dependency lines `trustedge-core = "X.Y"` → `sealedge-core = "X.Y"`.

    - `crates/core/AUTHENTICATION.md` (2 hits): Auth guide. `trustedge_core::auth` → `sealedge_core::auth`.

    - `crates/core/BENCHMARKS.md` (4 hits): Benchmark results. Crate refs update; numeric benchmark data stays verbatim.

    - `crates/core/PERFORMANCE.md` (2 hits): Same pattern.

    - `crates/core/SOFTWARE_HSM_TEST_REPORT.md` (2 hits): Report. Product name mentions update; report content (test vectors, dates) stays verbatim.

    - `crates/cli/README.md` (2 hits): Thin README for the main CLI crate.

    - `crates/seal-cli/README.md` (2 hits): README for the archive/attestation CLI. This crate used to be `trustedge-trst-cli`; double-check Cargo.toml shows `name = "sealedge-seal-cli"` or similar. README cargo-install snippet and binary invocations (`seal keygen`, `seal wrap`, `seal verify`, `seal attest-sbom`, `seal verify-attestation`).

    - `crates/seal-protocols/README.md` (2 hits): Thin README. Cargo name `sealedge-seal-protocols`.

    - `crates/seal-wasm/README.md` (29 hits): WASM crate README. `trustedge-trst-wasm`/`trustedge-wasm` references (depending on old naming) → `sealedge-seal-wasm` per Cargo.toml. JS import statements `import { … } from 'trustedge-wasm'` → `from 'sealedge-wasm'` (or the actual npm package name in the Cargo.toml / package.json).

    - `crates/seal-wasm/pkg-bundler/README.md`: Bundler-specific sub-README.

    - `crates/wasm/README.md` (28 hits): Either a duplicate or distinct crate (Step 0 investigation clarifies). Sweep fully regardless.

    - `crates/wasm/pkg-bundler/README.md`: Bundler-specific.

    - `crates/experimental/pubky/README.md`: Experimental pubky crate. Cargo name `sealedge-pubky`.

    - `crates/experimental/pubky-advanced/README.md`: Cargo name `sealedge-pubky-advanced`.

    **Step 3 (per-file grep after editing):**
    ```
    grep -nE '[Tt]rustedge|\.trst\b|\.te-attestation|TRUSTEDGE_|trustedge_(core|cli|platform|wasm|types|trst)' "$f" | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|trustedgelabs\.com'
    ```
    Expected: empty. Investigate any hits.

    **Step 4 (README ↔ Cargo.toml cross-check):**
    For each crate README, verify:
    ```bash
    # Extract crate name from Cargo.toml
    CRATE_NAME=$(awk -F'"' '/^name =/ {print $2; exit}' crates/<path>/Cargo.toml)
    # Confirm README mentions that exact name
    grep -q "$CRATE_NAME" crates/<path>/README.md
    ```
    If README mentions the wrong crate name, fix it.
  </action>
  <verify>
    <automated>bash -c 'set -e; FAIL=0; for f in crates/cli/README.md crates/core/README.md crates/core/AUTHENTICATION.md crates/core/BENCHMARKS.md crates/core/PERFORMANCE.md crates/core/SOFTWARE_HSM_TEST_REPORT.md crates/seal-cli/README.md crates/seal-protocols/README.md crates/seal-wasm/README.md crates/seal-wasm/pkg-bundler/README.md crates/wasm/README.md crates/wasm/pkg-bundler/README.md crates/experimental/pubky/README.md crates/experimental/pubky-advanced/README.md; do grep -q "Project: sealedge" "$f" || { echo "FAIL header: $f"; FAIL=1; }; done; exit $FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - Every file in `<files>`: `grep -nE '[Tt]rustedge|\.trst\b|\.te-attestation|TRUSTEDGE_' "$f" | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|trustedgelabs\.com'` returns empty
    - Every file in `<files>`: `grep -q "Project: sealedge" "$f"` succeeds
    - Every file in `<files>`: `grep -q "GitHub: https://github.com/TrustEdge-Labs/sealedge" "$f"` succeeds
    - crates/core/README.md: `grep -c "sealedge-core" crates/core/README.md` returns >0
    - crates/core/README.md: `grep -c "trustedge-core" crates/core/README.md` returns 0
    - crates/seal-cli/README.md: `grep -q "seal keygen\|seal wrap\|seal verify" crates/seal-cli/README.md` succeeds
    - crates/seal-cli/README.md: `grep -c "trst keygen\|trst wrap\|trst verify" crates/seal-cli/README.md` returns 0
    - crates/seal-wasm/README.md + crates/wasm/README.md: `grep -c "trustedge-wasm" "$f"` returns 0 for each
    - Cross-check: for each crate README in `<files>` with a peer Cargo.toml, the `[package] name` value from Cargo.toml appears at least once in the README (verified by reading both)
    - SUMMARY contains a note documenting the crates/wasm vs crates/seal-wasm investigation outcome
  </acceptance_criteria>
  <done>
    14 crate-level markdown files updated. Cargo.toml ↔ README crate-name alignment verified. crates/wasm investigation findings recorded in SUMMARY. No stale brand references outside allowlist.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Example, deploy, web demo, GitHub templates, scripts README sweep</name>
  <files>examples/cam.video/README.md, deploy/digitalocean/README-deploy.md, web/demo/README.md, .github/README.md, .github/pull_request_template.md, scripts/README.md, scripts/project/README.md</files>
  <read_first>
    - .planning/phases/86-documentation-sweep/86-CONTEXT.md (D-06, D-10, D-12)
    - Each file in `<files>` end-to-end
    - examples/cam.video/Cargo.toml (to confirm crate/bin names if examples/ README snippets reference them)
  </read_first>
  <behavior>
    - `examples/cam.video/README.md` walks through the cam.video example using `seal` binary invocations, `.seal` archive, `sealedge-platform-server` endpoint URLs
    - `deploy/digitalocean/README-deploy.md` references `sealedge-platform-server` binary, `SEALEDGE_*` env vars, and the new Docker image name / repo URL
    - `web/demo/README.md` describes the demo HTML under the sealedge brand
    - `.github/README.md` and `.github/pull_request_template.md` describe the repo/product as sealedge
    - `scripts/README.md` and `scripts/project/README.md` describe the scripts under sealedge; script descriptions updated
  </behavior>
  <action>
    Apply the same 28-step substitution rubric from Task 1 Step 1 to every file in `<files>`.

    **Per-file notes:**

    - `examples/cam.video/README.md` (14 hits): Walks through recording and wrapping a video clip, submitting for verification. Every `trst` invocation → `seal`; `.trst` archive → `.seal`; `.te-attestation.json` → `.se-attestation.json`; any mention of `trustedge-platform-server` endpoint → `sealedge-platform-server`.

    - `deploy/digitalocean/README-deploy.md` (2 hits): DigitalOcean deploy guide. Binary name `trustedge-platform-server` → `sealedge-platform-server`. Docker image tags — if the doc references a specific image tag like `trustedge-labs/trustedge-platform-server:v5.x`, update the product side to `sealedge-platform-server:v6.0` (org slug stays). Env var names `TRUSTEDGE_*` → `SEALEDGE_*` if any appear.

    - `web/demo/README.md` (9 hits): Demo HTML landing page. Product references → sealedge; paths (if this file is served as part of a verify page) match the shipped demo.

    - `.github/README.md` (2 hits): Org-level meta README (rare surface — most orgs serve this at `TrustEdge-Labs/.github`). Product refs → sealedge.

    - `.github/pull_request_template.md` (2 hits): PR template. Any checklist item naming the product → sealedge. Does NOT reference crate-name specifics typically — just product-name surface.

    - `scripts/README.md` (2 hits): Script directory overview. Header + title + description. Line 108 mentions `trustedge-core/` directory (per scout earlier: "use `cargo bench` in the `trustedge-core/` directory") — this is a post-Phase-83 stale reference; the directory is now `crates/core/` per Phase 83. Update both the crate-name form AND the path form:
      - `trustedge-core/` crate-directory reference → `sealedge-core` (use crate name) OR `crates/core/` (use directory path); pick whichever the surrounding prose demands. The actual directory path is `crates/core/`.

    - `scripts/project/README.md` (1+ hits): Project-subscripts README. Header + title.

    **Per-file grep after editing** — same pattern as Task 1 Step 3.
  </action>
  <verify>
    <automated>bash -c 'set -e; FAIL=0; for f in examples/cam.video/README.md deploy/digitalocean/README-deploy.md web/demo/README.md .github/README.md .github/pull_request_template.md scripts/README.md scripts/project/README.md; do grep -q "Project: sealedge" "$f" || { echo "FAIL header: $f"; FAIL=1; }; done; exit $FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - Every file in `<files>`: `grep -nE '[Tt]rustedge|\.trst\b|\.te-attestation|TRUSTEDGE_' "$f" | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|trustedgelabs\.com'` returns empty
    - Every file in `<files>`: `grep -q "Project: sealedge" "$f"` succeeds
    - `examples/cam.video/README.md`: `grep -q "seal " examples/cam.video/README.md` OR `grep -q "\`seal\`" examples/cam.video/README.md` succeeds (binary rename landed)
    - `examples/cam.video/README.md`: `grep -q "\.seal" examples/cam.video/README.md` succeeds (new extension)
    - `examples/cam.video/README.md`: `grep -c "\.te-attestation" examples/cam.video/README.md` returns 0
    - `deploy/digitalocean/README-deploy.md`: `grep -q "sealedge-platform-server" deploy/digitalocean/README-deploy.md` succeeds
    - `deploy/digitalocean/README-deploy.md`: `grep -c "trustedge-platform-server" deploy/digitalocean/README-deploy.md` returns 0
    - `.github/pull_request_template.md`: `grep -c "trustedge" .github/pull_request_template.md` returns 0 (no product references remain) BUT the file still contains `TrustEdge-Labs` if it did before (verify with `grep -c "TrustEdge-Labs" "$f"` pre/post)
    - `scripts/README.md`: `grep -c "trustedge-core" scripts/README.md` returns 0 (stale directory reference updated)
  </acceptance_criteria>
  <done>
    7 additional markdown surfaces updated. Example walkthrough, deploy guide, demo README, GitHub templates, and script directory READMEs all describe the product as sealedge.
  </done>
</task>

</tasks>

<verification>
After both tasks complete:

```bash
# All 21 files clean of stale brand references (excluding preserved tokens)
for f in crates/cli/README.md crates/core/README.md crates/core/AUTHENTICATION.md crates/core/BENCHMARKS.md crates/core/PERFORMANCE.md crates/core/SOFTWARE_HSM_TEST_REPORT.md crates/seal-cli/README.md crates/seal-protocols/README.md crates/seal-wasm/README.md crates/seal-wasm/pkg-bundler/README.md crates/wasm/README.md crates/wasm/pkg-bundler/README.md crates/experimental/pubky/README.md crates/experimental/pubky-advanced/README.md examples/cam.video/README.md deploy/digitalocean/README-deploy.md web/demo/README.md .github/README.md .github/pull_request_template.md scripts/README.md scripts/project/README.md; do
  hits=$(grep -nE '[Tt]rustedge|\.trst\b|\.te-attestation|TRUSTEDGE_' "$f" 2>/dev/null | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|trustedgelabs\.com' | wc -l)
  [ "$hits" -ne 0 ] && echo "STALE: $f ($hits hits)"
done
# Expected: no STALE output

# Headers
for f in crates/cli/README.md crates/core/README.md crates/seal-cli/README.md examples/cam.video/README.md deploy/digitalocean/README-deploy.md scripts/README.md; do
  grep -q "Project: sealedge" "$f" || echo "MISSING header: $f"
done
# Expected: no MISSING output
```

Cross-ref: cargo build after sweep should still succeed (touching .md files doesn't break build, but `include_str!("../README.md")` patterns can move compiled strings — verify with `cargo build --workspace --locked`).

```bash
cargo build --workspace --locked
```
Expected: exit 0.
</verification>

<success_criteria>
- 21 non-root non-docs markdown surfaces updated
- Crate README ↔ Cargo.toml crate-name alignment verified
- crates/wasm vs crates/seal-wasm investigation logged in SUMMARY
- Example walkthrough uses `seal` binary, `.seal` archive, `.se-attestation.json`
- Deploy guide uses `sealedge-platform-server`
- GitHub templates use sealedge
- Workspace build still green (`cargo build --workspace --locked` exits 0)
- DOCS-01 (root overflow) / DOCS-02 (docs overflow) / DOCS-04 (examples README) all partially addressed by this plan
</success_criteria>

<output>
After completion, create `.planning/phases/86-documentation-sweep/86-03-SUMMARY.md`:
- 21 files updated
- crates/wasm vs crates/seal-wasm investigation findings (Cargo.toml `name` values, source-file diff note)
- Cargo.toml ↔ README crate-name alignment table
- Any stale references found that warranted judgment calls
</output>
