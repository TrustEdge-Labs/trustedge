---
phase: 86-documentation-sweep
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/README.md
  - docs/architecture.md
  - docs/roadmap.md
  - docs/landing-page.md
  - docs/manifest_cam_video.md
  - docs/third-party-attestation-guide.md
  - docs/yubikey-guide.md
  - docs/developer/coding-standards.md
  - docs/developer/development.md
  - docs/developer/testing.md
  - docs/developer/testing-patterns.md
  - docs/developer/wasm-testing.md
  - docs/designs/sbom-attestation-wedge.md
  - docs/hardware/SECURE_NODE_MVP.md
  - docs/legal/cla.md
  - docs/legal/copyright.md
  - docs/legal/dco.md
  - docs/legal/enterprise.md
  - docs/legal/licensing.md
  - docs/technical/format.md
  - docs/technical/protocol.md
  - docs/technical/threat-model.md
  - docs/technical/universal-backend.md
  - docs/user/authentication.md
  - docs/user/cli.md
  - docs/user/troubleshooting.md
  - docs/user/examples.md
  - docs/user/examples/README.md
  - docs/user/examples/getting-started.md
  - docs/user/examples/installation.md
  - docs/user/examples/audio.md
  - docs/user/examples/network.md
  - docs/user/examples/attestation.md
  - docs/user/examples/integration.md
  - docs/user/examples/development.md
  - docs/user/examples/backends.md
  - docs/user/examples/trst-archives.md
autonomous: true
requirements:
  - DOCS-02
tags:
  - docs
  - rebrand
  - developer-docs
must_haves:
  truths:
    - "Every .md file under docs/ describes the product as sealedge with updated crate names, binary names, file extensions, and env-var prefixes"
    - "docs/architecture.md, docs/user/cli.md, docs/developer/development.md, docs/developer/testing.md all reflect Phase 83/84/85 renames"
    - "docs/legal/** preserves TRUSTEDGE LABS LLC legal entity in attribution language while updating non-attribution product references"
    - "docs/user/examples/trst-archives.md title and content reference .seal archives (filename stays to preserve URL, but all in-content references to the archive extension use .seal)"
    - "MPL-2.0 header in every touched docs/ .md file reads `Project: sealedge` and `GitHub: https://github.com/TrustEdge-Labs/sealedge`"
  artifacts:
    - path: docs/architecture.md
      provides: "High-level architecture guide"
      contains: "sealedge-core"
    - path: docs/user/cli.md
      provides: "CLI reference"
      contains: "sealedge"
    - path: docs/user/examples/trst-archives.md
      provides: "Archive examples page"
      contains: ".seal"
    - path: docs/legal/copyright.md
      provides: "Copyright doc — entity preserved, product renamed"
      contains: "TRUSTEDGE LABS LLC"
  key_links:
    - from: docs/README.md
      to: docs/architecture.md, docs/user/cli.md, docs/developer/*.md
      via: "table of contents cross-references"
      pattern: "\\[.*\\]\\(.*\\.md\\)"
    - from: docs/user/examples/trst-archives.md
      to: "seal binary invocations in shell examples"
      via: "code blocks demonstrating archive operations"
      pattern: "seal (keygen|wrap|verify|unwrap)"
---

<objective>
Sweep all of `docs/**` (37 markdown files across 7 subtrees) so every developer/user doc reflects the sealedge product name, new crate/binary names, new file extensions, and new env-var prefixes. Apply entity-vs-product discipline to `docs/legal/**` — `TRUSTEDGE LABS LLC` legal entity stays; product references update per casing rules.

Purpose: `docs/**` is the developer and user manual. DOCS-02 requires these to reflect Phase 83/84/85 renames; a reader who lands on `docs/user/cli.md` must see the same binary names as `CLAUDE.md` and the shipped binaries.
Output: 37 updated markdown files across `docs/`, `docs/developer/`, `docs/designs/`, `docs/hardware/`, `docs/legal/`, `docs/technical/`, `docs/user/`, and `docs/user/examples/`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/phases/86-documentation-sweep/86-CONTEXT.md
@.planning/phases/85-code-sweep-headers-text-metadata/85-CONTEXT.md
@CLAUDE.md

<interfaces>
<!-- Same rename mappings as 86-01-PLAN.md — see that plan's <interfaces> block for full tables. -->
<!-- Summary reminder: -->

Crate rename: `trustedge-*` → `sealedge-*`
Binary rename: `trustedge`→`sealedge`, `trst`→`seal`, `trustedge-{server,client,platform-server}`→`sealedge-{server,client,platform-server}`, `inspect-trst`→`inspect-seal`
Extension rename: `.trst`→`.seal`, `.te-attestation.json`→`.se-attestation.json`
Env-var rename: `TRUSTEDGE_*`→`SEALEDGE_*`
URL rename: `github.com/TrustEdge-Labs/trustedge` → `github.com/TrustEdge-Labs/sealedge`
Wire constants (for prose that quotes them): `TRUSTEDGE-KEY-V1`→`SEALEDGE-KEY-V1`, `TRUSTEDGE_ENVELOPE_V1`→`SEALEDGE_ENVELOPE_V1`, etc. (see MIGRATION.md v6.0 section for full table)

Preserve UNCHANGED:
  TrustEdge-Labs          (org)
  TRUSTEDGE LABS LLC      (legal entity — all of docs/legal/**)
  trustedgelabs.com       (domain)
  Generic "trust"/"edge"  (trust boundary, edge device, etc.)

MPL-2.0 header template (every docs/**/*.md opens with this HTML comment):
  <!--
  Copyright (c) 2025 TRUSTEDGE LABS LLC
  MPL-2.0: https://mozilla.org/MPL/2.0/
  Project: sealedge — Privacy and trust at the edge.
  GitHub: https://github.com/TrustEdge-Labs/sealedge
  -->
</interfaces>

<subtree_strategy>
Files split into three tasks by subtree nature:

Task 1 (bulk straight-sweep, 26 files): docs top level + developer/ + designs/ + hardware/ + technical/ + user/* (non-examples)
Task 2 (examples subtree, 11 files): docs/user/examples/** — highest density of shell snippets, .trst/.te-attestation mentions
Task 3 (legal subtree, 5 files): docs/legal/** — entity-vs-product discipline (D-05); per-occurrence judgment required

This split keeps per-task scope manageable and isolates the legal subtree's unique discipline from bulk sweeps.
</subtree_strategy>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| developer ← docs/** | Developer trusts docs to reflect actual crate/binary names. Mismatch = broken onboarding, failed cargo commands. |
| user ← docs/user/** | User trusts CLI docs to match shipped binary. Mismatch = CLI exit codes, confusion. |
| legal review ← docs/legal/** | Legal reviewer trusts attribution language. Mistakenly renaming `TRUSTEDGE LABS LLC` = corrupted attribution. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-86-10   | Tampering | docs/legal/** | mitigate | Task 3 applies per-occurrence judgment; acceptance criterion asserts `TRUSTEDGE LABS LLC` count preserved |
| T-86-11   | Information disclosure | docs/technical/threat-model.md | accept | Technical threat model text updates but content unchanged; prose-only rename |
| T-86-12   | Repudiation | docs/legal/copyright.md | mitigate | Entity vs product distinction enforced; post-sweep grep confirms entity preserved |

No source code, auth paths, or crypto primitives are modified. Attack surface = markdown prose only.
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Bulk sweep — docs top level, developer/, designs/, hardware/, technical/, user/*</name>
  <files>docs/README.md, docs/architecture.md, docs/roadmap.md, docs/landing-page.md, docs/manifest_cam_video.md, docs/third-party-attestation-guide.md, docs/yubikey-guide.md, docs/developer/coding-standards.md, docs/developer/development.md, docs/developer/testing.md, docs/developer/testing-patterns.md, docs/developer/wasm-testing.md, docs/designs/sbom-attestation-wedge.md, docs/hardware/SECURE_NODE_MVP.md, docs/technical/format.md, docs/technical/protocol.md, docs/technical/threat-model.md, docs/technical/universal-backend.md, docs/user/authentication.md, docs/user/cli.md, docs/user/troubleshooting.md, docs/user/examples.md</files>
  <read_first>
    - .planning/phases/86-documentation-sweep/86-CONTEXT.md (D-04, D-12)
    - Each file in `<files>` end-to-end before editing it
  </read_first>
  <behavior>
    - Every MPL-2.0 header becomes the sealedge form (Project + GitHub lines)
    - Every product-identifying `trustedge`/`TrustEdge` mention becomes sealedge/Sealedge per casing
    - Every binary name, crate name, file extension, env var, wire constant, and repo URL updates per the rename map
    - `TrustEdge-Labs` org, `TRUSTEDGE LABS LLC` entity, and `trustedgelabs.com` domain stay unchanged
    - `docs/designs/sbom-attestation-wedge.md` gets D-02-style hybrid treatment if the doc narrates past intent (header updates; design-history body preserved where it describes what shipped at the time)
  </behavior>
  <action>
    Apply the same 9-step substitution rubric from 86-01-PLAN.md Task 1 to every file in `<files>`. Specifically, for each file:

    **Step A (header):**
    - Replace the MPL-2.0 header's `Project: trustedge — Privacy and trust at the edge.` with `Project: sealedge — Privacy and trust at the edge.`
    - Replace `GitHub: https://github.com/TrustEdge-Labs/trustedge` with `GitHub: https://github.com/TrustEdge-Labs/sealedge`

    **Step B (title):**
    - `# TrustEdge <X>` → `# Sealedge <X>` (preserve subheader structure)

    **Step C (straight substitutions in order — do the longer / more specific replacements before the shorter ones to avoid stacking):**

    1. `trustedge-platform-server` → `sealedge-platform-server`
    2. `trustedge-trst-protocols` → `sealedge-seal-protocols`
    3. `trustedge-trst-wasm` → `sealedge-seal-wasm`
    4. `trustedge-trst-cli` → `sealedge-seal-cli`
    5. `trustedge-platform` → `sealedge-platform`
    6. `trustedge-server` → `sealedge-server`
    7. `trustedge-client` → `sealedge-client`
    8. `trustedge-core` → `sealedge-core`
    9. `trustedge-types` → `sealedge-types`
    10. `trustedge-wasm` → `sealedge-wasm`
    11. `trustedge-cli` → `sealedge-cli`
    12. `inspect-trst` → `inspect-seal`
    13. `TrustEdge-Labs/trustedge` (URL) → `TrustEdge-Labs/sealedge`
    14. `TRUSTEDGE_DEVICE_ID` → `SEALEDGE_DEVICE_ID`
    15. `TRUSTEDGE_SALT` → `SEALEDGE_SALT`
    16. `TRUSTEDGE-KEY-V1` → `SEALEDGE-KEY-V1`
    17. `TRUSTEDGE_ENVELOPE_V1` → `SEALEDGE_ENVELOPE_V1`
    18. `.te-attestation.json` → `.se-attestation.json`
    19. `.trst` → `.seal` (archive extension — BEFORE `trst` binary so trailing-period forms resolve first)
    20. Backticked bare binary `` `trst` `` → `` `seal` ``
    21. Standalone `trst` binary in shell snippets (e.g., `cargo run -p ... -- ...`, `./target/release/trst`, `$ trst …`) → `seal`
    22. Bare binary `trustedge` in shell/code blocks → `sealedge` (careful: NOT `trustedge-<X>` crate names — those are already handled above; NOT `TrustEdge-Labs`; NOT `trustedgelabs.com`)
    23. Lowercase prose `trustedge` (mid-sentence product name) → `sealedge`
    24. Sentence-start or title-cased product name `TrustEdge` (NOT `TrustEdge-Labs`, NOT `TRUSTEDGE LABS LLC`) → `Sealedge`

    **Step D (per-file verifications — check these specific items by filename):**

    - `docs/architecture.md`: ~30 hits. Includes architecture diagrams textually describing "trustedge-core contains …" — becomes "sealedge-core contains …". Also `crates/trustedge-core/src/...` path references. Inspect `crates/` layout: Phase 83 renamed crate directories; path patterns in this doc may need both the crate-name segment AND the path-segment renamed (e.g., `crates/trustedge-core/` → `crates/core/` if the directory was renamed). Check actual filesystem with `ls crates/` and update path prose to match.

    - `docs/user/cli.md`: 26 hits. Contains shell snippets — every `trst` invocation becomes `seal`; every `trustedge` invocation becomes `sealedge`; every env var prefix becomes `SEALEDGE_*`.

    - `docs/developer/testing.md`: 92 hits — the densest file. Lots of `cargo test -p trustedge-core` → `cargo test -p sealedge-core`; `cargo test -p trustedge-platform` → `cargo test -p sealedge-platform`.

    - `docs/developer/wasm-testing.md`: 17 hits. Likely references `trustedge-wasm` crate and `trustedge-trst-wasm` — map to `sealedge-wasm` and `sealedge-seal-wasm`.

    - `docs/technical/format.md` + `docs/technical/protocol.md`: wire format / protocol specs. If the doc quotes specific wire-format constants (`TRUSTEDGE-KEY-V1`, `TRUSTEDGE_ENVELOPE_V1`, `b"TRST"` magic), replace with the v6.0 values per the rename map. Magic bytes `TRST` → `SEAL` when discussed in format-spec context. If the doc has a "historical format" section (similar to CHANGELOG D-02 hybrid), leave historical-description paragraphs intact and add a brief note; most likely it's a forward-only spec in which case straight sweep is correct.

    - `docs/technical/threat-model.md`: preserve "trust boundary", "edge device" generic terms; update product mentions only.

    - `docs/designs/sbom-attestation-wedge.md`: 6 hits. This is a design doc for an already-shipped feature (v5.0 SBOM attestation). Apply D-02-style hybrid: header → sealedge; title → Sealedge; body descriptions of "what was built" can be updated to sealedge terms (this is a design doc, not a changelog entry); any quoted release-note language from v5.0 can stay historical.

    - `docs/hardware/SECURE_NODE_MVP.md`: 2 hits, minimal sweep.

    - `docs/roadmap.md`: if it references historical project phases using "trustedge" name, apply judgment — a forward-looking roadmap uses `sealedge`; a historical archive uses hybrid treatment.

    - `docs/landing-page.md`: marketing copy for trustedgelabs.com content. Product references → Sealedge; domain `trustedgelabs.com` stays.

    - `docs/user/examples.md` (the overview file, not the `examples/` subdir — handled in Task 2).

    **Step E (final per-file grep):** After editing each file, run:
    ```
    grep -nE '[Tt]rustedge|\.trst\b|\.te-attestation|TRUSTEDGE_' "$f" | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|trustedgelabs\.com'
    ```
    Expected: empty output. If output appears, investigate — it may be a generic-term false positive (keep), a historical-preservation case (hybrid), or a missed substitution (fix).
  </action>
  <verify>
    <automated>bash -c 'set -e; FAIL=0; for f in docs/README.md docs/architecture.md docs/roadmap.md docs/landing-page.md docs/manifest_cam_video.md docs/third-party-attestation-guide.md docs/yubikey-guide.md docs/developer/coding-standards.md docs/developer/development.md docs/developer/testing.md docs/developer/testing-patterns.md docs/developer/wasm-testing.md docs/designs/sbom-attestation-wedge.md docs/hardware/SECURE_NODE_MVP.md docs/technical/format.md docs/technical/protocol.md docs/technical/threat-model.md docs/technical/universal-backend.md docs/user/authentication.md docs/user/cli.md docs/user/troubleshooting.md docs/user/examples.md; do grep -q "Project: sealedge" "$f" || { echo "FAIL header: $f"; FAIL=1; }; done; exit $FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - For every file in `<files>`: `grep -nE '[Tt]rustedge|\.trst\b|\.te-attestation|TRUSTEDGE_' "$f" | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|trustedgelabs\.com'` returns empty output
    - For every file in `<files>`: `grep -q "Project: sealedge" "$f"` succeeds (header updated)
    - For every file in `<files>`: `grep -q "GitHub: https://github.com/TrustEdge-Labs/sealedge" "$f"` succeeds
    - `grep -c "trustedge-core\|trustedge-platform\|trustedge-cli\|trustedge-wasm\|trustedge-types\|trustedge-trst" docs/architecture.md docs/developer/*.md docs/technical/*.md` returns 0 across all listed files
    - `grep -c "sealedge-core" docs/architecture.md` returns >0 (forward substitution applied)
    - `docs/developer/testing.md` — `grep -c "trustedge" docs/developer/testing.md` returns 0 (the densest file is clean)
    - `docs/user/cli.md` — `grep -q " seal " docs/user/cli.md` or `grep -q "\`seal\`" docs/user/cli.md` succeeds (binary rename landed)
    - `docs/technical/format.md` — if the file quoted magic bytes `TRST`, `grep -q "SEAL" docs/technical/format.md` succeeds and `grep -c "TRST" docs/technical/format.md` returns 0 (or the occurrences appear in a historical-preservation paragraph)
  </acceptance_criteria>
  <done>
    22 files updated with full rename map applied. Each file passes the per-file stale-ref grep check. Headers all sealedge.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Examples subtree sweep (docs/user/examples/**)</name>
  <files>docs/user/examples/README.md, docs/user/examples/getting-started.md, docs/user/examples/installation.md, docs/user/examples/audio.md, docs/user/examples/network.md, docs/user/examples/attestation.md, docs/user/examples/integration.md, docs/user/examples/development.md, docs/user/examples/backends.md, docs/user/examples/trst-archives.md</files>
  <read_first>
    - .planning/phases/86-documentation-sweep/86-CONTEXT.md (D-04)
    - Each file in `<files>` end-to-end before editing it
    - README.md at repo root (for cross-reference consistency — this Task runs in Wave 1 parallel, so README.md may or may not be done; treat it as ground truth for command patterns either way since both should converge)
  </read_first>
  <behavior>
    - Every shell snippet invokes the new binary names (`sealedge`, `seal`, `sealedge-server`, `sealedge-client`, `sealedge-platform-server`)
    - Every archive example uses `.seal` extension; every attestation example uses `.se-attestation.json`
    - Every env-var example uses `SEALEDGE_*` prefix
    - `docs/user/examples/trst-archives.md` filename stays (URL preservation) but in-body archive-extension references update to `.seal`; page title may keep "Archives" or become "Seal Archives" per editor judgment — grep verifies the file body has no `.trst` hits
    - Every crate reference in `cargo test`/`cargo build`/`cargo run -p` updates to `sealedge-<X>`
  </behavior>
  <action>
    Apply the same 9-step substitution rubric (Task 1 Step C) to every file in `<files>`. Examples files are the highest density of shell snippets — expect many hits per file.

    **Per-file specific notes:**

    - `docs/user/examples/getting-started.md`: Likely the first doc a user touches. Every `cargo install trustedge-cli` or `cargo run -p trustedge-cli` → `cargo install sealedge-cli` / `cargo run -p sealedge-cli`. Every `trst keygen`/`trst wrap`/`trst verify` → `seal keygen`/`seal wrap`/`seal verify`. Binary paths `./target/release/trustedge`, `./target/release/trst` → `./target/release/sealedge`, `./target/release/seal`.

    - `docs/user/examples/installation.md`: `cargo install` snippets per crate.

    - `docs/user/examples/attestation.md`: Attestation workflow. `trst attest-sbom` → `seal attest-sbom`; `trst verify-attestation` → `seal verify-attestation`; `.te-attestation.json` → `.se-attestation.json` (multiple occurrences expected).

    - `docs/user/examples/network.md`: `trustedge-server` / `trustedge-client` → `sealedge-server` / `sealedge-client`. Auth env vars `TRUSTEDGE_*` → `SEALEDGE_*`.

    - `docs/user/examples/audio.md`: `cargo build -p trustedge-core --features audio` → `cargo build -p sealedge-core --features audio`. `cargo build --features audio -p trustedge-cli` → `cargo build --features audio -p sealedge-cli`.

    - `docs/user/examples/backends.md`: YubiKey + software HSM examples. Crate refs `trustedge-core` → `sealedge-core`. Feature flag names stay (e.g., `yubikey`, `keyring` — those are feature names, not brand).

    - `docs/user/examples/integration.md`: Example of embedding sealedge in another Rust app. `use trustedge_core::…` (underscore identifier form) → `use sealedge_core::…`. `[dependencies] trustedge-core = …` → `[dependencies] sealedge-core = …`.

    - `docs/user/examples/development.md`: Dev workflow. Build/test commands per the rename map.

    - `docs/user/examples/trst-archives.md`: **Filename stays** (URL preservation — internal links and any external bookmarks). Page title `# TrustEdge Archives` or similar → `# Sealedge Archives` (or `# Seal Archives` — executor's judgment, but must contain "sealedge" or "seal" branding). Body content: `.trst` → `.seal`; `trst` binary → `seal`; `TRST` magic bytes → `SEAL` if quoted; archive-structure diagrams `clip-<id>.trst/` → `clip-<id>.seal/`.

    - `docs/user/examples/README.md`: Index/TOC for the examples subdir. Cross-link `trst-archives.md` link text may update to "Seal Archives" even if the filename stays.

    **Step E (per-file grep, same as Task 1)** — after editing each, confirm zero stale hits.
  </action>
  <verify>
    <automated>bash -c 'set -e; FAIL=0; for f in docs/user/examples/README.md docs/user/examples/getting-started.md docs/user/examples/installation.md docs/user/examples/audio.md docs/user/examples/network.md docs/user/examples/attestation.md docs/user/examples/integration.md docs/user/examples/development.md docs/user/examples/backends.md docs/user/examples/trst-archives.md; do grep -q "Project: sealedge" "$f" || { echo "FAIL header: $f"; FAIL=1; }; done; exit $FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - For every file in `<files>`: `grep -nE '[Tt]rustedge|\.trst\b|\.te-attestation|TRUSTEDGE_' "$f" | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|trustedgelabs\.com'` returns empty output
    - `grep -c "\.seal" docs/user/examples/trst-archives.md` returns >0 (archive extension updated in body)
    - `grep -c "\.trst" docs/user/examples/trst-archives.md` returns 0 (no stale extension in body — filename excluded from body grep)
    - `grep -c "seal " docs/user/examples/getting-started.md` returns >0 OR `grep -q "\`seal\`" docs/user/examples/getting-started.md` succeeds (binary rename landed)
    - `grep -c "\.se-attestation\.json" docs/user/examples/attestation.md` returns >0
    - `grep -c "\.te-attestation\.json" docs/user/examples/attestation.md` returns 0
    - `grep -c "sealedge-server\|sealedge-client" docs/user/examples/network.md` returns >0
    - All 10 files have `Project: sealedge` header line
  </acceptance_criteria>
  <done>
    10 examples files updated. Archive extension, attestation extension, binary names, crate names, and env vars all use v6.0 names. `trst-archives.md` filename preserved for URL stability; body content updated to `.seal`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Legal subtree sweep (entity-vs-product discipline)</name>
  <files>docs/legal/cla.md, docs/legal/copyright.md, docs/legal/dco.md, docs/legal/enterprise.md, docs/legal/licensing.md</files>
  <read_first>
    - .planning/phases/86-documentation-sweep/86-CONTEXT.md (D-05 — legal discipline)
    - .planning/phases/85-code-sweep-headers-text-metadata/85-CONTEXT.md (D-03 entity preservation)
    - Each file in `<files>` end-to-end before editing it (per-occurrence judgment required)
  </read_first>
  <behavior>
    - `TRUSTEDGE LABS LLC` (and its capitalization variants used as legal-entity name) stays unchanged wherever it appears in attribution, copyright notices, license grants, or CLA/DCO sign-off language
    - Product references (non-attribution) — phrases like "the TrustEdge project", "TrustEdge software", "TrustEdge users" — update to sealedge/Sealedge per casing rules
    - MPL-2.0 header block's `Project: trustedge` → `Project: sealedge` (this is a product reference, not an entity attribution)
    - MPL-2.0 header block's `Copyright (c) 2025 TRUSTEDGE LABS LLC` line stays exactly
    - File titles may update (`# TrustEdge CLA` → `# Sealedge CLA`) because they name the product not the entity
    - If a legal doc cites past product names in legal-binding text (e.g., "version 1.0 of TrustEdge") add a clarifying note explaining the rename, do not rewrite historical version citations
  </behavior>
  <action>
    For EACH of the 5 files, walk line-by-line. This is not a bulk sed task — every occurrence of `TrustEdge` (without the `-Labs` suffix and not inside `TRUSTEDGE LABS LLC`) requires judgment: entity context or product context.

    **Decision rule:**
    - Is the phrase identifying the LEGAL ENTITY (e.g., "licensed to TRUSTEDGE LABS LLC", "assigned to TRUSTEDGE LABS LLC", signing attribution, CLA assignment)? **KEEP UNCHANGED.**
    - Is the phrase identifying the PRODUCT (e.g., "the TrustEdge software", "users of TrustEdge", "TrustEdge's codebase")? **UPDATE to sealedge/Sealedge per casing.**
    - Is the phrase a generic proper noun for the organization family (e.g., "the TrustEdge project"/"TrustEdge-Labs")? **Treat "TrustEdge project" as product (rename to "Sealedge project"); "TrustEdge-Labs" as org (preserve).**

    **Per-file notes:**

    - `docs/legal/copyright.md` (6 hits): Most density. Preserve every `TRUSTEDGE LABS LLC` copyright line. The MPL-2.0 header at top gets sealedge. Any body text describing "TrustEdge copyright policy" → "Sealedge copyright policy". Any quotation of a copyright notice used elsewhere in the repo — these are templates that the repo itself uses; they should match the current repo state. Look at the current `scripts/fix-copyright.sh` and the actual `.rs` headers (post-Phase 85): they show `Project: sealedge` with `Copyright (c) 2025 TRUSTEDGE LABS LLC`. This doc's example blocks should show the same.

    - `docs/legal/cla.md` (3 hits): Contributor License Agreement. Attribution ("You hereby grant TRUSTEDGE LABS LLC…") stays. Product-scope references ("contributions to the TrustEdge project") become "contributions to the Sealedge project".

    - `docs/legal/dco.md` (2 hits): Developer Certificate of Origin. Similar pattern — entity attribution stays, product references update.

    - `docs/legal/enterprise.md` (5 hits): Enterprise licensing. Product name in marketing/description context → Sealedge; entity in licensee language → TRUSTEDGE LABS LLC stays.

    - `docs/legal/licensing.md` (4 hits): License overview. MPL grant language stays verbatim; entity stays; product name updates.

    Apply the 9-step substitution rubric from Task 1 Step C but WITH the entity-preservation rule overriding any auto-substitution. Use Edit tool per-occurrence (not sed), since the entity-vs-product distinction requires judgment at each hit.
  </action>
  <verify>
    <automated>bash -c 'set -e; FAIL=0; for f in docs/legal/cla.md docs/legal/copyright.md docs/legal/dco.md docs/legal/enterprise.md docs/legal/licensing.md; do grep -q "Project: sealedge" "$f" || { echo "FAIL header: $f"; FAIL=1; }; grep -q "TRUSTEDGE LABS LLC" "$f" || { echo "FAIL entity: $f"; FAIL=1; }; done; exit $FAIL'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "Project: sealedge" docs/legal/cla.md docs/legal/copyright.md docs/legal/dco.md docs/legal/enterprise.md docs/legal/licensing.md` returns 1 for each file (header updated)
    - `grep -c "TRUSTEDGE LABS LLC" docs/legal/cla.md docs/legal/copyright.md docs/legal/dco.md docs/legal/enterprise.md docs/legal/licensing.md` returns ≥1 for each file (legal entity preserved — at minimum the MPL-2.0 header's copyright line)
    - `grep -c "TRUSTEDGE LABS LLC" docs/legal/copyright.md` returns the same count it did pre-edit (use pre-edit count from read_first phase as baseline — entity occurrences NOT decreased)
    - `docs/legal/cla.md`: `grep -c "TrustEdge project" docs/legal/cla.md` returns 0 AND `grep -c "Sealedge project" docs/legal/cla.md` returns >0 (product-context phrasing updated)
    - `grep -nE 'TrustEdge[^-]' docs/legal/cla.md docs/legal/copyright.md docs/legal/dco.md docs/legal/enterprise.md docs/legal/licensing.md | grep -v 'TRUSTEDGE LABS LLC' | grep -v 'TrustEdge-Labs'` returns empty — any remaining `TrustEdge` must be either the `-Labs` org or the `LABS LLC` entity
    - No verbatim legal-license text (e.g., the MPL-2.0 preamble quoted inside the doc) is altered except where it contains a product-reference line like `Project: trustedge`
  </acceptance_criteria>
  <done>
    5 legal files updated. `TRUSTEDGE LABS LLC` preserved (count unchanged from pre-edit). Product-context `TrustEdge` references renamed to `Sealedge`. Headers updated. Grep shows no unclassified `TrustEdge` remnants.
  </done>
</task>

</tasks>

<verification>
After all three tasks complete:

```bash
# Full subtree grep — expect empty except where body content intentionally quotes old constants in "historical" / "before" columns
grep -rnE '[Tt]rustedge|\.trst\b|\.te-attestation|TRUSTEDGE_' docs/ \
  | grep -vE 'TrustEdge-Labs|TRUSTEDGE LABS LLC|trustedgelabs\.com|docs/user/examples/trst-archives\.md:[0-9]+:(File|source)'

# Header check across all 37 files
for f in $(git ls-files 'docs/**/*.md'); do
  grep -q "Project: sealedge" "$f" || echo "MISSING header: $f"
done
# Expected: no MISSING output

# Legal subtree entity-preservation check
grep -c 'TRUSTEDGE LABS LLC' docs/legal/*.md
# Expected: every file >=1
```
</verification>

<success_criteria>
- All 37 `docs/**/*.md` files swept; every MPL-2.0 header reads `Project: sealedge` with the updated GitHub URL
- Every cargo/binary/env-var/extension reference reflects Phase 83/84/85 renames
- `docs/legal/**` preserves `TRUSTEDGE LABS LLC` entity wherever it appears (count unchanged)
- `docs/user/examples/trst-archives.md` filename preserved but body uses `.seal`
- DOCS-02 requirement addressed
</success_criteria>

<output>
After completion, create `.planning/phases/86-documentation-sweep/86-02-SUMMARY.md`:
- 37 files updated across 7 subtrees
- Task-level hit counts (bulk vs examples vs legal)
- Entity-preservation spot checks for legal subtree (pre/post-edit TRUSTEDGE LABS LLC count)
- Any ambiguous entity-vs-product calls the executor made and flagged for Plan 05's audit
- `docs/user/examples/trst-archives.md` filename-stability note
</output>
