---
phase: 83-crate-and-binary-rename
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/core/Cargo.toml
  - crates/core/src/lib.rs
  - crates/core/src/bin/trustedge-server.rs
  - crates/core/src/bin/trustedge-client.rs
  - crates/core/src/bin/inspect-trst.rs
  - crates/types/Cargo.toml
  - crates/platform/Cargo.toml
  - crates/platform-server/Cargo.toml
  - crates/platform-server/src/main.rs
  - crates/trustedge-cli/Cargo.toml
  - crates/trustedge-cli/src/main.rs
  - crates/wasm/Cargo.toml
  - crates/trst-protocols/Cargo.toml
  - crates/trst-cli/Cargo.toml
  - crates/trst-cli/src/main.rs
  - crates/trst-cli/tests/acceptance.rs
  - crates/trst-cli/tests/integration_tests.rs
  - crates/trst-wasm/Cargo.toml
  - examples/cam.video/Cargo.toml
  - examples/cam.video/record_and_wrap.rs
  - examples/cam.video/verify_cli.rs
  - (plus all other .rs files in crates/core, crates/types, crates/platform, crates/platform-server, crates/wasm, crates/trustedge-cli, crates/trst-cli, crates/trst-wasm, crates/trst-protocols with use trustedge_{core,types,platform,wasm} imports — roughly 60 files)
  - (directory rename) crates/trustedge-cli/ -> crates/cli/
autonomous: true
requirements: [REBRAND-01, REBRAND-02]

must_haves:
  truths:
    - "cargo metadata shows zero packages whose name starts with trustedge- (non-trst)"
    - "cargo check --workspace --locked compiles clean"
    - "cargo test --workspace passes all existing tests"
    - "Binary target names are sealedge, sealedge-server, sealedge-client, sealedge-platform-server (plus unchanged trst which renames in Plan 02)"
    - "No .rs file in the real tree contains use trustedge_core, use trustedge_types, use trustedge_platform, or use trustedge_wasm"
  artifacts:
    - path: "crates/cli/Cargo.toml"
      provides: "Renamed trustedge-cli crate at normalized short dir"
      contains: 'name = "sealedge-cli"'
    - path: "crates/core/src/bin/sealedge-server.rs"
      provides: "Renamed trustedge-server binary source"
    - path: "crates/core/src/bin/sealedge-client.rs"
      provides: "Renamed trustedge-client binary source"
    - path: "Cargo.toml"
      provides: "Workspace root with updated members list and sealedge-types workspace.dependencies entry"
      contains: '"crates/cli"'
  key_links:
    - from: "crates/cli/Cargo.toml [[bin]]"
      to: "src/main.rs"
      via: "path field unchanged but name = sealedge"
      pattern: 'name = "sealedge"'
    - from: "crates/core/Cargo.toml [[bin]] blocks"
      to: "src/bin/sealedge-server.rs and src/bin/sealedge-client.rs"
      via: "path fields reference renamed source files"
      pattern: 'path = "src/bin/sealedge-server.rs"'
    - from: "every crate Cargo.toml [dependencies]"
      to: "renamed workspace crate names"
      via: "inter-crate dep keys"
      pattern: 'sealedge-(core|types|platform|wasm) = '
---

<objective>
Rename the six non-trst root-workspace Cargo packages from `trustedge-*` to `sealedge-*` in one workspace-coherent atomic change, including binary targets, binary source filenames, crate directory normalization, inter-crate deps, workspace members, and all `use trustedge_*` identifier sweeps across the entire tree (including consumers in the not-yet-renamed trst-* crates). The workspace must build and test green at the commit boundary.

Purpose: Foundational blast radius for the v6.0 rebrand — all downstream plans depend on the core crate rename landing first.

Output: 6 root crates renamed (`trustedge-core`→`sealedge-core`, `trustedge-types`→`sealedge-types`, `trustedge-platform`→`sealedge-platform`, `trustedge-platform-server`→`sealedge-platform-server`, `trustedge-cli`→`sealedge-cli`, `trustedge-wasm`→`sealedge-wasm`), 4 binaries renamed (`trustedge`→`sealedge`, `trustedge-server`→`sealedge-server`, `trustedge-client`→`sealedge-client`, `trustedge-platform-server`→`sealedge-platform-server`), `crates/trustedge-cli/` directory normalized to `crates/cli/`, 2 bin source files renamed, ~60 `.rs` files updated with `use sealedge_*` identifiers.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/83-crate-and-binary-rename/83-CONTEXT.md
@.planning/phases/83-crate-and-binary-rename/83-PATTERNS.md
@CLAUDE.md
</context>

<interfaces>
<!-- Token transformation table (mechanical substitutions for this plan). -->
<!-- Out of scope for Plan 01: trst-* family crates (Plan 02), .trst extension literals (Plan 03), -->
<!-- package.json files (Plan 04), CI workflows / scripts (Plan 05), experimental crates (Plan 06). -->

Package name (Cargo.toml [package] name field):
  trustedge-core            -> sealedge-core
  trustedge-types           -> sealedge-types
  trustedge-platform        -> sealedge-platform
  trustedge-platform-server -> sealedge-platform-server
  trustedge-cli             -> sealedge-cli
  trustedge-wasm            -> sealedge-wasm

Library name (Cargo.toml [lib] name field; source code use statements):
  trustedge_core     -> sealedge_core
  trustedge_types    -> sealedge_types
  trustedge_platform -> sealedge_platform
  trustedge_wasm     -> sealedge_wasm

Binary target ([[bin]] name + matching src/bin/*.rs filename):
  trustedge                    -> sealedge                      (crates/cli/Cargo.toml, src/main.rs stays)
  trustedge-server             -> sealedge-server               (crates/core/Cargo.toml, src/bin/sealedge-server.rs)
  trustedge-client             -> sealedge-client               (crates/core/Cargo.toml, src/bin/sealedge-client.rs)
  trustedge-platform-server    -> sealedge-platform-server      (crates/platform-server/Cargo.toml, src/main.rs stays)

Crate directory rename:
  crates/trustedge-cli/ -> crates/cli/

Leave unchanged in Plan 01 (belong to Plan 02 or later plans):
  trustedge-trst-protocols / trustedge-trst-cli / trustedge-trst-wasm (Plan 02)
  trustedge-pubky / trustedge-pubky-advanced (Plan 06 experimental)
  trustedge-cam-video-examples (examples crate: package name stays in this plan's scope since it imports trustedge_core — the PACKAGE rename is scoped to Plan 02 alongside other cam-video decisions; HOWEVER its `use trustedge_core` imports DO update here because sealedge_core is the new lib name. Keep its own package name `trustedge-cam-video-examples` for now — Plan 02 will rename it.)
  software-hsm-demo (binary, does not contain trustedge token)
  All trst-cli test files (will still compile under this plan because they import use trustedge_core which gets renamed to use sealedge_core — their own package name and dir stay trustedge-trst-cli until Plan 02)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Rename 6 root crate packages, bin targets, bin source files, and crate directory</name>
  <read_first>
    - Cargo.toml (workspace root)
    - crates/core/Cargo.toml
    - crates/types/Cargo.toml
    - crates/platform/Cargo.toml
    - crates/platform-server/Cargo.toml
    - crates/trustedge-cli/Cargo.toml
    - crates/wasm/Cargo.toml
    - .planning/phases/83-crate-and-binary-rename/83-CONTEXT.md (decisions D-01, D-03, and Claude's Discretion on dir normalization)
    - .planning/phases/83-crate-and-binary-rename/83-PATTERNS.md §"File Inventory by Rename Category" Categories 1-4, 8
  </read_first>
  <files>
    Cargo.toml,
    crates/core/Cargo.toml,
    crates/core/src/bin/trustedge-server.rs (rename to sealedge-server.rs),
    crates/core/src/bin/trustedge-client.rs (rename to sealedge-client.rs),
    crates/types/Cargo.toml,
    crates/platform/Cargo.toml,
    crates/platform-server/Cargo.toml,
    crates/trustedge-cli/ (rename directory to crates/cli/),
    crates/cli/Cargo.toml (after dir rename),
    crates/wasm/Cargo.toml
  </files>
  <action>
Execute the following substitutions exactly.

**1. Rename crate directory:**
```
git mv crates/trustedge-cli crates/cli
```

**2. Update workspace root `Cargo.toml`:**
- Replace the `members` list entry `"crates/trustedge-cli"` with `"crates/cli"`. Keep all other members unchanged (the trst-* crates still live at their old paths — they rename in Plan 02).
- In `[workspace.dependencies]`, replace the key `trustedge-types` with `sealedge-types` (path `crates/types` stays).

**3. `crates/core/Cargo.toml` edits:**
- `[package] name = "trustedge-core"` -> `name = "sealedge-core"`
- `[lib] name = "trustedge_core"` -> `name = "sealedge_core"` (if the field is present; otherwise Cargo derives it from package name).
- In the two `[[bin]]` blocks for trustedge-server and trustedge-client:
  - `name = "trustedge-server"` -> `name = "sealedge-server"` and `path = "src/bin/trustedge-server.rs"` -> `path = "src/bin/sealedge-server.rs"`
  - `name = "trustedge-client"` -> `name = "sealedge-client"` and `path = "src/bin/trustedge-client.rs"` -> `path = "src/bin/sealedge-client.rs"`
- Leave `software-hsm-demo` block untouched.
- In `[dependencies]`: `trustedge-trst-protocols = { path = "../trst-protocols" }` stays (Plan 02 renames this key and path). `trustedge-types = { workspace = true }` -> `sealedge-types = { workspace = true }`.

**4. Rename binary source files:**
```
git mv crates/core/src/bin/trustedge-server.rs crates/core/src/bin/sealedge-server.rs
git mv crates/core/src/bin/trustedge-client.rs crates/core/src/bin/sealedge-client.rs
```

**5. `crates/types/Cargo.toml` edits:**
- `[package] name = "trustedge-types"` -> `name = "sealedge-types"`
- `[lib] name = "trustedge_types"` -> `name = "sealedge_types"` (if present).

**6. `crates/platform/Cargo.toml` edits:**
- `[package] name = "trustedge-platform"` -> `name = "sealedge-platform"`
- `[lib] name = "trustedge_platform"` -> `name = "sealedge_platform"` (if present).
- `[dependencies]`: `trustedge-types = { workspace = true }` -> `sealedge-types = { workspace = true }`; `trustedge-core = { path = "../core" }` -> `sealedge-core = { path = "../core" }`.
- `[features]` `yubikey = ["ca", "trustedge-core/yubikey"]` -> `yubikey = ["ca", "sealedge-core/yubikey"]`.

**7. `crates/platform-server/Cargo.toml` edits:**
- `[package] name = "trustedge-platform-server"` -> `name = "sealedge-platform-server"`
- `[[bin]] name = "trustedge-platform-server"` -> `name = "sealedge-platform-server"` (path `src/main.rs` stays).
- `[dependencies]`: `trustedge-platform = { path = "../platform", features = [...] }` -> `sealedge-platform = { path = "../platform", features = [...] }`; `trustedge-core = { path = "../core" }` -> `sealedge-core = { path = "../core" }`.
- `[features]` `postgres = ["trustedge-platform/postgres"]` -> `postgres = ["sealedge-platform/postgres"]`; `ca = ["trustedge-platform/ca"]` -> `ca = ["sealedge-platform/ca"]`.

**8. `crates/cli/Cargo.toml` edits (after dir rename):**
- `[package] name = "trustedge-cli"` -> `name = "sealedge-cli"`
- `[[bin]] name = "trustedge"` -> `name = "sealedge"` (path `src/main.rs` stays).
- `[dependencies]`: `trustedge-core = { path = "../core" }` -> `sealedge-core = { path = "../core" }`.
- `[features]`: `audio = ["trustedge-core/audio"]` -> `audio = ["sealedge-core/audio"]`; `keyring = ["trustedge-core/keyring"]` -> `keyring = ["sealedge-core/keyring"]`.

**9. `crates/wasm/Cargo.toml` edits:**
- `[package] name = "trustedge-wasm"` -> `name = "sealedge-wasm"`
- `[lib] name = "trustedge_wasm"` -> `name = "sealedge_wasm"` (if present).
- If any `[dependencies]` reference `trustedge-core` or `trustedge-types`, rename to `sealedge-*`.

**Do NOT touch in this task:**
- `crates/trst-protocols/Cargo.toml`, `crates/trst-cli/Cargo.toml`, `crates/trst-wasm/Cargo.toml` — Plan 02.
- `examples/cam.video/Cargo.toml` `[package] name = "trustedge-cam-video-examples"` — Plan 02 (but this task DOES update its `trustedge-core` dep reference to `sealedge-core` so the example still builds).
- Copyright header comments (`Project: trustedge`) — Phase 85.
- `documentation = "https://docs.rs/trustedge-core"` URL metadata — Phase 85.
- keywords/description prose — Phase 85.
  </action>
  <acceptance_criteria>
    - `rg -n '^name = "trustedge-(core|types|platform|platform-server|cli|wasm)"' --glob 'crates/**/Cargo.toml'` returns 0 matches.
    - `rg -n '^name = "sealedge-(core|types|platform|platform-server|cli|wasm)"' --glob 'crates/**/Cargo.toml'` returns exactly 6 matches (one per renamed root crate).
    - `rg -n '"crates/trustedge-cli"' Cargo.toml` returns 0 matches.
    - `rg -n '"crates/cli"' Cargo.toml` returns 1 match in the workspace members list.
    - `test ! -e crates/trustedge-cli` returns true (directory no longer exists).
    - `test -e crates/cli/Cargo.toml` returns true.
    - `test ! -e crates/core/src/bin/trustedge-server.rs && test ! -e crates/core/src/bin/trustedge-client.rs` returns true.
    - `test -e crates/core/src/bin/sealedge-server.rs && test -e crates/core/src/bin/sealedge-client.rs` returns true.
    - `rg -n 'name = "(trustedge|trustedge-server|trustedge-client|trustedge-platform-server)"' --glob 'crates/**/Cargo.toml'` returns 0 matches.
    - `rg -n 'name = "(sealedge|sealedge-server|sealedge-client|sealedge-platform-server)"' --glob 'crates/**/Cargo.toml'` returns 4 matches total (one each).
  </acceptance_criteria>
  <verify>
    <automated>test ! -d crates/trustedge-cli && test -d crates/cli && ! rg -q '^name = "trustedge-(core|types|platform|platform-server|cli|wasm)"$' crates/*/Cargo.toml && ! rg -q 'name = "(trustedge|trustedge-server|trustedge-client|trustedge-platform-server)"' crates/*/Cargo.toml</automated>
  </verify>
  <done>
    - Crate directory normalized: `crates/trustedge-cli/` renamed to `crates/cli/`.
    - 6 root crate `[package] name` fields show `sealedge-*` values.
    - 4 binary `[[bin]] name` fields show `sealedge*` values (plus still-unrenamed `trst` in Plan 02 and unchanged `software-hsm-demo`).
    - 2 binary source files renamed.
    - All inter-crate dep keys in edited Cargo.toml files reference `sealedge-*`.
    - Feature-flag strings (`trustedge-core/yubikey` etc.) rewritten to `sealedge-core/yubikey`.
  </done>
</task>

<task type="auto">
  <name>Task 2: Sweep `use trustedge_{core,types,platform,wasm}` identifiers to `use sealedge_*` across entire tree (including still-unrenamed trst-* crates and examples)</name>
  <read_first>
    - .planning/phases/83-crate-and-binary-rename/83-PATTERNS.md §"Category 9: use trustedge_* statements in .rs Sources" (64 files, ~264 occurrences)
    - crates/core/src/lib.rs (for doctest example lines: `use trustedge_core::Envelope;`)
    - crates/trst-cli/tests/acceptance.rs (has `use trustedge_core::TrstManifest`)
    - examples/cam.video/Cargo.toml and examples/cam.video/*.rs (need core dep rename + use statement rename)
  </read_first>
  <files>
    All .rs files in the real tree (exclude target/, .claude/worktrees/, .planning/) containing the tokens `trustedge_core`, `trustedge_types`, `trustedge_platform`, `trustedge_wasm`. Primary locations:
    - crates/core/src/**/*.rs (and src/lib.rs doctest)
    - crates/types/src/**/*.rs
    - crates/platform/src/**/*.rs (tests included)
    - crates/platform/tests/**/*.rs
    - crates/platform-server/src/**/*.rs
    - crates/wasm/src/**/*.rs
    - crates/cli/src/**/*.rs (the renamed former trustedge-cli)
    - crates/trst-cli/src/main.rs + tests/*.rs (consumer of trustedge_core; trst-cli itself renames in Plan 02)
    - crates/trst-wasm/src/**/*.rs (consumer of trustedge_core)
    - crates/trst-protocols/src/**/*.rs (likely no cross-crate imports; verify with grep)
    - examples/cam.video/*.rs (consumer of trustedge_core)
    - crates/core/examples/*.rs
    - examples/cam.video/Cargo.toml (ALSO update its `trustedge-core = { path = "../../crates/core" }` dep key to `sealedge-core`; DO NOT rename the package name `trustedge-cam-video-examples` — that belongs to Plan 02)
  </files>
  <action>
Replace every occurrence of the token `trustedge_core` with `sealedge_core`, `trustedge_types` with `sealedge_types`, `trustedge_platform` with `sealedge_platform`, and `trustedge_wasm` with `sealedge_wasm` in `.rs` files across the tree, **excluding** `trustedge_trst_protocols`, `trustedge_trst_wasm`, `trustedge_pubky`, `trustedge_pubky_advanced` (those belong to Plan 02 and Plan 06 respectively).

Recommended approach (do NOT use destructive find+sed without per-file review; use a scripted sed-in-place followed by `git diff --stat` + `cargo check --workspace` verification):

```bash
# Find affected files first.
rg -l --type rust 'trustedge_(core|types|platform|wasm)\b' \
  -g '!target/**' -g '!.claude/**' -g '!.planning/**' \
  | tee /tmp/plan01-files.txt

# Apply substitutions (ordered to avoid double-replace):
while read f; do
  sed -i -E \
    -e 's/\btrustedge_core\b/sealedge_core/g' \
    -e 's/\btrustedge_types\b/sealedge_types/g' \
    -e 's/\btrustedge_platform\b/sealedge_platform/g' \
    -e 's/\btrustedge_wasm\b/sealedge_wasm/g' \
    "$f"
done < /tmp/plan01-files.txt
```

**Crucial carve-out:** the regex `\btrustedge_(core|types|platform|wasm)\b` as written will NOT match `trustedge_trst_protocols` (different prefix tail) or `trustedge_pubky*`, so those stay intact for later plans. Verify this assumption with `rg` before bulk-editing.

**Also edit `examples/cam.video/Cargo.toml`:**
- `[dependencies]` `trustedge-core = { path = "../../crates/core" }` -> `sealedge-core = { path = "../../crates/core" }`.
- **Do NOT** rename `[package] name = "trustedge-cam-video-examples"` in this plan — that's Plan 02's scope, paired with the trst-family rename.

**Doctest lines:** In `crates/core/src/lib.rs`, `//! use trustedge_core::Envelope;` is an executable doctest — rename to `//! use sealedge_core::Envelope;`. Any other doctests with `trustedge_core`/`trustedge_types` imports must rename. **Leave the surrounding `//!` and `///` prose untouched** — Phase 86 sweeps prose.

**Lock-step validation after sweep:**
```bash
cargo check --workspace --locked          # Must compile green.
cargo test --workspace --locked --no-run  # All test binaries compile.
```

If compile fails, diagnose missing import updates with `rg` before committing. Common miss: macro-embedded crate paths (`format!("trustedge_core::...")` in error messages) — Phase 85 sweeps those; flag any that cause compile errors.
  </action>
  <acceptance_criteria>
    - `rg -n 'trustedge_(core|types|platform|wasm)\b' --glob '*.rs' --glob '!target/**' --glob '!.claude/**' --glob '!.planning/**'` returns 0 matches.
    - `rg -n 'trustedge_(trst_protocols|trst_wasm|pubky|pubky_advanced)\b' --glob '*.rs'` still returns matches (confirms Plan 02/06 scope intact).
    - `rg -c 'sealedge_(core|types|platform|wasm)\b' --glob '*.rs' --glob '!target/**'` returns non-zero counts across at least 50 files (confirms sweep landed broadly).
    - `cargo check --workspace --locked` exits 0.
    - `cargo test --workspace --locked --no-run` exits 0 (test binaries compile; actual test execution gated on Task 3).
    - `examples/cam.video/Cargo.toml` contains `sealedge-core = { path = "../../crates/core" }` and still contains `name = "trustedge-cam-video-examples"` (Plan 02 renames that).
  </acceptance_criteria>
  <verify>
    <automated>! rg -q 'trustedge_(core|types|platform|wasm)\b' --glob '*.rs' --glob '!target/**' --glob '!.claude/**' --glob '!.planning/**' && cargo check --workspace --locked</automated>
  </verify>
  <done>
    - Every `.rs` file across the real tree consistently uses `sealedge_core`/`sealedge_types`/`sealedge_platform`/`sealedge_wasm` identifiers.
    - `trustedge_trst_*` and `trustedge_pubky*` identifiers are preserved for later plans.
    - `examples/cam.video/Cargo.toml` dep keys updated; package name intentionally deferred to Plan 02.
    - Workspace compiles clean.
  </done>
</task>

<task type="auto">
  <name>Task 3: Run full test suite to confirm workspace-coherent rename landed green; commit atomically</name>
  <read_first>
    - CLAUDE.md §"Build & Test Commands" (confirms `cargo test --workspace` is the gate)
    - scripts/ci-check.sh (optional fuller validation)
  </read_first>
  <files>
    (no new files — this task runs the test suite and commits)
  </files>
  <action>
Run the workspace test suite and targeted feature-matrix checks to confirm nothing regressed under the new names:

```bash
cargo fmt --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace --locked
```

If `cargo fmt --check` reports diffs, run `cargo fmt` and include the changes in this commit (formatting churn from filename changes in module paths is acceptable).

If clippy emits warnings related to identifier renames (e.g., unused imports in files that used to import `trustedge_core` under an alias), fix them — those are consequences of this rename and belong in this commit.

**Commit atomically:**
```bash
git add -A
git commit -m "$(cat <<'EOF'
refactor(83-01): rename 6 root crates trustedge-* -> sealedge-*

Renames workspace packages trustedge-{core,types,platform,platform-server,cli,wasm}
to sealedge-* equivalents, normalizes crates/trustedge-cli/ -> crates/cli/ dir,
renames binary targets (trustedge, trustedge-server, trustedge-client,
trustedge-platform-server -> sealedge-*), renames two bin source files, and
updates ~60 .rs files to use sealedge_* identifiers.

Trst-family crates (Plan 02), .trst extension literals (Plan 03), package.json /
CI / scripts (Plans 04-05), and experimental crates (Plan 06) rename in
subsequent atomic commits in this phase.

Workspace coherent: cargo check --workspace, cargo test --workspace, and
cargo clippy -- -D warnings all green at this commit.

Requirements: REBRAND-01, REBRAND-02 (partial — trst binaries in Plan 02).

Co-Authored-By: Claude Opus 4.7 (1M context) <noreply@anthropic.com>
EOF
)"
```

**Do NOT push** — subsequent plans in the same phase build on this commit locally.
  </action>
  <acceptance_criteria>
    - `cargo fmt --check` exits 0.
    - `cargo clippy --workspace --all-targets -- -D warnings` exits 0.
    - `cargo test --workspace --locked` exits 0 (all ~471 existing tests pass).
    - `git log -1 --pretty=%s` returns `refactor(83-01): rename 6 root crates trustedge-* -> sealedge-*`.
    - `git status` reports clean tree post-commit.
  </acceptance_criteria>
  <verify>
    <automated>cargo fmt --check && cargo clippy --workspace --all-targets -- -D warnings && cargo test --workspace --locked</automated>
  </verify>
  <done>
    - Full workspace test suite green under renamed packages.
    - Commit landed with the specified message.
    - Ready for Plan 02 (trst-family rename) to build on top.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer -> Cargo workspace | Rename correctness — a mistyped identifier propagates across every downstream build. |
| Workspace -> CI | The `-p trustedge-*` flags in `.github/workflows/*.yml` still reference old names AFTER this commit; CI runs in Plan 05. Any `git push` before Plan 05 completes will fail CI — phase requires local-only commits until all plans land. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-83-01 | T (Tampering) | Bulk `sed -i` across 60+ .rs files | mitigate | Run `cargo check --workspace` immediately after each sed pass; abort + revert if compile fails. `git diff --stat` review before commit. |
| T-83-02 | D (Denial of Service) | Partial rename leaves workspace uncompilable | mitigate | Atomic commit — all three tasks must land or none. Workspace-coherent rule enforced by Task 3's `cargo check` gate. |
| T-83-03 | I (Information disclosure) | sed regex accidentally catches `trustedge_trst_protocols` | mitigate | Word-boundary regex `\b(core\|types\|platform\|wasm)\b` specifically excludes trst/pubky suffixes; Task 2 verification grep confirms `trustedge_trst_*` tokens preserved. |
| T-83-04 | R (Repudiation) | Crate rename without corresponding use-statement rename | mitigate | Task 2 uses `rg -l` to enumerate every affected file before sed; Task 3 fails commit if `cargo check` reports missing imports. |
</threat_model>

<verification>
- Full `cargo check --workspace --locked` green.
- Full `cargo test --workspace --locked` green (all existing tests pass under new names).
- `cargo clippy --workspace --all-targets -- -D warnings` clean.
- `cargo fmt --check` clean.
- Verification greps from PATTERNS.md return the expected shape:
  - Grep 2 (no `trustedge_{core,types,platform,wasm}` in .rs): 0 matches.
  - Grep 4 (no legacy `[[bin]]` names `trustedge|trustedge-server|trustedge-client|trustedge-platform-server`): 0 matches.
  - Grep 9-10 partial: the 4 non-trst bin source files renamed; `crates/trustedge-cli/` directory gone.
</verification>

<success_criteria>
- 6 root-workspace crates renamed `trustedge-*` -> `sealedge-*` in their `[package] name` fields.
- 4 binary targets renamed (`trustedge`, `trustedge-server`, `trustedge-client`, `trustedge-platform-server` -> `sealedge-*`).
- 2 bin source files renamed to match.
- `crates/trustedge-cli/` normalized to `crates/cli/`.
- ~60 `.rs` files updated to `use sealedge_*`.
- Workspace root `Cargo.toml` members list and `workspace.dependencies` updated.
- Single atomic commit with green `cargo test --workspace`.
</success_criteria>

<output>
After completion, create `.planning/phases/83-crate-and-binary-rename/83-01-SUMMARY.md` listing:
- Files renamed (with old -> new paths).
- Count of `.rs` files touched by the use-statement sweep.
- Final commit SHA.
- Confirmation that `cargo test --workspace` + clippy + fmt all green.
</output>
